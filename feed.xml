<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://saeed1262.github.io/feed.xml" rel="self" type="application/atom+xml" /><link href="https://saeed1262.github.io/" rel="alternate" type="text/html" hreflang="en" /><updated>2025-08-28T21:17:24+00:00</updated><id>https://saeed1262.github.io/feed.xml</id><title type="html">Saeed Ghorbani</title><subtitle>Research Scientist at Amazon Games specializing in machine learning, computer vision, computer graphics, and computer animation. Expert in probabilistic motion modeling and deep learning for human motion synthesis.
</subtitle><entry><title type="html">Interactive 3D N-Body Gravity Simulation with Barnes-Hut Optimization</title><link href="https://saeed1262.github.io/blog/2025/nbody-barnes-hut-3d/" rel="alternate" type="text/html" title="Interactive 3D N-Body Gravity Simulation with Barnes-Hut Optimization" /><published>2025-08-28T00:00:00+00:00</published><updated>2025-08-28T00:00:00+00:00</updated><id>https://saeed1262.github.io/blog/2025/nbody-barnes-hut-3d</id><content type="html" xml:base="https://saeed1262.github.io/blog/2025/nbody-barnes-hut-3d/"><![CDATA[<h2 id="why-i-built-this">Why I Built This</h2>

<p>I’ve always been fascinated by gravity. Not just the everyday kind that keeps us grounded, but the cosmic choreography that shapes entire galaxies. How do billions of stars dance together to form those beautiful spiral arms we see in Hubble images? What happens when two galaxies collide? These questions led me down a rabbit hole that eventually resulted in this interactive 3D simulation.</p>

<p>The <strong>N-body problem</strong> is basically this: if you have N objects all pulling on each other with gravity, how do they move? For two objects (like Earth and Moon), we can solve this exactly. But add just one more object and suddenly the math becomes… well, let’s just say there’s no clean solution. You need to simulate it step by step.</p>

<p>I wanted to show you two different approaches:</p>
<ul>
  <li><strong>Barnes-Hut Algorithm</strong>: A clever trick that makes the simulation blazing fast</li>
  <li><strong>Direct Summation</strong>: The brute-force approach where every particle feels every other particle</li>
</ul>

<p>But here’s what really excites me about this project…</p>

<h2 id="the-real-story-of-galaxy-formation">The Real Story of Galaxy Formation</h2>

<p>Before we dive into the simulation, let me clear up a common misconception. Galaxies didn’t form from explosions—that’s actually way cooler than that.</p>

<p>Picture this: About 13.8 billion years ago, the Big Bang created a universe that was almost perfectly smooth, but not quite. There were tiny density fluctuations—some regions had just a little bit more matter than others. These tiny differences were like seeds.</p>

<p>Over millions of years, gravity did its thing. The denser regions attracted more matter, growing slowly but steadily. Dark matter (which we can’t see but makes up most of the universe) clumped together first, forming invisible scaffolding called “dark matter halos.”</p>

<p>Then regular matter—hydrogen and helium gas—fell into these dark matter halos like water flowing into a bowl. As the gas collapsed, it started spinning (conservation of angular momentum), forming rotating discs. When the gas became dense and hot enough, stars began to form.</p>

<p><strong>This is exactly what you’ll see in the simulation!</strong> Those rotating discs that emerge aren’t just pretty patterns—they’re recreating billions of years of cosmic evolution in fast-forward. The spiral arms that develop naturally from gravitational instabilities? That’s how we think our own Milky Way got its iconic shape.</p>

<p>The galaxy merger scenarios I included show another crucial part of the story. Galaxies aren’t isolated islands—they’re constantly interacting, merging, and growing. Most large galaxies, including ours, are thought to be the result of many smaller galaxies merging over cosmic time.</p>

<p>So no explosions—just gravity, time, and the universe’s incredible ability to build structure from simplicity.</p>

<h2 id="this-is-how-we-study-the-universe">This Is How We Study the Universe</h2>

<p>When I started building this simulation, I didn’t realize I was essentially recreating the same methods that astronomers use to understand cosmic evolution. It turns out the algorithms running in your browser right now are the same ones powering some of the most ambitious scientific endeavors in human history.</p>

<h3 id="galaxy-formation-in-your-browser"><strong>Galaxy Formation in Your Browser</strong></h3>
<p>That rotating disc you’ll see in the simulation? That’s not just a pretty pattern—it’s how we think the Milky Way formed billions of years ago. Astrophysicists run massive versions of this simulation with millions of particles to understand how spiral arms emerge and evolve.</p>

<p>The “Galaxy Merger” preset I included recreates one of the most dramatic events in cosmic history. In about 4.5 billion years, our Milky Way will collide with Andromeda—and this is exactly how scientists study what will happen.</p>

<h3 id="cosmic-web-formation"><strong>Cosmic Web Formation</strong></h3>
<p>The same Barnes-Hut algorithm I implemented here scales up to track dark matter particles forming the largest structures in the universe—the cosmic web of filaments that spans hundreds of millions of light-years. When you watch particles clumping together in the simulation, you’re seeing the same physics that created every galaxy cluster we observe.</p>

<h3 id="a-1986-revolution"><strong>A 1986 Revolution</strong></h3>
<p>Josh Barnes and Piet Hut published their breakthrough algorithm in 1986, and it completely transformed computational astrophysics. Before their work, simulations were limited to a few thousand particles—nowhere near enough to model realistic galaxies. Their clever insight about treating distant groups of particles as single masses suddenly made simulations with millions of particles possible.</p>

<h3 id="modern-supercomputing"><strong>Modern Supercomputing</strong></h3>
<p>Today’s astrophysics codes like GADGET, AREPO, and RAMSES use GPU acceleration to simulate systems with <em>billions</em> of particles. The Millennium Simulation—one of the most famous—followed 21.6 billion particles over the entire history of the universe, from shortly after the Big Bang to today.</p>

<h3 id="what-youre-really-experiencing"><strong>What You’re Really Experiencing</strong></h3>
<p>When you play with this simulation, you’re not just watching pretty dots move around. You’re experiencing the same fundamental physics that governs:</p>
<ul>
  <li>How the cosmic web formed after the Big Bang</li>
  <li>Why our galaxy has spiral arms</li>
  <li>What will happen when Milky Way and Andromeda collide</li>
  <li>How globular clusters evolve over billions of years</li>
</ul>

<p>Every time you adjust the parameters and watch the system evolve, you’re doing the same kind of experiments that help us understand our place in the cosmos.</p>

<h2 id="the-physics-behind-the-simulation">The Physics Behind the Simulation</h2>

<h3 id="gravitational-n-body-dynamics">Gravitational N-Body Dynamics</h3>

<p>Each particle in our simulation follows Newton’s laws under gravitational forces from all other particles. The gravitational force between two particles with masses $m_i$ and $m_j$ separated by distance $r_{ij}$ is:</p>

\[\vec{F}_{ij} = -G \frac{m_i m_j}{|\vec{r}_{ij}|^3} \vec{r}_{ij}\]

<p>Where:</p>
<ul>
  <li>$G$ is the gravitational constant</li>
  <li>$\vec{r}_{ij} = \vec{r}_j - \vec{r}_i$ is the separation vector</li>
  <li>The negative sign indicates attraction</li>
</ul>

<p>The total force on particle $i$ is the sum over all other particles:</p>

\[\vec{F}_i = \sum_{j \neq i} \vec{F}_{ij}\]

<p>This leads to the equation of motion:</p>

\[m_i \frac{d^2\vec{r}_i}{dt^2} = \vec{F}_i\]

<h3 id="the-computational-challenge">The Computational Challenge</h3>

<p>For N particles, computing all pairwise forces requires $\frac{N(N-1)}{2} \approx \frac{N^2}{2}$ calculations per time step. This <strong>O(N²) scaling</strong> becomes prohibitively expensive for large systems:</p>

<ul>
  <li><strong>1,000 particles</strong>: ~500,000 force calculations</li>
  <li><strong>10,000 particles</strong>: ~50,000,000 force calculations</li>
  <li><strong>100,000 particles</strong>: ~5,000,000,000 force calculations</li>
</ul>

<p>Real astrophysical simulations often involve millions or billions of particles, making direct methods impractical.</p>

<h2 id="the-barnes-hut-algorithm">The Barnes-Hut Algorithm</h2>

<h3 id="core-principle">Core Principle</h3>

<p>The Barnes-Hut algorithm, developed by Josh Barnes and Piet Hut in 1986, reduces computational complexity from O(N²) to <strong>O(N log N)</strong> using a clever approximation: distant groups of particles can be treated as single massive particles located at their center of mass.</p>

<h3 id="spatial-decomposition-with-octrees">Spatial Decomposition with Octrees</h3>

<p>The algorithm works by:</p>

<ol>
  <li><strong>Spatial Subdivision</strong>: Recursively divide 3D space into octants (8 cubic regions)</li>
  <li><strong>Hierarchical Tree</strong>: Build an octree where each node represents a spatial region</li>
  <li><strong>Mass Distribution</strong>: Store total mass and center of mass for each node</li>
  <li><strong>Multipole Approximation</strong>: Use the multipole acceptance criterion to decide when to approximate</li>
</ol>

<div style="text-align: center; margin: 2rem 0;">
  <svg width="700" height="450" viewBox="0 0 700 450" style="background: linear-gradient(135deg, #0a0a0a 0%, #1a1a1a 100%); border: 1px solid #333; border-radius: 12px; box-shadow: 0 4px 20px rgba(0,0,0,0.3);">
    <!-- Title -->
    <text x="350" y="30" fill="#64ffda" font-size="18" font-weight="bold" text-anchor="middle" style="text-shadow: 0 0 10px rgba(100,255,218,0.5);">Barnes-Hut Octree Structure</text>
    
    <!-- 3D Space visualization -->
    <g transform="translate(60, 60)">
      <!-- Background for space region -->
      <rect x="-5" y="-5" width="210" height="210" fill="rgba(0,0,0,0.2)" stroke="none" rx="8" />
      
      <!-- Main cube outline -->
      <rect x="0" y="0" width="200" height="200" fill="none" stroke="#64ffda" stroke-width="3" rx="4" />
      
      <!-- Primary subdivision lines (main octant divisions) -->
      <line x1="100" y1="0" x2="100" y2="200" stroke="#4caf50" stroke-width="2" opacity="0.8" />
      <line x1="0" y1="100" x2="200" y2="100" stroke="#4caf50" stroke-width="2" opacity="0.8" />
      
      <!-- Secondary subdivision lines (finer divisions) -->
      <line x1="50" y1="50" x2="150" y2="50" stroke="#ff9800" stroke-width="1.5" opacity="0.6" />
      <line x1="50" y1="150" x2="150" y2="150" stroke="#ff9800" stroke-width="1.5" opacity="0.6" />
      <line x1="50" y1="50" x2="50" y2="150" stroke="#ff9800" stroke-width="1.5" opacity="0.6" />
      <line x1="150" y1="50" x2="150" y2="150" stroke="#ff9800" stroke-width="1.5" opacity="0.6" />
      
      <!-- Octant labels for clarity -->
      <text x="50" y="50" fill="#64ffda" font-size="10" text-anchor="middle" opacity="0.7">I</text>
      <text x="150" y="50" fill="#64ffda" font-size="10" text-anchor="middle" opacity="0.7">II</text>
      <text x="50" y="150" fill="#64ffda" font-size="10" text-anchor="middle" opacity="0.7">III</text>
      <text x="150" y="150" fill="#64ffda" font-size="10" text-anchor="middle" opacity="0.7">IV</text>
      
      <!-- Particles with glow effect -->
      <defs>
        <filter id="glow">
          <feGaussianBlur stdDeviation="2" result="coloredBlur" />
          <feMerge>
            <feMergeNode in="coloredBlur" />
            <feMergeNode in="SourceGraphic" />
          </feMerge>
        </filter>
      </defs>
      
      <circle cx="30" cy="30" r="3.5" fill="#ff6b6b" filter="url(#glow)" />
      <circle cx="80" cy="40" r="3.5" fill="#ff6b6b" filter="url(#glow)" />
      <circle cx="120" cy="70" r="3.5" fill="#ff6b6b" filter="url(#glow)" />
      <circle cx="160" cy="120" r="3.5" fill="#ff6b6b" filter="url(#glow)" />
      <circle cx="40" cy="160" r="3.5" fill="#ff6b6b" filter="url(#glow)" />
      <circle cx="180" cy="180" r="3.5" fill="#ff6b6b" filter="url(#glow)" />
      <circle cx="70" cy="180" r="3.5" fill="#ff6b6b" filter="url(#glow)" />
      <circle cx="170" cy="40" r="3.5" fill="#ff6b6b" filter="url(#glow)" />
      
      <!-- Center of mass indicators with enhanced visibility -->
      <circle cx="55" cy="35" r="5" fill="#4caf50" stroke="#fff" stroke-width="2" filter="url(#glow)" />
      <circle cx="145" cy="80" r="5" fill="#4caf50" stroke="#fff" stroke-width="2" filter="url(#glow)" />
      <circle cx="125" cy="150" r="5" fill="#4caf50" stroke="#fff" stroke-width="2" filter="url(#glow)" />
      <circle cx="55" cy="170" r="5" fill="#4caf50" stroke="#fff" stroke-width="2" filter="url(#glow)" />
      
      <!-- Space region label -->
      <text x="100" y="220" fill="#ccc" font-size="12" text-anchor="middle" font-weight="bold">3D Space Subdivision</text>
    </g>
    
    <!-- Tree structure with improved layout -->
    <g transform="translate(340, 70)">
      <!-- Tree background -->
      <rect x="-10" y="-10" width="180" height="140" fill="rgba(0,0,0,0.15)" stroke="none" rx="8" />
      
      <!-- Root node -->
      <rect x="65" y="0" width="50" height="30" fill="#64ffda" stroke="#fff" stroke-width="2" rx="4" />
      <text x="90" y="20" fill="#000" font-size="12" font-weight="bold" text-anchor="middle">Root</text>
      
      <!-- Connection lines with better styling -->
      <line x1="90" y1="30" x2="90" y2="50" stroke="#ccc" stroke-width="2" />
      <line x1="30" y1="55" x2="150" y2="55" stroke="#ccc" stroke-width="2" />
      <line x1="30" y1="50" x2="30" y2="55" stroke="#ccc" stroke-width="2" />
      <line x1="70" y1="50" x2="70" y2="55" stroke="#ccc" stroke-width="2" />
      <line x1="110" y1="50" x2="110" y2="55" stroke="#ccc" stroke-width="2" />
      <line x1="150" y1="50" x2="150" y2="55" stroke="#ccc" stroke-width="2" />
      
      <!-- Level 1 nodes with consistent styling -->
      <rect x="10" y="60" width="35" height="25" fill="#4caf50" stroke="#fff" stroke-width="1.5" rx="3" />
      <text x="27.5" y="76" fill="#fff" font-size="10" font-weight="bold" text-anchor="middle">Q1</text>
      
      <rect x="55" y="60" width="35" height="25" fill="#4caf50" stroke="#fff" stroke-width="1.5" rx="3" />
      <text x="72.5" y="76" fill="#fff" font-size="10" font-weight="bold" text-anchor="middle">Q2</text>
      
      <rect x="100" y="60" width="35" height="25" fill="#4caf50" stroke="#fff" stroke-width="1.5" rx="3" />
      <text x="117.5" y="76" fill="#fff" font-size="10" font-weight="bold" text-anchor="middle">Q3</text>
      
      <rect x="145" y="60" width="35" height="25" fill="#4caf50" stroke="#fff" stroke-width="1.5" rx="3" />
      <text x="162.5" y="76" fill="#fff" font-size="10" font-weight="bold" text-anchor="middle">Q4</text>
      
      <!-- Level 2 subdivision example -->
      <line x1="72.5" y1="85" x2="72.5" y2="100" stroke="#ccc" stroke-width="2" />
      <line x1="55" y1="105" x2="90" y2="105" stroke="#ccc" stroke-width="2" />
      <line x1="55" y1="100" x2="55" y2="105" stroke="#ccc" stroke-width="2" />
      <line x1="90" y1="100" x2="90" y2="105" stroke="#ccc" stroke-width="2" />
      
      <rect x="40" y="110" width="25" height="20" fill="#ff9800" stroke="#fff" stroke-width="1.5" rx="3" />
      <text x="52.5" y="123" fill="#fff" font-size="9" font-weight="bold" text-anchor="middle">2.1</text>
      
      <rect x="75" y="110" width="25" height="20" fill="#ff9800" stroke="#fff" stroke-width="1.5" rx="3" />
      <text x="87.5" y="123" fill="#fff" font-size="9" font-weight="bold" text-anchor="middle">2.2</text>
      
      <!-- Tree structure label -->
      <text x="90" y="150" fill="#ccc" font-size="12" text-anchor="middle" font-weight="bold">Hierarchical Tree</text>
    </g>
    
    <!-- Enhanced Legend -->
    <g transform="translate(60, 320)">
      <rect x="-10" y="0" width="220" height="60" fill="rgba(0,0,0,0.2)" stroke="#333" stroke-width="1" rx="8" />
      <text x="0" y="20" fill="#64ffda" font-size="14" font-weight="bold">Legend</text>
      
      <circle cx="15" cy="40" r="4" fill="#ff6b6b" filter="url(#glow)" />
      <text x="25" y="45" fill="#fff" font-size="12" font-weight="500">Particles</text>
      
      <circle cx="90" cy="40" r="5" fill="#4caf50" stroke="#fff" stroke-width="2" filter="url(#glow)" />
      <text x="103" y="45" fill="#fff" font-size="12" font-weight="500">Center of Mass</text>
    </g>


    <!-- Enhanced Explanation -->
    <g transform="translate(300, 320)">
      <rect x="-10" y="0" width="380" height="100" fill="rgba(0,0,0,0.2)" stroke="#333" stroke-width="1" rx="8" />
      <text x="0" y="20" fill="#64ffda" font-size="14" font-weight="bold">How It Works</text>
      <text x="0" y="40" fill="#fff" font-size="12">• 3D space recursively subdivided into octants (shown in 2D)</text>
      <text x="0" y="55" fill="#fff" font-size="12">• Each tree node stores total mass and center of mass</text>
      <text x="0" y="70" fill="#fff" font-size="12">• Subdivisions continue until each leaf contains ≤1 particle</text>
      <text x="0" y="85" fill="#fff" font-size="12">• Enables O(N log N) force calculations vs O(N²) direct method</text>
    </g>
  </svg>
</div>

<h3 id="the-multipole-acceptance-criterion">The Multipole Acceptance Criterion</h3>

<p>For each particle, we traverse the octree and at each node ask: <em>“Can I treat this entire subtree as a single particle?”</em></p>

<p>The criterion is: $\frac{s}{d} &lt; \theta$</p>

<p>Where:</p>
<ul>
  <li>$s$ = size of the tree node (spatial extent)</li>
  <li>$d$ = distance from particle to node’s center of mass</li>
  <li>$\theta$ = <strong>theta parameter</strong> (accuracy threshold)</li>
</ul>

<p><strong>If the criterion is satisfied</strong>: Treat the entire node as a single particle
<strong>If not</strong>: Descend into child nodes and repeat</p>

<div style="text-align: center; margin: 2rem 0;">
  <svg width="800" height="420" viewBox="0 0 800 420" style="background: linear-gradient(135deg, #0a0a0a 0%, #1a1a1a 100%); border: 1px solid #333; border-radius: 12px; box-shadow: 0 4px 20px rgba(0,0,0,0.3);">
    <!-- Title -->
    <text x="400" y="30" fill="#64ffda" font-size="18" font-weight="bold" text-anchor="middle" style="text-shadow: 0 0 10px rgba(100,255,218,0.5);">Multipole Acceptance Criterion: s/d &lt; θ</text>
    
    <!-- Glow filter for enhanced visuals -->
    <defs>
      <filter id="glow-criterion">
        <feGaussianBlur stdDeviation="3" result="coloredBlur" />
        <feMerge>
          <feMergeNode in="coloredBlur" />
          <feMergeNode in="SourceGraphic" />
        </feMerge>
      </filter>
    </defs>
    
    <!-- Case 1: Accept approximation (s/d < θ) -->
    <g transform="translate(70, 70)">
      <!-- Background box for clean organization -->
      <rect x="-20" y="-20" width="300" height="180" fill="rgba(76,175,80,0.1)" stroke="#4caf50" stroke-width="1" rx="12" />
      
      <!-- Title with cleaner styling -->
      <text x="130" y="0" fill="#4caf50" font-size="16" font-weight="bold" text-anchor="middle">✓ ACCEPT</text>
      <text x="130" y="18" fill="#ccc" font-size="12" text-anchor="middle">s/d &lt; θ</text>
      
      <!-- Particle with glow -->
      <circle cx="30" cy="70" r="6" fill="#ff6b6b" filter="url(#glow-criterion)" />
      <text x="30" y="95" fill="#fff" font-size="11" font-weight="500" text-anchor="middle">Particle</text>
      
      <!-- Tree node (distant) with cleaner design -->
      <rect x="180" y="50" width="60" height="60" fill="rgba(100,255,218,0.1)" stroke="#64ffda" stroke-width="2" rx="6" />
      <circle cx="210" cy="80" r="5" fill="#4caf50" filter="url(#glow-criterion)" />
      <text x="210" y="125" fill="#ccc" font-size="11" text-anchor="middle">Tree Node</text>
      
      <!-- Distance indicator with better styling -->
      <line x1="36" y1="70" x2="174" y2="80" stroke="#fff" stroke-width="2" stroke-dasharray="5,5" opacity="0.8" />
      <text x="105" y="65" fill="#fff" font-size="12" font-weight="bold" text-anchor="middle">d (large)</text>
      
      <!-- Size indicator -->
      <line x1="180" y1="40" x2="240" y2="40" stroke="#64ffda" stroke-width="3" />
      <text x="210" y="35" fill="#64ffda" font-size="12" font-weight="bold" text-anchor="middle">s</text>
      
      <!-- Result with cleaner presentation -->
      <text x="130" y="140" fill="#4caf50" font-size="12" font-weight="bold" text-anchor="middle">Single Mass Approximation ⚡</text>
    </g>
    
    <!-- Case 2: Reject approximation (s/d ≥ θ) -->
    <g transform="translate(430, 70)">
      <!-- Background box for clean organization -->
      <rect x="-20" y="-20" width="300" height="180" fill="rgba(244,67,54,0.1)" stroke="#f44336" stroke-width="1" rx="12" />
      
      <!-- Title with cleaner styling -->
      <text x="130" y="0" fill="#f44336" font-size="16" font-weight="bold" text-anchor="middle">✗ REJECT</text>
      <text x="130" y="18" fill="#ccc" font-size="12" text-anchor="middle">s/d ≥ θ</text>
      
      <!-- Particle (closer) -->
      <circle cx="70" cy="70" r="6" fill="#ff6b6b" filter="url(#glow-criterion)" />
      <text x="70" y="95" fill="#fff" font-size="11" font-weight="500" text-anchor="middle">Particle</text>
      
      <!-- Tree node (close) with subdivision -->
      <rect x="140" y="50" width="60" height="60" fill="rgba(244,67,54,0.1)" stroke="#f44336" stroke-width="2" rx="6" />
      <circle cx="170" cy="80" r="5" fill="#4caf50" filter="url(#glow-criterion)" />
      <text x="170" y="125" fill="#ccc" font-size="11" text-anchor="middle">Tree Node</text>
      
      <!-- Subdivision lines with better visibility -->
      <line x1="170" y1="50" x2="170" y2="110" stroke="#ff9800" stroke-width="2" opacity="0.8" />
      <line x1="140" y1="80" x2="200" y2="80" stroke="#ff9800" stroke-width="2" opacity="0.8" />
      
      <!-- Distance indicator -->
      <line x1="76" y1="70" x2="134" y2="80" stroke="#fff" stroke-width="2" stroke-dasharray="5,5" opacity="0.8" />
      <text x="105" y="65" fill="#fff" font-size="12" font-weight="bold" text-anchor="middle">d (small)</text>
      
      <!-- Size indicator -->
      <line x1="140" y1="40" x2="200" y2="40" stroke="#f44336" stroke-width="3" />
      <text x="170" y="35" fill="#f44336" font-size="12" font-weight="bold" text-anchor="middle">s</text>
      
      <!-- Result with cleaner presentation -->
      <text x="130" y="140" fill="#f44336" font-size="12" font-weight="bold" text-anchor="middle">Must Subdivide 🎯</text>
    </g>
    
    <!-- Simplified theta parameter explanation -->
    <g transform="translate(100, 280)">
      <!-- Background for better organization -->
      <rect x="-20" y="-10" width="640" height="100" fill="rgba(0,0,0,0.2)" stroke="#333" stroke-width="1" rx="12" />
      
      <text x="300" y="15" fill="#ff9800" font-size="16" font-weight="bold" text-anchor="middle">Theta Parameter Balance</text>
      
      <!-- Three clear zones with better spacing -->
      <g transform="translate(0, 30)">
        <!-- Small theta zone -->
        <rect x="0" y="0" width="180" height="50" fill="rgba(76,175,80,0.1)" stroke="#4caf50" stroke-width="1" rx="8" />
        <text x="90" y="20" fill="#4caf50" font-size="12" font-weight="bold" text-anchor="middle">Small θ (0.3-0.5)</text>
        <text x="90" y="35" fill="#ccc" font-size="10" text-anchor="middle">High accuracy, slower</text>
        
        <!-- Optimal theta zone -->
        <rect x="210" y="0" width="180" height="50" fill="rgba(100,255,218,0.1)" stroke="#64ffda" stroke-width="2" rx="8" />
        <text x="300" y="20" fill="#64ffda" font-size="12" font-weight="bold" text-anchor="middle">Optimal θ (0.5-0.8)</text>
        <text x="300" y="35" fill="#ccc" font-size="10" text-anchor="middle">Best balance, ~1% error</text>
        
        <!-- Large theta zone -->
        <rect x="420" y="0" width="180" height="50" fill="rgba(244,67,54,0.1)" stroke="#f44336" stroke-width="1" rx="8" />
        <text x="510" y="20" fill="#f44336" font-size="12" font-weight="bold" text-anchor="middle">Large θ (1.0-1.5)</text>
        <text x="510" y="35" fill="#ccc" font-size="10" text-anchor="middle">Fast, lower accuracy</text>
      </g>
    </g>
  </svg>
</div>

<h3 id="theta-parameter-trade-off">Theta Parameter Trade-off</h3>

<ul>
  <li><strong>Small θ (0.3-0.5)</strong>: High accuracy, more computations, approaches direct method</li>
  <li><strong>Large θ (1.0-1.5)</strong>: Fast computation, lower accuracy, more approximation error</li>
</ul>

<p>Typically θ ≈ 0.5-0.8 provides good balance between speed and accuracy.</p>

<h2 id="numerical-integration-leapfrog-method">Numerical Integration: Leapfrog Method</h2>

<p>We use the <strong>leapfrog integration</strong> scheme, which is symplectic (energy-conserving) and second-order accurate:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1. Kick (half step):    v(t+dt/2) = v(t) + a(t) × dt/2
2. Drift (full step):   x(t+dt) = x(t) + v(t+dt/2) × dt
3. Compute forces:      a(t+dt) = F(x(t+dt))/m
4. Kick (half step):    v(t+dt) = v(t+dt/2) + a(t+dt) × dt/2
</code></pre></div></div>

<p>This method preserves energy much better than simple Euler integration, crucial for long-term stability.</p>

<h2 id="simulation-setup-rotating-galactic-disc">Simulation Setup: Rotating Galactic Disc</h2>

<p>Our initial conditions create a <strong>rotating disc</strong> that mimics galaxy formation:</p>

<h3 id="position-distribution">Position Distribution</h3>
<p>Particles are distributed with radius $r \propto \sqrt{\text{random}}$ to create realistic density profiles, with small vertical dispersion to form a thin disc.</p>

<h3 id="velocity-profile">Velocity Profile</h3>
<p>Each particle receives approximately circular velocity plus perturbations:</p>
<ul>
  <li><strong>Circular velocity</strong>: $v_{\text{circ}} = \sqrt{\frac{GM_{\text{enc}}(r)}{r}}$</li>
  <li><strong>Bar-like shear</strong>: Creates spiral arm instabilities</li>
  <li><strong>Random velocities</strong>: Adds realistic velocity dispersion</li>
</ul>

<p>This setup naturally evolves into spiral galaxy structures through gravitational interactions.</p>

<h2 id="parameter-guide">Parameter Guide</h2>

<h3 id="theta-θ-barnes-hut-accuracy"><strong>Theta (θ): Barnes-Hut Accuracy</strong></h3>
<ul>
  <li><strong>Range</strong>: 0.3 - 1.5</li>
  <li><strong>Effect</strong>: Controls speed vs. accuracy trade-off</li>
  <li><strong>Low values</strong> (0.3-0.5): More accurate, slower computation</li>
  <li><strong>High values</strong> (1.0-1.5): Faster computation, more approximation</li>
  <li><strong>Recommended</strong>: 0.5-0.8 for good balance</li>
</ul>

<h3 id="time-step-dt-integration-precision"><strong>Time Step (dt): Integration Precision</strong></h3>
<ul>
  <li><strong>Range</strong>: 0.001 - 0.03</li>
  <li><strong>Effect</strong>: Smaller steps = better accuracy but slower simulation</li>
  <li><strong>Too large</strong>: Energy drift, numerical instabilities</li>
  <li><strong>Too small</strong>: Unnecessarily slow computation</li>
  <li><strong>Recommended</strong>: 0.01 for stable evolution</li>
</ul>

<h3 id="softening-ε-gravitational-smoothing"><strong>Softening (ε): Gravitational Smoothing</strong></h3>
<ul>
  <li><strong>Range</strong>: 0.002 - 0.05</li>
  <li><strong>Purpose</strong>: Prevents singular forces when particles get very close</li>
  <li><strong>Physics</strong>: Represents finite particle size or quantum effects</li>
  <li><strong>Effect</strong>: Larger values make forces smoother but less realistic</li>
  <li><strong>Recommended</strong>: 0.01 for stable dynamics</li>
</ul>

<h3 id="particles-n-system-size"><strong>Particles (N): System Size</strong></h3>
<ul>
  <li><strong>Range</strong>: 1,000 - 8,000</li>
  <li><strong>Performance</strong>:
    <ul>
      <li>Barnes-Hut scales as O(N log N)</li>
      <li>Direct method scales as O(N²)</li>
    </ul>
  </li>
  <li><strong>Memory</strong>: Higher N requires more RAM</li>
  <li><strong>Visual</strong>: More particles show finer structure</li>
</ul>

<h2 id="what-to-observe">What to Observe</h2>

<h3 id="spiral-arm-formation"><strong>Spiral Arm Formation</strong></h3>
<p>Watch how the initially smooth disc develops spiral density waves through gravitational instabilities. This mirrors real galaxy evolution.</p>

<h3 id="performance-comparison"><strong>Performance Comparison</strong></h3>
<p>Switch between Barnes-Hut and Direct methods to see the dramatic performance difference. With 3000+ particles, Barnes-Hut runs ~10-100× faster while maintaining good accuracy.</p>

<h3 id="energy-conservation"><strong>Energy Conservation</strong></h3>
<p>Monitor the energy drift graph. Good simulations maintain energy to within ~1-5%. Larger drift indicates numerical problems or inadequate time resolution.</p>

<p>Total energy: $E = K + U$</p>

<p><strong>Kinetic energy</strong>: 
\(K = \frac{1}{2}\sum_{i=1}^{N} m_i |\vec{v}_i|^2\)</p>

<p><strong>Potential energy</strong>: 
\(U = -\frac{1}{2}\sum_{i=1}^{N}\sum_{j \neq i} \frac{G m_i m_j}{|\vec{r}_{ij}|}\)</p>

<p><strong>Energy drift</strong>: 
\(\Delta E = \frac{E(t) - E(0)}{|E(0)|} \times 100\%\)</p>

<p>Good simulations maintain \(\|\Delta E\| &lt; 1\) over hundreds of dynamical times.</p>

<h3 id="dynamic-range"><strong>Dynamic Range</strong></h3>
<p>Observe both:</p>
<ul>
  <li><strong>Large-scale structure</strong>: Overall spiral pattern evolution</li>
  <li><strong>Small-scale dynamics</strong>: Individual particle orbits and interactions</li>
</ul>

<hr />

<h2 id="ready-to-play-god-with-gravity">Ready to Play God with Gravity?</h2>

<p>I’ve spent way too many hours tweaking this simulation (my browser history can confirm), and now it’s your turn! The controls below let you experiment with the same physics that shapes galaxies.</p>

<p><strong>Here’s what I love doing:</strong></p>
<ul>
  <li>Start with the “Single Disc” and watch spiral arms emerge naturally—it’s mesmerizing</li>
  <li>Switch to “Galaxy Merger” and witness cosmic destruction in real-time</li>
  <li>Crank up the particle count and watch Barnes-Hut work its magic (try comparing it with Direct mode… if you dare)</li>
  <li>Play with the theta parameter—you’ll see the accuracy vs. speed trade-off in action</li>
</ul>

<p>The energy drift graph at the bottom is my favorite debugging tool. If it stays flat, you’re doing physics right. If it goes crazy, well… that’s when things get interesting.</p>

<p>Go ahead, break the universe. I’ve already done it countless times while building this!</p>

<style>
* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

body {
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
  background: #0a0a0a;
  color: #e0e0e0;
}

#container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 1rem;
}

#controls {
  background: #1a1a1a;
  border: 1px solid #333;
  border-radius: 8px;
  padding: 1rem;
  margin-bottom: 1rem;
  display: flex;
  flex-wrap: wrap;
  gap: 1rem;
  align-items: center;
}

.control-group {
  display: flex;
  gap: 0.5rem;
  align-items: center;
}

button {
  background: #2a2a2a;
  color: #e0e0e0;
  border: 1px solid #444;
  padding: 0.5rem 1rem;
  border-radius: 4px;
  cursor: pointer;
  transition: background 0.2s;
}

button:hover {
  background: #3a3a3a;
}

button:active {
  background: #1a1a1a;
}

select {
  background: #2a2a2a;
  color: #e0e0e0;
  border: 1px solid #444;
  padding: 0.5rem;
  border-radius: 4px;
  cursor: pointer;
}

.slider-group {
  display: flex;
  align-items: center;
  gap: 0.5rem;
}

.slider-group label {
  font-size: 0.9rem;
  min-width: 80px;
  font-weight: bold;
  color: #fff;
}

.slider-group input[type="range"] {
  width: 120px;
}

.slider-group .value {
  font-family: monospace;
  min-width: 60px;
  text-align: right;
  color: #8cf;
  font-weight: bold;
}

#status {
  font-family: monospace;
  font-size: 0.9rem;
  color: #8cf;
  padding: 0.5rem;
  background: #1a1a1a;
  border-radius: 4px;
  flex: 1 1 100%;
  text-align: center;
}

#viz-container {
  position: relative;
  width: 100%;
  aspect-ratio: 16/9;
  background: #000;
  border: 1px solid #333;
  border-radius: 8px;
  overflow: hidden;
  margin-bottom: 1rem;
}

#viz {
  width: 100%;
  height: 100%;
}

#energy-container {
  background: #1a1a1a;
  border: 1px solid #333;
  border-radius: 8px;
  padding: 1rem;
  text-align: center;
}

#energy-container h3 {
  color: #fff;
  margin-bottom: 0.5rem;
  font-size: 1.1rem;
}

#energy {
  border: 1px solid #333;
  background: #0a0a0a;
  display: block;
  margin: 0 auto;
  max-width: 100%;
  width: 100%;
  height: 120px;
  max-height: 120px;
}

@media (max-width: 768px) {
  .control-group {
    flex: 1 1 100%;
  }
  
  .slider-group input[type="range"] {
    width: 100px;
  }
}

.no-webgl {
  display: flex;
  align-items: center;
  justify-content: center;
  height: 100%;
  font-size: 1.2rem;
  color: #f88;
  text-align: center;
  padding: 2rem;
}

</style>

<div id="container">
  <div id="controls">
    <div class="control-group">
      <button id="play-pause">Pause</button>
      <button id="reset">Reset</button>
      <button id="reseed">Re-seed</button>
    </div>
    
    <div class="control-group">
      <label for="mode">Mode:</label>
      <select id="mode">
        <option value="barnes-hut">Barnes–Hut (O(N log N))</option>
        <option value="direct">Direct (O(N²))</option>
      </select>
    </div>
    
    <div class="control-group">
      <label for="seed-type">Setup:</label>
      <select id="seed-type">
        <option value="single">Single Disc</option>
        <option value="merger">Galaxy Merger</option>
      </select>
    </div>
    
    <div class="control-group">
      <label for="render-mode">Render:</label>
      <select id="render-mode">
        <option value="shader">Custom Shader</option>
        <option value="halo">Soft Halo</option>
      </select>
    </div>
    
    <div class="slider-group">
      <label for="theta">Theta (θ):</label>
      <input type="range" id="theta" min="0.3" max="1.5" step="0.05" value="0.8" />
      <span class="value" id="theta-value">0.8</span>
    </div>
    
    <div class="slider-group">
      <label for="dt">Time Step (dt):</label>
      <input type="range" id="dt" min="0.001" max="0.03" step="0.001" value="0.01" />
      <span class="value" id="dt-value">0.010</span>
    </div>
    
    <div class="slider-group">
      <label for="eps">Softening (ε):</label>
      <input type="range" id="eps" min="0.002" max="0.05" step="0.001" value="0.01" />
      <span class="value" id="eps-value">0.010</span>
    </div>
    
    <div class="slider-group">
      <label for="npart">Particles (N):</label>
      <input type="range" id="npart" min="1000" max="8000" step="500" value="3000" />
      <span class="value" id="npart-value">3000</span>
    </div>
    
    <div class="slider-group">
      <label for="mass">Mass Scale:</label>
      <input type="range" id="mass" min="0.5" max="5.0" step="0.1" value="1.0" />
      <span class="value" id="mass-value">1.0×</span>
    </div>
    
    <div id="status">t=0.00 drift=0.00% mode=BH | step=0ms fps≈0</div>
  </div>
  
  <div id="viz-container">
    <div id="viz"></div>
  </div>
  
  <div id="energy-container">
    <h3>Energy Drift</h3>
    <canvas id="energy" width="800" height="120"></canvas>
  </div>
  
</div>

<script type="importmap">
{
  "imports": {
    "three": "https://cdn.jsdelivr.net/npm/three@0.160.0/build/three.module.js",
    "three/addons/": "https://cdn.jsdelivr.net/npm/three@0.160.0/examples/jsm/"
  }
}
</script>

<script type="module">
import * as THREE from 'three';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';

// Check WebGL support
if (!WebGLRenderingContext) {
  document.getElementById('viz').innerHTML = '<div class="no-webgl">WebGL is not supported in your browser</div>';
  throw new Error('WebGL not supported');
}

// Simulation parameters
let params = {
  theta: 0.8,
  dt: 0.01,
  eps: 0.01,
  N: 3000,
  mass: 1.0,
  mode: 'barnes-hut',
  playing: true,
  seedType: 'single',
  renderMode: 'shader'
};

// Simulation state
let time = 0;
let frameCount = 0;
let lastFrameTime = performance.now();
let stepTime = 0;
let fps = 0;
let E0 = null;
let energyDrift = 0;

// Arrays for physics
let x, y, z, vx, vy, vz, ax, ay, az, m;
let positions, speeds;

// Energy tracking
const energyHistory = new Float32Array(800);
let energyIndex = 0;
let energyCount = 0;

// Three.js objects
let scene, camera, renderer, controls, points, geometry;

// Octree for Barnes-Hut
class OctreeNode {
  constructor(cx, cy, cz, hs) {
    this.cx = cx;
    this.cy = cy;
    this.cz = cz;
    this.hs = hs;
    this.m = 0;
    this.comx = 0;
    this.comy = 0;
    this.comz = 0;
    this.body = -1;
    this.children = null;
  }
}

// Initialize arrays
function initArrays(n) {
  x = new Float32Array(n);
  y = new Float32Array(n);
  z = new Float32Array(n);
  vx = new Float32Array(n);
  vy = new Float32Array(n);
  vz = new Float32Array(n);
  ax = new Float32Array(n);
  ay = new Float32Array(n);
  az = new Float32Array(n);
  m = new Float32Array(n);
  positions = new Float32Array(n * 3);
  speeds = new Float32Array(n);
}

// Seed the system - rotating disc or galaxy merger
function seedSystem() {
  if (params.seedType === 'single') {
    seedSingleDisc();
  } else {
    seedGalaxyMerger();
  }
}

// Seed single rotating disc
function seedSingleDisc() {
  const n = params.N;
  const Rmax = 2.2;
  const totalMass = 1.0 * params.mass;
  const mi = totalMass / n;
  
  for (let i = 0; i < n; i++) {
    // Biased radial distribution
    const r = Rmax * Math.sqrt(Math.random());
    const phi = Math.random() * 2 * Math.PI;
    
    // Position
    x[i] = r * Math.cos(phi);
    y[i] = r * Math.sin(phi);
    z[i] = gaussianRandom() * 0.06;
    
    // Mass
    m[i] = mi;
    
    // Velocity - approximate circular with noise
    const Menc = totalMass * (r / Rmax) * (r / Rmax); // Simplified enclosed mass
    const vcirc = Math.sqrt(Menc / r);
    
    // Add slight bar-like shear for spiral arms
    const shear = 0.1 * Math.sin(2 * phi);
    
    vx[i] = -vcirc * Math.sin(phi) * (1 + shear) + gaussianRandom() * 0.01;
    vy[i] = vcirc * Math.cos(phi) * (1 + shear) + gaussianRandom() * 0.01;
    vz[i] = gaussianRandom() * 0.01;
  }
}

// Seed galaxy merger - two offset discs with different spins
function seedGalaxyMerger() {
  const n = params.N;
  const n1 = Math.floor(n * 0.6); // First galaxy gets 60% of particles
  const n2 = n - n1; // Second galaxy gets 40%
  
  const Rmax1 = 1.8;
  const Rmax2 = 1.4;
  const totalMass = 1.0 * params.mass;
  
  // Galaxy 1 - larger, positioned at (-1.5, 0, 0), counter-clockwise rotation
  for (let i = 0; i < n1; i++) {
    const r = Rmax1 * Math.sqrt(Math.random());
    const phi = Math.random() * 2 * Math.PI;
    
    // Position - offset left
    x[i] = -1.5 + r * Math.cos(phi);
    y[i] = r * Math.sin(phi);
    z[i] = gaussianRandom() * 0.05;
    
    // Mass
    m[i] = (totalMass * 0.6) / n1;
    
    // Velocity - counter-clockwise
    const Menc = totalMass * 0.6 * (r / Rmax1) * (r / Rmax1);
    const vcirc = Math.sqrt(Menc / Math.max(r, 0.1));
    
    vx[i] = -vcirc * Math.sin(phi) + gaussianRandom() * 0.02 + 0.3; // Approach velocity
    vy[i] = vcirc * Math.cos(phi) + gaussianRandom() * 0.02;
    vz[i] = gaussianRandom() * 0.01;
  }
  
  // Galaxy 2 - smaller, positioned at (1.5, 0, 0), clockwise rotation
  for (let i = n1; i < n; i++) {
    const r = Rmax2 * Math.sqrt(Math.random());
    const phi = Math.random() * 2 * Math.PI;
    
    // Position - offset right
    x[i] = 1.5 + r * Math.cos(phi);
    y[i] = r * Math.sin(phi);
    z[i] = gaussianRandom() * 0.04;
    
    // Mass
    m[i] = (totalMass * 0.4) / n2;
    
    // Velocity - clockwise (opposite rotation)
    const Menc = totalMass * 0.4 * (r / Rmax2) * (r / Rmax2);
    const vcirc = Math.sqrt(Menc / Math.max(r, 0.1));
    
    vx[i] = vcirc * Math.sin(phi) + gaussianRandom() * 0.02 - 0.3; // Approach velocity
    vy[i] = -vcirc * Math.cos(phi) + gaussianRandom() * 0.02;
    vz[i] = gaussianRandom() * 0.01;
  }
}

// Gaussian random number generator
function gaussianRandom() {
  let u = 0, v = 0;
  while (u === 0) u = Math.random();
  while (v === 0) v = Math.random();
  return Math.sqrt(-2.0 * Math.log(u)) * Math.cos(2.0 * Math.PI * v);
}

// Build octree
function buildOctree() {
  const n = params.N;
  
  // Find bounds
  let minX = x[0], maxX = x[0];
  let minY = y[0], maxY = y[0];
  let minZ = z[0], maxZ = z[0];
  
  for (let i = 1; i < n; i++) {
    minX = Math.min(minX, x[i]);
    maxX = Math.max(maxX, x[i]);
    minY = Math.min(minY, y[i]);
    maxY = Math.max(maxY, y[i]);
    minZ = Math.min(minZ, z[i]);
    maxZ = Math.max(maxZ, z[i]);
  }
  
  // Create cubic root with padding
  const cx = (minX + maxX) * 0.5;
  const cy = (minY + maxY) * 0.5;
  const cz = (minZ + maxZ) * 0.5;
  const hs = Math.max(maxX - minX, maxY - minY, maxZ - minZ) * 0.5 * 1.1;
  
  const root = new OctreeNode(cx, cy, cz, hs);
  
  // Insert bodies
  for (let i = 0; i < n; i++) {
    insertBody(root, i);
  }
  
  return root;
}

// Insert body into octree
function insertBody(node, i) {
  // If node is empty, place body here
  if (node.body === -1 && node.children === null) {
    node.body = i;
    node.m = m[i];
    node.comx = x[i];
    node.comy = y[i];
    node.comz = z[i];
    return;
  }
  
  // If node has a body, need to subdivide
  if (node.body !== -1) {
    const oldBody = node.body;
    node.body = -1;
    
    // Create children
    node.children = new Array(8);
    for (let j = 0; j < 8; j++) {
      const hs2 = node.hs * 0.5;
      const cx2 = node.cx + ((j & 1) ? hs2 : -hs2);
      const cy2 = node.cy + ((j & 2) ? hs2 : -hs2);
      const cz2 = node.cz + ((j & 4) ? hs2 : -hs2);
      node.children[j] = new OctreeNode(cx2, cy2, cz2, hs2);
    }
    
    // Reinsert old body
    insertBody(node, oldBody);
  }
  
  // Insert new body into appropriate child
  const idx = ((x[i] > node.cx) ? 1 : 0) |
               ((y[i] > node.cy) ? 2 : 0) |
               ((z[i] > node.cz) ? 4 : 0);
  insertBody(node.children[idx], i);
  
  // Update mass and center of mass
  const oldMass = node.m;
  node.m += m[i];
  node.comx = (node.comx * oldMass + x[i] * m[i]) / node.m;
  node.comy = (node.comy * oldMass + y[i] * m[i]) / node.m;
  node.comz = (node.comz * oldMass + z[i] * m[i]) / node.m;
}

// Compute forces using Barnes-Hut
function computeForcesBH() {
  const n = params.N;
  const root = buildOctree();
  const theta = params.theta;
  const eps2 = params.eps * params.eps;
  
  // Clear accelerations
  ax.fill(0);
  ay.fill(0);
  az.fill(0);
  
  // For each body, traverse tree
  for (let i = 0; i < n; i++) {
    const stack = [root];
    
    while (stack.length > 0) {
      const node = stack.pop();
      
      // Skip empty nodes
      if (node.m === 0) continue;
      
      // If leaf with self, skip
      if (node.body === i) continue;
      
      // Compute distance to COM
      const dx = node.comx - x[i];
      const dy = node.comy - y[i];
      const dz = node.comz - z[i];
      const dist2 = dx * dx + dy * dy + dz * dz + eps2;
      const dist = Math.sqrt(dist2);
      
      // Check multipole acceptance criterion
      if (node.children === null || (2 * node.hs) / dist < theta) {
        // Treat as single mass
        const f = node.m / (dist2 * dist);
        ax[i] += f * dx;
        ay[i] += f * dy;
        az[i] += f * dz;
      } else {
        // Descend into children
        for (let j = 0; j < 8; j++) {
          if (node.children[j].m > 0) {
            stack.push(node.children[j]);
          }
        }
      }
    }
  }
}

// Compute forces using direct summation
function computeForcesDirect() {
  const n = params.N;
  const eps2 = params.eps * params.eps;
  
  // Clear accelerations
  ax.fill(0);
  ay.fill(0);
  az.fill(0);
  
  // O(N²) pairwise forces
  for (let i = 0; i < n; i++) {
    for (let j = 0; j < n; j++) {
      if (i === j) continue;
      
      const dx = x[j] - x[i];
      const dy = y[j] - y[i];
      const dz = z[j] - z[i];
      const dist2 = dx * dx + dy * dy + dz * dz + eps2;
      const dist = Math.sqrt(dist2);
      const f = m[j] / (dist2 * dist);
      
      ax[i] += f * dx;
      ay[i] += f * dy;
      az[i] += f * dz;
    }
  }
}

// Compute potential energy using tree
function computePotential() {
  const n = params.N;
  const root = buildOctree();
  const theta = params.theta;
  const eps = params.eps;
  let U = 0;
  
  for (let i = 0; i < n; i++) {
    const stack = [root];
    
    while (stack.length > 0) {
      const node = stack.pop();
      
      if (node.m === 0) continue;
      if (node.body === i) continue;
      
      const dx = node.comx - x[i];
      const dy = node.comy - y[i];
      const dz = node.comz - z[i];
      const dist = Math.sqrt(dx * dx + dy * dy + dz * dz + eps * eps);
      
      if (node.children === null || (2 * node.hs) / dist < theta) {
        U -= m[i] * node.m / dist;
      } else {
        for (let j = 0; j < 8; j++) {
          if (node.children[j].m > 0) {
            stack.push(node.children[j]);
          }
        }
      }
    }
  }
  
  return U * 0.5; // Avoid double counting
}

// Compute total energy
function computeEnergy() {
  const n = params.N;
  
  // Kinetic energy
  let K = 0;
  for (let i = 0; i < n; i++) {
    const v2 = vx[i] * vx[i] + vy[i] * vy[i] + vz[i] * vz[i];
    K += 0.5 * m[i] * v2;
  }
  
  // Potential energy
  const U = computePotential();
  
  return K + U;
}


// Leapfrog integration step
function step() {
  const n = params.N;
  const dt = params.dt;
  
  const t0 = performance.now();
  
  // Kick (half step)
  for (let i = 0; i < n; i++) {
    vx[i] += 0.5 * dt * ax[i];
    vy[i] += 0.5 * dt * ay[i];
    vz[i] += 0.5 * dt * az[i];
  }
  
  // Drift
  for (let i = 0; i < n; i++) {
    x[i] += dt * vx[i];
    y[i] += dt * vy[i];
    z[i] += dt * vz[i];
  }
  
  // Recompute accelerations
  if (params.mode === 'barnes-hut') {
    computeForcesBH();
  } else {
    computeForcesDirect();
  }
  
  // Kick (half step)
  for (let i = 0; i < n; i++) {
    vx[i] += 0.5 * dt * ax[i];
    vy[i] += 0.5 * dt * ay[i];
    vz[i] += 0.5 * dt * az[i];
  }
  
  stepTime = performance.now() - t0;
  time += dt;
  
  // Update energy drift
  const E = computeEnergy();
  if (E0 === null) E0 = E;
  energyDrift = (E - E0) / Math.abs(E0);
  
  // Store in history
  energyHistory[energyIndex] = energyDrift;
  energyIndex = (energyIndex + 1) % energyHistory.length;
  if (energyCount < energyHistory.length) energyCount++;
}

// Update geometry buffers
function updateGeometry() {
  const n = params.N;
  
  for (let i = 0; i < n; i++) {
    positions[i * 3] = x[i];
    positions[i * 3 + 1] = y[i];
    positions[i * 3 + 2] = z[i];
    
    const v = Math.sqrt(vx[i] * vx[i] + vy[i] * vy[i] + vz[i] * vz[i]);
    speeds[i] = v;
  }
  
  geometry.attributes.position.needsUpdate = true;
  geometry.attributes.speed.needsUpdate = true;
}

// Draw energy drift graph
function drawEnergyGraph() {
  const canvas = document.getElementById('energy');
  const ctx = canvas.getContext('2d');
  const width = canvas.width;
  const height = canvas.height;
  
  // Clear
  ctx.fillStyle = '#0a0a0a';
  ctx.fillRect(0, 0, width, height);
  
  if (energyCount < 2) return;
  
  // Find min/max for adaptive scaling
  let minDrift = Infinity, maxDrift = -Infinity;
  for (let i = 0; i < energyCount; i++) {
    const idx = (energyIndex - energyCount + i + energyHistory.length) % energyHistory.length;
    const drift = energyHistory[idx];
    minDrift = Math.min(minDrift, drift);
    maxDrift = Math.max(maxDrift, drift);
  }
  
  // Ensure reasonable range
  const range = Math.max(Math.abs(minDrift), Math.abs(maxDrift), 0.001);
  const yScale = (height * 0.4) / range;
  
  // Draw axes
  ctx.strokeStyle = '#333';
  ctx.lineWidth = 1;
  ctx.beginPath();
  ctx.moveTo(0, height / 2);
  ctx.lineTo(width, height / 2);
  ctx.stroke();
  
  // Draw grid lines
  ctx.strokeStyle = '#222';
  ctx.lineWidth = 0.5;
  const gridLines = 4;
  for (let i = 1; i <= gridLines; i++) {
    const y1 = height / 2 - (i / gridLines) * height * 0.4;
    const y2 = height / 2 + (i / gridLines) * height * 0.4;
    ctx.beginPath();
    ctx.moveTo(0, y1);
    ctx.lineTo(width, y1);
    ctx.moveTo(0, y2);
    ctx.lineTo(width, y2);
    ctx.stroke();
  }
  
  // Draw drift curve
  ctx.strokeStyle = '#8cf';
  ctx.lineWidth = 2;
  ctx.beginPath();
  
  for (let i = 0; i < energyCount; i++) {
    const idx = (energyIndex - energyCount + i + energyHistory.length) % energyHistory.length;
    const xPos = (i / Math.max(energyCount - 1, 1)) * width;
    const yPos = height / 2 - energyHistory[idx] * yScale;
    
    if (i === 0) {
      ctx.moveTo(xPos, yPos);
    } else {
      ctx.lineTo(xPos, yPos);
    }
  }
  ctx.stroke();
  
  // Draw labels with better positioning
  ctx.fillStyle = '#ccc';
  ctx.font = '12px monospace';
  ctx.textAlign = 'left';
  ctx.fillText('0%', 5, height / 2 - 5);
  
  ctx.textAlign = 'right';
  ctx.fillText(`${(energyDrift * 100).toFixed(3)}%`, width - 5, 15);
  
  // Draw scale indicators
  ctx.fillStyle = '#888';
  ctx.font = '10px monospace';
  ctx.textAlign = 'right';
  ctx.fillText(`+${(range * 100).toFixed(2)}%`, width - 5, 25);
  ctx.fillText(`-${(range * 100).toFixed(2)}%`, width - 5, height - 5);
}

// Update status display
function updateStatus() {
  frameCount++;
  const now = performance.now();
  if (now - lastFrameTime > 1000) {
    fps = Math.round(frameCount * 1000 / (now - lastFrameTime));
    frameCount = 0;
    lastFrameTime = now;
  }
  
  const mode = params.mode === 'barnes-hut' ? 'BH' : 'Direct';
  const status = `t=${time.toFixed(2)} drift=${(energyDrift * 100).toFixed(2)}% mode=${mode} | step=${stepTime.toFixed(1)}ms fps≈${fps}`;
  document.getElementById('status').textContent = status;
}

// Create halo texture for soft particles
function createHaloTexture() {
  const canvas = document.createElement('canvas');
  canvas.width = 64;
  canvas.height = 64;
  const ctx = canvas.getContext('2d');
  
  const gradient = ctx.createRadialGradient(32, 32, 0, 32, 32, 32);
  gradient.addColorStop(0, 'rgba(255,255,255,1)');
  gradient.addColorStop(0.3, 'rgba(255,255,255,0.8)');
  gradient.addColorStop(0.7, 'rgba(255,255,255,0.2)');
  gradient.addColorStop(1, 'rgba(255,255,255,0)');
  
  ctx.fillStyle = gradient;
  ctx.fillRect(0, 0, 64, 64);
  
  return new THREE.CanvasTexture(canvas);
}

// Initialize Three.js
function initThree() {
  const container = document.getElementById('viz');
  
  // Scene
  scene = new THREE.Scene();
  scene.background = new THREE.Color(0x000000);
  
  // Camera
  camera = new THREE.PerspectiveCamera(60, container.clientWidth / container.clientHeight, 0.1, 100);
  camera.position.set(0, 5, 10);
  camera.lookAt(0, 0, 0);
  
  // Renderer
  renderer = new THREE.WebGLRenderer({ antialias: true });
  renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));
  renderer.setSize(container.clientWidth, container.clientHeight);
  container.appendChild(renderer.domElement);
  
  // Controls
  controls = new OrbitControls(camera, renderer.domElement);
  controls.enableDamping = true;
  controls.dampingFactor = 0.05;
  
  // Initialize arrays before creating geometry
  if (!positions || !speeds) {
    initArrays(params.N);
    seedSystem();
  }
  
  // Create points geometry with initialized arrays
  geometry = new THREE.BufferGeometry();
  geometry.setAttribute('position', new THREE.BufferAttribute(positions, 3));
  geometry.setAttribute('speed', new THREE.BufferAttribute(speeds, 1));
  
  // Create materials for both render modes
  createPointsMaterials();
  
  // Mark attributes as dynamic
  geometry.attributes.position.setUsage(THREE.DynamicDrawUsage);
  geometry.attributes.speed.setUsage(THREE.DynamicDrawUsage);
  
  // Handle resize
  const resizeObserver = new ResizeObserver(() => {
    camera.aspect = container.clientWidth / container.clientHeight;
    camera.updateProjectionMatrix();
    renderer.setSize(container.clientWidth, container.clientHeight);
  });
  resizeObserver.observe(container);
}

// Create materials for different render modes
function createPointsMaterials() {
  // Custom shader material
  const vertexShader = `
    attribute float speed;
    varying float vSpeed;
    uniform float uSize;
    uniform float uDevicePixelRatio;
    
    void main() {
      vSpeed = speed;
      vec4 mvPosition = modelViewMatrix * vec4(position, 1.0);
      gl_Position = projectionMatrix * mvPosition;
      gl_PointSize = uSize * uDevicePixelRatio * (1.0 / -mvPosition.z);
    }
  `;
  
  const fragmentShader = `
    varying float vSpeed;
    
    void main() {
      vec2 xy = gl_PointCoord.xy - vec2(0.5);
      float r = length(xy);
      if (r > 0.5) discard;
      
      float intensity = 1.0 - 2.0 * r;
      intensity = intensity * intensity;
      
      // Color by speed: cool -> white -> warm
      float t = clamp(vSpeed * 2.0, 0.0, 1.0);
      vec3 cool = vec3(0.2, 0.5, 1.0);
      vec3 white = vec3(1.0, 1.0, 1.0);
      vec3 warm = vec3(1.0, 0.8, 0.3);
      
      vec3 color;
      if (t < 0.5) {
        color = mix(cool, white, t * 2.0);
      } else {
        color = mix(white, warm, (t - 0.5) * 2.0);
      }
      
      gl_FragColor = vec4(color * intensity, intensity);
    }
  `;
  
  const shaderMaterial = new THREE.ShaderMaterial({
    uniforms: {
      uSize: { value: 20.0 },
      uDevicePixelRatio: { value: Math.min(window.devicePixelRatio, 2) }
    },
    vertexShader,
    fragmentShader,
    blending: THREE.AdditiveBlending,
    depthWrite: false,
    transparent: true
  });
  
  // Enhanced halo shader with texture and speed-based coloring
  const haloVertexShader = `
    attribute float speed;
    varying float vSpeed;
    uniform float uSize;
    uniform float uDevicePixelRatio;
    
    void main() {
      vSpeed = speed;
      vec4 mvPosition = modelViewMatrix * vec4(position, 1.0);
      gl_Position = projectionMatrix * mvPosition;
      gl_PointSize = uSize * 1.5 * uDevicePixelRatio * (1.0 / -mvPosition.z);
    }
  `;
  
  const haloFragmentShader = `
    varying float vSpeed;
    uniform sampler2D uTexture;
    
    void main() {
      vec4 texColor = texture2D(uTexture, gl_PointCoord);
      
      // Color by speed with enhanced vibrancy
      float t = clamp(vSpeed * 1.5, 0.0, 1.0);
      vec3 cool = vec3(0.3, 0.7, 1.0);
      vec3 white = vec3(1.0, 1.0, 1.0);
      vec3 warm = vec3(1.0, 0.6, 0.2);
      
      vec3 color;
      if (t < 0.5) {
        color = mix(cool, white, t * 2.0);
      } else {
        color = mix(white, warm, (t - 0.5) * 2.0);
      }
      
      // Enhanced glow effect
      float glow = texColor.a * (0.8 + 0.4 * sin(vSpeed * 10.0));
      gl_FragColor = vec4(color * glow, glow * 0.7);
    }
  `;
  
  const haloTexture = createHaloTexture();
  const haloMaterial = new THREE.ShaderMaterial({
    uniforms: {
      uSize: { value: 25.0 },
      uDevicePixelRatio: { value: Math.min(window.devicePixelRatio, 2) },
      uTexture: { value: haloTexture }
    },
    vertexShader: haloVertexShader,
    fragmentShader: haloFragmentShader,
    blending: THREE.AdditiveBlending,
    depthWrite: false,
    transparent: true
  });
  
  // Create points with current material
  const currentMaterial = params.renderMode === 'halo' ? haloMaterial : shaderMaterial;
  points = new THREE.Points(geometry, currentMaterial);
  scene.add(points);
  
  // Store materials for switching
  points.userData = {
    shaderMaterial,
    haloMaterial
  };
}

// Switch render mode
function switchRenderMode() {
  if (!points || !points.userData) return;
  
  const newMaterial = params.renderMode === 'halo'
    ? points.userData.haloMaterial
    : points.userData.shaderMaterial;
    
  points.material = newMaterial;
}

// Initialize simulation
function init() {
  // Only reinitialize if needed (arrays already initialized in initThree)
  if (!x || x.length !== params.N) {
    initArrays(params.N);
    seedSystem();
  }
  
  // Initial force computation
  if (params.mode === 'barnes-hut') {
    computeForcesBH();
  } else {
    computeForcesDirect();
  }
  
  // Reset energy baseline
  E0 = null;
  time = 0;
  energyCount = 0;
  energyIndex = 0;
  energyHistory.fill(0);
}

// Reset simulation
function reset() {
  seedSystem();
  init();
  updateGeometry();
}

// Animation loop
function animate() {
  requestAnimationFrame(animate);
  
  if (params.playing) {
    step();
    updateGeometry();
    updateStatus();
    drawEnergyGraph();
  }
  
  controls.update();
  renderer.render(scene, camera);
}

// UI event handlers
document.getElementById('play-pause').addEventListener('click', () => {
  params.playing = !params.playing;
  document.getElementById('play-pause').textContent = params.playing ? 'Pause' : 'Play';
});

document.getElementById('reset').addEventListener('click', reset);

document.getElementById('reseed').addEventListener('click', () => {
  seedSystem();
  reset();
});

document.getElementById('mode').addEventListener('change', (e) => {
  params.mode = e.target.value;
});

document.getElementById('theta').addEventListener('input', (e) => {
  params.theta = parseFloat(e.target.value);
  document.getElementById('theta-value').textContent = params.theta.toFixed(1);
});

document.getElementById('dt').addEventListener('input', (e) => {
  params.dt = parseFloat(e.target.value);
  document.getElementById('dt-value').textContent = params.dt.toFixed(3);
});

document.getElementById('eps').addEventListener('input', (e) => {
  params.eps = parseFloat(e.target.value);
  document.getElementById('eps-value').textContent = params.eps.toFixed(3);
});

document.getElementById('npart').addEventListener('input', (e) => {
  const newN = parseInt(e.target.value);
  document.getElementById('npart-value').textContent = newN;
  
  if (newN !== params.N) {
    params.N = newN;
    initArrays(params.N);
    
    // Update geometry with new size
    geometry.setAttribute('position', new THREE.BufferAttribute(positions, 3).setUsage(THREE.DynamicDrawUsage));
    geometry.setAttribute('speed', new THREE.BufferAttribute(speeds, 1).setUsage(THREE.DynamicDrawUsage));
    
    reset();
  }
});

document.getElementById('mass').addEventListener('input', (e) => {
  params.mass = parseFloat(e.target.value);
  document.getElementById('mass-value').textContent = params.mass.toFixed(1) + '×';
  console.log('Mass parameter changed to:', params.mass);
  reset(); // Reset simulation with new mass scaling
});

// New stretch goal event handlers
document.getElementById('seed-type').addEventListener('change', (e) => {
  params.seedType = e.target.value;
  reset(); // Reseed with new configuration
});

document.getElementById('render-mode').addEventListener('change', (e) => {
  params.renderMode = e.target.value;
  switchRenderMode();
});

// Initialize display values
document.getElementById('mass-value').textContent = params.mass.toFixed(1) + '×';

// Start
initThree();
init();
updateGeometry();  // Populate geometry with initial particle positions
animate();
</script>]]></content><author><name></name></author><category term="physics" /><category term="simulation" /><category term="n-body" /><category term="barnes-hut" /><category term="galaxy" /><category term="gravitational" /><category term="astrophysics" /><summary type="html"><![CDATA[Explore the dynamics of gravitational systems with this interactive 3D simulation featuring both Barnes-Hut (O(N log N)) and direct-sum (O(N²)) methods.]]></summary></entry><entry><title type="html">Quaternions, SO(3), and Gimbal Lock — An Interactive Intuition</title><link href="https://saeed1262.github.io/blog/2024/quaternion/" rel="alternate" type="text/html" title="Quaternions, SO(3), and Gimbal Lock — An Interactive Intuition" /><published>2024-10-24T00:00:00+00:00</published><updated>2024-10-24T00:00:00+00:00</updated><id>https://saeed1262.github.io/blog/2024/quaternion</id><content type="html" xml:base="https://saeed1262.github.io/blog/2024/quaternion/"><![CDATA[<h1 id="quaternion-based-3d-rotation-from-theory-to-practice">Quaternion-Based 3D Rotation: From Theory to Practice</h1>

<p><strong>Prerequisites</strong>: Basic linear algebra (vectors, matrices), trigonometry, and familiarity with 3D coordinate systems.</p>

<p>3D rotations are everywhere in computer graphics, robotics, and game development. Whether you’re rotating a character in Unity, controlling a drone, or animating a 3D model, you need a robust way to represent and manipulate rotations. This post explores why the industry has largely moved from Euler angles to quaternions, combining mathematical rigor with practical examples.</p>

<h2 id="the-problem-why-rotations-are-tricky">The Problem: Why Rotations Are Tricky</h2>

<p>Representing 3D rotations might seem straightforward - just use three angles for yaw, pitch, and roll, right? Unfortunately, this intuitive approach runs into serious mathematical and practical problems that have plagued developers for decades.</p>

<h3 id="a-quick-refresher-what-are-rotations">A Quick Refresher: What Are Rotations?</h3>

<p>In 3D space, rotations form what mathematicians call the <strong>Special Orthogonal Group SO(3)</strong>. Don’t let the fancy name intimidate you - this simply means:</p>
<ul>
  <li><strong>Special</strong>: Determinant = 1 (proper rotations, no reflections)</li>
  <li><strong>Orthogonal</strong>: Preserve distances and angles</li>
  <li><strong>Group</strong>: You can combine rotations, and the result is still a rotation</li>
</ul>

<p>Think of SO(3) as the set of all possible 3D orientations an object can have. A cube can be rotated in countless ways, but it’s still the same cube - just oriented differently.</p>

<h2 id="the-euler-angle-problem-gimbal-lock-in-action">The Euler Angle Problem: Gimbal Lock in Action</h2>

<p>Before diving into quaternions, let’s understand why Euler angles cause problems. Euler angles represent rotations using three sequential rotations around different axes - typically yaw (Z), pitch (Y), and roll (X).</p>

<h3 id="real-world-example-aircraft-control">Real-World Example: Aircraft Control</h3>

<p>Imagine you’re programming a flight simulator or drone controller:</p>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Typical Euler angle representation</span>
<span class="kd">class</span> <span class="nc">EulerRotation</span> <span class="p">{</span>
    <span class="nf">constructor</span><span class="p">(</span><span class="nx">yaw</span> <span class="o">=</span> <span class="mi">0</span><span class="p">,</span> <span class="nx">pitch</span> <span class="o">=</span> <span class="mi">0</span><span class="p">,</span> <span class="nx">roll</span> <span class="o">=</span> <span class="mi">0</span><span class="p">)</span> <span class="p">{</span>
        <span class="k">this</span><span class="p">.</span><span class="nx">yaw</span> <span class="o">=</span> <span class="nx">yaw</span><span class="p">;</span>    <span class="c1">// Rotation around Z-axis (heading)</span>
        <span class="k">this</span><span class="p">.</span><span class="nx">pitch</span> <span class="o">=</span> <span class="nx">pitch</span><span class="p">;</span> <span class="c1">// Rotation around Y-axis (nose up/down)</span>
        <span class="k">this</span><span class="p">.</span><span class="nx">roll</span> <span class="o">=</span> <span class="nx">roll</span><span class="p">;</span>   <span class="c1">// Rotation around X-axis (bank left/right)</span>
    <span class="p">}</span>
    
    <span class="c1">// This seems simple... but problems lurk beneath</span>
    <span class="nf">toRotationMatrix</span><span class="p">()</span> <span class="p">{</span>
        <span class="kd">const</span> <span class="nx">cy</span> <span class="o">=</span> <span class="nb">Math</span><span class="p">.</span><span class="nf">cos</span><span class="p">(</span><span class="k">this</span><span class="p">.</span><span class="nx">yaw</span><span class="p">),</span> <span class="nx">sy</span> <span class="o">=</span> <span class="nb">Math</span><span class="p">.</span><span class="nf">sin</span><span class="p">(</span><span class="k">this</span><span class="p">.</span><span class="nx">yaw</span><span class="p">);</span>
        <span class="kd">const</span> <span class="nx">cp</span> <span class="o">=</span> <span class="nb">Math</span><span class="p">.</span><span class="nf">cos</span><span class="p">(</span><span class="k">this</span><span class="p">.</span><span class="nx">pitch</span><span class="p">),</span> <span class="nx">sp</span> <span class="o">=</span> <span class="nb">Math</span><span class="p">.</span><span class="nf">sin</span><span class="p">(</span><span class="k">this</span><span class="p">.</span><span class="nx">pitch</span><span class="p">);</span>
        <span class="kd">const</span> <span class="nx">cr</span> <span class="o">=</span> <span class="nb">Math</span><span class="p">.</span><span class="nf">cos</span><span class="p">(</span><span class="k">this</span><span class="p">.</span><span class="nx">roll</span><span class="p">),</span> <span class="nx">sr</span> <span class="o">=</span> <span class="nb">Math</span><span class="p">.</span><span class="nf">sin</span><span class="p">(</span><span class="k">this</span><span class="p">.</span><span class="nx">roll</span><span class="p">);</span>
        
        <span class="c1">// Combined rotation: R = R_z(yaw) * R_y(pitch) * R_x(roll)</span>
        <span class="k">return</span> <span class="p">[</span>
            <span class="p">[</span><span class="nx">cy</span><span class="o">*</span><span class="nx">cp</span><span class="p">,</span> <span class="nx">cy</span><span class="o">*</span><span class="nx">sp</span><span class="o">*</span><span class="nx">sr</span> <span class="o">-</span> <span class="nx">sy</span><span class="o">*</span><span class="nx">cr</span><span class="p">,</span> <span class="nx">cy</span><span class="o">*</span><span class="nx">sp</span><span class="o">*</span><span class="nx">cr</span> <span class="o">+</span> <span class="nx">sy</span><span class="o">*</span><span class="nx">sr</span><span class="p">],</span>
            <span class="p">[</span><span class="nx">sy</span><span class="o">*</span><span class="nx">cp</span><span class="p">,</span> <span class="nx">sy</span><span class="o">*</span><span class="nx">sp</span><span class="o">*</span><span class="nx">sr</span> <span class="o">+</span> <span class="nx">cy</span><span class="o">*</span><span class="nx">cr</span><span class="p">,</span> <span class="nx">sy</span><span class="o">*</span><span class="nx">sp</span><span class="o">*</span><span class="nx">cr</span> <span class="o">-</span> <span class="nx">cy</span><span class="o">*</span><span class="nx">sr</span><span class="p">],</span>
            <span class="p">[</span><span class="o">-</span><span class="nx">sp</span><span class="p">,</span>   <span class="nx">cp</span><span class="o">*</span><span class="nx">sr</span><span class="p">,</span>            <span class="nx">cp</span><span class="o">*</span><span class="nx">cr</span>           <span class="p">]</span>
        <span class="p">];</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<p>This looks clean and intuitive. The problem emerges when the pitch approaches ±90°.</p>

<h3 id="the-gimbal-lock-problem-explained">The Gimbal Lock Problem Explained</h3>

<p><strong>Gimbal lock</strong> occurs when two rotation axes become parallel, effectively losing one degree of freedom. Here’s what happens:</p>

<ol>
  <li><strong>Normal operation</strong>: All three axes are independent</li>
  <li><strong>Approaching ±90° pitch</strong>: The yaw and roll axes start to align</li>
  <li><strong>At ±90° pitch</strong>: Yaw and roll do the same thing - you lose control!</li>
</ol>

<p>This isn’t a coding bug - it’s a fundamental mathematical limitation called a <strong>singularity</strong>.</p>

<h3 id="why-this-matters-practical-consequences">Why This Matters: Practical Consequences</h3>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Example: Smooth rotation that breaks near gimbal lock</span>
<span class="kd">function</span> <span class="nf">interpolateEulerAngles</span><span class="p">(</span><span class="nx">start</span><span class="p">,</span> <span class="nx">end</span><span class="p">,</span> <span class="nx">t</span><span class="p">)</span> <span class="p">{</span>
    <span class="c1">// Linear interpolation - seems reasonable?</span>
    <span class="k">return</span> <span class="p">{</span>
        <span class="na">yaw</span><span class="p">:</span> <span class="nx">start</span><span class="p">.</span><span class="nx">yaw</span> <span class="o">+</span> <span class="p">(</span><span class="nx">end</span><span class="p">.</span><span class="nx">yaw</span> <span class="o">-</span> <span class="nx">start</span><span class="p">.</span><span class="nx">yaw</span><span class="p">)</span> <span class="o">*</span> <span class="nx">t</span><span class="p">,</span>
        <span class="na">pitch</span><span class="p">:</span> <span class="nx">start</span><span class="p">.</span><span class="nx">pitch</span> <span class="o">+</span> <span class="p">(</span><span class="nx">end</span><span class="p">.</span><span class="nx">pitch</span> <span class="o">-</span> <span class="nx">start</span><span class="p">.</span><span class="nx">pitch</span><span class="p">)</span> <span class="o">*</span> <span class="nx">t</span><span class="p">,</span>
        <span class="na">roll</span><span class="p">:</span> <span class="nx">start</span><span class="p">.</span><span class="nx">roll</span> <span class="o">+</span> <span class="p">(</span><span class="nx">end</span><span class="p">.</span><span class="nx">roll</span> <span class="o">-</span> <span class="nx">start</span><span class="p">.</span><span class="nx">roll</span><span class="p">)</span> <span class="o">*</span> <span class="nx">t</span>
    <span class="p">};</span>
<span class="p">}</span>

<span class="c1">// This causes problems:</span>
<span class="kd">const</span> <span class="k">from</span> <span class="o">=</span> <span class="p">{</span><span class="na">yaw</span><span class="p">:</span> <span class="mi">0</span><span class="p">,</span> <span class="na">pitch</span><span class="p">:</span> <span class="mi">89</span><span class="p">,</span> <span class="na">roll</span><span class="p">:</span> <span class="mi">0</span><span class="p">};</span>      <span class="c1">// Almost at singularity</span>
<span class="kd">const</span> <span class="nx">to</span> <span class="o">=</span> <span class="p">{</span><span class="na">yaw</span><span class="p">:</span> <span class="mi">180</span><span class="p">,</span> <span class="na">pitch</span><span class="p">:</span> <span class="mi">89</span><span class="p">,</span> <span class="na">roll</span><span class="p">:</span> <span class="mi">0</span><span class="p">};</span>      <span class="c1">// Still near singularity</span>
<span class="c1">// Result: Violent spinning as yaw "wraps around" at singularity!</span>
</code></pre></div></div>

<p><strong>Gimbal Lock Demonstration</strong>: In the interactive visualization below, observe how the Euler angle system breaks down. When pitch approaches ±90°, the yaw and roll axes become parallel, causing a loss of one degree of freedom.</p>

<p><strong>Try this</strong>: Set pitch to ±90° and notice how yaw and roll controls do the same thing!</p>

<div class="panel">
    <h3>Gimbal Rig</h3>
    <div class="canvas-container">
        <canvas id="gimbalCanvas"></canvas>
    </div>
    <div class="controls">
        <div class="control-group">
            <label><span style="color: #64ffda;">Yaw (Z-axis)</span> <span class="value-display" id="yawValue">0°</span></label>
            <input type="range" class="slider" id="yawSlider" min="-180" max="180" value="0" step="1" />
        </div>
        <div class="control-group">
            <label><span style="color: #4caf50;">Pitch (Y-axis)</span> <span class="value-display" id="pitchValue">0°</span></label>
            <input type="range" class="slider" id="pitchSlider" min="-90" max="90" value="0" step="1" />
        </div>
        <div class="control-group">
            <label><span style="color: #ff9800;">Roll (X-axis)</span> <span class="value-display" id="rollValue">0°</span></label>
            <input type="range" class="slider" id="rollSlider" min="-180" max="180" value="0" step="1" />
        </div>
    </div>
    <div class="warning" id="gimbalWarning">
        GIMBAL LOCK! Yaw and Roll axes are aligned - you've lost a degree of freedom!
    </div>
    <div class="info-box">
        <strong>Try this:</strong> Set pitch to ±90° and notice how yaw and roll controls do the same thing. This is gimbal lock - the curse of Euler angles!
    </div>
</div>

<p>The degeneracy occurs because Euler angles represent rotations as a composition of rotations about fixed axes: R = R_z(ψ)R_y(θ)R_x(φ), where the middle rotation R_y(θ) at θ = ±π/2 aligns the first and third rotation axes.</p>

<div class="panel">
    <h3>Quaternion Sphere (S³)</h3>
    <div class="canvas-container">
        <canvas id="quaternionCanvas"></canvas>
    </div>
    <div class="quaternion-display">
        <div class="quat-component">
            <div class="label">x</div>
            <div class="value" id="quatX">0.000</div>
        </div>
        <div class="quat-component">
            <div class="label">y</div>
            <div class="value" id="quatY">0.000</div>
        </div>
        <div class="quat-component">
            <div class="label">z</div>
            <div class="value" id="quatZ">0.000</div>
        </div>
        <div class="quat-component">
            <div class="label">w</div>
            <div class="value" id="quatW">1.000</div>
        </div>
    </div>
    <div class="controls">
        <div class="control-group">
            <label><span style="color: #64ffda;">Sphere Yaw</span> <span class="value-display" id="sphereYawValue">0°</span></label>
            <input type="range" class="slider" id="sphereYawSlider" min="-180" max="180" value="0" step="1" />
        </div>
        <div class="control-group">
            <label><span style="color: #4caf50;">Sphere Pitch</span> <span class="value-display" id="spherePitchValue">0°</span></label>
            <input type="range" class="slider" id="spherePitchSlider" min="-90" max="90" value="0" step="1" />
        </div>
        <div class="control-group">
            <label><span style="color: #ff9800;">Sphere Roll</span> <span class="value-display" id="sphereRollValue">0°</span></label>
            <input type="range" class="slider" id="sphereRollSlider" min="-180" max="180" value="0" step="1" />
        </div>
    </div>
    <div class="controls">
        <div class="control-group">
            <label><span style="color: #e91e63;">Quaternion X</span> <span class="value-display" id="quatXValue">0.000</span></label>
            <input type="range" class="slider" id="quatXSlider" min="-1" max="1" value="0" step="0.001" />
        </div>
        <div class="control-group">
            <label><span style="color: #9c27b0;">Quaternion Y</span> <span class="value-display" id="quatYValue">0.000</span></label>
            <input type="range" class="slider" id="quatYSlider" min="-1" max="1" value="0" step="0.001" />
        </div>
        <div class="control-group">
            <label><span style="color: #3f51b5;">Quaternion Z</span> <span class="value-display" id="quatZValue">0.000</span></label>
            <input type="range" class="slider" id="quatZSlider" min="-1" max="1" value="0" step="0.001" />
        </div>
        <div class="control-group">
            <label><span style="color: #009688;">Quaternion W</span> <span class="value-display" id="quatWValue">1.000</span></label>
            <input type="range" class="slider" id="quatWSlider" min="-1" max="1" value="1" step="0.001" />
        </div>
    </div>
    <div class="controls">
        <button class="button" id="showQuaternionFeatures">Show Antipodal Points &amp; Geodesic Arc</button>
    </div>
    <div class="info-box">
        <strong>Independent Controls:</strong> This quaternion sphere has its own Euler angle and quaternion component sliders. Changes in one automatically update the other while maintaining unit length through automatic normalization. All updates are real-time and bidirectional.
    </div>
</div>

<h2 id="the-hopf-fibration-understanding-quaternion-geometry">The Hopf Fibration: Understanding Quaternion Geometry</h2>

<p>Before diving into the quaternion solution, it’s worth understanding one of the most beautiful mathematical structures that explains <em>why</em> quaternions work so elegantly: the <strong>Hopf fibration</strong>.</p>

<h3 id="what-is-the-hopf-fibration">What Is the Hopf Fibration?</h3>

<p>The Hopf fibration is a mathematical mapping discovered by Heinz Hopf in 1931 that reveals the deep geometric structure underlying quaternions. Think of it as a way of organizing the 4D quaternion space that makes 3D rotations naturally emerge.</p>

<p><strong>The Basic Setup:</strong></p>
<ul>
  <li><strong>Total Space</strong>: The 3-sphere (S³) - where unit quaternions live</li>
  <li><strong>Base Space</strong>: The 2-sphere (S²) - representing all possible 3D rotation axes</li>
  <li><strong>Fiber</strong>: Circles (S¹) - representing rotations around each axis</li>
</ul>

<h3 id="the-intuitive-picture">The Intuitive Picture</h3>

<p>Imagine you’re holding a globe (the 2-sphere S²). Each point on this globe represents a possible rotation axis in 3D space - north pole might be the Z-axis, equator points represent X-Y plane axes, and so on.</p>

<p>Now, for each point on this globe, imagine a circle floating above it in 4D space. This circle represents all the different amounts you can rotate around that particular axis - 0°, 90°, 180°, 270°, back to 0°. These circles are the “fibers” of the Hopf fibration.</p>

<p>The remarkable thing is that these circles never intersect, even though they fill up the entire 4D space of the 3-sphere. It’s like having infinite circles, each dedicated to one rotation axis, perfectly organized in 4D space without any collisions.</p>

<h3 id="why-this-matters-for-quaternions">Why This Matters for Quaternions</h3>

<p>This structure explains several key properties of quaternions:</p>

<p><strong>1. No Singularities</strong>: Unlike Euler angles, which break down at certain orientations (gimbal lock), the Hopf fibration shows that every possible 3D rotation corresponds to a smooth path on the 3-sphere. There are no “holes” or “edges” where the math breaks down.</p>

<p><strong>2. Double Coverage</strong>: Each 3D rotation corresponds to exactly two antipodal points on S³ (q and -q represent the same rotation). The Hopf fibration reveals this as a natural consequence of the fiber structure - each fiber maps to the same rotation axis, but parameterized twice around the circle.</p>

<p><strong>3. Smooth Interpolation</strong>: SLERP works perfectly because it follows great circle arcs on S³, which project down to the shortest rotation paths on the rotation group SO(3). The Hopf fibration guarantees these paths exist and are unique.</p>

<h3 id="the-mathematical-beauty">The Mathematical Beauty</h3>

<table>
  <tbody>
    <tr>
      <td>The Hopf fibration can be written elegantly using complex numbers. If we represent a unit quaternion as two complex numbers (z₁, z₂) where</td>
      <td>z₁</td>
      <td>² +</td>
      <td>z₂</td>
      <td>² = 1, then the Hopf map is:</td>
    </tr>
  </tbody>
</table>

<table>
  <tbody>
    <tr>
      <td><strong>Hopf map</strong>: (z₁, z₂) → (z₁z̄₂,</td>
      <td>z₁</td>
      <td>² -</td>
      <td>z₂</td>
      <td>²)</td>
    </tr>
  </tbody>
</table>

<p>This simple formula encodes the entire relationship between 4D quaternion space and 3D rotation space. The first component gives the rotation axis, while the second component relates to the rotation angle.</p>

<h3 id="real-world-implications">Real-World Implications</h3>

<p>Understanding the Hopf fibration helps explain why:</p>
<ul>
  <li><strong>Flight simulators</strong> use quaternions internally, even if they display Euler angles to pilots</li>
  <li><strong>Robot control systems</strong> can achieve smoother, more predictable motion with quaternions</li>
  <li><strong>3D animation software</strong> can avoid gimbal lock artifacts in character rigs</li>
  <li><strong>Spacecraft attitude control</strong> becomes more reliable and efficient</li>
</ul>

<p>The Hopf fibration isn’t just abstract mathematics - it’s the geometric reason why quaternions provide such a robust, singularity-free way to handle 3D rotations in practice.</p>

<div class="panel">
    <h3>Hopf Fibration: The True S³ → S² Mapping</h3>
    <div class="canvas-container" style="height: 500px;">
        <canvas id="hopfCanvas"></canvas>
    </div>
    <div class="hopf-info-panel">
        <h4>Mathematical Structure</h4>
        <p><strong>Base Space:</strong> 2-sphere (S²)</p>
        <p><strong>Total Space:</strong> 3-sphere (S³)</p>
        <p><strong>Fiber:</strong> Circle (S¹)</p>
        <p>Each point on S² corresponds to a circle on S³. The visualization shows fibers as colored curves using stereographic projection from the 4D quaternion sphere.</p>
    </div>
    <div class="controls">
        <div class="control-group">
            <label>Rotation Speed <span class="value-display" id="hopfRotationSpeedValue">0.5</span></label>
            <input type="range" class="slider" id="hopfRotationSpeed" min="0" max="2" value="0.5" step="0.1" />
        </div>
        <div class="control-group">
            <label>Fiber Count <span class="value-display" id="hopfFiberCountValue">16</span></label>
            <input type="range" class="slider" id="hopfFiberCount" min="8" max="32" value="16" step="2" />
        </div>
        <div class="control-group">
            <label>Animation Phase <span class="value-display" id="hopfAnimationPhaseValue">0.00</span></label>
            <input type="range" class="slider" id="hopfAnimationPhase" min="0" max="6.28" value="0" step="0.1" />
        </div>
        <div class="control-group">
            <label>Base Sphere Size <span class="value-display" id="hopfBaseSphereValue">1.0</span></label>
            <input type="range" class="slider" id="hopfBaseSphere" min="0.5" max="2" value="1" step="0.1" />
        </div>
    </div>
    <div class="controls">
        <button class="button" id="hopfToggleAnimation">Pause</button>
        <button class="button" id="hopfResetView">Reset View</button>
    </div>
    <div class="info-box">
        <strong>Advanced Hopf Fibration:</strong> This visualization uses proper complex number representation and stereographic projection to show the true S³ → S² → S¹ fiber bundle structure. Each colored fiber represents a great circle on the 4D quaternion sphere that projects to a single point on the 2D base sphere. The mathematical elegance of this structure is what makes quaternions so powerful for representing rotations!
    </div>
</div>

<h2 id="enter-quaternions-a-better-way-to-rotate">Enter Quaternions: A Better Way to Rotate</h2>

<p>Quaternions might seem mysterious at first, but they’re actually quite elegant once you understand the core idea. Think of them as an extension of complex numbers to 3D rotations.</p>

<h3 id="what-is-a-quaternion">What Is a Quaternion?</h3>

<p>A quaternion is a 4-component number: <strong>q = (x, y, z, w)</strong> where:</p>
<ul>
  <li><strong>(x, y, z)</strong>: The rotation axis (like a vector pointing through the object)</li>
  <li><strong>w</strong>: Related to the rotation angle (cosine of half-angle)</li>
</ul>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Quaternion class - much more stable than Euler angles</span>
<span class="kd">class</span> <span class="nc">Quaternion</span> <span class="p">{</span>
    <span class="nf">constructor</span><span class="p">(</span><span class="nx">x</span> <span class="o">=</span> <span class="mi">0</span><span class="p">,</span> <span class="nx">y</span> <span class="o">=</span> <span class="mi">0</span><span class="p">,</span> <span class="nx">z</span> <span class="o">=</span> <span class="mi">0</span><span class="p">,</span> <span class="nx">w</span> <span class="o">=</span> <span class="mi">1</span><span class="p">)</span> <span class="p">{</span>
        <span class="k">this</span><span class="p">.</span><span class="nx">x</span> <span class="o">=</span> <span class="nx">x</span><span class="p">;</span> <span class="k">this</span><span class="p">.</span><span class="nx">y</span> <span class="o">=</span> <span class="nx">y</span><span class="p">;</span> <span class="k">this</span><span class="p">.</span><span class="nx">z</span> <span class="o">=</span> <span class="nx">z</span><span class="p">;</span> <span class="k">this</span><span class="p">.</span><span class="nx">w</span> <span class="o">=</span> <span class="nx">w</span><span class="p">;</span>
    <span class="p">}</span>
    
    <span class="c1">// Create quaternion from axis-angle (most intuitive)</span>
    <span class="kd">static</span> <span class="nf">fromAxisAngle</span><span class="p">(</span><span class="nx">axis</span><span class="p">,</span> <span class="nx">angle</span><span class="p">)</span> <span class="p">{</span>
        <span class="kd">const</span> <span class="nx">halfAngle</span> <span class="o">=</span> <span class="nx">angle</span> <span class="o">*</span> <span class="mf">0.5</span><span class="p">;</span>
        <span class="kd">const</span> <span class="nx">sin</span> <span class="o">=</span> <span class="nb">Math</span><span class="p">.</span><span class="nf">sin</span><span class="p">(</span><span class="nx">halfAngle</span><span class="p">);</span>
        <span class="kd">const</span> <span class="nx">cos</span> <span class="o">=</span> <span class="nb">Math</span><span class="p">.</span><span class="nf">cos</span><span class="p">(</span><span class="nx">halfAngle</span><span class="p">);</span>
        
        <span class="k">return</span> <span class="k">new</span> <span class="nc">Quaternion</span><span class="p">(</span>
            <span class="nx">axis</span><span class="p">.</span><span class="nx">x</span> <span class="o">*</span> <span class="nx">sin</span><span class="p">,</span>
            <span class="nx">axis</span><span class="p">.</span><span class="nx">y</span> <span class="o">*</span> <span class="nx">sin</span><span class="p">,</span>
            <span class="nx">axis</span><span class="p">.</span><span class="nx">z</span> <span class="o">*</span> <span class="nx">sin</span><span class="p">,</span>
            <span class="nx">cos</span>
        <span class="p">);</span>
    <span class="p">}</span>
    
    <span class="c1">// The magic: no gimbal lock!</span>
    <span class="kd">static</span> <span class="nf">fromEuler</span><span class="p">(</span><span class="nx">yaw</span><span class="p">,</span> <span class="nx">pitch</span><span class="p">,</span> <span class="nx">roll</span><span class="p">)</span> <span class="p">{</span>
        <span class="kd">const</span> <span class="nx">cy</span> <span class="o">=</span> <span class="nb">Math</span><span class="p">.</span><span class="nf">cos</span><span class="p">(</span><span class="nx">yaw</span> <span class="o">*</span> <span class="mf">0.5</span><span class="p">);</span>
        <span class="kd">const</span> <span class="nx">sy</span> <span class="o">=</span> <span class="nb">Math</span><span class="p">.</span><span class="nf">sin</span><span class="p">(</span><span class="nx">yaw</span> <span class="o">*</span> <span class="mf">0.5</span><span class="p">);</span>
        <span class="kd">const</span> <span class="nx">cp</span> <span class="o">=</span> <span class="nb">Math</span><span class="p">.</span><span class="nf">cos</span><span class="p">(</span><span class="nx">pitch</span> <span class="o">*</span> <span class="mf">0.5</span><span class="p">);</span>
        <span class="kd">const</span> <span class="nx">sp</span> <span class="o">=</span> <span class="nb">Math</span><span class="p">.</span><span class="nf">sin</span><span class="p">(</span><span class="nx">pitch</span> <span class="o">*</span> <span class="mf">0.5</span><span class="p">);</span>
        <span class="kd">const</span> <span class="nx">cr</span> <span class="o">=</span> <span class="nb">Math</span><span class="p">.</span><span class="nf">cos</span><span class="p">(</span><span class="nx">roll</span> <span class="o">*</span> <span class="mf">0.5</span><span class="p">);</span>
        <span class="kd">const</span> <span class="nx">sr</span> <span class="o">=</span> <span class="nb">Math</span><span class="p">.</span><span class="nf">sin</span><span class="p">(</span><span class="nx">roll</span> <span class="o">*</span> <span class="mf">0.5</span><span class="p">);</span>

        <span class="k">return</span> <span class="k">new</span> <span class="nc">Quaternion</span><span class="p">(</span>
            <span class="nx">sr</span> <span class="o">*</span> <span class="nx">cp</span> <span class="o">*</span> <span class="nx">cy</span> <span class="o">-</span> <span class="nx">cr</span> <span class="o">*</span> <span class="nx">sp</span> <span class="o">*</span> <span class="nx">sy</span><span class="p">,</span>
            <span class="nx">cr</span> <span class="o">*</span> <span class="nx">sp</span> <span class="o">*</span> <span class="nx">cy</span> <span class="o">+</span> <span class="nx">sr</span> <span class="o">*</span> <span class="nx">cp</span> <span class="o">*</span> <span class="nx">sy</span><span class="p">,</span>
            <span class="nx">cr</span> <span class="o">*</span> <span class="nx">cp</span> <span class="o">*</span> <span class="nx">sy</span> <span class="o">-</span> <span class="nx">sr</span> <span class="o">*</span> <span class="nx">sp</span> <span class="o">*</span> <span class="nx">cy</span><span class="p">,</span>
            <span class="nx">cr</span> <span class="o">*</span> <span class="nx">cp</span> <span class="o">*</span> <span class="nx">cy</span> <span class="o">+</span> <span class="nx">sr</span> <span class="o">*</span> <span class="nx">sp</span> <span class="o">*</span> <span class="nx">sy</span>
        <span class="p">);</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="why-quaternions-work-the-key-insight">Why Quaternions Work: The Key Insight</h3>

<p>Quaternions live on a <strong>4D unit sphere</strong> (mathematically called S³). This extra dimension is what eliminates gimbal lock! Here’s the crucial insight:</p>

<ul>
  <li><strong>Euler angles</strong>: 3 parameters trying to represent 3D rotations → singularities inevitable</li>
  <li><strong>Quaternions</strong>: 4 parameters (with 1 constraint: unit length) → no singularities!</li>
</ul>

<p>Think of it like this: imagine trying to wrap a globe with flat paper without wrinkles - impossible! But if you allow the paper to stretch into 3D, it becomes easy. Similarly, that extra quaternion dimension eliminates the “wrinkles” (singularities) in rotation space.</p>

<h3 id="the-double-cover-property">The Double Cover Property</h3>

<p>Here’s something fascinating: both <strong>q</strong> and <strong>-q</strong> represent the same rotation! This “double coverage” might seem redundant, but it’s actually a feature:</p>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Both of these represent the same rotation:</span>
<span class="kd">const</span> <span class="nx">q1</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Quaternion</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="mi">1</span><span class="p">);</span>    <span class="c1">// Identity rotation</span>
<span class="kd">const</span> <span class="nx">q2</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Quaternion</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="o">-</span><span class="mi">1</span><span class="p">);</span>   <span class="c1">// Same rotation!</span>

<span class="c1">// This redundancy eliminates singularities</span>
<span class="kd">function</span> <span class="nf">ensureContinuity</span><span class="p">(</span><span class="nx">currentQ</span><span class="p">,</span> <span class="nx">previousQ</span><span class="p">)</span> <span class="p">{</span>
    <span class="c1">// Choose the closer of q or -q to maintain smooth motion</span>
    <span class="kd">const</span> <span class="nx">dot1</span> <span class="o">=</span> <span class="nf">dotProduct</span><span class="p">(</span><span class="nx">currentQ</span><span class="p">,</span> <span class="nx">previousQ</span><span class="p">);</span>
    <span class="kd">const</span> <span class="nx">dot2</span> <span class="o">=</span> <span class="nf">dotProduct</span><span class="p">(</span><span class="nf">negateQuaternion</span><span class="p">(</span><span class="nx">currentQ</span><span class="p">),</span> <span class="nx">previousQ</span><span class="p">);</span>
    
    <span class="k">return</span> <span class="nb">Math</span><span class="p">.</span><span class="nf">abs</span><span class="p">(</span><span class="nx">dot2</span><span class="p">)</span> <span class="o">&gt;</span> <span class="nb">Math</span><span class="p">.</span><span class="nf">abs</span><span class="p">(</span><span class="nx">dot1</span><span class="p">)</span> <span class="p">?</span>
           <span class="nf">negateQuaternion</span><span class="p">(</span><span class="nx">currentQ</span><span class="p">)</span> <span class="p">:</span> <span class="nx">currentQ</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="real-world-applications">Real-World Applications</h3>

<p><strong>Unity Game Engine Example:</strong></p>
<div class="language-csharp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Unity uses quaternions internally for all rotations</span>
<span class="n">Transform</span> <span class="n">player</span><span class="p">;</span>

<span class="c1">// Smooth rotation without gimbal lock</span>
<span class="n">Quaternion</span> <span class="n">targetRotation</span> <span class="p">=</span> <span class="n">Quaternion</span><span class="p">.</span><span class="nf">LookRotation</span><span class="p">(</span><span class="n">direction</span><span class="p">);</span>
<span class="n">player</span><span class="p">.</span><span class="n">rotation</span> <span class="p">=</span> <span class="n">Quaternion</span><span class="p">.</span><span class="nf">Slerp</span><span class="p">(</span><span class="n">player</span><span class="p">.</span><span class="n">rotation</span><span class="p">,</span> <span class="n">targetRotation</span><span class="p">,</span> <span class="n">Time</span><span class="p">.</span><span class="n">deltaTime</span> <span class="p">*</span> <span class="n">rotateSpeed</span><span class="p">);</span>

<span class="c1">// This just works - no special cases needed!</span>
</code></pre></div></div>

<p><strong>Robotics Example:</strong></p>
<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Robot arm joint control using quaternions</span>
<span class="k">class</span> <span class="nc">RobotJoint</span> <span class="p">{</span>
    <span class="n">Quaternion</span> <span class="n">currentOrientation</span><span class="p">;</span>
    <span class="n">Quaternion</span> <span class="n">targetOrientation</span><span class="p">;</span>
    
    <span class="kt">void</span> <span class="n">updateJoint</span><span class="p">(</span><span class="kt">float</span> <span class="n">deltaTime</span><span class="p">)</span> <span class="p">{</span>
        <span class="c1">// SLERP provides smooth, constant-speed rotation</span>
        <span class="n">currentOrientation</span> <span class="o">=</span> <span class="n">slerp</span><span class="p">(</span><span class="n">currentOrientation</span><span class="p">,</span>
                                 <span class="n">targetOrientation</span><span class="p">,</span>
                                 <span class="n">deltaTime</span> <span class="o">*</span> <span class="n">jointSpeed</span><span class="p">);</span>
        
        <span class="c1">// Convert to motor control signals</span>
        <span class="n">applyJointRotation</span><span class="p">(</span><span class="n">currentOrientation</span><span class="p">.</span><span class="n">toRotationMatrix</span><span class="p">());</span>
    <span class="p">}</span>
<span class="p">};</span>
</code></pre></div></div>

<p><strong>The Mathematics Behind the Magic:</strong></p>

<p>The quaternion sphere visualization above demonstrates this double coverage. The antipodal points ±q on the 4D sphere both map to the same rotation matrix, providing redundancy that eliminates singular configurations. This is why quaternions can represent any 3D rotation smoothly - they live in a higher-dimensional space that “unwraps” the problematic topology of 3D rotation space.</p>

<p><strong>For the mathematically inclined:</strong> Quaternions form a <strong>Lie group</strong> isomorphic to SU(2), which double-covers SO(3). The <strong>hairy ball theorem</strong> proves that any 3-parameter system must have singularities, but quaternions sidestep this by using 4 parameters with a constraint.</p>

<h2 id="slerp-vs-lerp-the-battle-of-interpolation-methods">SLERP vs LERP: The Battle of Interpolation Methods</h2>

<p>When animating rotations, you need to smoothly transition from one orientation to another. This is where <strong>SLERP</strong> (Spherical Linear Interpolation) shines compared to simple linear interpolation of Euler angles.</p>

<h3 id="the-problem-with-euler-interpolation">The Problem with Euler Interpolation</h3>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Naive Euler angle interpolation - DON'T DO THIS!</span>
<span class="kd">function</span> <span class="nf">badRotationInterp</span><span class="p">(</span><span class="nx">startEuler</span><span class="p">,</span> <span class="nx">endEuler</span><span class="p">,</span> <span class="nx">t</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">return</span> <span class="p">{</span>
        <span class="na">yaw</span><span class="p">:</span> <span class="nx">startEuler</span><span class="p">.</span><span class="nx">yaw</span> <span class="o">+</span> <span class="p">(</span><span class="nx">endEuler</span><span class="p">.</span><span class="nx">yaw</span> <span class="o">-</span> <span class="nx">startEuler</span><span class="p">.</span><span class="nx">yaw</span><span class="p">)</span> <span class="o">*</span> <span class="nx">t</span><span class="p">,</span>
        <span class="na">pitch</span><span class="p">:</span> <span class="nx">startEuler</span><span class="p">.</span><span class="nx">pitch</span> <span class="o">+</span> <span class="p">(</span><span class="nx">endEuler</span><span class="p">.</span><span class="nx">pitch</span> <span class="o">-</span> <span class="nx">startEuler</span><span class="p">.</span><span class="nx">pitch</span><span class="p">)</span> <span class="o">*</span> <span class="nx">t</span><span class="p">,</span>
        <span class="na">roll</span><span class="p">:</span> <span class="nx">startEuler</span><span class="p">.</span><span class="nx">roll</span> <span class="o">+</span> <span class="p">(</span><span class="nx">endEuler</span><span class="p">.</span><span class="nx">roll</span> <span class="o">-</span> <span class="nx">startEuler</span><span class="p">.</span><span class="nx">roll</span><span class="p">)</span> <span class="o">*</span> <span class="nx">t</span>
    <span class="p">};</span>
<span class="p">}</span>

<span class="c1">// Why this fails:</span>
<span class="kd">const</span> <span class="k">from</span> <span class="o">=</span> <span class="p">{</span><span class="na">yaw</span><span class="p">:</span> <span class="mi">0</span><span class="p">,</span> <span class="na">pitch</span><span class="p">:</span> <span class="mi">0</span><span class="p">,</span> <span class="na">roll</span><span class="p">:</span> <span class="mi">0</span><span class="p">};</span>
<span class="kd">const</span> <span class="nx">to</span> <span class="o">=</span> <span class="p">{</span><span class="na">yaw</span><span class="p">:</span> <span class="mi">180</span><span class="p">,</span> <span class="na">pitch</span><span class="p">:</span> <span class="mi">0</span><span class="p">,</span> <span class="na">roll</span><span class="p">:</span> <span class="mi">0</span><span class="p">};</span>
<span class="c1">// Linear interpolation spins through intermediate orientations</span>
<span class="c1">// that aren't on the shortest rotation path!</span>
</code></pre></div></div>

<h3 id="slerp-the-right-way">SLERP: The Right Way</h3>

<p>SLERP finds the shortest path between two orientations and moves along it at constant angular speed:</p>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Proper quaternion SLERP implementation</span>
<span class="kd">function</span> <span class="nf">slerp</span><span class="p">(</span><span class="nx">q1</span><span class="p">,</span> <span class="nx">q2</span><span class="p">,</span> <span class="nx">t</span><span class="p">)</span> <span class="p">{</span>
    <span class="c1">// Ensure we take the shorter path (handle double cover)</span>
    <span class="kd">let</span> <span class="nx">dot</span> <span class="o">=</span> <span class="nx">q1</span><span class="p">.</span><span class="nx">x</span><span class="o">*</span><span class="nx">q2</span><span class="p">.</span><span class="nx">x</span> <span class="o">+</span> <span class="nx">q1</span><span class="p">.</span><span class="nx">y</span><span class="o">*</span><span class="nx">q2</span><span class="p">.</span><span class="nx">y</span> <span class="o">+</span> <span class="nx">q1</span><span class="p">.</span><span class="nx">z</span><span class="o">*</span><span class="nx">q2</span><span class="p">.</span><span class="nx">z</span> <span class="o">+</span> <span class="nx">q1</span><span class="p">.</span><span class="nx">w</span><span class="o">*</span><span class="nx">q2</span><span class="p">.</span><span class="nx">w</span><span class="p">;</span>
    
    <span class="c1">// If dot &lt; 0, use -q2 to ensure shorter path</span>
    <span class="k">if </span><span class="p">(</span><span class="nx">dot</span> <span class="o">&lt;</span> <span class="mi">0</span><span class="p">)</span> <span class="p">{</span>
        <span class="nx">q2</span> <span class="o">=</span> <span class="p">{</span><span class="na">x</span><span class="p">:</span> <span class="o">-</span><span class="nx">q2</span><span class="p">.</span><span class="nx">x</span><span class="p">,</span> <span class="na">y</span><span class="p">:</span> <span class="o">-</span><span class="nx">q2</span><span class="p">.</span><span class="nx">y</span><span class="p">,</span> <span class="na">z</span><span class="p">:</span> <span class="o">-</span><span class="nx">q2</span><span class="p">.</span><span class="nx">z</span><span class="p">,</span> <span class="na">w</span><span class="p">:</span> <span class="o">-</span><span class="nx">q2</span><span class="p">.</span><span class="nx">w</span><span class="p">};</span>
        <span class="nx">dot</span> <span class="o">=</span> <span class="o">-</span><span class="nx">dot</span><span class="p">;</span>
    <span class="p">}</span>
    
    <span class="c1">// Handle nearly parallel quaternions</span>
    <span class="k">if </span><span class="p">(</span><span class="nx">dot</span> <span class="o">&gt;</span> <span class="mf">0.9995</span><span class="p">)</span> <span class="p">{</span>
        <span class="c1">// Use linear interpolation for numerical stability</span>
        <span class="kd">const</span> <span class="nx">result</span> <span class="o">=</span> <span class="p">{</span>
            <span class="na">x</span><span class="p">:</span> <span class="nx">q1</span><span class="p">.</span><span class="nx">x</span> <span class="o">+</span> <span class="nx">t</span> <span class="o">*</span> <span class="p">(</span><span class="nx">q2</span><span class="p">.</span><span class="nx">x</span> <span class="o">-</span> <span class="nx">q1</span><span class="p">.</span><span class="nx">x</span><span class="p">),</span>
            <span class="na">y</span><span class="p">:</span> <span class="nx">q1</span><span class="p">.</span><span class="nx">y</span> <span class="o">+</span> <span class="nx">t</span> <span class="o">*</span> <span class="p">(</span><span class="nx">q2</span><span class="p">.</span><span class="nx">y</span> <span class="o">-</span> <span class="nx">q1</span><span class="p">.</span><span class="nx">y</span><span class="p">),</span>
            <span class="na">z</span><span class="p">:</span> <span class="nx">q1</span><span class="p">.</span><span class="nx">z</span> <span class="o">+</span> <span class="nx">t</span> <span class="o">*</span> <span class="p">(</span><span class="nx">q2</span><span class="p">.</span><span class="nx">z</span> <span class="o">-</span> <span class="nx">q1</span><span class="p">.</span><span class="nx">z</span><span class="p">),</span>
            <span class="na">w</span><span class="p">:</span> <span class="nx">q1</span><span class="p">.</span><span class="nx">w</span> <span class="o">+</span> <span class="nx">t</span> <span class="o">*</span> <span class="p">(</span><span class="nx">q2</span><span class="p">.</span><span class="nx">w</span> <span class="o">-</span> <span class="nx">q1</span><span class="p">.</span><span class="nx">w</span><span class="p">)</span>
        <span class="p">};</span>
        <span class="k">return</span> <span class="nf">normalize</span><span class="p">(</span><span class="nx">result</span><span class="p">);</span>
    <span class="p">}</span>
    
    <span class="c1">// Standard SLERP formula</span>
    <span class="kd">const</span> <span class="nx">theta</span> <span class="o">=</span> <span class="nb">Math</span><span class="p">.</span><span class="nf">acos</span><span class="p">(</span><span class="nb">Math</span><span class="p">.</span><span class="nf">abs</span><span class="p">(</span><span class="nx">dot</span><span class="p">));</span>
    <span class="kd">const</span> <span class="nx">sinTheta</span> <span class="o">=</span> <span class="nb">Math</span><span class="p">.</span><span class="nf">sin</span><span class="p">(</span><span class="nx">theta</span><span class="p">);</span>
    <span class="kd">const</span> <span class="nx">scale1</span> <span class="o">=</span> <span class="nb">Math</span><span class="p">.</span><span class="nf">sin</span><span class="p">((</span><span class="mi">1</span><span class="o">-</span><span class="nx">t</span><span class="p">)</span> <span class="o">*</span> <span class="nx">theta</span><span class="p">)</span> <span class="o">/</span> <span class="nx">sinTheta</span><span class="p">;</span>
    <span class="kd">const</span> <span class="nx">scale2</span> <span class="o">=</span> <span class="nb">Math</span><span class="p">.</span><span class="nf">sin</span><span class="p">(</span><span class="nx">t</span> <span class="o">*</span> <span class="nx">theta</span><span class="p">)</span> <span class="o">/</span> <span class="nx">sinTheta</span><span class="p">;</span>
    
    <span class="k">return</span> <span class="p">{</span>
        <span class="na">x</span><span class="p">:</span> <span class="nx">scale1</span> <span class="o">*</span> <span class="nx">q1</span><span class="p">.</span><span class="nx">x</span> <span class="o">+</span> <span class="nx">scale2</span> <span class="o">*</span> <span class="nx">q2</span><span class="p">.</span><span class="nx">x</span><span class="p">,</span>
        <span class="na">y</span><span class="p">:</span> <span class="nx">scale1</span> <span class="o">*</span> <span class="nx">q1</span><span class="p">.</span><span class="nx">y</span> <span class="o">+</span> <span class="nx">scale2</span> <span class="o">*</span> <span class="nx">q2</span><span class="p">.</span><span class="nx">y</span><span class="p">,</span>
        <span class="na">z</span><span class="p">:</span> <span class="nx">scale1</span> <span class="o">*</span> <span class="nx">q1</span><span class="p">.</span><span class="nx">z</span> <span class="o">+</span> <span class="nx">scale2</span> <span class="o">*</span> <span class="nx">q2</span><span class="p">.</span><span class="nx">z</span><span class="p">,</span>
        <span class="na">w</span><span class="p">:</span> <span class="nx">scale1</span> <span class="o">*</span> <span class="nx">q1</span><span class="p">.</span><span class="nx">w</span> <span class="o">+</span> <span class="nx">scale2</span> <span class="o">*</span> <span class="nx">q2</span><span class="p">.</span><span class="nx">w</span>
    <span class="p">};</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="real-world-application-examples">Real-World Application Examples</h3>

<p><strong>Camera Movement in Games:</strong></p>
<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Smooth camera transitions using SLERP</span>
<span class="kd">class</span> <span class="nc">Camera</span> <span class="p">{</span>
    <span class="nf">constructor</span><span class="p">()</span> <span class="p">{</span>
        <span class="k">this</span><span class="p">.</span><span class="nx">orientation</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Quaternion</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="mi">1</span><span class="p">);</span>
        <span class="k">this</span><span class="p">.</span><span class="nx">targetOrientation</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Quaternion</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="mi">1</span><span class="p">);</span>
    <span class="p">}</span>
    
    <span class="nf">lookAt</span><span class="p">(</span><span class="nx">target</span><span class="p">)</span> <span class="p">{</span>
        <span class="c1">// Calculate desired orientation</span>
        <span class="kd">const</span> <span class="nx">direction</span> <span class="o">=</span> <span class="nx">Vector3</span><span class="p">.</span><span class="nf">subtract</span><span class="p">(</span><span class="nx">target</span><span class="p">,</span> <span class="k">this</span><span class="p">.</span><span class="nx">position</span><span class="p">);</span>
        <span class="k">this</span><span class="p">.</span><span class="nx">targetOrientation</span> <span class="o">=</span> <span class="nx">Quaternion</span><span class="p">.</span><span class="nf">lookRotation</span><span class="p">(</span><span class="nx">direction</span><span class="p">);</span>
    <span class="p">}</span>
    
    <span class="nf">update</span><span class="p">(</span><span class="nx">deltaTime</span><span class="p">)</span> <span class="p">{</span>
        <span class="c1">// Smooth interpolation - no jumpy camera movement!</span>
        <span class="kd">const</span> <span class="nx">speed</span> <span class="o">=</span> <span class="mf">2.0</span><span class="p">;</span> <span class="c1">// rotation speed</span>
        <span class="k">this</span><span class="p">.</span><span class="nx">orientation</span> <span class="o">=</span> <span class="nf">slerp</span><span class="p">(</span><span class="k">this</span><span class="p">.</span><span class="nx">orientation</span><span class="p">,</span>
                                <span class="k">this</span><span class="p">.</span><span class="nx">targetOrientation</span><span class="p">,</span>
                                <span class="nx">deltaTime</span> <span class="o">*</span> <span class="nx">speed</span><span class="p">);</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<p><strong>Robot Arm Control:</strong></p>
<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Smooth robot joint movement using SLERP
</span><span class="k">class</span> <span class="nc">RobotArm</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">joint_orientations</span> <span class="o">=</span> <span class="p">[</span><span class="n">Quaternion</span><span class="p">.</span><span class="nf">identity</span><span class="p">()</span> <span class="k">for</span> <span class="n">_</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">6</span><span class="p">)]</span>
        <span class="n">self</span><span class="p">.</span><span class="n">target_orientations</span> <span class="o">=</span> <span class="p">[</span><span class="n">Quaternion</span><span class="p">.</span><span class="nf">identity</span><span class="p">()</span> <span class="k">for</span> <span class="n">_</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">6</span><span class="p">)]</span>
    
    <span class="k">def</span> <span class="nf">move_to_position</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">target_pose</span><span class="p">,</span> <span class="n">duration</span><span class="p">):</span>
        <span class="c1"># Calculate target joint orientations using inverse kinematics
</span>        <span class="n">self</span><span class="p">.</span><span class="n">target_orientations</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">inverse_kinematics</span><span class="p">(</span><span class="n">target_pose</span><span class="p">)</span>
        
        <span class="c1"># SLERP ensures smooth, natural-looking motion
</span>        <span class="n">start_time</span> <span class="o">=</span> <span class="n">time</span><span class="p">.</span><span class="nf">time</span><span class="p">()</span>
        <span class="k">while</span> <span class="n">time</span><span class="p">.</span><span class="nf">time</span><span class="p">()</span> <span class="o">-</span> <span class="n">start_time</span> <span class="o">&lt;</span> <span class="n">duration</span><span class="p">:</span>
            <span class="n">t</span> <span class="o">=</span> <span class="p">(</span><span class="n">time</span><span class="p">.</span><span class="nf">time</span><span class="p">()</span> <span class="o">-</span> <span class="n">start_time</span><span class="p">)</span> <span class="o">/</span> <span class="n">duration</span>
            
            <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">6</span><span class="p">):</span>
                <span class="n">self</span><span class="p">.</span><span class="n">joint_orientations</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">=</span> <span class="nf">slerp</span><span class="p">(</span>
                    <span class="n">self</span><span class="p">.</span><span class="n">joint_orientations</span><span class="p">[</span><span class="n">i</span><span class="p">],</span>
                    <span class="n">self</span><span class="p">.</span><span class="n">target_orientations</span><span class="p">[</span><span class="n">i</span><span class="p">],</span>
                    <span class="n">t</span>
                <span class="p">)</span>
            
            <span class="n">self</span><span class="p">.</span><span class="nf">update_joint_motors</span><span class="p">()</span>
            <span class="n">time</span><span class="p">.</span><span class="nf">sleep</span><span class="p">(</span><span class="mf">0.016</span><span class="p">)</span>  <span class="c1"># 60 FPS update
</span></code></pre></div></div>

<h3 id="why-slerp-works-the-mathematical-insight">Why SLERP Works: The Mathematical Insight</h3>

<p>The key insight is that SLERP follows <strong>great circle arcs</strong> on the quaternion sphere - these are the shortest paths between rotations, just like how airplane routes follow great circles on Earth.</p>

<p><strong>Mathematical Properties of SLERP:</strong></p>
<ul>
  <li><strong>Constant Angular Velocity</strong>: Objects rotate at steady speed (no speed-up/slow-down artifacts)</li>
  <li><strong>Shortest Path</strong>: Always takes the most direct route between orientations</li>
  <li><strong>Interpolation Property</strong>: slerp(q1, q2, 0) = q1 and slerp(q1, q2, 1) = q2</li>
  <li><strong>Composition Invariant</strong>: Results don’t depend on coordinate system choice</li>
</ul>

<p><strong>Euler LERP Problems:</strong></p>
<ul>
  <li><strong>Variable Speed</strong>: Object speed changes unpredictably during rotation</li>
  <li><strong>Non-optimal Path</strong>: Takes longer, curved routes through “rotation space”</li>
  <li><strong>Gimbal Lock Sensitivity</strong>: Can produce wild spinning near singularities</li>
</ul>

<h3 id="visualization-analysis">Visualization Analysis</h3>

<p>The interactive demonstrations below show these differences clearly:</p>

<p><strong>Left Panel - Angular Velocity Profile</strong>:
This shows rotation speed over time (0 to 1). SLERP maintains constant angular velocity - the line is flat! Euler LERP shows varying speed with unpredictable speed-ups and slow-downs.</p>

<p><strong>Right Panel - Quaternion Paths</strong>:
Shows actual rotation paths on the unit sphere. The blue SLERP path follows the shortest great circle arc, while the red Euler LERP path takes a curved, non-optimal route.</p>

<div class="panel wide-panel">
    <h3>Interpolation: Euler LERP vs SLERP</h3>
    <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 10px;">
        <div class="canvas-container" style="height: 300px;">
            <canvas id="slerpVelocityCanvas"></canvas>
        </div>
        <div class="canvas-container" style="height: 300px;">
            <canvas id="slerpPathCanvas"></canvas>
        </div>
    </div>
    <div class="controls">
        <div class="control-group">
            <label>Interpolation Parameter t <span class="value-display" id="tValue">0.0</span></label>
            <input type="range" class="slider" id="tSlider" min="0" max="1" value="0" step="0.01" />
        </div>
        <button class="button" id="animateInterp">Animate Interpolation</button>
        <button class="button" id="setRandomOrientations">Set Random Orientations</button>
    </div>
    <div class="info-box">
        <strong>Left Panel - Angular Velocity:</strong> Shows rotation speed over time (0 to 1)<br />
        • <span style="color: #2196f3;">Blue (SLERP)</span>: Constant angular velocity - the line is flat! (σ ≈ 0)<br />
        • <span style="color: #f44336;">Red (Euler LERP)</span>: Varying speed - speeds up and slows down (σ &gt; 0)<br />
        <br />
        <strong>Right Panel - Quaternion Paths:</strong> Shows actual rotation paths on the unit sphere<br />
        • <span style="color: #2196f3;">Blue path</span>: SLERP follows the shortest great circle arc<br />
        • <span style="color: #f44336;">Red path</span>: Euler LERP takes a curved, non-optimal path<br />
        <br />
        <strong>Key Insight:</strong> SLERP provides smooth, constant-speed rotation (like a spinning gyroscope), while Euler LERP causes jerky, unnatural motion with varying speeds. This is why SLERP is essential for animation and robotics!
    </div>
</div>

<h2 id="understanding-euler-angle-instability-a-visual-guide">Understanding Euler Angle Instability: A Visual Guide</h2>

<p>Why do Euler angles become unstable near certain orientations? The sensitivity heatmap below reveals the mathematical “danger zones” where small changes in input can cause huge, unpredictable rotations.</p>

<h3 id="what-are-we-looking-at">What Are We Looking At?</h3>

<p>Think of the heatmap as a “stability map” for Euler angle rotations:</p>
<ul>
  <li><strong>Blue regions</strong>: Safe - small input changes cause small rotation changes</li>
  <li><strong>Green regions</strong>: Caution - getting sensitive to small changes</li>
  <li><strong>Red regions</strong>: Dangerous - small changes can cause large jumps</li>
  <li><strong>Yellow regions</strong>: Gimbal lock! - the system breaks down completely</li>
</ul>

<h3 id="the-mathematics-behind-the-colors">The Mathematics Behind the Colors</h3>

<p>The colors represent the <strong>condition number</strong> of something called the Jacobian matrix. Don’t worry about the fancy names - here’s what matters:</p>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// This matrix tells us how sensitive rotations are to Euler angle changes</span>
<span class="kd">function</span> <span class="nf">calculateSensitivity</span><span class="p">(</span><span class="nx">yaw</span><span class="p">,</span> <span class="nx">pitch</span><span class="p">,</span> <span class="nx">roll</span><span class="p">)</span> <span class="p">{</span>
    <span class="c1">// Near pitch = ±90°, this matrix becomes "singular"</span>
    <span class="c1">// (mathematically broken)</span>
    <span class="kd">const</span> <span class="nx">cosP</span> <span class="o">=</span> <span class="nb">Math</span><span class="p">.</span><span class="nf">cos</span><span class="p">(</span><span class="nx">pitch</span><span class="p">);</span>
    <span class="kd">const</span> <span class="nx">sinP</span> <span class="o">=</span> <span class="nb">Math</span><span class="p">.</span><span class="nf">sin</span><span class="p">(</span><span class="nx">pitch</span><span class="p">);</span>
    
    <span class="c1">// The Jacobian matrix - relates angle changes to rotation speed</span>
    <span class="kd">const</span> <span class="nx">J</span> <span class="o">=</span> <span class="p">[</span>
        <span class="p">[</span><span class="mi">0</span><span class="p">,</span> <span class="o">-</span><span class="nb">Math</span><span class="p">.</span><span class="nf">sin</span><span class="p">(</span><span class="nx">yaw</span><span class="p">),</span> <span class="nb">Math</span><span class="p">.</span><span class="nf">cos</span><span class="p">(</span><span class="nx">yaw</span><span class="p">)</span> <span class="o">*</span> <span class="nx">cosP</span><span class="p">],</span>
        <span class="p">[</span><span class="mi">0</span><span class="p">,</span>  <span class="nb">Math</span><span class="p">.</span><span class="nf">cos</span><span class="p">(</span><span class="nx">yaw</span><span class="p">),</span> <span class="nb">Math</span><span class="p">.</span><span class="nf">sin</span><span class="p">(</span><span class="nx">yaw</span><span class="p">)</span> <span class="o">*</span> <span class="nx">cosP</span><span class="p">],</span>
        <span class="p">[</span><span class="mi">1</span><span class="p">,</span>  <span class="mi">0</span><span class="p">,</span>             <span class="o">-</span><span class="nx">sinP</span><span class="p">]</span>
    <span class="p">];</span>
    
    <span class="c1">// When pitch ≈ ±90°, cosP ≈ 0, making the matrix unstable</span>
    <span class="k">return</span> <span class="nf">calculateConditionNumber</span><span class="p">(</span><span class="nx">J</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="real-world-implications-1">Real-World Implications</h3>

<p><strong>Flight Simulator Example:</strong></p>
<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// This is why flight simulators avoid certain orientations</span>
<span class="kd">class</span> <span class="nc">FlightControl</span> <span class="p">{</span>
    <span class="nf">updateOrientation</span><span class="p">(</span><span class="nx">yawInput</span><span class="p">,</span> <span class="nx">pitchInput</span><span class="p">,</span> <span class="nx">rollInput</span><span class="p">)</span> <span class="p">{</span>
        <span class="kd">const</span> <span class="nx">sensitivity</span> <span class="o">=</span> <span class="nf">calculateSensitivity</span><span class="p">(</span>
            <span class="k">this</span><span class="p">.</span><span class="nx">currentYaw</span><span class="p">,</span> <span class="k">this</span><span class="p">.</span><span class="nx">currentPitch</span><span class="p">,</span> <span class="k">this</span><span class="p">.</span><span class="nx">currentRoll</span>
        <span class="p">);</span>
        
        <span class="k">if </span><span class="p">(</span><span class="nx">sensitivity</span> <span class="o">&gt;</span> <span class="nx">DANGER_THRESHOLD</span><span class="p">)</span> <span class="p">{</span>
            <span class="c1">// Switch to quaternion-based control near gimbal lock!</span>
            <span class="k">this</span><span class="p">.</span><span class="nx">useQuaternionControl</span> <span class="o">=</span> <span class="kc">true</span><span class="p">;</span>
            <span class="nx">console</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="dl">"</span><span class="s2">WARNING: Entering gimbal lock region - switching to quaternions</span><span class="dl">"</span><span class="p">);</span>
        <span class="p">}</span>
        
        <span class="c1">// Apply control inputs based on current control mode</span>
        <span class="k">if </span><span class="p">(</span><span class="k">this</span><span class="p">.</span><span class="nx">useQuaternionControl</span><span class="p">)</span> <span class="p">{</span>
            <span class="k">this</span><span class="p">.</span><span class="nf">updateWithQuaternions</span><span class="p">(</span><span class="nx">yawInput</span><span class="p">,</span> <span class="nx">pitchInput</span><span class="p">,</span> <span class="nx">rollInput</span><span class="p">);</span>
        <span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
            <span class="k">this</span><span class="p">.</span><span class="nf">updateWithEulerAngles</span><span class="p">(</span><span class="nx">yawInput</span><span class="p">,</span> <span class="nx">pitchInput</span><span class="p">,</span> <span class="nx">rollInput</span><span class="p">);</span>
        <span class="p">}</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="torus-topology-why-angles-wrap-around">Torus Topology: Why Angles “Wrap Around”</h3>

<p>The <strong>torus view</strong> shows something important: yaw and roll angles “wrap around” at ±180°. This is like how compass headings wrap from 359° back to 0°.</p>

<p><strong>Practical Example:</strong></p>
<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Angle wrapping causes problems in naive interpolation</span>
<span class="kd">const</span> <span class="nx">angle1</span> <span class="o">=</span> <span class="mi">170</span><span class="p">;</span>  <span class="c1">// degrees</span>
<span class="kd">const</span> <span class="nx">angle2</span> <span class="o">=</span> <span class="o">-</span><span class="mi">170</span><span class="p">;</span> <span class="c1">// degrees (equivalent to 190°)</span>

<span class="c1">// Naive interpolation: 170° → 0° → -170° (340° total rotation!)</span>
<span class="c1">// Smart interpolation: 170° → 180° → -170° (20° total rotation)</span>

<span class="kd">function</span> <span class="nf">smartAngleLerp</span><span class="p">(</span><span class="nx">a1</span><span class="p">,</span> <span class="nx">a2</span><span class="p">,</span> <span class="nx">t</span><span class="p">)</span> <span class="p">{</span>
    <span class="c1">// Handle the wrap-around case</span>
    <span class="kd">let</span> <span class="nx">diff</span> <span class="o">=</span> <span class="nx">a2</span> <span class="o">-</span> <span class="nx">a1</span><span class="p">;</span>
    <span class="k">if </span><span class="p">(</span><span class="nx">diff</span> <span class="o">&gt;</span> <span class="mi">180</span><span class="p">)</span> <span class="nx">diff</span> <span class="o">-=</span> <span class="mi">360</span><span class="p">;</span>
    <span class="k">if </span><span class="p">(</span><span class="nx">diff</span> <span class="o">&lt;</span> <span class="o">-</span><span class="mi">180</span><span class="p">)</span> <span class="nx">diff</span> <span class="o">+=</span> <span class="mi">360</span><span class="p">;</span>
    
    <span class="k">return</span> <span class="nx">a1</span> <span class="o">+</span> <span class="nx">diff</span> <span class="o">*</span> <span class="nx">t</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<p>This is yet another reason why quaternions are superior - they don’t have wrap-around problems!</p>

<p><strong>For the mathematically curious:</strong> The Jacobian matrix $J(ψ,θ,φ)$ relates small changes in Euler angles to angular velocity: $ω = J(ψ,θ,φ) \cdot [dψ/dt, dθ/dt, dφ/dt]^T$. Near gimbal lock $(θ ≈ ±π/2)$, $J$ becomes singular $(det(J) → 0)$, causing numerical instabilities and loss of controllability.</p>

<div class="panel wide-panel">
    <h3>Euler Angle Sensitivity Map (Torus Topology)</h3>
    <div class="canvas-container" style="height: 500px;">
        <canvas id="heatmapCanvas"></canvas>
    </div>
    <div class="controls">
        <div class="control-group">
            <label>Fixed Pitch Value <span class="value-display" id="heatmapPitchValue">0°</span></label>
            <input type="range" class="slider" id="heatmapPitchSlider" min="-90" max="90" value="0" step="5" />
        </div>
        <div class="control-group">
            <label><span style="color: #64ffda;">Torus Yaw</span> <span class="value-display" id="torusYawValue">0°</span></label>
            <input type="range" class="slider" id="torusYawSlider" min="-180" max="180" value="0" step="1" />
        </div>
        <div class="control-group">
            <label><span style="color: #ff9800;">Torus Roll</span> <span class="value-display" id="torusRollValue">0°</span></label>
            <input type="range" class="slider" id="torusRollSlider" min="-180" max="180" value="0" step="1" />
        </div>
        <button class="button" id="animateHeatmap">Animate Through Pitch Values</button>
        <button class="button" id="toggleTorusView">Toggle Torus/Flat View</button>
    </div>
    <div class="info-box">
        <strong>Color coding:</strong> <span style="color: #0066ff;">Blue</span> = stable, <span style="color: #00ff00;">Green</span> = sensitive, <span style="color: #ff0000;">Red</span> = highly singular, <span style="color: #ffff00;">Yellow</span> = gimbal lock!<br />
        This map shows how sensitive the rotation is to small changes in yaw/roll for a given pitch. The torus topology reveals how yaw and roll wrap around at ±180°. As pitch approaches ±90°, the entire map turns red/yellow, showing that yaw and roll become coupled and the coordinate system becomes singular.
    </div>
</div>

<h2 id="practical-implementation-guide-making-quaternions-work-for-you">Practical Implementation Guide: Making Quaternions Work for You</h2>

<p>Ready to start using quaternions in your projects? Here are the essential patterns and best practices that will save you hours of debugging.</p>

<h3 id="essential-quaternion-class-implementation">Essential Quaternion Class Implementation</h3>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">class</span> <span class="nc">Quaternion</span> <span class="p">{</span>
    <span class="nf">constructor</span><span class="p">(</span><span class="nx">x</span> <span class="o">=</span> <span class="mi">0</span><span class="p">,</span> <span class="nx">y</span> <span class="o">=</span> <span class="mi">0</span><span class="p">,</span> <span class="nx">z</span> <span class="o">=</span> <span class="mi">0</span><span class="p">,</span> <span class="nx">w</span> <span class="o">=</span> <span class="mi">1</span><span class="p">)</span> <span class="p">{</span>
        <span class="k">this</span><span class="p">.</span><span class="nx">x</span> <span class="o">=</span> <span class="nx">x</span><span class="p">;</span> <span class="k">this</span><span class="p">.</span><span class="nx">y</span> <span class="o">=</span> <span class="nx">y</span><span class="p">;</span> <span class="k">this</span><span class="p">.</span><span class="nx">z</span> <span class="o">=</span> <span class="nx">z</span><span class="p">;</span> <span class="k">this</span><span class="p">.</span><span class="nx">w</span> <span class="o">=</span> <span class="nx">w</span><span class="p">;</span>
    <span class="p">}</span>
    
    <span class="c1">// CRITICAL: Always normalize after operations to prevent drift</span>
    <span class="nf">normalize</span><span class="p">()</span> <span class="p">{</span>
        <span class="kd">const</span> <span class="nx">length</span> <span class="o">=</span> <span class="nb">Math</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">(</span><span class="k">this</span><span class="p">.</span><span class="nx">x</span><span class="o">*</span><span class="k">this</span><span class="p">.</span><span class="nx">x</span> <span class="o">+</span> <span class="k">this</span><span class="p">.</span><span class="nx">y</span><span class="o">*</span><span class="k">this</span><span class="p">.</span><span class="nx">y</span> <span class="o">+</span> <span class="k">this</span><span class="p">.</span><span class="nx">z</span><span class="o">*</span><span class="k">this</span><span class="p">.</span><span class="nx">z</span> <span class="o">+</span> <span class="k">this</span><span class="p">.</span><span class="nx">w</span><span class="o">*</span><span class="k">this</span><span class="p">.</span><span class="nx">w</span><span class="p">);</span>
        <span class="k">if </span><span class="p">(</span><span class="nx">length</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="p">)</span> <span class="p">{</span>
            <span class="kd">const</span> <span class="nx">invLength</span> <span class="o">=</span> <span class="mi">1</span> <span class="o">/</span> <span class="nx">length</span><span class="p">;</span>
            <span class="k">this</span><span class="p">.</span><span class="nx">x</span> <span class="o">*=</span> <span class="nx">invLength</span><span class="p">;</span>
            <span class="k">this</span><span class="p">.</span><span class="nx">y</span> <span class="o">*=</span> <span class="nx">invLength</span><span class="p">;</span>
            <span class="k">this</span><span class="p">.</span><span class="nx">z</span> <span class="o">*=</span> <span class="nx">invLength</span><span class="p">;</span>
            <span class="k">this</span><span class="p">.</span><span class="nx">w</span> <span class="o">*=</span> <span class="nx">invLength</span><span class="p">;</span>
        <span class="p">}</span>
        <span class="k">return</span> <span class="k">this</span><span class="p">;</span>
    <span class="p">}</span>
    
    <span class="c1">// Multiply quaternions (order matters!)</span>
    <span class="nf">multiply</span><span class="p">(</span><span class="nx">other</span><span class="p">)</span> <span class="p">{</span>
        <span class="k">return</span> <span class="k">new</span> <span class="nc">Quaternion</span><span class="p">(</span>
            <span class="k">this</span><span class="p">.</span><span class="nx">w</span><span class="o">*</span><span class="nx">other</span><span class="p">.</span><span class="nx">x</span> <span class="o">+</span> <span class="k">this</span><span class="p">.</span><span class="nx">x</span><span class="o">*</span><span class="nx">other</span><span class="p">.</span><span class="nx">w</span> <span class="o">+</span> <span class="k">this</span><span class="p">.</span><span class="nx">y</span><span class="o">*</span><span class="nx">other</span><span class="p">.</span><span class="nx">z</span> <span class="o">-</span> <span class="k">this</span><span class="p">.</span><span class="nx">z</span><span class="o">*</span><span class="nx">other</span><span class="p">.</span><span class="nx">y</span><span class="p">,</span>
            <span class="k">this</span><span class="p">.</span><span class="nx">w</span><span class="o">*</span><span class="nx">other</span><span class="p">.</span><span class="nx">y</span> <span class="o">-</span> <span class="k">this</span><span class="p">.</span><span class="nx">x</span><span class="o">*</span><span class="nx">other</span><span class="p">.</span><span class="nx">z</span> <span class="o">+</span> <span class="k">this</span><span class="p">.</span><span class="nx">y</span><span class="o">*</span><span class="nx">other</span><span class="p">.</span><span class="nx">w</span> <span class="o">+</span> <span class="k">this</span><span class="p">.</span><span class="nx">z</span><span class="o">*</span><span class="nx">other</span><span class="p">.</span><span class="nx">x</span><span class="p">,</span>
            <span class="k">this</span><span class="p">.</span><span class="nx">w</span><span class="o">*</span><span class="nx">other</span><span class="p">.</span><span class="nx">z</span> <span class="o">+</span> <span class="k">this</span><span class="p">.</span><span class="nx">x</span><span class="o">*</span><span class="nx">other</span><span class="p">.</span><span class="nx">y</span> <span class="o">-</span> <span class="k">this</span><span class="p">.</span><span class="nx">y</span><span class="o">*</span><span class="nx">other</span><span class="p">.</span><span class="nx">x</span> <span class="o">+</span> <span class="k">this</span><span class="p">.</span><span class="nx">z</span><span class="o">*</span><span class="nx">other</span><span class="p">.</span><span class="nx">w</span><span class="p">,</span>
            <span class="k">this</span><span class="p">.</span><span class="nx">w</span><span class="o">*</span><span class="nx">other</span><span class="p">.</span><span class="nx">w</span> <span class="o">-</span> <span class="k">this</span><span class="p">.</span><span class="nx">x</span><span class="o">*</span><span class="nx">other</span><span class="p">.</span><span class="nx">x</span> <span class="o">-</span> <span class="k">this</span><span class="p">.</span><span class="nx">y</span><span class="o">*</span><span class="nx">other</span><span class="p">.</span><span class="nx">y</span> <span class="o">-</span> <span class="k">this</span><span class="p">.</span><span class="nx">z</span><span class="o">*</span><span class="nx">other</span><span class="p">.</span><span class="nx">z</span>
        <span class="p">);</span>
    <span class="p">}</span>
    
    <span class="c1">// Convert to rotation matrix (for graphics APIs)</span>
    <span class="nf">toMatrix</span><span class="p">()</span> <span class="p">{</span>
        <span class="kd">const</span> <span class="nx">x2</span> <span class="o">=</span> <span class="k">this</span><span class="p">.</span><span class="nx">x</span> <span class="o">+</span> <span class="k">this</span><span class="p">.</span><span class="nx">x</span><span class="p">,</span> <span class="nx">y2</span> <span class="o">=</span> <span class="k">this</span><span class="p">.</span><span class="nx">y</span> <span class="o">+</span> <span class="k">this</span><span class="p">.</span><span class="nx">y</span><span class="p">,</span> <span class="nx">z2</span> <span class="o">=</span> <span class="k">this</span><span class="p">.</span><span class="nx">z</span> <span class="o">+</span> <span class="k">this</span><span class="p">.</span><span class="nx">z</span><span class="p">;</span>
        <span class="kd">const</span> <span class="nx">xx</span> <span class="o">=</span> <span class="k">this</span><span class="p">.</span><span class="nx">x</span> <span class="o">*</span> <span class="nx">x2</span><span class="p">,</span> <span class="nx">xy</span> <span class="o">=</span> <span class="k">this</span><span class="p">.</span><span class="nx">x</span> <span class="o">*</span> <span class="nx">y2</span><span class="p">,</span> <span class="nx">xz</span> <span class="o">=</span> <span class="k">this</span><span class="p">.</span><span class="nx">x</span> <span class="o">*</span> <span class="nx">z2</span><span class="p">;</span>
        <span class="kd">const</span> <span class="nx">yy</span> <span class="o">=</span> <span class="k">this</span><span class="p">.</span><span class="nx">y</span> <span class="o">*</span> <span class="nx">y2</span><span class="p">,</span> <span class="nx">yz</span> <span class="o">=</span> <span class="k">this</span><span class="p">.</span><span class="nx">y</span> <span class="o">*</span> <span class="nx">z2</span><span class="p">,</span> <span class="nx">zz</span> <span class="o">=</span> <span class="k">this</span><span class="p">.</span><span class="nx">z</span> <span class="o">*</span> <span class="nx">z2</span><span class="p">;</span>
        <span class="kd">const</span> <span class="nx">wx</span> <span class="o">=</span> <span class="k">this</span><span class="p">.</span><span class="nx">w</span> <span class="o">*</span> <span class="nx">x2</span><span class="p">,</span> <span class="nx">wy</span> <span class="o">=</span> <span class="k">this</span><span class="p">.</span><span class="nx">w</span> <span class="o">*</span> <span class="nx">y2</span><span class="p">,</span> <span class="nx">wz</span> <span class="o">=</span> <span class="k">this</span><span class="p">.</span><span class="nx">w</span> <span class="o">*</span> <span class="nx">z2</span><span class="p">;</span>
        
        <span class="k">return</span> <span class="p">[</span>
            <span class="mi">1</span><span class="o">-</span><span class="p">(</span><span class="nx">yy</span><span class="o">+</span><span class="nx">zz</span><span class="p">),</span> <span class="nx">xy</span><span class="o">-</span><span class="nx">wz</span><span class="p">,</span>     <span class="nx">xz</span><span class="o">+</span><span class="nx">wy</span><span class="p">,</span>     <span class="mi">0</span><span class="p">,</span>
            <span class="nx">xy</span><span class="o">+</span><span class="nx">wz</span><span class="p">,</span>     <span class="mi">1</span><span class="o">-</span><span class="p">(</span><span class="nx">xx</span><span class="o">+</span><span class="nx">zz</span><span class="p">),</span> <span class="nx">yz</span><span class="o">-</span><span class="nx">wx</span><span class="p">,</span>     <span class="mi">0</span><span class="p">,</span>
            <span class="nx">xz</span><span class="o">-</span><span class="nx">wy</span><span class="p">,</span>     <span class="nx">yz</span><span class="o">+</span><span class="nx">wx</span><span class="p">,</span>     <span class="mi">1</span><span class="o">-</span><span class="p">(</span><span class="nx">xx</span><span class="o">+</span><span class="nx">yy</span><span class="p">),</span> <span class="mi">0</span><span class="p">,</span>
            <span class="mi">0</span><span class="p">,</span>         <span class="mi">0</span><span class="p">,</span>         <span class="mi">0</span><span class="p">,</span>         <span class="mi">1</span>
        <span class="p">];</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="safe-slerp-implementation">Safe SLERP Implementation</h3>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">function</span> <span class="nf">safeSlerp</span><span class="p">(</span><span class="nx">q1</span><span class="p">,</span> <span class="nx">q2</span><span class="p">,</span> <span class="nx">t</span><span class="p">)</span> <span class="p">{</span>
    <span class="c1">// IMPORTANT: Handle the double cover - choose shorter path</span>
    <span class="kd">let</span> <span class="nx">dot</span> <span class="o">=</span> <span class="nx">q1</span><span class="p">.</span><span class="nx">x</span><span class="o">*</span><span class="nx">q2</span><span class="p">.</span><span class="nx">x</span> <span class="o">+</span> <span class="nx">q1</span><span class="p">.</span><span class="nx">y</span><span class="o">*</span><span class="nx">q2</span><span class="p">.</span><span class="nx">y</span> <span class="o">+</span> <span class="nx">q1</span><span class="p">.</span><span class="nx">z</span><span class="o">*</span><span class="nx">q2</span><span class="p">.</span><span class="nx">z</span> <span class="o">+</span> <span class="nx">q1</span><span class="p">.</span><span class="nx">w</span><span class="o">*</span><span class="nx">q2</span><span class="p">.</span><span class="nx">w</span><span class="p">;</span>
    
    <span class="c1">// If dot &lt; 0, quaternions are on opposite hemispheres</span>
    <span class="c1">// Negate one quaternion to take the shorter path</span>
    <span class="k">if </span><span class="p">(</span><span class="nx">dot</span> <span class="o">&lt;</span> <span class="mi">0</span><span class="p">)</span> <span class="p">{</span>
        <span class="nx">q2</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Quaternion</span><span class="p">(</span><span class="o">-</span><span class="nx">q2</span><span class="p">.</span><span class="nx">x</span><span class="p">,</span> <span class="o">-</span><span class="nx">q2</span><span class="p">.</span><span class="nx">y</span><span class="p">,</span> <span class="o">-</span><span class="nx">q2</span><span class="p">.</span><span class="nx">z</span><span class="p">,</span> <span class="o">-</span><span class="nx">q2</span><span class="p">.</span><span class="nx">w</span><span class="p">);</span>
        <span class="nx">dot</span> <span class="o">=</span> <span class="o">-</span><span class="nx">dot</span><span class="p">;</span>
    <span class="p">}</span>
    
    <span class="c1">// Clamp dot to avoid numerical issues</span>
    <span class="nx">dot</span> <span class="o">=</span> <span class="nb">Math</span><span class="p">.</span><span class="nf">min</span><span class="p">(</span><span class="nb">Math</span><span class="p">.</span><span class="nf">max</span><span class="p">(</span><span class="nx">dot</span><span class="p">,</span> <span class="o">-</span><span class="mi">1</span><span class="p">),</span> <span class="mi">1</span><span class="p">);</span>
    
    <span class="c1">// Use linear interpolation for very close quaternions</span>
    <span class="k">if </span><span class="p">(</span><span class="nx">dot</span> <span class="o">&gt;</span> <span class="mf">0.9995</span><span class="p">)</span> <span class="p">{</span>
        <span class="k">return</span> <span class="k">new</span> <span class="nc">Quaternion</span><span class="p">(</span>
            <span class="nx">q1</span><span class="p">.</span><span class="nx">x</span> <span class="o">+</span> <span class="nx">t</span> <span class="o">*</span> <span class="p">(</span><span class="nx">q2</span><span class="p">.</span><span class="nx">x</span> <span class="o">-</span> <span class="nx">q1</span><span class="p">.</span><span class="nx">x</span><span class="p">),</span>
            <span class="nx">q1</span><span class="p">.</span><span class="nx">y</span> <span class="o">+</span> <span class="nx">t</span> <span class="o">*</span> <span class="p">(</span><span class="nx">q2</span><span class="p">.</span><span class="nx">y</span> <span class="o">-</span> <span class="nx">q1</span><span class="p">.</span><span class="nx">y</span><span class="p">),</span>
            <span class="nx">q1</span><span class="p">.</span><span class="nx">z</span> <span class="o">+</span> <span class="nx">t</span> <span class="o">*</span> <span class="p">(</span><span class="nx">q2</span><span class="p">.</span><span class="nx">z</span> <span class="o">-</span> <span class="nx">q1</span><span class="p">.</span><span class="nx">z</span><span class="p">),</span>
            <span class="nx">q1</span><span class="p">.</span><span class="nx">w</span> <span class="o">+</span> <span class="nx">t</span> <span class="o">*</span> <span class="p">(</span><span class="nx">q2</span><span class="p">.</span><span class="nx">w</span> <span class="o">-</span> <span class="nx">q1</span><span class="p">.</span><span class="nx">w</span><span class="p">)</span>
        <span class="p">).</span><span class="nf">normalize</span><span class="p">();</span>
    <span class="p">}</span>
    
    <span class="c1">// Standard SLERP</span>
    <span class="kd">const</span> <span class="nx">theta</span> <span class="o">=</span> <span class="nb">Math</span><span class="p">.</span><span class="nf">acos</span><span class="p">(</span><span class="nx">dot</span><span class="p">);</span>
    <span class="kd">const</span> <span class="nx">sinTheta</span> <span class="o">=</span> <span class="nb">Math</span><span class="p">.</span><span class="nf">sin</span><span class="p">(</span><span class="nx">theta</span><span class="p">);</span>
    <span class="kd">const</span> <span class="nx">scale1</span> <span class="o">=</span> <span class="nb">Math</span><span class="p">.</span><span class="nf">sin</span><span class="p">((</span><span class="mi">1</span><span class="o">-</span><span class="nx">t</span><span class="p">)</span> <span class="o">*</span> <span class="nx">theta</span><span class="p">)</span> <span class="o">/</span> <span class="nx">sinTheta</span><span class="p">;</span>
    <span class="kd">const</span> <span class="nx">scale2</span> <span class="o">=</span> <span class="nb">Math</span><span class="p">.</span><span class="nf">sin</span><span class="p">(</span><span class="nx">t</span> <span class="o">*</span> <span class="nx">theta</span><span class="p">)</span> <span class="o">/</span> <span class="nx">sinTheta</span><span class="p">;</span>
    
    <span class="k">return</span> <span class="k">new</span> <span class="nc">Quaternion</span><span class="p">(</span>
        <span class="nx">scale1</span> <span class="o">*</span> <span class="nx">q1</span><span class="p">.</span><span class="nx">x</span> <span class="o">+</span> <span class="nx">scale2</span> <span class="o">*</span> <span class="nx">q2</span><span class="p">.</span><span class="nx">x</span><span class="p">,</span>
        <span class="nx">scale1</span> <span class="o">*</span> <span class="nx">q1</span><span class="p">.</span><span class="nx">y</span> <span class="o">+</span> <span class="nx">scale2</span> <span class="o">*</span> <span class="nx">q2</span><span class="p">.</span><span class="nx">y</span><span class="p">,</span>
        <span class="nx">scale1</span> <span class="o">*</span> <span class="nx">q1</span><span class="p">.</span><span class="nx">z</span> <span class="o">+</span> <span class="nx">scale2</span> <span class="o">*</span> <span class="nx">q2</span><span class="p">.</span><span class="nx">z</span><span class="p">,</span>
        <span class="nx">scale1</span> <span class="o">*</span> <span class="nx">q1</span><span class="p">.</span><span class="nx">w</span> <span class="o">+</span> <span class="nx">scale2</span> <span class="o">*</span> <span class="nx">q2</span><span class="p">.</span><span class="nx">w</span>
    <span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="common-pitfalls-and-how-to-avoid-them">Common Pitfalls and How to Avoid Them</h3>

<p><strong>1. Forgetting to Normalize</strong></p>
<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// BAD: Quaternions drift over time</span>
<span class="kd">let</span> <span class="nx">rotation</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Quaternion</span><span class="p">(</span><span class="mf">0.1</span><span class="p">,</span> <span class="mf">0.2</span><span class="p">,</span> <span class="mf">0.3</span><span class="p">,</span> <span class="mf">0.9</span><span class="p">);</span>
<span class="k">for </span><span class="p">(</span><span class="kd">let</span> <span class="nx">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="nx">i</span> <span class="o">&lt;</span> <span class="mi">1000</span><span class="p">;</span> <span class="nx">i</span><span class="o">++</span><span class="p">)</span> <span class="p">{</span>
    <span class="nx">rotation</span> <span class="o">=</span> <span class="nx">rotation</span><span class="p">.</span><span class="nf">multiply</span><span class="p">(</span><span class="nx">deltaRotation</span><span class="p">);</span> <span class="c1">// Gets denormalized!</span>
<span class="p">}</span>

<span class="c1">// GOOD: Regular normalization</span>
<span class="kd">let</span> <span class="nx">rotation</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Quaternion</span><span class="p">(</span><span class="mf">0.1</span><span class="p">,</span> <span class="mf">0.2</span><span class="p">,</span> <span class="mf">0.3</span><span class="p">,</span> <span class="mf">0.9</span><span class="p">);</span>
<span class="k">for </span><span class="p">(</span><span class="kd">let</span> <span class="nx">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="nx">i</span> <span class="o">&lt;</span> <span class="mi">1000</span><span class="p">;</span> <span class="nx">i</span><span class="o">++</span><span class="p">)</span> <span class="p">{</span>
    <span class="nx">rotation</span> <span class="o">=</span> <span class="nx">rotation</span><span class="p">.</span><span class="nf">multiply</span><span class="p">(</span><span class="nx">deltaRotation</span><span class="p">).</span><span class="nf">normalize</span><span class="p">();</span> <span class="c1">// Stays unit length</span>
<span class="p">}</span>
</code></pre></div></div>

<p><strong>2. Wrong Multiplication Order</strong></p>
<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// WRONG: q1 * q2 means "apply q1, then q2"</span>
<span class="c1">// But many people think it means "apply q2, then q1"</span>
<span class="kd">const</span> <span class="nx">result</span> <span class="o">=</span> <span class="nx">rotation</span><span class="p">.</span><span class="nf">multiply</span><span class="p">(</span><span class="nx">deltaRotation</span><span class="p">);</span> <span class="c1">// Applies rotation first, then delta</span>

<span class="c1">// Always be explicit about what you mean:</span>
<span class="kd">const</span> <span class="nx">worldToLocal</span> <span class="o">=</span> <span class="nx">parentRotation</span><span class="p">.</span><span class="nf">multiply</span><span class="p">(</span><span class="nx">childRotation</span><span class="p">);</span>
<span class="kd">const</span> <span class="nx">localToWorld</span> <span class="o">=</span> <span class="nx">childRotation</span><span class="p">.</span><span class="nf">multiply</span><span class="p">(</span><span class="nx">parentRotation</span><span class="p">);</span>
</code></pre></div></div>

<p><strong>3. Interpolating Through the Long Path</strong></p>
<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// This can cause 340° rotation instead of 20°!</span>
<span class="kd">function</span> <span class="nf">badSlerp</span><span class="p">(</span><span class="nx">q1</span><span class="p">,</span> <span class="nx">q2</span><span class="p">,</span> <span class="nx">t</span><span class="p">)</span> <span class="p">{</span>
    <span class="c1">// Missing the dot product check - might take the long way around</span>
    <span class="k">return</span> <span class="nf">slerp</span><span class="p">(</span><span class="nx">q1</span><span class="p">,</span> <span class="nx">q2</span><span class="p">,</span> <span class="nx">t</span><span class="p">);</span> <span class="c1">// Could spin the wrong direction</span>
<span class="p">}</span>

<span class="c1">// Use safeSlerp() function above instead</span>
</code></pre></div></div>

<h3 id="integration-with-graphics-apis">Integration with Graphics APIs</h3>

<p><strong>WebGL/Three.js:</strong></p>
<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Convert quaternion to Three.js format</span>
<span class="kd">const</span> <span class="nx">threeQuat</span> <span class="o">=</span> <span class="k">new</span> <span class="nx">THREE</span><span class="p">.</span><span class="nc">Quaternion</span><span class="p">(</span><span class="nx">quat</span><span class="p">.</span><span class="nx">x</span><span class="p">,</span> <span class="nx">quat</span><span class="p">.</span><span class="nx">y</span><span class="p">,</span> <span class="nx">quat</span><span class="p">.</span><span class="nx">z</span><span class="p">,</span> <span class="nx">quat</span><span class="p">.</span><span class="nx">w</span><span class="p">);</span>
<span class="nx">mesh</span><span class="p">.</span><span class="nx">quaternion</span><span class="p">.</span><span class="nf">copy</span><span class="p">(</span><span class="nx">threeQuat</span><span class="p">);</span>
</code></pre></div></div>

<p><strong>Unity C#:</strong></p>
<div class="language-csharp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Unity has excellent quaternion support built-in</span>
<span class="k">public</span> <span class="k">class</span> <span class="nc">SmoothRotator</span> <span class="p">:</span> <span class="n">MonoBehaviour</span> <span class="p">{</span>
    <span class="k">public</span> <span class="kt">float</span> <span class="n">rotationSpeed</span> <span class="p">=</span> <span class="m">2.0f</span><span class="p">;</span>
    <span class="k">private</span> <span class="n">Quaternion</span> <span class="n">targetRotation</span><span class="p">;</span>
    
    <span class="k">void</span> <span class="nf">Update</span><span class="p">()</span> <span class="p">{</span>
        <span class="c1">// Smooth rotation using Unity's Slerp</span>
        <span class="n">transform</span><span class="p">.</span><span class="n">rotation</span> <span class="p">=</span> <span class="n">Quaternion</span><span class="p">.</span><span class="nf">Slerp</span><span class="p">(</span>
            <span class="n">transform</span><span class="p">.</span><span class="n">rotation</span><span class="p">,</span>
            <span class="n">targetRotation</span><span class="p">,</span>
            <span class="n">rotationSpeed</span> <span class="p">*</span> <span class="n">Time</span><span class="p">.</span><span class="n">deltaTime</span>
        <span class="p">);</span>
    <span class="p">}</span>
    
    <span class="k">public</span> <span class="k">void</span> <span class="nf">SetTarget</span><span class="p">(</span><span class="n">Vector3</span> <span class="n">direction</span><span class="p">)</span> <span class="p">{</span>
        <span class="n">targetRotation</span> <span class="p">=</span> <span class="n">Quaternion</span><span class="p">.</span><span class="nf">LookRotation</span><span class="p">(</span><span class="n">direction</span><span class="p">);</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="performance-tips">Performance Tips</h3>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Cache expensive operations</span>
<span class="kd">class</span> <span class="nc">RotationCache</span> <span class="p">{</span>
    <span class="nf">constructor</span><span class="p">()</span> <span class="p">{</span>
        <span class="k">this</span><span class="p">.</span><span class="nx">cachedMatrix</span> <span class="o">=</span> <span class="kc">null</span><span class="p">;</span>
        <span class="k">this</span><span class="p">.</span><span class="nx">quaternionDirty</span> <span class="o">=</span> <span class="kc">true</span><span class="p">;</span>
    <span class="p">}</span>
    
    <span class="nf">setRotation</span><span class="p">(</span><span class="nx">quat</span><span class="p">)</span> <span class="p">{</span>
        <span class="k">this</span><span class="p">.</span><span class="nx">rotation</span> <span class="o">=</span> <span class="nx">quat</span><span class="p">;</span>
        <span class="k">this</span><span class="p">.</span><span class="nx">quaternionDirty</span> <span class="o">=</span> <span class="kc">true</span><span class="p">;</span> <span class="c1">// Mark matrix as needing recalculation</span>
    <span class="p">}</span>
    
    <span class="nf">getMatrix</span><span class="p">()</span> <span class="p">{</span>
        <span class="k">if </span><span class="p">(</span><span class="k">this</span><span class="p">.</span><span class="nx">quaternionDirty</span><span class="p">)</span> <span class="p">{</span>
            <span class="k">this</span><span class="p">.</span><span class="nx">cachedMatrix</span> <span class="o">=</span> <span class="k">this</span><span class="p">.</span><span class="nx">rotation</span><span class="p">.</span><span class="nf">toMatrix</span><span class="p">();</span>
            <span class="k">this</span><span class="p">.</span><span class="nx">quaternionDirty</span> <span class="o">=</span> <span class="kc">false</span><span class="p">;</span>
        <span class="p">}</span>
        <span class="k">return</span> <span class="k">this</span><span class="p">.</span><span class="nx">cachedMatrix</span><span class="p">;</span>
    <span class="p">}</span>
<span class="p">}</span>

<span class="c1">// For real-time applications, normalize only when needed</span>
<span class="kd">let</span> <span class="nx">normalizationCounter</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
<span class="kd">function</span> <span class="nf">updateRotation</span><span class="p">(</span><span class="nx">deltaQuat</span><span class="p">)</span> <span class="p">{</span>
    <span class="nx">rotation</span> <span class="o">=</span> <span class="nx">rotation</span><span class="p">.</span><span class="nf">multiply</span><span class="p">(</span><span class="nx">deltaQuat</span><span class="p">);</span>
    
    <span class="c1">// Normalize every 10 frames instead of every frame</span>
    <span class="k">if </span><span class="p">(</span><span class="o">++</span><span class="nx">normalizationCounter</span> <span class="o">%</span> <span class="mi">10</span> <span class="o">===</span> <span class="mi">0</span><span class="p">)</span> <span class="p">{</span>
        <span class="nx">rotation</span><span class="p">.</span><span class="nf">normalize</span><span class="p">();</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<p><strong>The Golden Rule</strong>: Always work with quaternions internally, only convert to Euler angles for human-readable displays or legacy API compatibility. Never interpolate or integrate Euler angles directly!</p>

<h2 id="geometric-and-topological-insights">Geometric and Topological Insights</h2>

<p>The fundamental advantage of quaternions stems from their natural geometric properties:</p>

<p><strong>Lie Group Structure</strong>: Unit quaternions form a Lie group isomorphic to $SU(2)$, with the exponential map providing a natural connection between the Lie algebra (3D angular velocity space) and the group manifold $(S^3)$.</p>

<p><strong>Minimal Representation</strong>: While quaternions use four parameters to represent three degrees of freedom, this redundancy is precisely what eliminates singularities. The constraint $||q|| = 1$ reduces the effective dimensionality to three while maintaining global validity.</p>

<p><strong>Geodesic Optimality</strong>: SLERP follows geodesics on $S^3$, which project to the shortest rotation paths in $SO(3)$. This ensures both mathematical optimality and physical realism in animation and control systems.</p>

<h2 id="practical-applications-and-performance-considerations">Practical Applications and Performance Considerations</h2>

<p>The mathematical superiority of quaternions translates directly into practical benefits:</p>

<ul>
  <li><strong>Robust Control Systems</strong>: Elimination of gimbal lock enables reliable attitude control in aerospace applications</li>
  <li><strong>Smooth Animation</strong>: Constant angular velocity interpolation produces natural-looking rotations in computer graphics</li>
  <li><strong>Numerical Stability</strong>: Better conditioning of rotation operations reduces accumulation of numerical errors</li>
  <li><strong>Compact Representation</strong>: Four parameters vs. nine for rotation matrices, with inherent orthogonality constraints</li>
</ul>

<h2 id="conclusion-mathematical-rigor-in-computational-practice">Conclusion: Mathematical Rigor in Computational Practice</h2>

<p>The choice between Euler angles and quaternions is not merely one of computational convenience—it reflects a deeper understanding of the mathematical structure underlying 3D rotations. While Euler angles offer intuitive parameterization for human interfaces, quaternions provide the mathematically principled foundation necessary for robust computational systems.</p>

<p>The interactive visualizations demonstrate these theoretical principles through direct manipulation, revealing how mathematical abstractions manifest as concrete computational behaviors. This convergence of theory and practice exemplifies the power of choosing representations that align with the underlying mathematical reality.</p>

<hr />

<p><em>Explore the interactive mathematical demonstrations above to develop geometric intuition for these abstract concepts.</em></p>

<!-- <!DOCTYPE html> -->
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>3D Rotations: Understanding Gimbal Lock</title>
    <style>
        body {
            margin: 0;
            padding: 20px;
            font-family: 'Segoe UI', system-ui, sans-serif;
            background: linear-gradient(135deg, #0f0f23 0%, #1a1a2e 50%, #16213e 100%);
            color: #e0e0e0;
            min-height: 100vh;
        }
        
        .container {
            max-width: 1400px;
            margin: 0 auto;
        }
        
        .header {
            text-align: center;
            margin-bottom: 30px;
        }
        
        .header h1 {
            font-size: 2.5rem;
            background: linear-gradient(135deg, #64ffda, #7c4dff);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            margin-bottom: 10px;
        }
        
        .header p {
            font-size: 1.1rem;
            opacity: 0.8;
            max-width: 800px;
            margin: 0 auto;
        }
        
        .visualization-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
            margin-bottom: 30px;
        }
        
        .wide-panel {
            grid-column: 1 / -1;
        }
        
        .panel {
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(10px);
            border-radius: 15px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            padding: 20px;
            position: relative;
            overflow: hidden;
        }
        
        .panel h3 {
            margin: 0 0 15px 0;
            font-size: 1.3rem;
            color: #64ffda;
            display: flex;
            align-items: center;
            gap: 10px;
        }
        
        .canvas-container {
            position: relative;
            width: 100%;
            height: 400px;
            border-radius: 10px;
            overflow: hidden;
            background: #000;
        }
        
        canvas {
            display: block;
            width: 100%;
            height: 100%;
        }
        
        .controls {
            margin-top: 15px;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 15px;
        }
        
        .control-group {
            display: flex;
            flex-direction: column;
            gap: 8px;
        }
        
        .control-group label {
            font-size: 0.9rem;
            color: #b0b0b0;
            font-weight: 500;
        }
        
        .slider {
            -webkit-appearance: none;
            appearance: none;
            width: 100%;
            height: 6px;
            border-radius: 3px;
            background: rgba(255, 255, 255, 0.1);
            outline: none;
        }
        
        .slider::-webkit-slider-thumb {
            -webkit-appearance: none;
            appearance: none;
            width: 18px;
            height: 18px;
            border-radius: 50%;
            background: #64ffda;
            cursor: pointer;
            box-shadow: 0 0 10px rgba(100, 255, 218, 0.3);
        }
        
        .value-display {
            font-family: 'Courier New', monospace;
            color: #64ffda;
            font-weight: bold;
        }
        
        .warning {
            background: rgba(255, 107, 107, 0.15);
            border: 1px solid rgba(255, 107, 107, 0.3);
            border-radius: 8px;
            padding: 12px;
            margin-top: 10px;
            color: #ff6b6b;
            font-weight: bold;
            display: none;
        }
        
        .warning.active {
            display: block;
            animation: pulse 1s ease-in-out infinite alternate;
        }
        
        @keyframes pulse {
            from { opacity: 0.7; }
            to { opacity: 1; }
        }
        
        .button {
            background: linear-gradient(135deg, #7c4dff, #536dfe);
            border: none;
            border-radius: 8px;
            padding: 10px 20px;
            color: white;
            font-weight: bold;
            cursor: pointer;
            transition: all 0.3s ease;
        }
        
        .button:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(124, 77, 255, 0.4);
        }
        
        .info-box {
            background: rgba(100, 255, 218, 0.1);
            border: 1px solid rgba(100, 255, 218, 0.3);
            border-radius: 8px;
            padding: 15px;
            margin-top: 15px;
            font-size: 0.9rem;
            line-height: 1.5;
        }
        
        .quaternion-display {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 10px;
            margin-top: 10px;
        }
        
        .quat-component {
            text-align: center;
            padding: 8px;
            background: rgba(255, 255, 255, 0.05);
            border-radius: 6px;
            font-family: 'Courier New', monospace;
        }
        
        .quat-component .label {
            font-size: 0.8rem;
            opacity: 0.7;
            margin-bottom: 4px;
        }
        
        .quat-component .value {
            font-weight: bold;
            color: #64ffda;
        }
        
        .hopf-info-panel {
            background: rgba(100, 255, 218, 0.1);
            border: 1px solid rgba(100, 255, 218, 0.3);
            border-radius: 8px;
            padding: 15px;
            margin: 10px 0;
            font-size: 0.9rem;
            line-height: 1.5;
        }
        
        .hopf-info-panel h4 {
            margin: 0 0 10px 0;
            font-size: 1.1rem;
            color: #64ffda;
        }
        
        .hopf-info-panel p {
            margin: 5px 0;
            color: #e0e0e0;
        }
    </style>
</head>
<body>
    <div class="container">
    </div>

    <!-- Three.js for 3D rendering -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/three@0.128.0/examples/js/controls/TrackballControls.js"></script>

    <script>
        // ===============================================================================
        // COMPREHENSIVE MATHEMATICAL ANALYSIS OF QUATERNION SPHERE VISUALIZATION
        // ===============================================================================
        
        // Mathematical Constants and Tolerances
        const MATH_EPSILON = 1e-15;
        const QUATERNION_TOLERANCE = 1e-12;
        const GEODESIC_RESOLUTION = 100;
        const ANTIPODAL_THRESHOLD = 0.999999; // cos(θ) for nearly antipodal quaternions
        
        // Mathematical Analysis State
        const mathAnalysis = {
            unitConstraintViolations: [],
            geodesicErrors: [],
            singularityDetections: [],
            temporalCoherence: [],
            projectionErrors: [],
            frameCount: 0
        };

        // ===============================================================================
        // ENHANCED QUATERNION OPERATIONS WITH MATHEMATICAL VERIFICATION
        // ===============================================================================
        
        // Unit quaternion normalization with constraint verification
        const qNorm = ([x, y, z, w]) => {
            const normSq = x*x + y*y + z*z + w*w;
            const norm = Math.sqrt(normSq);
            
            // Mathematical verification: Check if quaternion is already unit
            const unitConstraintError = Math.abs(norm - 1.0);
            if (unitConstraintError > QUATERNION_TOLERANCE) {
                mathAnalysis.unitConstraintViolations.push({
                    frame: mathAnalysis.frameCount,
                    originalNorm: norm,
                    error: unitConstraintError,
                    quaternion: [x, y, z, w]
                });
            }
            
            // Handle near-zero quaternions (degenerate case)
            if (norm < MATH_EPSILON) {
                console.warn("Mathematical Analysis: Degenerate quaternion detected, defaulting to identity");
                return [0, 0, 0, 1];
            }
            
            const s = 1 / norm;
            const result = [x*s, y*s, z*s, w*s];
            
            // Post-normalization verification
            const resultNorm = Math.sqrt(result[0]*result[0] + result[1]*result[1] +
                                       result[2]*result[2] + result[3]*result[3]);
            const postNormError = Math.abs(resultNorm - 1.0);
            
            if (postNormError > QUATERNION_TOLERANCE) {
                console.error("Mathematical Analysis: Post-normalization error:", postNormError);
            }
            
            return result;
        };

        // Quaternion multiplication with mathematical verification
        const qMul = ([ax, ay, az, aw], [bx, by, bz, bw]) => {
            const result = [
                aw*bx + ax*bw + ay*bz - az*by,  // i component
                aw*by - ax*bz + ay*bw + az*bx,  // j component
                aw*bz + ax*by - ay*bx + az*bw,  // k component
                aw*bw - ax*bx - ay*by - az*bz   // scalar component
            ];
            
            // Mathematical verification: Product of unit quaternions should be unit
            const aNorm = Math.sqrt(ax*ax + ay*ay + az*az + aw*aw);
            const bNorm = Math.sqrt(bx*bx + by*by + bz*bz + bw*bw);
            const resultNorm = Math.sqrt(result[0]*result[0] + result[1]*result[1] +
                                       result[2]*result[2] + result[3]*result[3]);
            
            const expectedNorm = aNorm * bNorm;
            const multiplicativeError = Math.abs(resultNorm - expectedNorm);
            
            if (multiplicativeError > QUATERNION_TOLERANCE) {
                console.warn("Mathematical Analysis: Quaternion multiplication norm error:",
                           multiplicativeError);
            }
            
            return result;
        };

        const qFromAxisAngle = ([ux, uy, uz], th) => {
            const s = Math.sin(th / 2);
            return qNorm([ux*s, uy*s, uz*s, Math.cos(th / 2)]);
        };

        const qFromEulerZYX = (yaw, pitch, roll) => {
            const cy = Math.cos(yaw / 2), sy = Math.sin(yaw / 2);
            const cp = Math.cos(pitch / 2), sp = Math.sin(pitch / 2);
            const cr = Math.cos(roll / 2), sr = Math.sin(roll / 2);
            
            return qNorm([
                sr*cp*cy - cr*sp*sy,
                cr*sp*cy + sr*cp*sy,
                cr*cp*sy - sr*sp*cy,
                cr*cp*cy + sr*sp*sy
            ]);
        };

        // ===============================================================================
        // GEODESIC ANALYSIS AND SLERP WITH MATHEMATICAL VERIFICATION
        // ===============================================================================
        
        // Calculate geodesic distance on S³ between two quaternions
        const geodesicDistance = (qa, qb) => {
            // Ensure we take the shorter path (handle double cover)
            let dot = Math.abs(qa[0]*qb[0] + qa[1]*qb[1] + qa[2]*qb[2] + qa[3]*qb[3]);
            dot = Math.min(1.0, Math.max(-1.0, dot)); // Clamp for numerical stability
            return Math.acos(dot);
        };
        
        // Analyze antipodal points and their geodesic properties
        const analyzeAntipodalPoints = (q) => {
            const [x, y, z, w] = q;
            const antipodalQ = [-x, -y, -z, -w];
            
            // Verify that q and -q represent the same rotation
            const rotationMatrix1 = qToMatrix(q);
            const rotationMatrix2 = qToMatrix(antipodalQ);
            
            // Calculate matrix difference (should be zero for same rotation)
            let maxMatrixDiff = 0;
            for (let i = 0; i < 16; i++) {
                const diff = Math.abs(rotationMatrix1[i] - rotationMatrix2[i]);
                maxMatrixDiff = Math.max(maxMatrixDiff, diff);
            }
            
            // Calculate antipodal geodesic distance (should be π for true antipodal points)
            const dot = x*(-x) + y*(-y) + z*(-z) + w*(-w); // Should be -1 for unit quaternions
            const clampedDot = Math.min(1.0, Math.max(-1.0, dot));
            // For antipodal points: dot = -1, so arccos(-1) = π
            const antipodalGeodesicDist = Math.acos(clampedDot);
            
            // For true antipodal points on unit sphere, distance should be π
            const expectedDistance = Math.PI;
            const geodesicError = Math.abs(antipodalGeodesicDist - expectedDistance);
            
            // Additional verification: dot product should be -1 for perfect antipodal points
            const dotProductError = Math.abs(dot - (-1.0));
            
            return {
                q: q,
                antipodal: antipodalQ,
                geodesicDistance: antipodalGeodesicDist,
                geodesicError: geodesicError,
                dotProduct: dot,
                dotProductError: dotProductError,
                rotationMatrixError: maxMatrixDiff,
                isValidAntipodal: geodesicError < QUATERNION_TOLERANCE * 100 && maxMatrixDiff < QUATERNION_TOLERANCE * 10
            };
        };
        
        // Enhanced SLERP with comprehensive mathematical analysis
        const qSlerp = (a, b, t) => {
            // Input validation
            if (t < 0 || t > 1) {
                console.warn("Mathematical Analysis: SLERP parameter t outside [0,1]:", t);
            }
            
            // Ensure inputs are normalized
            const aNorm = qNorm(a);
            const bNorm = qNorm(b);
            let [ax, ay, az, aw] = aNorm;
            let [bx, by, bz, bw] = bNorm;
            
            // Calculate dot product for geodesic analysis
            let dot = ax*bx + ay*by + az*bz + aw*bw;
            const originalDot = dot;
            
            // Handle double cover: choose shorter path on S³
            if (dot < 0) {
                dot = -dot;
                bx = -bx; by = -by; bz = -bz; bw = -bw;
            }
            
            // Mathematical analysis: Record geodesic properties
            const geodesicAngle = Math.acos(Math.min(1, Math.abs(originalDot)));
            mathAnalysis.geodesicErrors.push({
                frame: mathAnalysis.frameCount,
                geodesicAngle: geodesicAngle,
                dotProduct: originalDot,
                shortestPath: dot > Math.abs(originalDot)
            });
            
            // Handle near-parallel quaternions (cosine close to 1)
            if (dot > 0.9995) {
                // Use linear interpolation for numerical stability
                const result = qNorm([ax + t*(bx-ax), ay + t*(by-ay), az + t*(bz-az), aw + t*(bw-aw)]);
                
                // Verify constant angular velocity property is maintained
                const midPoint = qSlerp(aNorm, bNorm, 0.5);
                const dist1 = geodesicDistance(aNorm, midPoint);
                const dist2 = geodesicDistance(midPoint, bNorm);
                const velocityError = Math.abs(dist1 - dist2);
                
                if (velocityError > QUATERNION_TOLERANCE) {
                    console.warn("Mathematical Analysis: SLERP velocity consistency error:", velocityError);
                }
                
                return result;
            }
            
            // Handle antipodal quaternions (dot ≈ 0 after negation) - NON-RECURSIVE
            if (dot < 0.001) {
                console.log("Mathematical Analysis: Handling antipodal quaternions with non-recursive method");
                
                // For antipodal quaternions, we need to interpolate through a great semicircle
                // Use simple linear interpolation and then normalize (NLERP) for stability
                const result = qNorm([
                    ax + t * (bx - ax),
                    ay + t * (by - ay),
                    az + t * (bz - az),
                    aw + t * (bw - aw)
                ]);
                
                // Record this as a special case in mathematical analysis
                mathAnalysis.geodesicErrors.push({
                    frame: mathAnalysis.frameCount,
                    type: 'antipodal-nlerp-fallback',
                    t: t,
                    originalDot: originalDot,
                    message: 'Used NLERP for antipodal quaternions to avoid recursion'
                });
                
                return result;
            }
            
            // Standard SLERP calculation
            const theta = Math.acos(Math.min(1, dot));
            const sinTheta = Math.sin(theta);
            
            // Check for numerical stability
            if (Math.abs(sinTheta) < MATH_EPSILON) {
                console.warn("Mathematical Analysis: SLERP numerical instability detected");
                return qNorm([ax + t*(bx-ax), ay + t*(by-ay), az + t*(bz-az), aw + t*(bw-aw)]);
            }
            
            const aCoeff = Math.sin((1-t)*theta) / sinTheta;
            const bCoeff = Math.sin(t*theta) / sinTheta;
            
            const result = [ax*aCoeff + bx*bCoeff, ay*aCoeff + by*bCoeff,
                          az*aCoeff + bz*bCoeff, aw*aCoeff + bw*bCoeff];
            
            // Mathematical verification: Check that result lies on unit sphere
            const resultNorm = Math.sqrt(result[0]*result[0] + result[1]*result[1] +
                                       result[2]*result[2] + result[3]*result[3]);
            const normError = Math.abs(resultNorm - 1.0);
            
            if (normError > QUATERNION_TOLERANCE) {
                console.warn("Mathematical Analysis: SLERP result not on unit sphere:", normError);
            }
            
            // Verify geodesic property: distances should scale linearly with t
            if (t > 0 && t < 1) {
                const distTotal = geodesicDistance(aNorm, bNorm);
                const distPartial = geodesicDistance(aNorm, result);
                const expectedPartial = t * distTotal;
                const geodesicLinearityError = Math.abs(distPartial - expectedPartial);
                
                if (geodesicLinearityError > QUATERNION_TOLERANCE * 10) {
                    mathAnalysis.geodesicErrors.push({
                        frame: mathAnalysis.frameCount,
                        t: t,
                        geodesicLinearityError: geodesicLinearityError,
                        distTotal: distTotal,
                        distPartial: distPartial
                    });
                }
            }
            
            return result;
        };

        const qToMatrix = ([x, y, z, qw]) => {
            const xx = x*x, yy = y*y, zz = z*z;
            const xy = x*y, xz = x*z, xw = x*qw;
            const yz = y*z, yw = y*qw, zw = z*qw;
            
            return [
                1-2*(yy+zz), 2*(xy-zw), 2*(xz+yw), 0,
                2*(xy+zw), 1-2*(xx+zz), 2*(yz-xw), 0,
                2*(xz-yw), 2*(yz+xw), 1-2*(xx+yy), 0,
                0, 0, 0, 1
            ];
        };

        // Convert quaternion to Euler angles (ZYX order)
        const qToEulerZYX = ([x, y, z, w]) => {
            const sinr_cosp = 2 * (w * x + y * z);
            const cosr_cosp = 1 - 2 * (x * x + y * y);
            const roll = Math.atan2(sinr_cosp, cosr_cosp);
            
            const sinp = 2 * (w * y - z * x);
            const pitch = Math.abs(sinp) >= 1 ? Math.sign(sinp) * Math.PI / 2 : Math.asin(sinp);
            
            const siny_cosp = 2 * (w * z + x * y);
            const cosy_cosp = 1 - 2 * (y * y + z * z);
            const yaw = Math.atan2(siny_cosp, cosy_cosp);
            
            return [yaw, pitch, roll];
        };

        // ===============================================================================
        // ENHANCED QUATERNION OPERATIONS WITH MATHEMATICAL VERIFICATION
        // ===============================================================================
        
        // Conjugate quaternion with verification
        const qConj = ([x, y, z, w]) => {
            const result = [-x, -y, -z, w];
            
            // Mathematical verification: ||q*|| = ||q|| for unit quaternions
            const originalNorm = Math.sqrt(x*x + y*y + z*z + w*w);
            const conjugateNorm = Math.sqrt(x*x + y*y + z*z + w*w); // Same calculation
            const normError = Math.abs(originalNorm - conjugateNorm);
            
            if (normError > QUATERNION_TOLERANCE) {
                console.warn("Mathematical Analysis: Conjugate norm error:", normError);
            }
            
            return result;
        };

        // Quaternion dot product with bounds checking
        const qDot = (a, b) => {
            const result = a[0]*b[0] + a[1]*b[1] + a[2]*b[2] + a[3]*b[3];
            
            // For unit quaternions, dot product should be in [-1, 1]
            if (Math.abs(result) > 1.0 + QUATERNION_TOLERANCE) {
                console.warn("Mathematical Analysis: Dot product outside [-1,1]:", result);
            }
            
            return result;
        };

        // Enhanced axis-angle extraction with singularity handling
        const qToAxisAngle = ([x, y, z, w]) => {
            // Ensure w is positive for canonical representation
            let qx = x, qy = y, qz = z, qw = w;
            if (w < 0) {
                qx = -x; qy = -y; qz = -z; qw = -w;
            }
            
            const angle = 2 * Math.acos(Math.min(1, Math.max(-1, qw)));
            
            // Handle near-identity quaternion (small angle)
            if (Math.abs(angle) < MATH_EPSILON) {
                return [[1, 0, 0], 0];
            }
            
            const s = Math.sin(angle / 2);
            
            // Handle singularity when s ≈ 0 (shouldn't happen for valid quaternions)
            if (Math.abs(s) < MATH_EPSILON) {
                mathAnalysis.singularityDetections.push({
                    frame: mathAnalysis.frameCount,
                    type: 'axis-angle-extraction',
                    quaternion: [x, y, z, w],
                    angle: angle,
                    sinHalfAngle: s
                });
                return [[1, 0, 0], angle];
            }
            
            // Extract and normalize axis
            let axis = [qx/s, qy/s, qz/s];
            const axisNorm = Math.sqrt(axis[0]*axis[0] + axis[1]*axis[1] + axis[2]*axis[2]);
            
            // Normalize axis to ensure unit length (handle floating point precision errors)
            if (axisNorm > MATH_EPSILON) {
                axis = [axis[0]/axisNorm, axis[1]/axisNorm, axis[2]/axisNorm];
            }
            
            // Mathematical verification: only warn for significant axis normalization errors
            const axisNormError = Math.abs(axisNorm - 1.0);
            if (axisNormError > QUATERNION_TOLERANCE * 10) {
                console.warn("Mathematical Analysis: Non-unit rotation axis:", axisNormError);
            }
            
            return [axis, angle];
        };
        
        // ===============================================================================
        // SO(3) TO S³ MAPPING VERIFICATION
        // ===============================================================================
        
        // Verify the double cover relationship: q and -q map to same rotation
        const verifyDoubleCover = (q) => {
            const [x, y, z, w] = q;
            const antipodalQ = [-x, -y, -z, -w];
            
            // Convert both quaternions to rotation matrices
            const R1 = qToMatrix(q);
            const R2 = qToMatrix(antipodalQ);
            
            // Calculate Frobenius norm of difference
            let frobeniusNorm = 0;
            for (let i = 0; i < 16; i++) {
                const diff = R1[i] - R2[i];
                frobeniusNorm += diff * diff;
            }
            frobeniusNorm = Math.sqrt(frobeniusNorm);
            
            // Verify that both quaternions produce the same rotation
            const doubleCoverError = frobeniusNorm;
            
            if (doubleCoverError > QUATERNION_TOLERANCE) {
                console.error("Mathematical Analysis: Double cover violation:", doubleCoverError);
                mathAnalysis.geodesicErrors.push({
                    frame: mathAnalysis.frameCount,
                    type: 'double-cover-violation',
                    error: doubleCoverError,
                    quaternion: q
                });
            }
            
            return {
                isValid: doubleCoverError < QUATERNION_TOLERANCE,
                error: doubleCoverError,
                rotationMatrix1: R1,
                rotationMatrix2: R2
            };
        };
        
        // Verify SO(3) properties of rotation matrix
        const verifySO3Properties = (rotationMatrix) => {
            const R = rotationMatrix;
            
            // Check orthogonality: R^T * R = I
            const RTR = [];
            for (let i = 0; i < 3; i++) {
                for (let j = 0; j < 3; j++) {
                    let sum = 0;
                    for (let k = 0; k < 3; k++) {
                        sum += R[k*4 + i] * R[k*4 + j]; // R^T[i][k] * R[k][j]
                    }
                    RTR[i*3 + j] = sum;
                }
            }
            
            // Check if RTR is identity matrix
            let orthogonalityError = 0;
            for (let i = 0; i < 3; i++) {
                for (let j = 0; j < 3; j++) {
                    const expected = (i === j) ? 1 : 0;
                    const error = Math.abs(RTR[i*3 + j] - expected);
                    orthogonalityError = Math.max(orthogonalityError, error);
                }
            }
            
            // Check determinant = 1 (proper rotation)
            const det = R[0] * (R[5] * R[10] - R[6] * R[9]) -
                       R[1] * (R[4] * R[10] - R[6] * R[8]) +
                       R[2] * (R[4] * R[9] - R[5] * R[8]);
            const determinantError = Math.abs(det - 1.0);
            
            return {
                isValidSO3: orthogonalityError < QUATERNION_TOLERANCE && determinantError < QUATERNION_TOLERANCE,
                orthogonalityError: orthogonalityError,
                determinantError: determinantError,
                determinant: det
            };
        };
        
        // ===============================================================================
        // STEREOGRAPHIC PROJECTION AND TEMPORAL COHERENCE ANALYSIS
        // ===============================================================================
        
        // Enhanced stereographic projection with error analysis
        const stereographicProjection = (q, fromPole = 'north') => {
            const [x, y, z, w] = qNorm(q); // Ensure unit quaternion
            
            let projectedX, projectedY, projectionError = 0;
            
            if (fromPole === 'north') {
                // Project from north pole (w = 1)
                if (Math.abs(1 - w) < MATH_EPSILON) {
                    // Near north pole - use Taylor expansion for numerical stability
                    const factor = 2 / (1 + MATH_EPSILON);
                    projectedX = x * factor;
                    projectedY = y * factor;
                    projectionError = MATH_EPSILON;
                } else {
                    const denominator = 1 - w;
                    projectedX = x / denominator;
                    projectedY = y / denominator;
                    
                    // Check for numerical issues
                    if (Math.abs(denominator) < QUATERNION_TOLERANCE) {
                        projectionError = Math.abs(denominator);
                        mathAnalysis.projectionErrors.push({
                            frame: mathAnalysis.frameCount,
                            type: 'stereographic-singularity',
                            quaternion: q,
                            denominator: denominator,
                            error: projectionError
                        });
                    }
                }
            } else {
                // Project from south pole (w = -1)
                if (Math.abs(1 + w) < MATH_EPSILON) {
                    const factor = 2 / (1 + MATH_EPSILON);
                    projectedX = x * factor;
                    projectedY = y * factor;
                    projectionError = MATH_EPSILON;
                } else {
                    const denominator = 1 + w;
                    projectedX = x / denominator;
                    projectedY = y / denominator;
                    
                    if (Math.abs(denominator) < QUATERNION_TOLERANCE) {
                        projectionError = Math.abs(denominator);
                    }
                }
            }
            
            return {
                x: projectedX,
                y: projectedY,
                error: projectionError,
                isValid: projectionError < QUATERNION_TOLERANCE
            };
        };
        
        // Temporal coherence analysis for antipodal motion vectors
        const analyzeTemporalCoherence = (currentQ, previousQ, dt) => {
            if (!previousQ) return null;
            
            const currentNorm = qNorm(currentQ);
            const previousNorm = qNorm(previousQ);
            
            // Calculate angular velocity
            const deltaQ = qMul(currentNorm, qConj(previousNorm));
            const [deltaAxis, deltaAngle] = qToAxisAngle(deltaQ);
            const angularVelocity = deltaAngle / dt;
            
            // Analyze antipodal consistency
            const currentAntipodal = [-currentNorm[0], -currentNorm[1], -currentNorm[2], -currentNorm[3]];
            const previousAntipodal = [-previousNorm[0], -previousNorm[1], -previousNorm[2], -previousNorm[3]];
            
            // Check if antipodal pairs maintain same relative motion
            const antipodalDeltaQ = qMul(currentAntipodal, qConj(previousAntipodal));
            const [antipodalDeltaAxis, antipodalDeltaAngle] = qToAxisAngle(antipodalDeltaQ);
            const antipodalAngularVelocity = antipodalDeltaAngle / dt;
            
            // Temporal coherence error
            const velocityCoherenceError = Math.abs(angularVelocity - antipodalAngularVelocity);
            const axisCoherenceError = Math.acos(Math.min(1, Math.abs(
                deltaAxis[0] * antipodalDeltaAxis[0] +
                deltaAxis[1] * antipodalDeltaAxis[1] +
                deltaAxis[2] * antipodalDeltaAxis[2]
            )));
            
            const coherenceAnalysis = {
                frame: mathAnalysis.frameCount,
                angularVelocity: angularVelocity,
                antipodalAngularVelocity: antipodalAngularVelocity,
                velocityCoherenceError: velocityCoherenceError,
                axisCoherenceError: axisCoherenceError,
                isCoherent: velocityCoherenceError < QUATERNION_TOLERANCE && axisCoherenceError < QUATERNION_TOLERANCE
            };
            
            mathAnalysis.temporalCoherence.push(coherenceAnalysis);
            
            return coherenceAnalysis;
        };

        // Global state
        let currentQuaternion = [0, 0, 0, 1];
        let sphereQuaternion = [0, 0, 0, 1]; // Independent quaternion for sphere visualization
        let sphereEuler = [0, 0, 0]; // Independent Euler angles for sphere
        let showAntipodes = false;
        let showGeodesicArc = false;
        let animating = false;
        let quaternionHistory = [[0, 0, 0, 1]]; // For drift correction
        let frameCount = 0;
        let showTorusView = true;
        let updatingFromSlider = false; // Prevent circular updates

        // Canvas contexts (will be initialized in init function)
        let heatmapCtx, slerpVelocityCtx;
        
        // Three.js for SLERP path visualization
        let slerpScene, slerpCamera, slerpRenderer, slerpControls;
        let slerpSphere, slerpPathLine, eulerPathLine;
        let slerpCurrentPoint, eulerCurrentPoint;

        // Utility functions
        const deg2rad = (deg) => deg * Math.PI / 180;
        const rad2deg = (rad) => rad * 180 / Math.PI;

        const resizeCanvases = () => {
            // Only resize 2D canvases (gimbal canvas is handled by Three.js)
            const canvases = [
                document.getElementById('quaternionCanvas'),
                document.getElementById('heatmapCanvas'),
                document.getElementById('slerpVelocityCanvas')
            ];
            
            canvases.forEach(canvas => {
                if (canvas) {
                    const rect = canvas.parentElement.getBoundingClientRect();
                    canvas.width = rect.width * window.devicePixelRatio;
                    canvas.height = rect.height * window.devicePixelRatio;
                    canvas.style.width = rect.width + 'px';
                    canvas.style.height = rect.height + 'px';
                    
                    const ctx = canvas.getContext('2d');
                    if (ctx) {
                        ctx.scale(window.devicePixelRatio, window.devicePixelRatio);
                    }
                }
            });
        };

        // THREE.JS 3D Gimbal Visualization
        let scene, camera, renderer, controls;
        let gimbalRings = [];
        let coordinateAxes = [];
        let centerSphere;

        // THREE.JS 3D Quaternion Sphere Visualization
        let quaternionScene, quaternionCamera, quaternionRenderer, quaternionControls;
        let quaternionSphere;
        let quaternionPoint, antipodalPoint;
        let geodesicCurve;

        const initThreeJS = () => {
            const canvas = document.getElementById('gimbalCanvas');
            const width = canvas.clientWidth;
            const height = canvas.clientHeight;

            // Scene
            scene = new THREE.Scene();
            scene.background = new THREE.Color(0x000000);

            // Camera
            camera = new THREE.PerspectiveCamera(75, width / height, 0.1, 1000);
            camera.position.set(3, 2, 4);

            // Renderer
            renderer = new THREE.WebGLRenderer({ canvas: canvas, antialias: true });
            renderer.setSize(width, height);
            renderer.setPixelRatio(window.devicePixelRatio);
            renderer.shadowMap.enabled = true;
            renderer.shadowMap.type = THREE.PCFSoftShadowMap;

            // Trackball Controls for free-form 3D navigation
            controls = new THREE.TrackballControls(camera, renderer.domElement);
            
            // Configure trackball behavior
            controls.rotateSpeed = 1.5;
            controls.zoomSpeed = 1.2;
            controls.panSpeed = 0.8;
            controls.noZoom = false;
            controls.noPan = false;
            controls.staticMoving = true;
            controls.dynamicDampingFactor = 0.3;
            controls.keys = [65, 83, 68]; // A, S, D keys for pan
            
            // Set zoom limits
            controls.minDistance = 2;
            controls.maxDistance = 8;

            // Lighting
            const ambientLight = new THREE.AmbientLight(0x404040, 0.4);
            scene.add(ambientLight);

            const directionalLight = new THREE.DirectionalLight(0xffffff, 0.8);
            directionalLight.position.set(5, 5, 5);
            directionalLight.castShadow = true;
            directionalLight.shadow.mapSize.width = 2048;
            directionalLight.shadow.mapSize.height = 2048;
            scene.add(directionalLight);

            const pointLight = new THREE.PointLight(0x64ffda, 0.3, 100);
            pointLight.position.set(-3, 3, -3);
            scene.add(pointLight);

            // Create gimbal rings
            createGimbalRings();
            
            // Create coordinate axes
            createCoordinateAxes();
            
            // Create center sphere
            createCenterSphere();

            // Add grid helper
            const gridHelper = new THREE.GridHelper(4, 20, 0x333333, 0x111111);
            gridHelper.position.y = -2;
            scene.add(gridHelper);

            // Start animation loop
            animate();
        };

        const createGimbalRings = () => {
            // Create gimbal hierarchy: yaw contains pitch, pitch contains roll
            const yawGroup = new THREE.Group(); // Outermost group for yaw
            const pitchGroup = new THREE.Group(); // Middle group for pitch
            const rollGroup = new THREE.Group(); // Inner group for roll
            
            // Set up hierarchy: yaw -> pitch -> roll
            scene.add(yawGroup);
            yawGroup.add(pitchGroup);
            pitchGroup.add(rollGroup);
            
            const ringConfigs = [
                { radius: 1.8, tube: 0.08, color: 0x64ffda, name: 'yaw', group: yawGroup },
                { radius: 1.4, tube: 0.06, color: 0x4caf50, name: 'pitch', group: pitchGroup },
                { radius: 1.0, tube: 0.04, color: 0xff9800, name: 'roll', group: rollGroup }
            ];

            ringConfigs.forEach((config, index) => {
                const geometry = new THREE.TorusGeometry(config.radius, config.tube, 16, 100);
                
                // For yaw ring, create a gradient material to make rotation visible
                let material;
                if (index === 0) {
                    // Create a custom shader material with gradient for yaw ring
                    material = new THREE.ShaderMaterial({
                        uniforms: {
                            color1: { value: new THREE.Color(0x64ffda) },
                            color2: { value: new THREE.Color(0x1e88e5) }
                        },
                        vertexShader: `
                            varying vec3 vPosition;
                            void main() {
                                vPosition = position;
                                gl_Position = projectionMatrix * modelViewMatrix * vec4(position, 1.0);
                            }
                        `,
                        fragmentShader: `
                            uniform vec3 color1;
                            uniform vec3 color2;
                            varying vec3 vPosition;
                            void main() {
                                float mixFactor = (atan(vPosition.y, vPosition.x) + 3.14159) / (2.0 * 3.14159);
                                vec3 color = mix(color1, color2, mixFactor);
                                gl_FragColor = vec4(color, 0.9);
                            }
                        `,
                        transparent: true
                    });
                } else {
                    material = new THREE.MeshPhongMaterial({
                        color: config.color,
                        shininess: 100,
                        transparent: true,
                        opacity: 0.8
                    });
                }
                
                const ring = new THREE.Mesh(geometry, material);
                ring.castShadow = true;
                ring.receiveShadow = true;
                ring.userData = { name: config.name, originalColor: config.color, group: config.group };
                
                // Orient rings in their natural rotation planes
                if (index === 0) { // Yaw ring - rotates around Z (lies in XY plane)
                    // Default orientation is correct
                } else if (index === 1) { // Pitch ring - rotates around Y (lies in XZ plane)
                    ring.rotation.x = Math.PI / 2;
                } else if (index === 2) { // Roll ring - rotates around X (lies in YZ plane)
                    ring.rotation.z = Math.PI / 2;
                }
                
                // Add ring to its respective group
                config.group.add(ring);
                gimbalRings.push(ring);
                
                // Add visual markers to make rotation visible
                const markerGeometry = new THREE.SphereGeometry(0.05, 8, 8);
                const markerMaterial = new THREE.MeshPhongMaterial({
                    color: 0xffffff,
                    emissive: config.color,
                    emissiveIntensity: 0.3
                });
                
                // Add markers around each ring - more for yaw to make rotation visible
                const markerCount = (index === 0) ? 8 : 4; // More markers for yaw ring
                for (let i = 0; i < markerCount; i++) {
                    const angle = (i / markerCount) * Math.PI * 2;
                    const marker = new THREE.Mesh(markerGeometry, markerMaterial);
                    
                    if (index === 0) { // Yaw ring markers
                        marker.position.set(
                            config.radius * Math.cos(angle),
                            config.radius * Math.sin(angle),
                            0
                        );
                        // Add colored markers at cardinal points for yaw ring
                        if (i === 0) {
                            // Front marker - bright cyan
                            marker.material = marker.material.clone();
                            marker.material.color.setHex(0x00ffff);
                            marker.material.emissive.setHex(0x00ffff);
                            marker.material.emissiveIntensity = 0.5;
                            marker.scale.set(1.5, 1.5, 1.5);
                        } else if (i === markerCount / 2) {
                            // Back marker - darker
                            marker.material = marker.material.clone();
                            marker.material.color.setHex(0x006666);
                            marker.material.emissive.setHex(0x003333);
                        }
                    } else if (index === 1) { // Pitch ring markers
                        marker.position.set(
                            config.radius * Math.cos(angle),
                            0,
                            config.radius * Math.sin(angle)
                        );
                    } else if (index === 2) { // Roll ring markers
                        marker.position.set(
                            0,
                            config.radius * Math.cos(angle),
                            config.radius * Math.sin(angle)
                        );
                    }
                    
                    marker.castShadow = true;
                    config.group.add(marker);
                }
                
                // Add a visual strip/band on the yaw ring to make rotation obvious
                if (index === 0) {
                    // Create a colored band on the yaw ring
                    const bandGeometry = new THREE.BoxGeometry(config.radius * 0.3, 0.15, 0.15);
                    const bandMaterial = new THREE.MeshPhongMaterial({
                        color: 0x00ffff,
                        emissive: 0x00ffff,
                        emissiveIntensity: 0.3,
                        shininess: 100
                    });
                    const band = new THREE.Mesh(bandGeometry, bandMaterial);
                    band.position.set(config.radius, 0, 0);
                    band.castShadow = true;
                    config.group.add(band);
                    
                    // Add another contrasting band
                    const band2 = new THREE.Mesh(bandGeometry, bandMaterial.clone());
                    band2.material.color.setHex(0xff00ff);
                    band2.material.emissive.setHex(0xff00ff);
                    band2.position.set(-config.radius, 0, 0);
                    band2.castShadow = true;
                    config.group.add(band2);
                }
                
                // Add arrow indicator for rotation direction - larger for yaw
                const arrowGroup = new THREE.Group();
                const arrowSize = (index === 0) ? 0.06 : 0.03;
                const arrowLength = (index === 0) ? 0.2 : 0.12;
                const arrowGeometry = new THREE.ConeGeometry(arrowSize, arrowLength, 8);
                const arrowMaterial = new THREE.MeshPhongMaterial({
                    color: config.color,
                    emissive: config.color,
                    emissiveIntensity: 0.4
                });
                const arrow = new THREE.Mesh(arrowGeometry, arrowMaterial);
                
                if (index === 0) { // Yaw ring arrow - make it more visible
                    arrow.position.set(config.radius + 0.25, 0, 0);
                    arrow.rotation.z = -Math.PI / 2;
                    
                    // Add a second arrow on opposite side for yaw
                    const arrow2 = new THREE.Mesh(arrowGeometry, arrowMaterial);
                    arrow2.position.set(-config.radius - 0.25, 0, 0);
                    arrow2.rotation.z = Math.PI / 2;
                    arrowGroup.add(arrow2);
                    
                    // Add rotation direction indicator text sprite
                    const canvas = document.createElement('canvas');
                    canvas.width = 128;
                    canvas.height = 64;
                    const context = canvas.getContext('2d');
                    context.fillStyle = '#64ffda';
                    context.font = 'bold 24px Arial';
                    context.fillText('YAW', 30, 40);
                    
                    const texture = new THREE.CanvasTexture(canvas);
                    const spriteMaterial = new THREE.SpriteMaterial({ map: texture, opacity: 0.8 });
                    const sprite = new THREE.Sprite(spriteMaterial);
                    sprite.position.set(0, 0, config.radius + 0.5);
                    sprite.scale.set(0.5, 0.25, 1);
                    config.group.add(sprite);
                } else if (index === 1) { // Pitch ring arrow
                    arrow.position.set(config.radius + 0.15, 0, 0);
                    arrow.rotation.y = Math.PI / 2;
                    arrow.rotation.z = -Math.PI / 2;
                } else if (index === 2) { // Roll ring arrow
                    arrow.position.set(0, config.radius + 0.15, 0);
                    arrow.rotation.x = Math.PI / 2;
                }
                
                arrowGroup.add(arrow);
                config.group.add(arrowGroup);
                
                // Add reference axis line for yaw ring to make rotation clearer
                if (index === 0) {
                    const lineGeometry = new THREE.BufferGeometry();
                    const lineVertices = new Float32Array([
                        0, 0, -0.5,
                        0, 0, 0.5
                    ]);
                    lineGeometry.setAttribute('position', new THREE.BufferAttribute(lineVertices, 3));
                    const lineMaterial = new THREE.LineBasicMaterial({
                        color: 0x64ffda,
                        linewidth: 3,
                        opacity: 0.5,
                        transparent: true
                    });
                    const axisLine = new THREE.Line(lineGeometry, lineMaterial);
                    config.group.add(axisLine);
                }
            });
            
            // Store groups for easy access
            gimbalRings.yawGroup = yawGroup;
            gimbalRings.pitchGroup = pitchGroup;
            gimbalRings.rollGroup = rollGroup;
        };

        const createCoordinateAxes = () => {
            const axisLength = 1.5;
            const axisConfigs = [
                { color: 0xff4444, direction: new THREE.Vector3(1, 0, 0), label: 'X' },
                { color: 0x44ff44, direction: new THREE.Vector3(0, 1, 0), label: 'Y' },
                { color: 0x4444ff, direction: new THREE.Vector3(0, 0, 1), label: 'Z' }
            ];

            axisConfigs.forEach(config => {
                // Create arrow
                const arrowHelper = new THREE.ArrowHelper(
                    config.direction,
                    new THREE.Vector3(0, 0, 0),
                    axisLength,
                    config.color,
                    0.2,
                    0.1
                );
                arrowHelper.line.material.linewidth = 3;
                
                scene.add(arrowHelper);
                coordinateAxes.push(arrowHelper);
            });
        };

        const createCenterSphere = () => {
            const geometry = new THREE.SphereGeometry(0.05, 32, 32);
            const material = new THREE.MeshPhongMaterial({
                color: 0xffffff,
                shininess: 100,
                emissive: 0x222222
            });
            
            centerSphere = new THREE.Mesh(geometry, material);
            centerSphere.castShadow = true;
            scene.add(centerSphere);
        };

        const updateGimbalFromSliders = () => {
            const yaw = deg2rad(parseFloat(document.getElementById('yawSlider').value));
            const pitch = deg2rad(parseFloat(document.getElementById('pitchSlider').value));
            const roll = deg2rad(parseFloat(document.getElementById('rollSlider').value));

            // Check for gimbal lock
            const isGimbalLock = Math.abs(Math.cos(pitch)) < 0.15;

            // Update gimbal groups - each ring rotates in its own group
            if (gimbalRings.yawGroup && gimbalRings.pitchGroup && gimbalRings.rollGroup) {
                // Yaw group rotates around Z-axis
                gimbalRings.yawGroup.rotation.z = yaw;
                
                // Pitch group rotates around Y-axis (and is inside yaw group)
                gimbalRings.pitchGroup.rotation.y = pitch;
                
                // Roll group rotates around X-axis (and is inside pitch group)
                gimbalRings.rollGroup.rotation.x = roll;

                // Update colors based on gimbal lock (skip yaw ring as it has custom shader)
                gimbalRings.forEach((ring, index) => {
                    if (ring.material.color) { // Only update if material has color property (not shader material)
                        ring.material.color.setHex(isGimbalLock ? 0xff6b6b : ring.userData.originalColor);
                        ring.material.emissive.setHex(isGimbalLock ? 0x220000 : 0x000000);
                    } else if (ring.material.uniforms) { // Update shader material for yaw ring
                        if (isGimbalLock) {
                            ring.material.uniforms.color1.value.setHex(0xff6b6b);
                            ring.material.uniforms.color2.value.setHex(0xff4444);
                        } else {
                            ring.material.uniforms.color1.value.setHex(0x64ffda);
                            ring.material.uniforms.color2.value.setHex(0x1e88e5);
                        }
                    }
                });

                // Keep controls active during gimbal lock so users can experience it
                // Just visually indicate the gimbal lock state without disabling controls
                const rollSlider = document.getElementById('rollSlider');
                
                if (rollSlider && isGimbalLock) {
                    // Visual indication of gimbal lock without disabling
                    rollSlider.style.borderColor = '#ff6b6b';
                    rollSlider.style.boxShadow = '0 0 5px rgba(255, 107, 107, 0.5)';
                } else if (rollSlider) {
                    rollSlider.style.borderColor = '';
                    rollSlider.style.boxShadow = '';
                }
            }

            // Update coordinate axes - they should be in the final roll group to show final orientation
            if (coordinateAxes.length >= 3 && gimbalRings.rollGroup) {
                // Remove axes from scene and add to roll group so they rotate with final orientation
                coordinateAxes.forEach(axis => {
                    if (axis.parent !== gimbalRings.rollGroup) {
                        if (axis.parent) axis.parent.remove(axis);
                        gimbalRings.rollGroup.add(axis);
                    }
                });
            }

            // Update gimbal lock warning
            document.getElementById('gimbalWarning').classList.toggle('active', isGimbalLock);
        };

        const animate = () => {
            requestAnimationFrame(animate);
            
            controls.update();
            
            // Don't rotate the entire scene - let the user control the view
            // The gimbal rings should be the only things rotating
            
            renderer.render(scene, camera);
        };

        const resizeThreeJS = () => {
            const canvas = document.getElementById('gimbalCanvas');
            const width = canvas.clientWidth;
            const height = canvas.clientHeight;
            
            camera.aspect = width / height;
            camera.updateProjectionMatrix();
            renderer.setSize(width, height);
        };

        // Replace the old drawGimbal function
        const drawGimbal = () => {
            if (renderer) {
                updateGimbalFromSliders();
                // Also update quaternion sphere
                updateQuaternionSphere3D();
            }
        };

        // Initialize 3D Quaternion Sphere Visualization
        const initQuaternionSphere3D = () => {
            const canvas = document.getElementById('quaternionCanvas');
            const width = canvas.clientWidth;
            const height = canvas.clientHeight;

            // Scene
            quaternionScene = new THREE.Scene();
            quaternionScene.background = new THREE.Color(0x000011);

            // Camera
            quaternionCamera = new THREE.PerspectiveCamera(75, width / height, 0.1, 1000);
            quaternionCamera.position.set(2.5, 1.5, 3);

            // Renderer
            quaternionRenderer = new THREE.WebGLRenderer({ canvas: canvas, antialias: true });
            quaternionRenderer.setSize(width, height);
            quaternionRenderer.setPixelRatio(window.devicePixelRatio);
            quaternionRenderer.shadowMap.enabled = true;
            quaternionRenderer.shadowMap.type = THREE.PCFSoftShadowMap;

            // Trackball Controls for free-form 3D navigation
            quaternionControls = new THREE.TrackballControls(quaternionCamera, quaternionRenderer.domElement);
            quaternionControls.rotateSpeed = 1.0;
            quaternionControls.zoomSpeed = 1.2;
            quaternionControls.panSpeed = 0.8;
            quaternionControls.noZoom = false;
            quaternionControls.noPan = false;
            quaternionControls.staticMoving = true;
            quaternionControls.dynamicDampingFactor = 0.3;
            quaternionControls.minDistance = 1.5;
            quaternionControls.maxDistance = 6;

            // Lighting
            const ambientLight = new THREE.AmbientLight(0x404040, 0.6);
            quaternionScene.add(ambientLight);

            const directionalLight = new THREE.DirectionalLight(0x64ffda, 0.8);
            directionalLight.position.set(3, 3, 3);
            directionalLight.castShadow = true;
            quaternionScene.add(directionalLight);

            const pointLight = new THREE.PointLight(0x7c4dff, 0.4, 100);
            pointLight.position.set(-2, 2, -2);
            quaternionScene.add(pointLight);

            // Create main quaternion sphere (wireframe)
            const sphereGeometry = new THREE.SphereGeometry(1, 32, 32);
            const sphereMaterial = new THREE.MeshPhongMaterial({
                color: 0xaaaaaa,
                wireframe: true,
                transparent: true,
                opacity: 0.6
            });
            quaternionSphere = new THREE.Mesh(sphereGeometry, sphereMaterial);
            quaternionScene.add(quaternionSphere);

            // Create solid sphere for depth reference
            const solidSphereMaterial = new THREE.MeshPhongMaterial({
                color: 0x111133,
                transparent: true,
                opacity: 0.1
            });
            const solidSphere = new THREE.Mesh(sphereGeometry, solidSphereMaterial);
            quaternionScene.add(solidSphere);

            // Create quaternion point
            const pointGeometry = new THREE.SphereGeometry(0.05, 16, 16);
            const pointMaterial = new THREE.MeshPhongMaterial({
                color: 0x64ffda,
                emissive: 0x64ffda,
                emissiveIntensity: 0.3
            });
            quaternionPoint = new THREE.Mesh(pointGeometry, pointMaterial);
            quaternionScene.add(quaternionPoint);

            // Create antipodal point
            const antipodalMaterial = new THREE.MeshPhongMaterial({
                color: 0xff6b6b,
                emissive: 0xff6b6b,
                emissiveIntensity: 0.3
            });
            antipodalPoint = new THREE.Mesh(pointGeometry, antipodalMaterial);
            antipodalPoint.visible = false; // Initially hidden
            quaternionScene.add(antipodalPoint);

            // Create coordinate axes for reference
            const axisLength = 1.3;
            const axisConfigs = [
                { color: 0xff4444, direction: new THREE.Vector3(1, 0, 0), label: 'X' },
                { color: 0x44ff44, direction: new THREE.Vector3(0, 1, 0), label: 'Y' },
                { color: 0x4444ff, direction: new THREE.Vector3(0, 0, 1), label: 'Z' }
            ];

            axisConfigs.forEach(config => {
                const arrowHelper = new THREE.ArrowHelper(
                    config.direction,
                    new THREE.Vector3(0, 0, 0),
                    axisLength,
                    config.color,
                    0.1,
                    0.05
                );
                arrowHelper.line.material.linewidth = 2;
                quaternionScene.add(arrowHelper);
            });

            // Add grid helper
            const gridHelper = new THREE.GridHelper(2, 10, 0x333333, 0x111111);
            gridHelper.position.y = -1.2;
            quaternionScene.add(gridHelper);

            // Start animation loop for quaternion sphere
            const animateQuaternionSphere = () => {
                requestAnimationFrame(animateQuaternionSphere);
                quaternionControls.update();
                quaternionRenderer.render(quaternionScene, quaternionCamera);
            };
            animateQuaternionSphere();
        };

        // ===============================================================================
        // ENHANCED HOPF FIBRATION VISUALIZATION: TRUE S³ → S² MAPPING
        // ===============================================================================
        
        // Hopf visualization global variables
        let hopfScene, hopfCamera, hopfRenderer;
        let hopfBaseSphere, hopfFibers = [];
        let hopfAnimationId, hopfIsAnimating = true;
        let hopfTime = 0;
        
        // Hopf parameters with superior mathematical representation
        let hopfParams = {
            rotationSpeed: 0.5,
            fiberCount: 16,
            animationPhase: 0,
            baseSphereSize: 1.0
        };
        
        // Initialize superior Hopf Fibration 3D visualization
        const initHopfFibration3D = () => {
            const canvas = document.getElementById('hopfCanvas');
            const width = canvas.clientWidth;
            const height = canvas.clientHeight;
            
            // Scene setup with elegant background
            hopfScene = new THREE.Scene();
            hopfScene.background = new THREE.Color(0x000000);
            
            // Camera with optimal positioning
            hopfCamera = new THREE.PerspectiveCamera(75, width / height, 0.1, 1000);
            hopfCamera.position.set(5, 3, 5);
            hopfCamera.lookAt(0, 0, 0);
            
            // Enhanced renderer setup
            hopfRenderer = new THREE.WebGLRenderer({ canvas: canvas, antialias: true });
            hopfRenderer.setSize(width, height);
            hopfRenderer.setPixelRatio(window.devicePixelRatio);
            hopfRenderer.shadowMap.enabled = true;
            hopfRenderer.shadowMap.type = THREE.PCFSoftShadowMap;
            
            // Sophisticated lighting setup
            const ambientLight = new THREE.AmbientLight(0x404040, 0.3);
            hopfScene.add(ambientLight);
            
            const directionalLight = new THREE.DirectionalLight(0xffffff, 0.8);
            directionalLight.position.set(5, 5, 5);
            directionalLight.castShadow = true;
            hopfScene.add(directionalLight);
            
            const pointLight1 = new THREE.PointLight(0x4a9eff, 0.6);
            pointLight1.position.set(-5, 3, 2);
            hopfScene.add(pointLight1);
            
            const pointLight2 = new THREE.PointLight(0x00d4ff, 0.4);
            pointLight2.position.set(5, -3, -2);
            hopfScene.add(pointLight2);
            
            // Create base sphere and fibers
            createHopfBaseSphere();
            createHopfFibers();
            
            // Setup mouse controls
            setupHopfMouseControls();
            
            // Start animation loop
            animateHopfFibration();
        };
        
        function createHopfBaseSphere() {
            const geometry = new THREE.SphereGeometry(hopfParams.baseSphereSize, 32, 32);
            const material = new THREE.MeshPhongMaterial({
                color: 0x2196F3,
                transparent: true,
                opacity: 0.3,
                wireframe: false
            });
            
            if (hopfBaseSphere) hopfScene.remove(hopfBaseSphere);
            hopfBaseSphere = new THREE.Mesh(geometry, material);
            hopfScene.add(hopfBaseSphere);
            
            // Add wireframe overlay for mathematical clarity
            const wireframeGeometry = new THREE.SphereGeometry(hopfParams.baseSphereSize, 16, 16);
            const wireframeMaterial = new THREE.MeshBasicMaterial({
                color: 0x4a9eff,
                wireframe: true,
                transparent: true,
                opacity: 0.6
            });
            const wireframe = new THREE.Mesh(wireframeGeometry, wireframeMaterial);
            hopfBaseSphere.add(wireframe);
        }
        
        function createHopfFibers() {
            // Clear existing fibers with proper disposal
            hopfFibers.forEach(fiber => {
                hopfScene.remove(fiber);
                if (fiber.geometry) fiber.geometry.dispose();
                if (fiber.material) fiber.material.dispose();
            });
            hopfFibers = [];
            
            const fiberCount = hopfParams.fiberCount;
            
            for (let i = 0; i < fiberCount; i++) {
                const phi = (i / fiberCount) * Math.PI;
                for (let j = 0; j < fiberCount; j++) {
                    const theta = (j / fiberCount) * 2 * Math.PI;
                    
                    // Base point on S² using spherical coordinates
                    const basePoint = {
                        x: Math.sin(phi) * Math.cos(theta),
                        y: Math.sin(phi) * Math.sin(theta),
                        z: Math.cos(phi)
                    };
                    
                    // Create mathematically accurate fiber curve
                    const fiberCurve = createHopfFiberCurve(basePoint, i, j);
                    hopfScene.add(fiberCurve);
                    hopfFibers.push(fiberCurve);
                }
            }
        }
        
        function createHopfFiberCurve(basePoint, i, j) {
            const points = [];
            const segments = 64;
            
            // Generate points for the Hopf fiber using proper complex representation
            for (let t = 0; t <= segments; t++) {
                const s = (t / segments) * 2 * Math.PI;
                
                // Hopf fibration mapping from S³ to S² with animation phase
                const hopfPoint = hopfFibrationMapping(basePoint, s + hopfParams.animationPhase);
                
                // Stereographic projection from S³ to R³
                const projectedPoint = stereographicProjectionHopf(hopfPoint);
                points.push(new THREE.Vector3(projectedPoint.x, projectedPoint.y, projectedPoint.z));
            }
            
            const curve = new THREE.CatmullRomCurve3(points, true);
            const geometry = new THREE.TubeGeometry(curve, segments, 0.02, 8, true);
            
            // Elegant color scheme based on mathematical position
            const hue = (i / hopfParams.fiberCount + j / hopfParams.fiberCount) * 0.5;
            const color = new THREE.Color().setHSL(hue, 0.8, 0.6);
            
            const material = new THREE.MeshPhongMaterial({
                color: color,
                transparent: true,
                opacity: 0.8,
                emissive: color,
                emissiveIntensity: 0.2
            });
            
            return new THREE.Mesh(geometry, material);
        }
        
        function hopfFibrationMapping(basePoint, t) {
            // Mathematically accurate Hopf fibration using complex representation
            const { x, y, z } = basePoint;
            
            // Convert to complex coordinates for proper S³ representation
            const z1_real = Math.sqrt((1 + z) / 2) * Math.cos(t);
            const z1_imag = Math.sqrt((1 + z) / 2) * Math.sin(t);
            const z2_real = Math.sqrt((1 - z) / 2) * Math.cos(Math.atan2(y, x) + t);
            const z2_imag = Math.sqrt((1 - z) / 2) * Math.sin(Math.atan2(y, x) + t);
            
            return { x: z1_real, y: z1_imag, z: z2_real, w: z2_imag };
        }
        
        function stereographicProjectionHopf(point4D) {
            // Enhanced stereographic projection from S³ to R³
            const { x, y, z, w } = point4D;
            const denom = 1 - w;
            
            // Handle the south pole singularity gracefully
            if (Math.abs(denom) < 0.001) {
                return { x: 0, y: 0, z: 0 };
            }
            
            const scale = 2;
            return {
                x: scale * x / denom,
                y: scale * y / denom,
                z: scale * z / denom
            };
        }
        
        function setupHopfMouseControls() {
            let mouseDown = false;
            let mouseX = 0, mouseY = 0;
            let cameraRadius = 8;
            let cameraTheta = 0;
            let cameraPhi = Math.PI / 4;
            
            hopfRenderer.domElement.addEventListener('mousedown', (e) => {
                mouseDown = true;
                mouseX = e.clientX;
                mouseY = e.clientY;
            });
            
            hopfRenderer.domElement.addEventListener('mouseup', () => {
                mouseDown = false;
            });
            
            hopfRenderer.domElement.addEventListener('mousemove', (e) => {
                if (!mouseDown) return;
                
                const deltaX = e.clientX - mouseX;
                const deltaY = e.clientY - mouseY;
                
                cameraTheta -= deltaX * 0.01;
                cameraPhi = Math.max(0.1, Math.min(Math.PI - 0.1, cameraPhi + deltaY * 0.01));
                
                hopfCamera.position.x = cameraRadius * Math.sin(cameraPhi) * Math.cos(cameraTheta);
                hopfCamera.position.y = cameraRadius * Math.cos(cameraPhi);
                hopfCamera.position.z = cameraRadius * Math.sin(cameraPhi) * Math.sin(cameraTheta);
                hopfCamera.lookAt(0, 0, 0);
                
                mouseX = e.clientX;
                mouseY = e.clientY;
            });
            
            hopfRenderer.domElement.addEventListener('wheel', (e) => {
                cameraRadius = Math.max(2, Math.min(20, cameraRadius + e.deltaY * 0.01));
                hopfCamera.position.x = cameraRadius * Math.sin(cameraPhi) * Math.cos(cameraTheta);
                hopfCamera.position.y = cameraRadius * Math.cos(cameraPhi);
                hopfCamera.position.z = cameraRadius * Math.sin(cameraPhi) * Math.sin(cameraTheta);
                hopfCamera.lookAt(0, 0, 0);
                e.preventDefault();
            });
        }
        
        function animateHopfFibration() {
            hopfAnimationId = requestAnimationFrame(animateHopfFibration);
            
            if (hopfIsAnimating) {
                hopfTime += 0.01 * hopfParams.rotationSpeed;
                
                // Elegant scene rotation
                hopfScene.rotation.y = hopfTime * 0.3;
                
                // Update animation phase automatically for smooth fiber evolution
                hopfParams.animationPhase = hopfTime * 2;
                const phaseSlider = document.getElementById('hopfAnimationPhase');
                const phaseValue = document.getElementById('hopfAnimationPhaseValue');
                if (phaseSlider && phaseValue) {
                    phaseSlider.value = hopfParams.animationPhase % (2 * Math.PI);
                    phaseValue.textContent = (hopfParams.animationPhase % (2 * Math.PI)).toFixed(2);
                }
                
                // Recreate fibers periodically for smooth animation (performance optimized)
                if (Math.floor(hopfTime * 60) % 3 === 0) {
                    createHopfFibers();
                }
            }
            
            hopfRenderer.render(hopfScene, hopfCamera);
        }
        
        // Resize function for Hopf visualization
        const resizeHopfVisualization = () => {
            if (!hopfRenderer) return;
            
            const canvas = document.getElementById('hopfCanvas');
            const width = canvas.clientWidth;
            const height = canvas.clientHeight;
            
            hopfCamera.aspect = width / height;
            hopfCamera.updateProjectionMatrix();
            hopfRenderer.setSize(width, height);
        };

        // ===============================================================================
        // 3D QUATERNION SPHERE VISUALIZATION WITH GEODESIC ARC
        // ===============================================================================
        
        const updateQuaternionSphere3D = () => {
            if (!quaternionScene || !quaternionPoint) return;
            
            // Use independent sphere quaternion instead of currentQuaternion
            const [x, y, z, qw] = sphereQuaternion;
            
            // SIMPLE & CLEAR: Direct quaternion visualization that makes W changes visible
            // Maps quaternion (x,y,z,w) to 3D sphere position where all components affect position
            
            // Use a hybrid approach: mix the vector part (x,y,z) with W influence
            // This ensures W changes are clearly visible while maintaining intuitive behavior
            
            let displayX = x;
            let displayY = y;
            let displayZ = z + qw * 0.3; // W component influences Z position directly
            
            // Handle the case where all components are near zero
            const totalMagnitude = Math.sqrt(displayX*displayX + displayY*displayY + displayZ*displayZ);
            
            if (totalMagnitude > 1e-10) {
                // Normalize to unit sphere for display
                const scale = 1.0 / totalMagnitude;
                quaternionPoint.position.set(
                    displayX * scale,
                    displayY * scale,
                    displayZ * scale
                );
                
                // Update antipodal point (representing -q)
                if (showAntipodes) {
                    antipodalPoint.visible = true;
                    // Antipodal point: negate all components
                    let antiX = -displayX * scale;
                    let antiY = -displayY * scale;
                    let antiZ = -displayZ * scale;
                    antipodalPoint.position.set(antiX, antiY, antiZ);
                } else {
                    antipodalPoint.visible = false;
                }
            } else {
                // Degenerate case - place at default positions
                quaternionPoint.position.set(0, 0, 1);
                if (showAntipodes) {
                    antipodalPoint.visible = true;
                    antipodalPoint.position.set(0, 0, -1);
                } else {
                    antipodalPoint.visible = false;
                }
            }
            
            // Visual feedback - constant size now since position properly changes
            quaternionPoint.material.emissiveIntensity = 0.6;
            quaternionPoint.scale.setScalar(1.0);
            
            if (showAntipodes) {
                antipodalPoint.material.emissiveIntensity = 0.6;
                antipodalPoint.scale.setScalar(1.0);
            }
            
            // Update geodesic arc
            updateGeodesicArc3D();
            
            // Update all quaternion display values (both sets)
            document.getElementById('quatX').textContent = x.toFixed(6);
            document.getElementById('quatY').textContent = y.toFixed(6);
            document.getElementById('quatZ').textContent = z.toFixed(6);
            document.getElementById('quatW').textContent = qw.toFixed(6);
            
            // Update slider values without triggering events
            if (!updatingFromSlider) {
                document.getElementById('quatXValue').textContent = x.toFixed(3);
                document.getElementById('quatYValue').textContent = y.toFixed(3);
                document.getElementById('quatZValue').textContent = z.toFixed(3);
                document.getElementById('quatWValue').textContent = qw.toFixed(3);
                document.getElementById('quatXSlider').value = x;
                document.getElementById('quatYSlider').value = y;
                document.getElementById('quatZSlider').value = z;
                document.getElementById('quatWSlider').value = qw;
            }
        };
        
        // Update sphere quaternion from Euler angles - FIXED VERSION
        const updateSphereFromEuler = () => {
            if (updatingFromSlider) return;
            updatingFromSlider = true;
            
            // Convert Euler to quaternion
            const [yaw, pitch, roll] = sphereEuler;
            sphereQuaternion = qNorm(qFromEulerZYX(yaw, pitch, roll)); // Auto-normalize
            
            // Update all displays
            document.getElementById('sphereYawValue').textContent = `${rad2deg(yaw).toFixed(1)}°`;
            document.getElementById('spherePitchValue').textContent = `${rad2deg(pitch).toFixed(1)}°`;
            document.getElementById('sphereRollValue').textContent = `${rad2deg(roll).toFixed(1)}°`;
            
            // Update quaternion component sliders and displays
            document.getElementById('quatXSlider').value = sphereQuaternion[0];
            document.getElementById('quatYSlider').value = sphereQuaternion[1];
            document.getElementById('quatZSlider').value = sphereQuaternion[2];
            document.getElementById('quatWSlider').value = sphereQuaternion[3];
            
            document.getElementById('quatXValue').textContent = sphereQuaternion[0].toFixed(3);
            document.getElementById('quatYValue').textContent = sphereQuaternion[1].toFixed(3);
            document.getElementById('quatZValue').textContent = sphereQuaternion[2].toFixed(3);
            document.getElementById('quatWValue').textContent = sphereQuaternion[3].toFixed(3);
            
            // Update 3D visualization - THIS WAS MISSING!
            updateQuaternionSphere3D();
            
            // Update Hopf fibration visualization
            updateHopfVisualization();
            
            updatingFromSlider = false;
        };
        
        // Update sphere Euler angles from quaternion - FIXED VERSION
        const updateEulerFromSphere = () => {
            if (updatingFromSlider) return;
            updatingFromSlider = true;
            
            // Auto-normalize quaternion first
            sphereQuaternion = qNorm(sphereQuaternion);
            
            // Convert to Euler angles
            sphereEuler = qToEulerZYX(sphereQuaternion);
            
            // Update Euler sliders and displays
            document.getElementById('sphereYawSlider').value = rad2deg(sphereEuler[0]);
            document.getElementById('spherePitchSlider').value = rad2deg(sphereEuler[1]);
            document.getElementById('sphereRollSlider').value = rad2deg(sphereEuler[2]);
            
            document.getElementById('sphereYawValue').textContent = `${rad2deg(sphereEuler[0]).toFixed(1)}°`;
            document.getElementById('spherePitchValue').textContent = `${rad2deg(sphereEuler[1]).toFixed(1)}°`;
            document.getElementById('sphereRollValue').textContent = `${rad2deg(sphereEuler[2]).toFixed(1)}°`;
            
            // Update quaternion displays to reflect normalization
            document.getElementById('quatXValue').textContent = sphereQuaternion[0].toFixed(3);
            document.getElementById('quatYValue').textContent = sphereQuaternion[1].toFixed(3);
            document.getElementById('quatZValue').textContent = sphereQuaternion[2].toFixed(3);
            document.getElementById('quatWValue').textContent = sphereQuaternion[3].toFixed(3);
            
            // Update sliders to reflect normalization
            document.getElementById('quatXSlider').value = sphereQuaternion[0];
            document.getElementById('quatYSlider').value = sphereQuaternion[1];
            document.getElementById('quatZSlider').value = sphereQuaternion[2];
            document.getElementById('quatWSlider').value = sphereQuaternion[3];
            
            // Update 3D visualization - THIS WAS MISSING!
            updateQuaternionSphere3D();
            
            // Update Hopf fibration visualization
            updateHopfVisualization();
            
            updatingFromSlider = false;
        };

        const updateGeodesicArc3D = () => {
            console.log("updateGeodesicArc3D called, showGeodesicArc:", showGeodesicArc);
            
            // Remove existing geodesic curve
            if (geodesicCurve) {
                quaternionScene.remove(geodesicCurve);
                geodesicCurve = null;
            }
            
            if (!showGeodesicArc || !quaternionScene) return;
            
            const [x, y, z, qw] = sphereQuaternion; // Use sphere quaternion instead
            console.log("Sphere quaternion:", sphereQuaternion);
            
            // Calculate the projected points on sphere surface
            const xyz_norm = Math.sqrt(x*x + y*y + z*z);
            let point1, point2;
            
            if (xyz_norm > 1e-10) {
                const scale = 1.0 / xyz_norm;
                point1 = new THREE.Vector3(x * scale, y * scale, z * scale);
                point2 = new THREE.Vector3(-x * scale, -y * scale, -z * scale);
            } else {
                // Degenerate case
                point1 = new THREE.Vector3(0, 0, 1);
                point2 = new THREE.Vector3(0, 0, -1);
            }
            
            console.log("Point1:", point1);
            console.log("Point2:", point2);
            
            // Create a simple great circle arc using many points
            const geodesicSteps = 50;
            const points = [];
            
            // Calculate the rotation axis (cross product of point1 and point2)
            const axis = new THREE.Vector3().crossVectors(point1, point2).normalize();
            const angle = point1.angleTo(point2);
            
            console.log("Rotation axis:", axis);
            console.log("Angle between points:", angle, "radians =", angle * 180 / Math.PI, "degrees");
            
            // If points are too close or opposite, create alternative path
            if (angle < 0.01 || angle > 3.13) {
                console.log("Points are too close or opposite, using alternative path");
                // Create path through an intermediate point
                const midPoint = new THREE.Vector3(1, 0, 0);
                if (Math.abs(point1.dot(midPoint)) > 0.9) {
                    midPoint.set(0, 1, 0);
                }
                
                // First half: point1 to midPoint
                for (let i = 0; i <= geodesicSteps/2; i++) {
                    const t = i / (geodesicSteps/2);
                    const interpolated = new THREE.Vector3().lerpVectors(point1, midPoint, t).normalize();
                    points.push(interpolated);
                }
                
                // Second half: midPoint to point2
                for (let i = 1; i <= geodesicSteps/2; i++) {
                    const t = i / (geodesicSteps/2);
                    const interpolated = new THREE.Vector3().lerpVectors(midPoint, point2, t).normalize();
                    points.push(interpolated);
                }
            } else {
                // Normal case: rotate around axis
                for (let i = 0; i <= geodesicSteps; i++) {
                    const t = i / geodesicSteps;
                    const rotationAngle = angle * t;
                    
                    const rotatedPoint = point1.clone();
                    rotatedPoint.applyAxisAngle(axis, rotationAngle);
                    points.push(rotatedPoint);
                }
            }
            
            console.log("Created", points.length, "points for geodesic");
            
            // Create line geometry instead of tube for better visibility
            const geometry = new THREE.BufferGeometry().setFromPoints(points);
            const material = new THREE.LineBasicMaterial({
                color: 0xFFD700,
                linewidth: 5,
                transparent: false
            });
            
            geodesicCurve = new THREE.Line(geometry, material);
            quaternionScene.add(geodesicCurve);
            
            console.log("Geodesic arc added to scene");
        };

        const resizeQuaternionSphere3D = () => {
            if (!quaternionRenderer) return;
            
            const canvas = document.getElementById('quaternionCanvas');
            const width = canvas.clientWidth;
            const height = canvas.clientHeight;
            
            quaternionCamera.aspect = width / height;
            quaternionCamera.updateProjectionMatrix();
            quaternionRenderer.setSize(width, height);
        };
        
        // ===============================================================================
        // THREE.JS SLERP PATH VISUALIZATION
        // ===============================================================================
        
        const initSlerpPath3D = () => {
            const canvas = document.getElementById('slerpPathCanvas');
            const width = canvas.clientWidth;
            const height = canvas.clientHeight;
            
            // Scene - slightly brighter background
            slerpScene = new THREE.Scene();
            slerpScene.background = new THREE.Color(0x0a0a1a);
            
            // Camera - positioned to see blue SLERP arc better
            slerpCamera = new THREE.PerspectiveCamera(60, width / height, 0.1, 1000);
            slerpCamera.position.set(3.5, 2, 2.5);
            slerpCamera.lookAt(0, 0, 0);
            
            // Renderer
            slerpRenderer = new THREE.WebGLRenderer({ canvas: canvas, antialias: true });
            slerpRenderer.setSize(width, height);
            slerpRenderer.setPixelRatio(window.devicePixelRatio);
            
            // Controls
            slerpControls = new THREE.TrackballControls(slerpCamera, slerpRenderer.domElement);
            slerpControls.rotateSpeed = 1.0;
            slerpControls.zoomSpeed = 1.2;
            slerpControls.panSpeed = 0.8;
            slerpControls.minDistance = 1.5;
            slerpControls.maxDistance = 5;
            
            // Enhanced lighting for better visibility
            const ambientLight = new THREE.AmbientLight(0x606060, 0.8);
            slerpScene.add(ambientLight);
            
            const directionalLight = new THREE.DirectionalLight(0xffffff, 1.0);
            directionalLight.position.set(3, 3, 3);
            directionalLight.castShadow = true;
            slerpScene.add(directionalLight);
            
            // Add another directional light from opposite side
            const directionalLight2 = new THREE.DirectionalLight(0xffffff, 0.6);
            directionalLight2.position.set(-3, 2, -3);
            slerpScene.add(directionalLight2);
            
            // Add point lights for better sphere illumination
            const pointLight1 = new THREE.PointLight(0x88ccff, 0.5, 100);
            pointLight1.position.set(2, 3, 0);
            slerpScene.add(pointLight1);
            
            const pointLight2 = new THREE.PointLight(0xffaa88, 0.5, 100);
            pointLight2.position.set(-2, -1, 2);
            slerpScene.add(pointLight2);
            
            // Create main sphere (wireframe) - more transparent for path visibility
            const sphereGeometry = new THREE.SphereGeometry(1, 24, 24);
            const sphereMaterial = new THREE.MeshPhongMaterial({
                color: 0x666688,
                wireframe: true,
                transparent: true,
                opacity: 0.3,
                depthWrite: false
            });
            slerpSphere = new THREE.Mesh(sphereGeometry, sphereMaterial);
            slerpSphere.renderOrder = 1;
            slerpScene.add(slerpSphere);
            
            // Create solid sphere for depth - more transparent
            const solidSphereMaterial = new THREE.MeshPhongMaterial({
                color: 0x222244,
                transparent: true,
                opacity: 0.1,
                shininess: 50,
                depthWrite: false,
                side: THREE.BackSide  // Render only back side to reduce occlusion
            });
            const solidSphere = new THREE.Mesh(sphereGeometry, solidSphereMaterial);
            solidSphere.renderOrder = 0;
            slerpScene.add(solidSphere);
            
            // Add coordinate axes for reference
            const axesHelper = new THREE.AxesHelper(1.3);
            slerpScene.add(axesHelper);
            
            // Create start and end points - make them bigger
            const startGeometry = new THREE.SphereGeometry(0.06, 16, 16);
            const startMaterial = new THREE.MeshPhongMaterial({
                color: 0x4caf50,
                emissive: 0x4caf50,
                emissiveIntensity: 0.5
            });
            const startPoint = new THREE.Mesh(startGeometry, startMaterial);
            slerpScene.add(startPoint);
            
            const endGeometry = new THREE.SphereGeometry(0.06, 16, 16);
            const endMaterial = new THREE.MeshPhongMaterial({
                color: 0xff9800,
                emissive: 0xff9800,
                emissiveIntensity: 0.5
            });
            const endPoint = new THREE.Mesh(endGeometry, endMaterial);
            slerpScene.add(endPoint);
            
            // Create current position markers - make them bigger and more visible
            const currentGeometry = new THREE.SphereGeometry(0.08, 16, 16);
            
            const slerpCurrentMaterial = new THREE.MeshPhongMaterial({
                color: 0x2196f3,
                emissive: 0x2196f3,
                emissiveIntensity: 0.8
            });
            slerpCurrentPoint = new THREE.Mesh(currentGeometry, slerpCurrentMaterial);
            slerpScene.add(slerpCurrentPoint);
            
            const eulerCurrentMaterial = new THREE.MeshPhongMaterial({
                color: 0xf44336,
                emissive: 0xf44336,
                emissiveIntensity: 0.8
            });
            eulerCurrentPoint = new THREE.Mesh(currentGeometry, eulerCurrentMaterial);
            slerpScene.add(eulerCurrentPoint);
            
            // Start animation loop
            const animateSlerpPath = () => {
                requestAnimationFrame(animateSlerpPath);
                slerpControls.update();
                updateSlerpPaths3D();
                slerpRenderer.render(slerpScene, slerpCamera);
            };
            animateSlerpPath();
        };
        
        const updateSlerpPaths3D = () => {
            if (!slerpScene) return;
            
            const t = parseFloat(document.getElementById('tSlider').value);
            
            // Get Euler angles for start and end orientations
            const eulerA = qToEulerZYX(orientationA);
            const eulerB = qToEulerZYX(orientationB);
            
            // COMPREHENSIVE cleanup of all path-related objects
            const toRemove = [];
            slerpScene.children.forEach(child => {
                // Remove tubes (both SLERP and Euler)
                if (child.geometry && child.geometry.type === 'TubeGeometry') {
                    toRemove.push(child);
                }
                // Remove lines (both SLERP and Euler paths)
                if (child.geometry && child.geometry.type === 'BufferGeometry' && child.type === 'Line') {
                    toRemove.push(child);
                }
                // Remove path marker spheres (but keep start/end points)
                if (child.geometry && child.geometry.type === 'SphereGeometry' &&
                    child.material && (
                        child.material.color.getHex() === 0x64b5f6 || // SLERP path spheres
                        child.material.color.getHex() === 0xff6655    // Euler path spheres
                    )) {
                    toRemove.push(child);
                }
            });
            
            // Dispose of all removed objects properly
            toRemove.forEach(child => {
                slerpScene.remove(child);
                if (child.geometry) {
                    child.geometry.dispose();
                }
                if (child.material) {
                    if (child.material.map) child.material.map.dispose();
                    child.material.dispose();
                }
            });
            
            // Generate SLERP path points
            // Visualize by applying quaternion rotations to a reference vector
            const slerpPoints = [];
            const referenceVector = new THREE.Vector3(1, 0, 0); // Start with x-axis
            
            for (let i = 0; i <= 100; i++) {
                const t_i = i / 100;
                const q = qSlerp(orientationA, orientationB, t_i);
                const [qx, qy, qz, qw] = qNorm(q);
                
                // Convert quaternion to Three.js quaternion
                const threeQuat = new THREE.Quaternion(qx, qy, qz, qw);
                
                // Apply rotation to reference vector
                const point = referenceVector.clone();
                point.applyQuaternion(threeQuat);
                
                // Ensure point is on unit sphere (should already be, but just in case)
                point.normalize();
                
                slerpPoints.push(point);
            }
            
            // Create SLERP path - Thin and clean
            const slerpGeometry = new THREE.BufferGeometry().setFromPoints(slerpPoints);
            
            // Add thin tube geometry for SLERP path
            const slerpCurve = new THREE.CatmullRomCurve3(slerpPoints);
            const slerpTubeGeometry = new THREE.TubeGeometry(slerpCurve, 100, 0.025, 8, false); // Much thinner
            const slerpTubeMaterial = new THREE.MeshPhongMaterial({
                color: 0x2196f3,
                emissive: 0x1565c0,
                emissiveIntensity: 0.3,
                shininess: 100,
                transparent: false,
                depthTest: true
            });
            const slerpTube = new THREE.Mesh(slerpTubeGeometry, slerpTubeMaterial);
            slerpTube.renderOrder = 5;
            slerpScene.add(slerpTube);
            
            // Simple line for SLERP path
            const slerpLineMaterial = new THREE.LineBasicMaterial({
                color: 0x2196f3,
                linewidth: 2,
                transparent: false
            });
            slerpPathLine = new THREE.Line(slerpGeometry, slerpLineMaterial);
            slerpPathLine.renderOrder = 4;
            slerpScene.add(slerpPathLine);
            
            // Generate Euler LERP path points
            const eulerPoints = [];
            const refVector = new THREE.Vector3(1, 0, 0); // Use same reference vector approach
            
            for (let i = 0; i <= 100; i++) {
                const t_i = i / 100;
                const interpYaw = eulerA[0] + (eulerB[0] - eulerA[0]) * t_i;
                const interpPitch = eulerA[1] + (eulerB[1] - eulerA[1]) * t_i;
                const interpRoll = eulerA[2] + (eulerB[2] - eulerA[2]) * t_i;
                const q = qFromEulerZYX(interpYaw, interpPitch, interpRoll);
                const [qx, qy, qz, qw] = qNorm(q);
                
                // Convert quaternion to Three.js quaternion
                const threeQuat = new THREE.Quaternion(qx, qy, qz, qw);
                
                // Apply rotation to reference vector
                const point = refVector.clone();
                point.applyQuaternion(threeQuat);
                
                // Ensure point is on unit sphere
                point.normalize();
                
                eulerPoints.push(point);
            }
            
            // Create Euler LERP path line - thicker and more visible
            const eulerGeometry = new THREE.BufferGeometry().setFromPoints(eulerPoints);
            const eulerMaterial = new THREE.LineBasicMaterial({
                color: 0xff6655,
                linewidth: 5,
                transparent: false
            });
            eulerPathLine = new THREE.Line(eulerGeometry, eulerMaterial);
            slerpScene.add(eulerPathLine);
            
            // Add tube geometry for Euler LERP - keep thinner than SLERP
            const eulerCurve = new THREE.CatmullRomCurve3(eulerPoints);
            const eulerTubeGeometry = new THREE.TubeGeometry(eulerCurve, 100, 0.03, 8, false);
            const eulerTubeMaterial = new THREE.MeshPhongMaterial({
                color: 0xff6655,
                emissive: 0xff4433,
                emissiveIntensity: 0.6,
                shininess: 100,
                transparent: false,
                depthTest: true
            });
            const eulerTube = new THREE.Mesh(eulerTubeGeometry, eulerTubeMaterial);
            eulerTube.renderOrder = 2;  // Lower than SLERP
            slerpScene.add(eulerTube);
            
            // Update start and end points
            const startPos = slerpPoints[0];
            const endPos = slerpPoints[slerpPoints.length - 1];
            
            const startMarker = slerpScene.children.find(child =>
                child.material && child.material.color &&
                child.material.color.getHex() === 0x4caf50);
            if (startMarker) {
                startMarker.position.copy(startPos);
            }
            
            const endMarker = slerpScene.children.find(child =>
                child.material && child.material.color &&
                child.material.color.getHex() === 0xff9800);
            if (endMarker) {
                endMarker.position.copy(endPos);
            }
            
            // Update current positions
            const currentIndex = Math.floor(t * (slerpPoints.length - 1));
            if (slerpCurrentPoint && slerpPoints[currentIndex]) {
                slerpCurrentPoint.position.copy(slerpPoints[currentIndex]);
            }
            if (eulerCurrentPoint && eulerPoints[currentIndex]) {
                eulerCurrentPoint.position.copy(eulerPoints[currentIndex]);
            }
        };
        
        const resizeSlerpPath3D = () => {
            if (!slerpRenderer) return;
            
            const canvas = document.getElementById('slerpPathCanvas');
            const width = canvas.clientWidth;
            const height = canvas.clientHeight;
            
            slerpCamera.aspect = width / height;
            slerpCamera.updateProjectionMatrix();
            slerpRenderer.setSize(width, height);
        };

        // Simple, guaranteed-to-work approach focusing on visible results
        const eulerCondition = (yaw, pitch, roll) => {
            // Convert to degrees for easier understanding
            const pitchDeg = pitch * 180 / Math.PI;
            const yawDeg = yaw * 180 / Math.PI;
            const rollDeg = roll * 180 / Math.PI;
            
            // Base condition number primarily on pitch (main source of singularity)
            const pitchFactor = Math.abs(pitchDeg) / 90; // 0 to 1 as pitch goes from 0° to ±90°
            
            // Create dramatic variation based on pitch
            let baseCondition = -2 + pitchFactor * 8; // Goes from -2 to 6 as pitch increases
            
            // Add some variation based on yaw and roll for visual interest
            const yawVariation = 0.5 * Math.sin(yawDeg * Math.PI / 45); // -0.5 to 0.5
            const rollVariation = 0.3 * Math.cos(rollDeg * Math.PI / 30); // -0.3 to 0.3
            
            // Combine factors
            let condition = baseCondition + yawVariation + rollVariation;
            
            // Ensure gimbal lock regions (pitch near ±90°) are definitely red/yellow
            if (Math.abs(pitchDeg) > 80) {
                condition = Math.max(4, condition); // Force into red/yellow range
            }
            
            // Clamp to visualization range
            condition = Math.max(-3, Math.min(6, condition));
            
            // Debug output
            if (Math.random() < 0.01) {
                console.log(`Condition: pitch=${pitchDeg.toFixed(1)}°, condition=${condition.toFixed(2)}`);
            }
            
            return condition;
        };
        
        // Remove the rest of the finite difference calculation since we're using analytical
        const eulerConditionOld = (yaw, pitch, roll) => {
            // Keep the old finite difference code as backup
            const eps = 1e-4;
            
            return 0; // Placeholder - analytical version above is used
        };

        // Heatmap visualization with proper Jacobian and torus topology
        const drawHeatmap = () => {
            const canvas = document.getElementById('heatmapCanvas');
            const ctx = heatmapCtx;
            const width = canvas.clientWidth;
            const height = canvas.clientHeight;
            
            ctx.clearRect(0, 0, width, height);
            
            const fixedPitch = deg2rad(parseFloat(document.getElementById('heatmapPitchSlider').value));
            
            // Get current yaw and roll from torus-specific sliders
            const currentYaw = deg2rad(parseFloat(document.getElementById('torusYawSlider').value));
            const currentRoll = deg2rad(parseFloat(document.getElementById('torusRollSlider').value));
            
            // Get current pitch from gimbal sliders for position indicator
            const currentPitch = deg2rad(parseFloat(document.getElementById('pitchSlider').value));
            
            const resolution = 64;
            
            // Calculate condition numbers for the grid
            let maxCond = 0;
            const conditionGrid = [];
            
            for (let i = 0; i < resolution; i++) {
                conditionGrid[i] = [];
                for (let j = 0; j < resolution; j++) {
                    // Map grid indices to proper Euler angle ranges [-π, π]
                    const yaw = (i / resolution - 0.5) * 2 * Math.PI;   // [-π, π]
                    const roll = (j / resolution - 0.5) * 2 * Math.PI;  // [-π, π]
                    
                    const cond = eulerCondition(yaw, fixedPitch, roll);
                    conditionGrid[i][j] = cond;
                    maxCond = Math.max(maxCond, Math.min(cond, 1000));
                }
            }
            
            if (showTorusView) {
                // Draw as torus (3D projection)
                ctx.save();
                ctx.translate(width/2, height/2);
                
                const majorRadius = Math.min(width, height) * 0.2;
                const minorRadius = majorRadius * 0.3;
                
                // Draw torus mesh - CORRECTED MAPPING
                for (let i = 0; i < resolution; i++) {
                    for (let j = 0; j < resolution; j++) {
                        // Map grid to torus parameters [0, 2π] for continuous torus surface
                        const theta = (i / resolution) * 2 * Math.PI; // Major circle (yaw direction)
                        const phi = (j / resolution) * 2 * Math.PI;   // Minor circle (roll direction)
                        
                        // Standard torus parametric equations
                        const torusX = (majorRadius + minorRadius * Math.cos(phi)) * Math.cos(theta);
                        const torusY = (majorRadius + minorRadius * Math.cos(phi)) * Math.sin(theta);
                        const torusZ = minorRadius * Math.sin(phi);
                        
                        // Improved 3D to 2D projection with better depth perception
                        const scale = 0.9;
                        const projX = (torusX * 0.866 - torusY * 0.5) * scale; // 60° rotation for better view
                        const projY = (torusZ * 0.866 + torusY * 0.5) * scale;
                        
                        // Map torus coordinates back to Euler angle grid coordinates
                        // Torus uses [0, 2π], grid uses [-π, π] via (i/resolution - 0.5) * 2π
                        const yawAngle = theta - Math.PI; // Convert [0, 2π] to [-π, π]
                        const rollAngle = phi - Math.PI;  // Convert [0, 2π] to [-π, π]
                        
                        // Map back to grid indices
                        const gridI = Math.floor(((yawAngle + Math.PI) / (2 * Math.PI)) * resolution);
                        const gridJ = Math.floor(((rollAngle + Math.PI) / (2 * Math.PI)) * resolution);
                        
                        // Clamp to valid indices
                        const clampedI = Math.max(0, Math.min(resolution - 1, gridI));
                        const clampedJ = Math.max(0, Math.min(resolution - 1, gridJ));
                        
                        const logCondValue = conditionGrid[clampedI][clampedJ];
                        
                        // Color mapping as specified: Blue=stable, Green=sensitive, Red=singular, Yellow=gimbal lock
                        let r, g, b;
                        
                        if (logCondValue < -3) {
                            // Very stable (Blue)
                            r = 0; g = 102; b = 255;
                        } else if (logCondValue < -1) {
                            // Stable to sensitive transition (Blue to Green)
                            const t = (logCondValue + 3) / 2; // Map [-3,-1] to [0,1]
                            r = 0;
                            g = Math.floor(102 + 153 * t); // 102 to 255
                            b = Math.floor(255 * (1 - t));  // 255 to 0
                        } else if (logCondValue < 1) {
                            // Sensitive (Green)
                            r = 0; g = 255; b = 0;
                        } else if (logCondValue < 3) {
                            // Sensitive to singular (Green to Red)
                            const t = (logCondValue - 1) / 2; // Map [1,3] to [0,1]
                            r = Math.floor(255 * t);     // 0 to 255
                            g = Math.floor(255 * (1 - t)); // 255 to 0
                            b = 0;
                        } else if (logCondValue < 4.5) {
                            // Highly singular (Red)
                            r = 255; g = 0; b = 0;
                        } else if (logCondValue < 6) {
                            // SMOOTH Red to Yellow transition
                            const t = (logCondValue - 4.5) / 1.5; // Map [4.5,6] to [0,1]
                            r = 255; // Always full red
                            g = Math.floor(255 * t);     // 0 to 255 (adds yellow)
                            b = 0;   // No blue
                        } else {
                            // Pure gimbal lock region (Yellow)
                            r = 255; g = 255; b = 0;
                        }
                        
                        // Enhanced depth calculation for better 3D perception
                        const viewAngle = Math.PI / 6; // 30 degree viewing angle
                        const depth = torusX * Math.cos(viewAngle) + torusY * Math.sin(viewAngle);
                        const normalizedDepth = (depth + (majorRadius + minorRadius)) / (2 * (majorRadius + minorRadius));
                        const opacity = Math.max(0.3, 0.4 + 0.5 * normalizedDepth);
                        const size = Math.max(1.5, 2 + normalizedDepth * 1.5);
                        
                        ctx.fillStyle = `rgba(${r}, ${g}, ${b}, ${opacity})`;
                        ctx.beginPath();
                        ctx.arc(projX, projY, size, 0, 2 * Math.PI);
                        ctx.fill();
                    }
                }
                
                // Draw coordinate labels on torus
                ctx.strokeStyle = 'rgba(255, 255, 255, 0.3)';
                ctx.lineWidth = 1;
                
                // Draw major circle (yaw)
                ctx.beginPath();
                ctx.arc(0, 0, majorRadius, 0, 2 * Math.PI);
                ctx.stroke();
                
                // ENHANCED: Show current yaw/roll position on torus
                // Always show the position indicator - torus sliders work independently
                {
                    // Map current yaw/roll to torus coordinates
                    const currentTheta = (currentYaw + Math.PI); // Map [-π,π] to [0,2π]
                    const currentPhi = (currentRoll + Math.PI);   // Map [-π,π] to [0,2π]
                    
                    // Calculate torus position
                    const indicatorX = (majorRadius + minorRadius * Math.cos(currentPhi)) * Math.cos(currentTheta);
                    const indicatorY = (majorRadius + minorRadius * Math.cos(currentPhi)) * Math.sin(currentTheta);
                    const indicatorZ = minorRadius * Math.sin(currentPhi);
                    
                    // Project to 2D
                    const scale = 0.9;
                    const projIndicatorX = (indicatorX * 0.866 - indicatorY * 0.5) * scale;
                    const projIndicatorY = (indicatorZ * 0.866 + indicatorY * 0.5) * scale;
                    
                    // Draw prominent indicator
                    const indicatorSize = 8;
                    
                    // Outer glow
                    ctx.fillStyle = 'rgba(255, 255, 255, 0.3)';
                    ctx.beginPath();
                    ctx.arc(projIndicatorX, projIndicatorY, indicatorSize + 4, 0, 2 * Math.PI);
                    ctx.fill();
                    
                    // Main indicator
                    ctx.fillStyle = '#ff00ff'; // Bright magenta
                    ctx.beginPath();
                    ctx.arc(projIndicatorX, projIndicatorY, indicatorSize, 0, 2 * Math.PI);
                    ctx.fill();
                    
                    // Inner highlight
                    ctx.fillStyle = '#ffffff';
                    ctx.beginPath();
                    ctx.arc(projIndicatorX, projIndicatorY, indicatorSize - 3, 0, 2 * Math.PI);
                    ctx.fill();
                    
                    // Cross-hairs for precision
                    ctx.strokeStyle = '#ff00ff';
                    ctx.lineWidth = 2;
                    ctx.beginPath();
                    ctx.moveTo(projIndicatorX - 12, projIndicatorY);
                    ctx.lineTo(projIndicatorX + 12, projIndicatorY);
                    ctx.moveTo(projIndicatorX, projIndicatorY - 12);
                    ctx.lineTo(projIndicatorX, projIndicatorY + 12);
                    ctx.stroke();
                    
                    // Label
                    ctx.fillStyle = '#ff00ff';
                    ctx.font = 'bold 11px sans-serif';
                    ctx.fillText('Current Position', projIndicatorX + 15, projIndicatorY - 15);
                    ctx.font = '9px sans-serif';
                    ctx.fillText(`Y:${rad2deg(currentYaw).toFixed(0)}° R:${rad2deg(currentRoll).toFixed(0)}°`,
                               projIndicatorX + 15, projIndicatorY - 2);
                }
                
                // Enhanced torus topology labels and guides
                ctx.fillStyle = '#ffffff';
                ctx.font = 'bold 12px sans-serif';
                ctx.fillText(`Torus View (Pitch = ${rad2deg(fixedPitch).toFixed(0)}°)`, -majorRadius, -majorRadius - minorRadius - 35);
                
                ctx.font = '10px sans-serif';
                ctx.fillStyle = 'rgba(255, 255, 255, 0.9)';
                ctx.fillText('Yaw: major circle →', majorRadius * 0.8, majorRadius + 25);
                ctx.fillText('Roll: minor circle ↻', -majorRadius * 0.8, -majorRadius - 5);
                
                // Show current torus position info
                ctx.fillStyle = '#00ff00';
                ctx.font = 'bold 10px sans-serif';
                ctx.fillText(`Torus Position - Y:${rad2deg(currentYaw).toFixed(0)}° R:${rad2deg(currentRoll).toFixed(0)}°`, -majorRadius, majorRadius + 35);
                
                // Enhanced coordinate reference with better visibility
                ctx.strokeStyle = 'rgba(255, 255, 255, 0.4)';
                ctx.lineWidth = 1;
                ctx.setLineDash([4, 2]);
                
                // Major circle outline (yaw direction)
                ctx.beginPath();
                ctx.arc(0, 0, majorRadius, 0, 2 * Math.PI);
                ctx.stroke();
                
                // Show fewer minor circle examples for clarity
                const minorPositions = [0, Math.PI/2, Math.PI, 3*Math.PI/2];
                minorPositions.forEach(angle => {
                    const centerX = majorRadius * Math.cos(angle);
                    const centerY = majorRadius * Math.sin(angle);
                    ctx.beginPath();
                    ctx.arc(centerX, centerY, minorRadius, 0, 2 * Math.PI);
                    ctx.stroke();
                });
                
                ctx.setLineDash([]);
                
                // Add directional indicators
                ctx.fillStyle = 'rgba(100, 255, 218, 0.8)';
                ctx.font = '14px sans-serif';
                
                // Yaw direction arrow
                ctx.fillText('⟲', majorRadius + 15, 5);
                // Roll direction indicator
                ctx.fillText('⟳', majorRadius - 5, minorRadius + 20);
                
                ctx.restore();
                
                // Add topology note
                ctx.fillStyle = 'rgba(100, 255, 218, 0.8)';
                ctx.font = 'bold 12px sans-serif';
                ctx.fillText('Torus View: Angles wrap at ±180°', 10, height - 10);
            } else {
                // Original flat view
                const imageData = ctx.createImageData(resolution, resolution);
                const data = imageData.data;
                
                // Render with proper logarithmic scaling
                for (let i = 0; i < resolution; i++) {
                    for (let j = 0; j < resolution; j++) {
                        const logCondValue = conditionGrid[i][j];
                        
                        // Add wrapping indicators at edges
                        const isEdgeX = (i === 0 || i === resolution - 1);
                        const isEdgeY = (j === 0 || j === resolution - 1);
                        
                        const pixelIndex = (j * resolution + i) * 4;
                        
                        // Use same color mapping as torus view
                        let r, g, b;
                        
                        if (logCondValue < -3) {
                            // Very stable (Blue)
                            r = 0; g = 102; b = 255;
                        } else if (logCondValue < -1) {
                            // Stable to sensitive transition (Blue to Green)
                            const t = (logCondValue + 3) / 2; // Map [-3,-1] to [0,1]
                            r = 0;
                            g = Math.floor(102 + 153 * t); // 102 to 255
                            b = Math.floor(255 * (1 - t));  // 255 to 0
                        } else if (logCondValue < 1) {
                            // Sensitive (Green)
                            r = 0; g = 255; b = 0;
                        } else if (logCondValue < 3) {
                            // Sensitive to singular (Green to Red)
                            const t = (logCondValue - 1) / 2; // Map [1,3] to [0,1]
                            r = Math.floor(255 * t);     // 0 to 255
                            g = Math.floor(255 * (1 - t)); // 255 to 0
                            b = 0;
                        } else if (logCondValue < 4.5) {
                            // Highly singular (Red)
                            r = 255; g = 0; b = 0;
                        } else if (logCondValue < 6) {
                            // SMOOTH Red to Yellow transition
                            const t = (logCondValue - 4.5) / 1.5; // Map [4.5,6] to [0,1]
                            r = 255; // Always full red
                            g = Math.floor(255 * t);     // 0 to 255 (adds yellow)
                            b = 0;   // No blue
                        } else {
                            // Pure gimbal lock region (Yellow)
                            r = 255; g = 255; b = 0;
                        }
                        
                        data[pixelIndex] = r;
                        data[pixelIndex + 1] = g;
                        data[pixelIndex + 2] = b;
                        
                        // Highlight edges to show wrapping
                        if (isEdgeX || isEdgeY) {
                            data[pixelIndex + 3] = 200;
                        } else {
                            data[pixelIndex + 3] = 255;
                        }
                    }
                }
                
                // Scale up the heatmap
                const tempCanvas = document.createElement('canvas');
                tempCanvas.width = resolution;
                tempCanvas.height = resolution;
                const tempCtx = tempCanvas.getContext('2d');
                tempCtx.putImageData(imageData, 0, 0);
                
                ctx.imageSmoothingEnabled = false;
                ctx.drawImage(tempCanvas, 0, 0, width, height);
                
                // Draw proper wrapping indicators
                ctx.strokeStyle = 'rgba(100, 255, 218, 0.6)';
                ctx.lineWidth = 3;
                ctx.setLineDash([8, 4]);
                
                // Yaw wrapping (left-right edges are connected)
                ctx.beginPath();
                ctx.moveTo(0, 0);
                ctx.lineTo(0, height);
                ctx.moveTo(width-1, 0);
                ctx.lineTo(width-1, height);
                ctx.stroke();
                
                // Roll wrapping (top-bottom edges are connected)
                ctx.beginPath();
                ctx.moveTo(0, 0);
                ctx.lineTo(width, 0);
                ctx.moveTo(0, height-1);
                ctx.lineTo(width, height-1);
                ctx.stroke();
                
                ctx.setLineDash([]);
                
                // Add wrap indication arrows
                ctx.fillStyle = 'rgba(100, 255, 218, 0.8)';
                ctx.font = '14px sans-serif';
                
                // Yaw wrap arrows
                ctx.save();
                ctx.translate(5, height/2);
                ctx.rotate(-Math.PI/2);
                ctx.fillText('↔', -8, 0);
                ctx.restore();
                
                ctx.save();
                ctx.translate(width-15, height/2);
                ctx.rotate(-Math.PI/2);
                ctx.fillText('↔', -8, 0);
                ctx.restore();
                
                // Roll wrap arrows
                ctx.fillText('↕', width/2-5, 15);
                ctx.fillText('↕', width/2-5, height-5);
                
                // Draw enhanced labels and scale
                ctx.fillStyle = '#ffffff';
                ctx.font = '12px sans-serif';
                ctx.fillText('Yaw [-180°, 180°] →', width - 140, height - 10);
                ctx.save();
                ctx.translate(15, height/2 + 30);
                ctx.rotate(-Math.PI/2);
                ctx.fillText('Roll [-180°, 180°] ↑', 0, 0);
                ctx.restore();
                
                // Add coordinate grid markers
                ctx.fillStyle = 'rgba(255, 255, 255, 0.7)';
                ctx.font = '10px sans-serif';
                
                // Yaw markers
                const yawMarkers = [-180, -90, 0, 90, 180];
                yawMarkers.forEach(angle => {
                    const x = ((angle + 180) / 360) * width;
                    ctx.fillText(`${angle}°`, x - 10, height - 20);
                });
                
                // Roll markers
                const rollMarkers = [-180, -90, 0, 90, 180];
                rollMarkers.forEach(angle => {
                    const y = ((angle + 180) / 360) * height;
                    ctx.save();
                    ctx.translate(25, y + 3);
                    ctx.fillText(`${angle}°`, 0, 0);
                    ctx.restore();
                });
                
                // Add current yaw/roll position indicator for flat view
                {
                    // Map current yaw/roll to flat view coordinates
                    const flatYawX = ((currentYaw * 180 / Math.PI + 180) / 360) * width;
                    const flatRollY = ((currentRoll * 180 / Math.PI + 180) / 360) * height;
                    
                    // Clamp to visible area
                    const clampedX = Math.max(20, Math.min(width - 20, flatYawX));
                    const clampedY = Math.max(20, Math.min(height - 40, flatRollY));
                    
                    const indicatorSize = 10;
                    
                    // Outer glow
                    ctx.fillStyle = 'rgba(255, 255, 255, 0.4)';
                    ctx.beginPath();
                    ctx.arc(clampedX, clampedY, indicatorSize + 4, 0, 2 * Math.PI);
                    ctx.fill();
                    
                    // Main indicator - bright magenta like torus view
                    ctx.fillStyle = '#ff00ff';
                    ctx.beginPath();
                    ctx.arc(clampedX, clampedY, indicatorSize, 0, 2 * Math.PI);
                    ctx.fill();
                    
                    // Inner highlight
                    ctx.fillStyle = '#ffffff';
                    ctx.beginPath();
                    ctx.arc(clampedX, clampedY, indicatorSize - 4, 0, 2 * Math.PI);
                    ctx.fill();
                    
                    // Cross-hairs for precision
                    ctx.strokeStyle = '#ff00ff';
                    ctx.lineWidth = 2;
                    ctx.beginPath();
                    ctx.moveTo(clampedX - 15, clampedY);
                    ctx.lineTo(clampedX + 15, clampedY);
                    ctx.moveTo(clampedX, clampedY - 15);
                    ctx.lineTo(clampedX, clampedY + 15);
                    ctx.stroke();
                    
                    // Label with current values
                    ctx.fillStyle = '#ff00ff';
                    ctx.font = 'bold 11px sans-serif';
                    ctx.fillText('Current Position', clampedX + 18, clampedY - 15);
                    ctx.font = '9px sans-serif';
                    ctx.fillText(`Y:${rad2deg(currentYaw).toFixed(0)}° R:${rad2deg(currentRoll).toFixed(0)}°`,
                               clampedX + 18, clampedY - 2);
                    
                    // Draw connection lines to axes for clarity
                    ctx.strokeStyle = 'rgba(255, 0, 255, 0.3)';
                    ctx.lineWidth = 1;
                    ctx.setLineDash([2, 2]);
                    ctx.beginPath();
                    // Vertical line to yaw axis
                    ctx.moveTo(clampedX, clampedY);
                    ctx.lineTo(clampedX, height - 35);
                    // Horizontal line to roll axis
                    ctx.moveTo(clampedX, clampedY);
                    ctx.lineTo(35, clampedY);
                    ctx.stroke();
                    ctx.setLineDash([]);
                }
            }
            
            // Add pitch value indicator
            ctx.font = 'bold 14px sans-serif';
            ctx.fillStyle = Math.abs(fixedPitch) > deg2rad(80) ? '#ff6b6b' : '#64ffda';
            ctx.fillText(`Pitch: ${rad2deg(fixedPitch).toFixed(0)}°`, 10, 20);
            
            // Add singularity warning if near gimbal lock
            if (Math.abs(Math.cos(fixedPitch)) < 0.15) {
                ctx.fillStyle = '#ffff00';
                ctx.font = 'bold 16px sans-serif';
                ctx.fillText('NEAR SINGULARITY!', width/2 - 80, 25);
            }
        };

        // SLERP comparison visualization - COMPLETELY REDESIGNED
        let orientationA = [0, 0, 0, 1];
        let orientationB = qFromEulerZYX(deg2rad(90), deg2rad(45), deg2rad(30));

        const drawSlerpVelocity = () => {
            const canvas = document.getElementById('slerpVelocityCanvas');
            const ctx = slerpVelocityCtx;
            const width = canvas.clientWidth;
            const height = canvas.clientHeight;
            
            ctx.clearRect(0, 0, width, height);
            
            const t = parseFloat(document.getElementById('tSlider').value);
            
            // Get Euler angles for start and end orientations
            const eulerA = qToEulerZYX(orientationA);
            const eulerB = qToEulerZYX(orientationB);
            
            // =========================================================================
            // Angular Velocity Comparison (Shows speed consistency)
            // =========================================================================
            
            // Compute angular velocities for both methods
            const slerpVelocities = [];
            const eulerVelocities = [];
            const slerpOrientations = [];
            const eulerOrientations = [];
            
            const numSamples = 100;
            const dt = 1.0 / numSamples;
            
            for (let i = 0; i <= numSamples; i++) {
                const t_i = i / numSamples;
                
                // SLERP interpolation
                const qSlerped = qSlerp(orientationA, orientationB, t_i);
                slerpOrientations.push(qSlerped);
                
                // Euler LERP interpolation
                const interpYaw = eulerA[0] + (eulerB[0] - eulerA[0]) * t_i;
                const interpPitch = eulerA[1] + (eulerB[1] - eulerA[1]) * t_i;
                const interpRoll = eulerA[2] + (eulerB[2] - eulerA[2]) * t_i;
                const qEuler = qFromEulerZYX(interpYaw, interpPitch, interpRoll);
                eulerOrientations.push(qEuler);
                
                // Calculate angular velocities (skip first point)
                if (i > 0) {
                    // SLERP angular velocity
                    const slerpDeltaQ = qMul(qSlerped, qConj(slerpOrientations[i-1]));
                    const [slerpAxis, slerpAngle] = qToAxisAngle(slerpDeltaQ);
                    const slerpVelocity = slerpAngle / dt;
                    slerpVelocities.push(slerpVelocity);
                    
                    // Euler LERP angular velocity
                    const eulerDeltaQ = qMul(qEuler, qConj(eulerOrientations[i-1]));
                    const [eulerAxis, eulerAngle] = qToAxisAngle(eulerDeltaQ);
                    const eulerVelocity = eulerAngle / dt;
                    eulerVelocities.push(eulerVelocity);
                }
            }
            
            // Find max velocity for scaling
            const maxVelocity = Math.max(...slerpVelocities, ...eulerVelocities);
            const minVelocity = Math.min(...slerpVelocities, ...eulerVelocities);
            const velocityRange = maxVelocity - minVelocity || 1;
            
            // Draw panel background
            ctx.fillStyle = 'rgba(255, 255, 255, 0.02)';
            ctx.fillRect(0, 0, width, height);
            
            // Draw velocity graphs
            const graphHeight = height * 0.7;
            const graphY = height * 0.15;
            
            // Draw grid lines
            ctx.strokeStyle = 'rgba(255, 255, 255, 0.1)';
            ctx.lineWidth = 1;
            for (let i = 0; i <= 4; i++) {
                const y = graphY + (graphHeight * i / 4);
                ctx.beginPath();
                ctx.moveTo(10, y);
                ctx.lineTo(width - 10, y);
                ctx.stroke();
            }
            
            // SLERP velocity (should be constant) - BLUE
            ctx.strokeStyle = '#2196f3';
            ctx.lineWidth = 3;
            ctx.beginPath();
            for (let i = 0; i < slerpVelocities.length; i++) {
                const x = 10 + (width - 20) * (i / slerpVelocities.length);
                const normalizedVel = (slerpVelocities[i] - minVelocity) / velocityRange;
                const y = graphY + graphHeight - (normalizedVel * graphHeight);
                
                if (i === 0) ctx.moveTo(x, y);
                else ctx.lineTo(x, y);
            }
            ctx.stroke();
            
            // Draw SLERP velocity value (should be nearly constant)
            const avgSlerpVel = slerpVelocities.reduce((a, b) => a + b, 0) / slerpVelocities.length;
            const slerpVariance = Math.sqrt(slerpVelocities.reduce((sum, v) => sum + Math.pow(v - avgSlerpVel, 2), 0) / slerpVelocities.length);
            
            // Euler LERP velocity (varies) - RED
            ctx.strokeStyle = '#f44336';
            ctx.lineWidth = 3;
            ctx.beginPath();
            for (let i = 0; i < eulerVelocities.length; i++) {
                const x = 10 + (width - 20) * (i / eulerVelocities.length);
                const normalizedVel = (eulerVelocities[i] - minVelocity) / velocityRange;
                const y = graphY + graphHeight - (normalizedVel * graphHeight);
                
                if (i === 0) ctx.moveTo(x, y);
                else ctx.lineTo(x, y);
            }
            ctx.stroke();
            
            // Calculate Euler velocity variance
            const avgEulerVel = eulerVelocities.reduce((a, b) => a + b, 0) / eulerVelocities.length;
            const eulerVariance = Math.sqrt(eulerVelocities.reduce((sum, v) => sum + Math.pow(v - avgEulerVel, 2), 0) / eulerVelocities.length);
            
            // Draw current position marker
            const currentVelIndex = Math.min(Math.floor(t * (slerpVelocities.length - 1)), slerpVelocities.length - 1);
            const markerX = 10 + (width - 20) * t;
            
            // Vertical line at current t
            ctx.strokeStyle = 'rgba(255, 255, 255, 0.3)';
            ctx.lineWidth = 1;
            ctx.setLineDash([4, 2]);
            ctx.beginPath();
            ctx.moveTo(markerX, graphY);
            ctx.lineTo(markerX, graphY + graphHeight);
            ctx.stroke();
            ctx.setLineDash([]);
            
            // SLERP current point
            const slerpNormVel = (slerpVelocities[currentVelIndex] - minVelocity) / velocityRange;
            const slerpMarkerY = graphY + graphHeight - (slerpNormVel * graphHeight);
            ctx.fillStyle = '#2196f3';
            ctx.beginPath();
            ctx.arc(markerX, slerpMarkerY, 5, 0, 2 * Math.PI);
            ctx.fill();
            
            // Euler current point
            const eulerNormVel = (eulerVelocities[currentVelIndex] - minVelocity) / velocityRange;
            const eulerMarkerY = graphY + graphHeight - (eulerNormVel * graphHeight);
            ctx.fillStyle = '#f44336';
            ctx.beginPath();
            ctx.arc(markerX, eulerMarkerY, 5, 0, 2 * Math.PI);
            ctx.fill();
            
            // Labels for panel
            ctx.fillStyle = '#ffffff';
            ctx.font = 'bold 12px sans-serif';
            ctx.fillText('Angular Velocity', width/2 - 45, 15);
            
            ctx.font = '10px sans-serif';
            ctx.fillStyle = 'rgba(255, 255, 255, 0.6)';
            ctx.fillText('0', 5, graphY + graphHeight + 12);
            ctx.fillText('t', width - 15, graphY + graphHeight + 12);
            ctx.fillText('1', width - 10, graphY + graphHeight + 12);
            
            // Velocity stats
            ctx.fillStyle = '#2196f3';
            ctx.font = 'bold 11px sans-serif';
            ctx.fillText(`SLERP: σ=${slerpVariance.toFixed(3)}`, 15, height - 25);
            ctx.fillText('(constant speed)', 15, height - 10);
            
            ctx.fillStyle = '#f44336';
            ctx.fillText(`Euler: σ=${eulerVariance.toFixed(3)}`, 15, height - 55);
            ctx.fillText('(varying speed)', 15, height - 40);
        };

        // ===============================================================================
        // ENHANCED EVENT HANDLERS WITH MATHEMATICAL ANALYSIS
        // ===============================================================================
        
        let previousQuaternion = null;
        let lastUpdateTime = performance.now();
        
        const updateFromSliders = () => {
            const currentTime = performance.now();
            const deltaTime = (currentTime - lastUpdateTime) / 1000.0; // Convert to seconds
            
            const yaw = deg2rad(parseFloat(document.getElementById('yawSlider').value));
            const pitch = deg2rad(parseFloat(document.getElementById('pitchSlider').value));
            const roll = deg2rad(parseFloat(document.getElementById('rollSlider').value));
            
            // Store previous quaternion for temporal analysis
            previousQuaternion = currentQuaternion ? [...currentQuaternion] : null;
            
            currentQuaternion = qFromEulerZYX(yaw, pitch, roll);
            
            // ===============================================================================
            // COMPREHENSIVE MATHEMATICAL VERIFICATION AND ANALYSIS
            // ===============================================================================
            
            // Unit quaternion constraint verification
            const quaternionNorm = Math.sqrt(currentQuaternion[0]**2 + currentQuaternion[1]**2 +
                                           currentQuaternion[2]**2 + currentQuaternion[3]**2);
            const normError = Math.abs(quaternionNorm - 1.0);
            
            if (normError > QUATERNION_TOLERANCE) {
                console.warn("Mathematical Analysis: Unit quaternion constraint violated:", normError);
                mathAnalysis.unitConstraintViolations.push({
                    frame: mathAnalysis.frameCount,
                    error: normError,
                    quaternion: [...currentQuaternion]
                });
            }
            
            // Temporal coherence analysis
            if (previousQuaternion && deltaTime > 0) {
                const coherenceAnalysis = analyzeTemporalCoherence(currentQuaternion, previousQuaternion, deltaTime);
                if (coherenceAnalysis && !coherenceAnalysis.isCoherent) {
                    // Store for analysis but don't spam console
                    mathAnalysis.temporalCoherence.push({
                        frame: mathAnalysis.frameCount,
                        type: 'coherence-violation',
                        velocityError: coherenceAnalysis.velocityCoherenceError,
                        axisError: coherenceAnalysis.axisCoherenceError
                    });
                }
            }
            
            // SO(3) mapping verification
            const rotMatrix = qToMatrix(currentQuaternion);
            const so3Analysis = verifySO3Properties(rotMatrix);
            if (!so3Analysis.isValidSO3) {
                console.error("Mathematical Analysis: SO(3) property violation:", so3Analysis);
            }
            
            // Double cover consistency check
            const doubleCoverAnalysis = verifyDoubleCover(currentQuaternion);
            if (!doubleCoverAnalysis.isValid) {
                console.error("Mathematical Analysis: Double cover violation:", doubleCoverAnalysis.error);
            }
            
            // Antipodal point analysis (store results but reduce console spam)
            const antipodalAnalysis = analyzeAntipodalPoints(currentQuaternion);
            // Only log significant antipodal inconsistencies
            if (!antipodalAnalysis.isValidAntipodal && antipodalAnalysis.geodesicError > 0.1) {
                console.warn("Mathematical Analysis: Significant antipodal inconsistency:", {
                    geodesicError: antipodalAnalysis.geodesicError,
                    rotationMatrixError: antipodalAnalysis.rotationMatrixError
                });
            }
            
            // Stereographic projection error check
            const stereoProjection = stereographicProjection(currentQuaternion, 'north');
            if (!stereoProjection.isValid) {
                mathAnalysis.projectionErrors.push({
                    frame: mathAnalysis.frameCount,
                    error: stereoProjection.error,
                    quaternion: [...currentQuaternion]
                });
            }
            
            // Enhanced drift correction with mathematical verification
            frameCount++;
            mathAnalysis.frameCount++;
            
            if (frameCount % 10 === 0) {
                const preNormQuaternion = [...currentQuaternion];
                currentQuaternion = qNorm(currentQuaternion);
                
                // Verify normalization effectiveness
                const postNormError = Math.abs(Math.sqrt(
                    currentQuaternion[0]**2 + currentQuaternion[1]**2 +
                    currentQuaternion[2]**2 + currentQuaternion[3]**2
                ) - 1.0);
                
                if (postNormError > QUATERNION_TOLERANCE) {
                    console.error("Mathematical Analysis: Normalization failed:", postNormError);
                }
                
                // Enhanced quaternion history with continuity analysis
                quaternionHistory.push([...currentQuaternion]);
                if (quaternionHistory.length > 10) {
                    quaternionHistory.shift();
                }
                
                // Geodesic continuity check
                if (quaternionHistory.length > 1) {
                    const prev = quaternionHistory[quaternionHistory.length - 2];
                    const dot = qDot(currentQuaternion, prev);
                    const geodesicDist = geodesicDistance(currentQuaternion, prev);
                    
                    // Choose shorter geodesic path (handle double cover)
                    if (dot < 0) {
                        currentQuaternion = currentQuaternion.map(v => -v);
                        quaternionHistory[quaternionHistory.length - 1] = currentQuaternion;
                        
                        // Verify the flip improved continuity
                        const newDot = qDot(currentQuaternion, prev);
                        const newGeodist = geodesicDistance(currentQuaternion, prev);
                        
                        if (newGeodist > geodesicDist) {
                            console.warn("Mathematical Analysis: Quaternion flip did not improve continuity");
                        }
                    }
                }
            }
            
            // Update displays with enhanced precision
            document.getElementById('yawValue').textContent = `${rad2deg(yaw).toFixed(1)}°`;
            document.getElementById('pitchValue').textContent = `${rad2deg(pitch).toFixed(1)}°`;
            document.getElementById('rollValue').textContent = `${rad2deg(roll).toFixed(1)}°`;
            
            // Enhanced quaternion display with normalization status
            const quatX = document.getElementById('quatX');
            const quatY = document.getElementById('quatY');
            const quatZ = document.getElementById('quatZ');
            const quatW = document.getElementById('quatW');
            
            quatX.textContent = currentQuaternion[0].toFixed(6);
            quatY.textContent = currentQuaternion[1].toFixed(6);
            quatZ.textContent = currentQuaternion[2].toFixed(6);
            quatW.textContent = currentQuaternion[3].toFixed(6);
            
            // Color-code quaternion components based on mathematical validity
            const validColor = '#64ffda';
            const warningColor = '#ff9800';
            const errorColor = '#ff6b6b';
            
            const componentColor = normError < QUATERNION_TOLERANCE ? validColor :
                                 (normError < QUATERNION_TOLERANCE * 10 ? warningColor : errorColor);
            
            quatX.style.color = componentColor;
            quatY.style.color = componentColor;
            quatZ.style.color = componentColor;
            quatW.style.color = componentColor;
            
            // Redraw visualizations with enhanced mathematical analysis
            drawGimbal();
            
            // Update 3D quaternion sphere visualization with mathematical insights
            updateQuaternionSphere3D();
            
            // Update timestamp for next temporal analysis
            lastUpdateTime = currentTime;
            
            // Console output for significant mathematical events (throttled)
            if (frameCount % 60 === 0) { // Every 60 frames
                if (mathAnalysis.unitConstraintViolations.length > 0) {
                    console.log("Mathematical Analysis Summary:");
                    console.log(`  Unit constraint violations: ${mathAnalysis.unitConstraintViolations.length}`);
                    console.log(`  Geodesic errors: ${mathAnalysis.geodesicErrors.length}`);
                    console.log(`  Singularity detections: ${mathAnalysis.singularityDetections.length}`);
                    console.log(`  Projection errors: ${mathAnalysis.projectionErrors.length}`);
                    console.log(`  Temporal coherence issues: ${mathAnalysis.temporalCoherence.filter(t => !t.isCoherent).length}`);
                }
            }
        };

        // Initialize everything
        const init = () => {
            // Initialize Three.js for gimbal visualization
            initThreeJS();
            
            // Initialize 3D quaternion sphere visualization
            initQuaternionSphere3D();
            
            // Initialize Hopf Fibration visualization
            initHopfFibration3D();
            
            // Initialize other canvas contexts (only 2D canvases)
            heatmapCtx = document.getElementById('heatmapCanvas').getContext('2d');
            slerpVelocityCtx = document.getElementById('slerpVelocityCanvas').getContext('2d');
            
            // Initialize Three.js for SLERP path visualization
            initSlerpPath3D();
            
            resizeCanvases();
            
            // Set up slider event listeners for main gimbal
            ['yawSlider', 'pitchSlider', 'rollSlider'].forEach(id => {
                document.getElementById(id).addEventListener('input', updateFromSliders);
            });
            
            // Set up event listeners for independent sphere Euler angle sliders - FIXED
            ['sphereYawSlider', 'spherePitchSlider', 'sphereRollSlider'].forEach((id, index) => {
                document.getElementById(id).addEventListener('input', (e) => {
                    if (updatingFromSlider) return;
                    
                    // Update sphere Euler state
                    sphereEuler[index] = deg2rad(parseFloat(e.target.value));
                    
                    // Convert to quaternion and update everything
                    updateSphereFromEuler();
                });
            });
            
            // Set up event listeners for quaternion component sliders - FIXED
            ['quatXSlider', 'quatYSlider', 'quatZSlider', 'quatWSlider'].forEach((id, index) => {
                document.getElementById(id).addEventListener('input', (e) => {
                    if (updatingFromSlider) return;
                    
                    // Update quaternion component
                    const value = parseFloat(e.target.value);
                    sphereQuaternion[index] = value;
                    
                    // Update the corresponding value display immediately
                    const componentNames = ['X', 'Y', 'Z', 'W'];
                    document.getElementById(`quat${componentNames[index]}Value`).textContent = value.toFixed(3);
                    
                    // Always auto-normalize and update Euler angles
                    updateEulerFromSphere();
                });
            });
            
            document.getElementById('heatmapPitchSlider').addEventListener('input', () => {
                const pitch = parseFloat(document.getElementById('heatmapPitchSlider').value);
                document.getElementById('heatmapPitchValue').textContent = `${pitch}°`;
                drawHeatmap();
            });
            
            // Add event listeners for torus yaw and roll sliders
            document.getElementById('torusYawSlider').addEventListener('input', () => {
                const yaw = parseFloat(document.getElementById('torusYawSlider').value);
                document.getElementById('torusYawValue').textContent = `${yaw}°`;
                drawHeatmap();
            });
            
            document.getElementById('torusRollSlider').addEventListener('input', () => {
                const roll = parseFloat(document.getElementById('torusRollSlider').value);
                document.getElementById('torusRollValue').textContent = `${roll}°`;
                drawHeatmap();
            });
            
            document.getElementById('tSlider').addEventListener('input', () => {
                const t = parseFloat(document.getElementById('tSlider').value);
                document.getElementById('tValue').textContent = t.toFixed(2);
                drawSlerpVelocity();
                updateSlerpPaths3D();
                // Update geodesic arc to show current position
                if (showGeodesicArc) {
                    updateQuaternionSphere3D();
                }
            });
            
            document.getElementById('showQuaternionFeatures').addEventListener('click', () => {
                // Toggle both features together
                showAntipodes = !showAntipodes;
                showGeodesicArc = showAntipodes; // Keep them in sync
                
                document.getElementById('showQuaternionFeatures').textContent =
                    showAntipodes ? 'Hide Antipodal Points & Geodesic Arc' : 'Show Antipodal Points & Geodesic Arc';
                updateQuaternionSphere3D();
            });
            
            // Hopf Fibration event listeners - FIXED INITIALIZATION
            // Enhanced Hopf Fibration event listeners with null checks
            const hopfRotationSpeedEl = document.getElementById('hopfRotationSpeed');
            if (hopfRotationSpeedEl) {
                hopfRotationSpeedEl.addEventListener('input', (e) => {
                    hopfParams.rotationSpeed = parseFloat(e.target.value);
                    const valueEl = document.getElementById('hopfRotationSpeedValue');
                    if (valueEl) valueEl.textContent = hopfParams.rotationSpeed.toFixed(1);
                });
            }
            
            const hopfFiberCountEl = document.getElementById('hopfFiberCount');
            if (hopfFiberCountEl) {
                hopfFiberCountEl.addEventListener('input', (e) => {
                    hopfParams.fiberCount = parseInt(e.target.value);
                    const valueEl = document.getElementById('hopfFiberCountValue');
                    if (valueEl) valueEl.textContent = hopfParams.fiberCount;
                    if (typeof createHopfFibers === 'function') createHopfFibers();
                });
            }
            
            const hopfAnimationPhaseEl = document.getElementById('hopfAnimationPhase');
            if (hopfAnimationPhaseEl) {
                hopfAnimationPhaseEl.addEventListener('input', (e) => {
                    hopfParams.animationPhase = parseFloat(e.target.value);
                    const valueEl = document.getElementById('hopfAnimationPhaseValue');
                    if (valueEl) valueEl.textContent = hopfParams.animationPhase.toFixed(2);
                    if (typeof createHopfFibers === 'function') createHopfFibers();
                });
            }
            
            const hopfBaseSphereEl = document.getElementById('hopfBaseSphere');
            if (hopfBaseSphereEl) {
                hopfBaseSphereEl.addEventListener('input', (e) => {
                    hopfParams.baseSphereSize = parseFloat(e.target.value);
                    const valueEl = document.getElementById('hopfBaseSphereValue');
                    if (valueEl) valueEl.textContent = hopfParams.baseSphereSize.toFixed(1);
                    if (typeof createHopfBaseSphere === 'function') createHopfBaseSphere();
                });
            }
            
            const hopfToggleAnimationEl = document.getElementById('hopfToggleAnimation');
            if (hopfToggleAnimationEl) {
                hopfToggleAnimationEl.addEventListener('click', () => {
                    hopfIsAnimating = !hopfIsAnimating;
                    hopfToggleAnimationEl.textContent = hopfIsAnimating ? 'Pause' : 'Play';
                });
            }
            
            const hopfResetViewEl = document.getElementById('hopfResetView');
            if (hopfResetViewEl) {
                hopfResetViewEl.addEventListener('click', () => {
                    if (hopfCamera) {
                        hopfCamera.position.set(5, 3, 5);
                        hopfCamera.lookAt(0, 0, 0);
                        hopfTime = 0;
                    }
                });
            }
            
            document.getElementById('setRandomOrientations').addEventListener('click', () => {
                orientationA = qFromAxisAngle([Math.random()-0.5, Math.random()-0.5, Math.random()-0.5], Math.random() * Math.PI);
                orientationB = qFromAxisAngle([Math.random()-0.5, Math.random()-0.5, Math.random()-0.5], Math.random() * Math.PI);
                // Store globally for geodesic visualization
                window.orientationA = orientationA;
                window.orientationB = orientationB;
                drawSlerpVelocity();
                updateSlerpPaths3D();
                updateQuaternionSphere3D(); // Redraw to show geodesic
            });
            
            let heatmapAnimating = false;
            document.getElementById('animateHeatmap').addEventListener('click', () => {
                if (heatmapAnimating) return;
                heatmapAnimating = true;
                
                let pitch = -90;
                const animate = () => {
                    document.getElementById('heatmapPitchSlider').value = pitch;
                    document.getElementById('heatmapPitchValue').textContent = `${pitch}°`;
                    drawHeatmap();
                    
                    pitch += 2;
                    if (pitch <= 90) {
                        setTimeout(animate, 50);
                    } else {
                        heatmapAnimating = false;
                    }
                };
                animate();
            });
            
            document.getElementById('toggleTorusView').addEventListener('click', () => {
                showTorusView = !showTorusView;
                document.getElementById('toggleTorusView').textContent =
                    showTorusView ? 'Switch to Flat View' : 'Switch to Torus View';
                drawHeatmap();
            });
            
            // Set initial button text based on default state
            document.getElementById('toggleTorusView').textContent = 'Switch to Flat View';
            
            let interpAnimating = false;
            document.getElementById('animateInterp').addEventListener('click', () => {
                if (interpAnimating) return;
                interpAnimating = true;
                
                let t = 0;
                const animate = () => {
                    document.getElementById('tSlider').value = t;
                    document.getElementById('tValue').textContent = t.toFixed(2);
                    drawSlerpVelocity();
                    updateSlerpPaths3D();
                    
                    t += 0.01;
                    if (t <= 1) {
                        setTimeout(animate, 20);
                    } else {
                        interpAnimating = false;
                        // Reset
                        setTimeout(() => {
                            t = 0;
                            document.getElementById('tSlider').value = t;
                            document.getElementById('tValue').textContent = t.toFixed(2);
                            drawSlerpVelocity();
                            updateSlerpPaths3D();
                        }, 500);
                    }
                };
                animate();
            });
            
            // Initialize orientations for geodesic arc
            window.orientationA = orientationA;
            window.orientationB = orientationB;
            
            // Initialize sphere controls with default values
            sphereQuaternion = [0, 0, 0, 1];
            sphereEuler = [0, 0, 0];
            
            // Initialize sphere Euler displays
            document.getElementById('sphereYawValue').textContent = '0°';
            document.getElementById('spherePitchValue').textContent = '0°';
            document.getElementById('sphereRollValue').textContent = '0°';
            
            // Initialize quaternion component displays
            document.getElementById('quatXValue').textContent = '0.000';
            document.getElementById('quatYValue').textContent = '0.000';
            document.getElementById('quatZValue').textContent = '0.000';
            document.getElementById('quatWValue').textContent = '1.000';
            
            // Initial draw with enhanced Hopf Fibration
            updateFromSliders();
            updateQuaternionSphere3D(); // Initialize sphere visualization
            drawHeatmap();
            drawSlerpVelocity();
            updateSlerpPaths3D();
            
            // Initialize enhanced Hopf Fibration with proper parameters
            setTimeout(() => {
                console.log('Initializing enhanced Hopf Fibration...');
                if (hopfFibers.length > 0) {
                    console.log(`Hopf fibers successfully generated: ${hopfFibers.length}`);
                }
                
                // Set initial UI values
                document.getElementById('hopfRotationSpeedValue').textContent = hopfParams.rotationSpeed.toFixed(1);
                document.getElementById('hopfFiberCountValue').textContent = hopfParams.fiberCount;
                document.getElementById('hopfAnimationPhaseValue').textContent = hopfParams.animationPhase.toFixed(2);
                document.getElementById('hopfBaseSphereValue').textContent = hopfParams.baseSphereSize.toFixed(1);
            }, 100);
        };

        // Handle window resize
        window.addEventListener('resize', () => {
            setTimeout(() => {
                resizeCanvases();
                resizeThreeJS();
                resizeQuaternionSphere3D();
                resizeHopfVisualization();
                resizeSlerpPath3D();
                updateFromSliders();
                drawHeatmap();
                drawSlerpVelocity();
                updateSlerpPaths3D();
            }, 100);
        });

        // Start when page loads
        document.addEventListener('DOMContentLoaded', init);

    </script>
</body>
</html>]]></content><author><name></name></author><category term="physics" /><category term="graphics" /><category term="games" /><category term="rotations" /><category term="quaternions" /><category term="so3" /><summary type="html"><![CDATA[Why Euler angles hit singularities, how quaternions live on S³, and why SLERP makes interpolation behave. Zero-build, in-browser demo.]]></summary></entry><entry><title type="html">PID Controllers for Humans</title><link href="https://saeed1262.github.io/blog/2024/PID/" rel="alternate" type="text/html" title="PID Controllers for Humans" /><published>2024-09-07T00:00:00+00:00</published><updated>2024-09-07T00:00:00+00:00</updated><id>https://saeed1262.github.io/blog/2024/PID</id><content type="html" xml:base="https://saeed1262.github.io/blog/2024/PID/"><![CDATA[<h2 id="the-invisible-hand-of-control">The Invisible Hand of Control</h2>

<p>PID controllers are everywhere. They keep your drone hovering steady in windy conditions, smooth out your camera’s autofocus, and fire the precise thrusters that guide spacecraft to dock with the International Space Station. Yet for most people, PID remains a mysterious black box of mathematical coefficients.</p>

<p>What if we could see PID in action? What if we could <em>feel</em> how each parameter shapes the behavior of a control system?</p>

<h2 id="the-mathematical-foundation">The Mathematical Foundation</h2>

<p>A PID controller combines three fundamental control strategies into a single, elegant algorithm:</p>

\[u(t) = K_p e(t) + K_i \int_0^t e(\tau) d\tau + K_d \frac{d}{dt}e(t)\]

<p>Where:</p>
<ul>
  <li><strong>u(t)</strong> is the control output</li>
  <li><strong>e(t) = r(t) - y(t)</strong> is the error between reference and actual values</li>
  <li>\(K_p, K_i, K_d\) are the proportional, integral, and derivative gains</li>
</ul>

<p>This deceptively simple equation powers everything from the thermostat in your home to the guidance systems of interplanetary spacecraft.</p>

<h2 id="the-challenge-docking-in-space">The Challenge: Docking in Space</h2>

<p>Imagine you’re piloting a spacecraft approaching a space station. You need to bring your velocity to zero at exactly the right position - overshoot and you crash, undershoot and you drift away. This is a classic control problem that demonstrates the essential challenge of feedback control: how do you smoothly guide a dynamic system to a desired state?</p>

<p>The physics are unforgiving. In space, there’s no friction to naturally slow you down. Every thrust burns precious fuel. The slightest miscalculation can send you spinning into the void. This is where PID control shines - it provides a systematic, mathematically grounded approach to this complex problem.</p>

<h2 id="interactive-spacecraft-docking-simulator">Interactive Spacecraft Docking Simulator</h2>

<p>Try adjusting the PID parameters below. Watch how <strong>P</strong> (Proportional) responds to current error, <strong>I</strong> (Integral) eliminates steady-state error, and <strong>D</strong> (Derivative) dampens oscillations. Toggle wind gusts to see how the controller adapts to disturbances.</p>

<style>
/* Enhanced PID Demo Styles with WCAG Compliance */
.pid-demo-container {
  max-width: 1200px;
  margin: 30px auto;
  font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
  background-color: var(--global-card-bg-color);
  border-radius: 12px;
  padding: 24px;
  box-shadow: var(--global-box-shadow-lg);
  border: 1px solid var(--global-border-color);
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

.control-panel {
  background-color: var(--global-bg-color-secondary);
  padding: 24px;
  border-radius: 12px;
  margin-bottom: 20px;
  box-shadow: var(--global-box-shadow-sm);
  border: 1px solid var(--global-border-color);
}

.control-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: 24px;
  margin-bottom: 20px;
}

.control-group {
  background-color: var(--global-card-bg-color);
  padding: 20px;
  border-radius: 8px;
  border: 1px solid var(--global-border-color);
  box-shadow: var(--global-box-shadow-sm);
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  position: relative;
}

.control-group:hover {
  border-color: var(--global-theme-color);
  box-shadow: var(--global-box-shadow-md);
  transform: translateY(-2px);
  background-color: var(--global-card-hover-bg);
}

.control-label {
  font-weight: 700;
  font-size: 18px;
  color: var(--global-heading-color);
  margin-bottom: 12px;
  display: block;
  text-shadow: none;
  letter-spacing: 0.5px;
}

.control-value {
  background: var(--global-theme-color);
  color: #FFFFFF;
  padding: 8px 16px;
  border-radius: 20px;
  font-weight: 700;
  font-size: 16px;
  min-width: 70px;
  display: inline-block;
  text-align: center;
  margin-left: 12px;
  box-shadow: var(--global-box-shadow-sm);
  transition: all 0.2s ease;
}

.control-description {
  color: var(--global-text-color-light);
  font-size: 14px;
  margin-top: 8px;
  font-weight: 500;
}

.pid-slider {
  width: 100%;
  height: 10px;
  border-radius: 5px;
  background: var(--global-border-color);
  outline: none;
  margin-top: 12px;
  cursor: pointer;
  -webkit-appearance: none;
  appearance: none;
  transition: background 0.3s ease;
}

.pid-slider:focus {
  background: var(--global-text-color-light);
  box-shadow: 0 0 0 3px rgba(var(--global-theme-color-rgb), 0.25);
}

.pid-slider::-webkit-slider-thumb {
  -webkit-appearance: none;
  appearance: none;
  width: 28px;
  height: 28px;
  border-radius: 50%;
  background: var(--global-theme-color);
  cursor: pointer;
  border: 4px solid var(--global-card-bg-color);
  box-shadow: var(--global-box-shadow-md);
  transition: all 0.2s ease;
}

.pid-slider::-webkit-slider-thumb:hover {
  transform: scale(1.15);
  box-shadow: var(--global-box-shadow-lg);
  background: var(--global-theme-color-dark);
}

.pid-slider::-moz-range-thumb {
  width: 28px;
  height: 28px;
  border-radius: 50%;
  background: var(--global-theme-color);
  cursor: pointer;
  border: 4px solid var(--global-card-bg-color);
  box-shadow: var(--global-box-shadow-md);
}

.control-buttons {
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 24px;
  flex-wrap: wrap;
}

.reset-btn {
  background: var(--global-gradient-primary);
  color: #FFFFFF;
  border: none;
  padding: 14px 28px;
  border-radius: 8px;
  font-weight: 700;
  font-size: 16px;
  cursor: pointer;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  box-shadow: var(--global-box-shadow-md);
  text-transform: uppercase;
  letter-spacing: 1px;
}

.reset-btn:hover {
  transform: translateY(-3px);
  box-shadow: var(--global-box-shadow-lg);
}

.preset-btn {
  background-color: var(--global-card-bg-color);
  color: var(--global-text-color);
  border: 2px solid var(--global-border-color);
  padding: 12px 20px;
  border-radius: 8px;
  font-weight: 600;
  font-size: 14px;
  cursor: pointer;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  box-shadow: var(--global-box-shadow-sm);
}

.preset-btn:hover {
  transform: translateY(-2px);
  box-shadow: var(--global-box-shadow-md);
  background: var(--global-gradient-primary);
  color: #FFFFFF;
  border-color: transparent;
}

.wind-toggle-container {
  display: flex;
  align-items: center;
  background-color: var(--global-card-bg-color);
  padding: 14px 24px;
  border-radius: 8px;
  border: 1px solid var(--global-border-color);
  cursor: pointer;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

.wind-toggle-container:hover {
  border-color: var(--global-accent-color);
  background-color: var(--global-card-hover-bg);
  transform: translateY(-1px);
}

.wind-checkbox {
  width: 22px;
  height: 22px;
  margin-right: 12px;
  cursor: pointer;
}

.wind-label {
  font-weight: 600;
  color: var(--global-text-color);
  font-size: 16px;
  cursor: pointer;
  margin: 0;
}

.visualization-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 20px;
  margin-bottom: 16px;
}

.simulation-container {
  height: 350px;
  background-color: var(--global-bg-color-secondary);
  border-radius: 8px;
  position: relative;
  overflow: hidden;
  border: 1px solid var(--global-border-color);
  box-shadow: var(--global-box-shadow-sm);
}

.plots-container {
  display: grid;
  grid-template-rows: 1fr 1fr;
  gap: 12px;
  height: 350px;
}

.error-plot-container, .control-plot-container {
  background-color: var(--global-card-bg-color);
  border: 1px solid var(--global-border-color);
  border-radius: 8px;
  box-shadow: var(--global-box-shadow-sm);
  position: relative;
}

.plot-title {
  position: absolute;
  top: 8px;
  left: 12px;
  font-weight: 700;
  font-size: 14px;
  color: var(--global-heading-color);
  z-index: 10;
  background-color: var(--global-bg-color-secondary);
  padding: 4px 8px;
  border-radius: 4px;
  border: 1px solid var(--global-border-color);
}

.status-display {
  background-color: var(--global-bg-color-secondary);
  padding: 16px;
  border-radius: 8px;
  margin-top: 16px;
  border: 1px solid var(--global-border-color);
  box-shadow: var(--global-box-shadow-sm);
}

.status-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
  gap: 16px;
  text-align: center;
}

.status-item {
  background-color: var(--global-card-bg-color);
  padding: 12px;
  border-radius: 6px;
  border: 1px solid var(--global-border-color);
}

.status-label {
  font-weight: 600;
  color: var(--global-text-color-light);
  font-size: 12px;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.status-value {
  font-weight: 700;
  color: var(--global-heading-color);
  font-size: 18px;
  margin-top: 4px;
}

.preset-buttons {
  display: flex;
  gap: 12px;
  flex-wrap: wrap;
  justify-content: center;
  margin-top: 16px;
}

.comparison-mode {
  background: rgba(var(--global-accent-color-rgb), 0.1);
  border: 2px solid var(--global-accent-color);
}

/* Responsive design */
@media (max-width: 768px) {
  .visualization-grid {
    grid-template-columns: 1fr;
  }
  
  .control-grid {
    grid-template-columns: 1fr;
  }
  
  .simulation-container,
  .plots-container {
    height: 300px;
  }
  
  .status-grid {
    grid-template-columns: repeat(2, 1fr);
  }
}

/* Animation effects */
@keyframes pulse {
  0% { opacity: 0.6; }
  50% { opacity: 1; }
  100% { opacity: 0.6; }
}

.loading {
  animation: pulse 2s infinite;
}

@keyframes glow {
  0% { box-shadow: 0 0 5px rgba(var(--global-theme-color-rgb), 0.4); }
  50% { box-shadow: 0 0 20px rgba(var(--global-theme-color-rgb), 0.7); }
  100% { box-shadow: 0 0 5px rgba(var(--global-theme-color-rgb), 0.4); }
}

.active-control {
  animation: glow 2s infinite;
}
</style>

<div class="pid-demo-container" id="pid-demo">
  <div class="control-panel">
    <div class="control-grid">
      <div class="control-group" id="p-control">
        <label class="control-label">
          P (Proportional)
          <span class="control-value" id="p-value">1.0</span>
        </label>
        <div class="control-description">Current error response strength</div>
        <input type="range" class="pid-slider" id="p-slider" min="0" max="5" step="0.1" value="1.0" aria-label="Proportional gain control" />
      </div>
      <div class="control-group" id="i-control">
        <label class="control-label">
          I (Integral)
          <span class="control-value" id="i-value">0.1</span>
        </label>
        <div class="control-description">Accumulated error correction</div>
        <input type="range" class="pid-slider" id="i-slider" min="0" max="2" step="0.05" value="0.1" aria-label="Integral gain control" />
      </div>
      <div class="control-group" id="d-control">
        <label class="control-label">
          D (Derivative)
          <span class="control-value" id="d-value">0.3</span>
        </label>
        <div class="control-description">Error change rate damping</div>
        <input type="range" class="pid-slider" id="d-slider" min="0" max="2" step="0.05" value="0.3" aria-label="Derivative gain control" />
      </div>
    </div>
    
    <div class="control-buttons">
      <button class="reset-btn" id="reset-btn" aria-label="Reset simulation">🔄 Reset</button>
      <button class="preset-btn" id="oscillating-btn">Oscillating</button>
      <button class="preset-btn" id="overdamped-btn">Overdamped</button>
      <button class="preset-btn" id="critically-damped-btn">Critical</button>
      <label class="wind-toggle-container">
        <input type="checkbox" class="wind-checkbox" id="wind-toggle" aria-label="Toggle wind disturbances" />
        <span class="wind-label">🌪️ Disturbances</span>
      </label>
    </div>
  </div>
  
  <div class="visualization-grid">
    <div class="simulation-container" id="simulation-container">
      <canvas id="spacecraft-canvas" style="width: 100%; height: 100%;"></canvas>
    </div>
    <div class="plots-container">
      <div class="error-plot-container" id="error-plot">
        <div class="plot-title">Position Error Over Time</div>
      </div>
      <div class="control-plot-container" id="control-plot">
        <div class="plot-title">Control Signal (Thrust)</div>
      </div>
    </div>
  </div>
  
  <div class="status-display">
    <div class="status-grid">
      <div class="status-item">
        <div class="status-label">Position</div>
        <div class="status-value" id="position-value">-8.00</div>
      </div>
      <div class="status-item">
        <div class="status-label">Velocity</div>
        <div class="status-value" id="velocity-value">0.00</div>
      </div>
      <div class="status-item">
        <div class="status-label">Error</div>
        <div class="status-value" id="error-value">8.00</div>
      </div>
      <div class="status-item">
        <div class="status-label">Thrust</div>
        <div class="status-value" id="thrust-value">0.00</div>
      </div>
      <div class="status-item">
        <div class="status-label">Integral</div>
        <div class="status-value" id="integral-value">0.00</div>
      </div>
      <div class="status-item">
        <div class="status-label">Derivative</div>
        <div class="status-value" id="derivative-value">0.00</div>
      </div>
    </div>
  </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/d3@7.8.5/dist/d3.min.js"></script>

<script>
// Enhanced PID Controller with detailed component tracking
class PIDController {
  constructor(kp = 1.0, ki = 0.1, kd = 0.3) {
    this.kp = kp;
    this.ki = ki;
    this.kd = kd;
    this.integral = 0;
    this.previousError = 0;
    this.dt = 0.016; // ~60fps
    this.maxIntegral = 10; // Prevent integral windup
    
    // Track individual components for visualization
    this.pComponent = 0;
    this.iComponent = 0;
    this.dComponent = 0;
  }
  
  update(error) {
    // Proportional component
    this.pComponent = this.kp * error;
    
    // Integral component with windup protection
    this.integral += error * this.dt;
    this.integral = Math.max(-this.maxIntegral, Math.min(this.maxIntegral, this.integral));
    this.iComponent = this.ki * this.integral;
    
    // Derivative component with smoothing
    const derivative = (error - this.previousError) / this.dt;
    this.dComponent = this.kd * derivative;
    
    // Total PID output
    const output = this.pComponent + this.iComponent + this.dComponent;
    
    this.previousError = error;
    return output;
  }
  
  reset() {
    this.integral = 0;
    this.previousError = 0;
    this.pComponent = 0;
    this.iComponent = 0;
    this.dComponent = 0;
  }
  
  setGains(kp, ki, kd) {
    this.kp = kp;
    this.ki = ki;
    this.kd = kd;
  }
}

// Enhanced simulation state with detailed tracking
const sim = {
  position: -8,
  velocity: 0,
  target: 0,
  pid: new PIDController(),
  time: 0,
  errorHistory: [],
  controlHistory: [],
  positionHistory: [],
  velocityHistory: [],
  windEnabled: false,
  maxHistory: 400,
  canvas: null,
  ctx: null,
  animationId: null
};

// Preset configurations to demonstrate different behaviors
const presets = {
  oscillating: { p: 3.0, i: 0.1, d: 0.1 },
  overdamped: { p: 0.5, i: 0.2, d: 1.5 },
  criticallyDamped: { p: 1.5, i: 0.3, d: 0.8 }
};

// Initialize enhanced 2D simulation
function initEnhanced2DSimulation() {
  const container = document.getElementById('simulation-container');
  sim.canvas = document.getElementById('spacecraft-canvas');
  sim.ctx = sim.canvas.getContext('2d');
  
  // Set canvas size
  function resizeCanvas() {
    const rect = container.getBoundingClientRect();
    sim.canvas.width = rect.width;
    sim.canvas.height = rect.height;
  }
  
  resizeCanvas();
  window.addEventListener('resize', resizeCanvas);
  
  // Start animation
  animate2D();
}

function animate2D() {
  // Update physics
  const error = sim.target - sim.position;
  const windForce = sim.windEnabled ? (Math.random() - 0.5) * 0.3 : 0;
  const controlSignal = sim.pid.update(error);
  const thrust = Math.max(-2, Math.min(2, controlSignal));
  
  // Physics integration
  sim.velocity += (thrust + windForce) * 0.016;
  sim.velocity *= 0.98; // damping
  sim.position += sim.velocity * 0.016;
  sim.time += 0.016;
  
  // Record history
  sim.errorHistory.push({ time: sim.time, value: Math.abs(error) });
  sim.controlHistory.push({ time: sim.time, value: thrust });
  sim.positionHistory.push({ time: sim.time, value: sim.position });
  sim.velocityHistory.push({ time: sim.time, value: sim.velocity });
  
  // Limit history
  if (sim.errorHistory.length > sim.maxHistory) {
    sim.errorHistory.shift();
    sim.controlHistory.shift();
    sim.positionHistory.shift();
    sim.velocityHistory.shift();
  }
  
  // Render simulation
  render2D();
  
  // Update status display
  updateStatusDisplay(error, thrust);
  
  sim.animationId = requestAnimationFrame(animate2D);
}

function render2D() {
  const canvas = sim.canvas;
  const ctx = sim.ctx;
  
  // Get CSS custom properties for consistent theming
  const computedStyle = getComputedStyle(document.documentElement);
  const bgColor = computedStyle.getPropertyValue('--global-bg-color-secondary').trim();
  const borderColor = computedStyle.getPropertyValue('--global-border-color').trim();
  const textColor = computedStyle.getPropertyValue('--global-text-color-light').trim();
  const themeColor = computedStyle.getPropertyValue('--global-theme-color').trim();
  const headingColor = computedStyle.getPropertyValue('--global-heading-color').trim();
  
  // Clear canvas with theme background
  ctx.fillStyle = bgColor;
  ctx.fillRect(0, 0, canvas.width, canvas.height);
  
  // Draw subtle grid for reference
  ctx.strokeStyle = borderColor;
  ctx.lineWidth = 1;
  const gridSize = 40;
  for (let x = 0; x < canvas.width; x += gridSize) {
    ctx.beginPath();
    ctx.moveTo(x, 0);
    ctx.lineTo(x, canvas.height);
    ctx.stroke();
  }
  for (let y = 0; y < canvas.height; y += gridSize) {
    ctx.beginPath();
    ctx.moveTo(0, y);
    ctx.lineTo(canvas.width, y);
    ctx.stroke();
  }
  
  const centerX = canvas.width / 2;
  const centerY = canvas.height / 2;
  const scale = Math.min(canvas.width, canvas.height) / 20;
  
  // Draw horizontal position line
  ctx.strokeStyle = textColor;
  ctx.lineWidth = 2;
  ctx.beginPath();
  ctx.moveTo(50, centerY);
  ctx.lineTo(canvas.width - 50, centerY);
  ctx.stroke();
  
  // Draw position scale markings
  ctx.fillStyle = textColor;
  ctx.font = '12px Inter, Arial, sans-serif';
  ctx.textAlign = 'center';
  for (let i = -10; i <= 10; i += 2) {
    const x = centerX + i * scale;
    if (x >= 50 && x <= canvas.width - 50) {
      ctx.strokeStyle = textColor;
      ctx.beginPath();
      ctx.moveTo(x, centerY - 5);
      ctx.lineTo(x, centerY + 5);
      ctx.stroke();
      ctx.fillText(i.toString(), x, centerY + 20);
    }
  }
  
  // Draw target position (setpoint)
  ctx.fillStyle = '#ff6b6b';
  ctx.strokeStyle = '#ff6b6b';
  ctx.lineWidth = 4;
  ctx.beginPath();
  ctx.moveTo(centerX, centerY - 40);
  ctx.lineTo(centerX, centerY + 40);
  ctx.stroke();
  
  ctx.fillStyle = '#ff6b6b';
  ctx.font = 'bold 14px Inter, Arial, sans-serif';
  ctx.textAlign = 'center';
  ctx.fillText('TARGET', centerX, centerY - 50);
  ctx.fillText('(Setpoint = 0)', centerX, centerY + 60);
  
  // Draw current position
  const currentX = centerX + sim.position * scale;
  if (currentX >= 30 && currentX <= canvas.width - 30) {
    // Position marker (circle)
    ctx.fillStyle = themeColor;
    ctx.beginPath();
    ctx.arc(currentX, centerY, 15, 0, Math.PI * 2);
    ctx.fill();
    
    // Position indicator line
    ctx.strokeStyle = themeColor;
    ctx.lineWidth = 3;
    ctx.beginPath();
    ctx.moveTo(currentX, centerY - 35);
    ctx.lineTo(currentX, centerY + 35);
    ctx.stroke();
    
    // Position label
    ctx.fillStyle = themeColor;
    ctx.font = 'bold 14px Inter, Arial, sans-serif';
    ctx.textAlign = 'center';
    ctx.fillText('CURRENT', currentX, centerY - 70);
    ctx.fillText(`Position: ${sim.position.toFixed(2)}`, currentX, centerY + 80);
  }
  
  // Error visualization (distance between target and current)
  const error = sim.target - sim.position;
  if (Math.abs(error) > 0.1) {
    const startX = Math.min(centerX, currentX);
    const endX = Math.max(centerX, currentX);
    const width = endX - startX;
    
    // Error area
    ctx.fillStyle = 'rgba(255, 212, 59, 0.4)';
    ctx.fillRect(startX, centerY - 12, width, 24);
    
    // Error border
    ctx.strokeStyle = '#ffd43b';
    ctx.lineWidth = 2;
    ctx.strokeRect(startX, centerY - 12, width, 24);
    
    // Error value label
    ctx.fillStyle = headingColor;
    ctx.font = 'bold 12px Inter, Arial, sans-serif';
    ctx.textAlign = 'center';
    ctx.fillText(`Error: ${error.toFixed(2)}`, (startX + endX) / 2, centerY - 20);
  }
  
  // Velocity vector (shows system momentum)
  if (Math.abs(sim.velocity) > 0.01) {
    ctx.strokeStyle = '#51cf66';
    ctx.fillStyle = '#51cf66';
    ctx.lineWidth = 4;
    
    const velEndX = currentX + sim.velocity * 200;
    const clampedVelEndX = Math.max(30, Math.min(canvas.width - 30, velEndX));
    
    // Velocity arrow
    ctx.beginPath();
    ctx.moveTo(currentX, centerY - 100);
    ctx.lineTo(clampedVelEndX, centerY - 100);
    ctx.stroke();
    
    // Arrow head
    if (Math.abs(sim.velocity) > 0.05) {
      const direction = sim.velocity > 0 ? 1 : -1;
      ctx.beginPath();
      ctx.moveTo(clampedVelEndX, centerY - 100);
      ctx.lineTo(clampedVelEndX - 8 * direction, centerY - 105);
      ctx.lineTo(clampedVelEndX - 8 * direction, centerY - 95);
      ctx.fill();
    }
    
    // Velocity label
    ctx.font = 'bold 12px Inter, Arial, sans-serif';
    ctx.textAlign = 'center';
    ctx.fillText(`Velocity: ${sim.velocity.toFixed(3)}`, currentX, centerY - 115);
  }
  
  // Control output visualization (thrust/force applied)
  const thrust = sim.controlHistory[sim.controlHistory.length - 1]?.value || 0;
  if (Math.abs(thrust) > 0.01) {
    const barHeight = Math.abs(thrust) * 60;
    const barY = centerY + 120;
    
    // Control signal bar
    ctx.fillStyle = thrust > 0 ? themeColor : '#ff6b6b';
    ctx.fillRect(currentX - 15, barY - (thrust > 0 ? barHeight : 0), 30, barHeight);
    
    // Bar outline
    ctx.strokeStyle = headingColor;
    ctx.lineWidth = 2;
    ctx.strokeRect(currentX - 15, barY - (thrust > 0 ? barHeight : 0), 30, barHeight);
    
    // Control signal label
    ctx.fillStyle = headingColor;
    ctx.font = 'bold 12px Inter, Arial, sans-serif';
    ctx.textAlign = 'center';
    ctx.fillText(`Control: ${thrust.toFixed(3)}`, currentX, barY + 20);
    ctx.fillText(thrust > 0 ? '(Forward)' : '(Backward)', currentX, barY + 35);
  }
  
  // Success indicator
  const distance = Math.abs(sim.position);
  if (distance < 0.1 && Math.abs(sim.velocity) < 0.05) {
    ctx.fillStyle = 'rgba(81, 207, 102, 0.3)';
    ctx.fillRect(0, 0, canvas.width, canvas.height);
    
    ctx.fillStyle = '#51cf66';
    ctx.font = 'bold 24px Inter, Arial, sans-serif';
    ctx.textAlign = 'center';
    ctx.fillText('✓ TARGET ACHIEVED!', canvas.width/2, 40);
    ctx.fillText('Position and Velocity at Zero', canvas.width/2, 70);
  }
  
  // System diagram labels
  ctx.fillStyle = textColor;
  ctx.font = 'bold 11px Inter, Arial, sans-serif';
  ctx.textAlign = 'left';
  ctx.fillText('PID Position Control System', 10, 20);
  
  // Legend
  ctx.fillStyle = textColor;
  ctx.font = '10px Inter, Arial, sans-serif';
  ctx.textAlign = 'left';
  const legendY = canvas.height - 60;
  ctx.fillText('• Theme circle: Current position', 10, legendY);
  ctx.fillText('• Red line: Target setpoint', 10, legendY + 12);
  ctx.fillText('• Yellow area: Position error', 10, legendY + 24);
  ctx.fillText('• Green arrow: Velocity', 10, legendY + 36);
  ctx.fillText('• Theme/Red bar: Control output', 200, legendY + 24);
}

function updateStatusDisplay(error, thrust) {
  document.getElementById('position-value').textContent = sim.position.toFixed(2);
  document.getElementById('velocity-value').textContent = sim.velocity.toFixed(2);
  document.getElementById('error-value').textContent = error.toFixed(2);
  document.getElementById('thrust-value').textContent = thrust.toFixed(2);
  document.getElementById('integral-value').textContent = sim.pid.integral.toFixed(2);
  document.getElementById('derivative-value').textContent = ((error - sim.pid.previousError) / sim.pid.dt).toFixed(2);
}

// Enhanced plotting with D3.js
function updatePlots() {
  updateErrorPlot();
  updateControlPlot();
}

function updateErrorPlot() {
  if (sim.errorHistory.length < 2) return;
  
  const container = document.getElementById('error-plot');
  container.innerHTML = '<div class="plot-title">Position Error Over Time</div>';
  
  const margin = { top: 30, right: 20, bottom: 40, left: 50 };
  const width = container.clientWidth - margin.left - margin.right;
  const height = container.clientHeight - margin.top - margin.bottom;
  
  if (width <= 0 || height <= 0) return;
  
  const svg = d3.select(container)
    .append("svg")
    .attr("width", width + margin.left + margin.right)
    .attr("height", height + margin.top + margin.bottom);
  
  const g = svg.append("g")
    .attr("transform", `translate(${margin.left},${margin.top})`);
  
  const xScale = d3.scaleLinear()
    .domain(d3.extent(sim.errorHistory, d => d.time))
    .range([0, width]);
  
  const yScale = d3.scaleLinear()
    .domain([0, d3.max(sim.errorHistory, d => d.value) || 1])
    .range([height, 0]);
  
  // Area chart
  const area = d3.area()
    .x(d => xScale(d.time))
    .y0(height)
    .y1(d => yScale(d.value))
    .curve(d3.curveMonotoneX);
  
  g.append("path")
    .datum(sim.errorHistory)
    .attr("fill", "rgba(220, 53, 69, 0.2)")
    .attr("d", area);
  
  // Line chart
  const line = d3.line()
    .x(d => xScale(d.time))
    .y(d => yScale(d.value))
    .curve(d3.curveMonotoneX);
  
  g.append("path")
    .datum(sim.errorHistory)
    .attr("fill", "none")
    .attr("stroke", "#dc3545")
    .attr("stroke-width", 2)
    .attr("d", line);
  
  // Axes
  g.append("g")
    .attr("transform", `translate(0,${height})`)
    .call(d3.axisBottom(xScale).ticks(5))
    .selectAll("text")
    .style("font-size", "11px");
  
  g.append("g")
    .call(d3.axisLeft(yScale).ticks(4))
    .selectAll("text")
    .style("font-size", "11px");
}

function updateControlPlot() {
  if (sim.controlHistory.length < 2) return;
  
  const container = document.getElementById('control-plot');
  container.innerHTML = '<div class="plot-title">Control Signal (Thrust)</div>';
  
  const margin = { top: 30, right: 20, bottom: 40, left: 50 };
  const width = container.clientWidth - margin.left - margin.right;
  const height = container.clientHeight - margin.top - margin.bottom;
  
  if (width <= 0 || height <= 0) return;
  
  const svg = d3.select(container)
    .append("svg")
    .attr("width", width + margin.left + margin.right)
    .attr("height", height + margin.top + margin.bottom);
  
  const g = svg.append("g")
    .attr("transform", `translate(${margin.left},${margin.top})`);
  
  const xScale = d3.scaleLinear()
    .domain(d3.extent(sim.controlHistory, d => d.time))
    .range([0, width]);
  
  const yExtent = d3.extent(sim.controlHistory, d => d.value);
  const yScale = d3.scaleLinear()
    .domain([Math.min(yExtent[0], -0.5), Math.max(yExtent[1], 0.5)])
    .range([height, 0]);
  
  // Zero line
  g.append("line")
    .attr("x1", 0)
    .attr("x2", width)
    .attr("y1", yScale(0))
    .attr("y2", yScale(0))
    .attr("stroke", "#666")
    .attr("stroke-dasharray", "3,3");
  
  // Area chart (positive and negative)
  const area = d3.area()
    .x(d => xScale(d.time))
    .y0(yScale(0))
    .y1(d => yScale(d.value))
    .curve(d3.curveMonotoneX);
  
  g.append("path")
    .datum(sim.controlHistory)
    .attr("fill", "rgba(0, 123, 255, 0.3)")
    .attr("d", area);
  
  // Line chart
  const line = d3.line()
    .x(d => xScale(d.time))
    .y(d => yScale(d.value))
    .curve(d3.curveMonotoneX);
  
  g.append("path")
    .datum(sim.controlHistory)
    .attr("fill", "none")
    .attr("stroke", "#007bff")
    .attr("stroke-width", 2)
    .attr("d", line);
  
  // Axes
  g.append("g")
    .attr("transform", `translate(0,${height})`)
    .call(d3.axisBottom(xScale).ticks(5))
    .selectAll("text")
    .style("font-size", "11px");
  
  g.append("g")
    .call(d3.axisLeft(yScale).ticks(4))
    .selectAll("text")
    .style("font-size", "11px");
}

// Initialize everything
document.addEventListener('DOMContentLoaded', function() {
  initEnhanced2DSimulation();
  setInterval(updatePlots, 250);
});

// Controls
const pSlider = document.getElementById('p-slider');
const iSlider = document.getElementById('i-slider');
const dSlider = document.getElementById('d-slider');
const pValue = document.getElementById('p-value');
const iValue = document.getElementById('i-value');
const dValue = document.getElementById('d-value');
const resetBtn = document.getElementById('reset-btn');
const windToggle = document.getElementById('wind-toggle');

function updatePID() {
  sim.pid.setGains(
    parseFloat(pSlider.value),
    parseFloat(iSlider.value),
    parseFloat(dSlider.value)
  );
}

function highlightActiveControl(controlId) {
  // Remove active class from all controls
  document.querySelectorAll('.control-group').forEach(el => {
    el.classList.remove('active-control');
  });
  
  // Add active class to current control
  document.getElementById(controlId).classList.add('active-control');
  
  // Remove after 2 seconds
  setTimeout(() => {
    document.getElementById(controlId).classList.remove('active-control');
  }, 2000);
}

pSlider.addEventListener('input', (e) => {
  pValue.textContent = e.target.value;
  updatePID();
  highlightActiveControl('p-control');
});

iSlider.addEventListener('input', (e) => {
  iValue.textContent = e.target.value;
  updatePID();
  highlightActiveControl('i-control');
});

dSlider.addEventListener('input', (e) => {
  dValue.textContent = e.target.value;
  updatePID();
  highlightActiveControl('d-control');
});

resetBtn.addEventListener('click', () => {
  sim.position = -8;
  sim.velocity = 0;
  sim.time = 0;
  sim.errorHistory = [];
  sim.controlHistory = [];
  sim.positionHistory = [];
  sim.velocityHistory = [];
  sim.pid.reset();
});

windToggle.addEventListener('change', (e) => {
  sim.windEnabled = e.target.checked;
});

// Preset buttons
document.getElementById('oscillating-btn').addEventListener('click', () => {
  const preset = presets.oscillating;
  pSlider.value = preset.p;
  iSlider.value = preset.i;
  dSlider.value = preset.d;
  pValue.textContent = preset.p;
  iValue.textContent = preset.i;
  dValue.textContent = preset.d;
  updatePID();
});

document.getElementById('overdamped-btn').addEventListener('click', () => {
  const preset = presets.overdamped;
  pSlider.value = preset.p;
  iSlider.value = preset.i;
  dSlider.value = preset.d;
  pValue.textContent = preset.p;
  iValue.textContent = preset.i;
  dValue.textContent = preset.d;
  updatePID();
});

document.getElementById('critically-damped-btn').addEventListener('click', () => {
  const preset = presets.criticallyDamped;
  pSlider.value = preset.p;
  iSlider.value = preset.i;
  dSlider.value = preset.d;
  pValue.textContent = preset.p;
  iValue.textContent = preset.i;
  dValue.textContent = preset.d;
  updatePID();
});

</script>

<h2 id="deep-dive-understanding-each-parameter">Deep Dive: Understanding Each Parameter</h2>

<h3 id="proportional-control-p-the-immediate-response">Proportional Control (P): The Immediate Response</h3>

<p>The proportional term provides an output that is directly proportional to the current error:</p>

\[u_p(t) = K_p \cdot e(t)\]

<p><strong>Behavior Characteristics:</strong></p>
<ul>
  <li><strong>Low P gain (&lt; 1.0)</strong>: Sluggish response, slow convergence to target</li>
  <li><strong>Moderate P gain (1.0-2.0)</strong>: Good balance of speed and stability</li>
  <li><strong>High P gain (&gt; 3.0)</strong>: Fast response but prone to oscillations and overshoot</li>
</ul>

<p>The proportional term is like a rubber band - the further you stretch it (larger error), the stronger it pulls back. However, pure proportional control has a fundamental limitation: <strong>steady-state error</strong>. If there’s any constant disturbance (like friction or gravity), the system will settle at a position where the P output exactly balances the disturbance, leaving a permanent error.</p>

<h3 id="integral-control-i-the-memory-keeper">Integral Control (I): The Memory Keeper</h3>

<p>The integral term accumulates error over time, providing a corrective action based on the history of errors:</p>

\[u_i(t) = K_i \int_0^t e(\tau) d\tau\]

<p><strong>Key Properties:</strong></p>
<ul>
  <li><strong>Eliminates steady-state error</strong>: By continuously accumulating error, it ensures the system eventually reaches the target</li>
  <li><strong>Slow response</strong>: Takes time to build up sufficient corrective action</li>
  <li><strong>Integral windup</strong>: Can become excessively large, causing instability and overshoot</li>
</ul>

<p><strong>Integral Windup Prevention:</strong></p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>if (integral &gt; maxIntegral) integral = maxIntegral;
if (integral &lt; -maxIntegral) integral = -maxIntegral;
</code></pre></div></div>

<h3 id="derivative-control-d-the-predictor">Derivative Control (D): The Predictor</h3>

<p>The derivative term responds to the rate of change of error, providing predictive control:</p>

\[u_d(t) = K_d \frac{d}{dt}e(t)\]

<p><strong>Benefits:</strong></p>
<ul>
  <li><strong>Damping</strong>: Reduces oscillations and overshoot</li>
  <li><strong>Anticipatory action</strong>: Responds to trends in error change</li>
  <li><strong>Stability improvement</strong>: Helps stabilize systems that would otherwise be unstable</li>
</ul>

<p><strong>Challenges:</strong></p>
<ul>
  <li><strong>Noise sensitivity</strong>: Amplifies high-frequency noise in the error signal</li>
  <li><strong>Derivative kick</strong>: Can cause sudden jumps in output when the setpoint changes</li>
</ul>

<h2 id="the-mathematics-of-stability">The Mathematics of Stability</h2>

<p>The stability of a PID-controlled system can be analyzed using classical control theory. The characteristic equation of a second-order system with PID control is:</p>

\[s^3 + \frac{K_d}{m}s^2 + \frac{K_p}{m}s + \frac{K_i}{m} = 0\]

<p>For stability, all poles must be in the left half-plane. This constrains the relationship between P, I, and D gains.</p>

<h3 id="ziegler-nichols-tuning-method">Ziegler-Nichols Tuning Method</h3>

<p>One of the most famous tuning methods, developed in 1942:</p>

<ol>
  <li><strong>Step 1</strong>: Set I=0, D=0, increase P until the system oscillates</li>
  <li><strong>Step 2</strong>: Record the critical gain K_c and oscillation period T_c</li>
  <li><strong>Step 3</strong>: Apply the tuning rules:
    <ul>
      <li>P-only: K_p = 0.5 × K_c</li>
      <li>PI: K_p = 0.45 × K_c, K_i = 1.2 × K_p / T_c</li>
      <li>PID: K_p = 0.6 × K_c, K_i = 2 × K_p / T_c, K_d = K_p × T_c / 8</li>
    </ul>
  </li>
</ol>

<h2 id="modern-pid-variants-and-enhancements">Modern PID Variants and Enhancements</h2>

<h3 id="anti-windup-mechanisms">Anti-Windup Mechanisms</h3>

<p><strong>Back-calculation method:</strong></p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>if (output &gt; outputMax) {
    integral = integral - (output - outputMax) / Ki;
    output = outputMax;
}
</code></pre></div></div>

<p><strong>Conditional integration:</strong></p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>if (abs(error) &lt; errorThreshold &amp;&amp; abs(output) &lt; outputMax) {
    integral += error * dt;
}
</code></pre></div></div>

<h3 id="derivative-on-measurement-dom">Derivative on Measurement (DoM)</h3>

<p>Instead of differentiating the error (which causes derivative kick), differentiate the process variable:</p>

\[u_d(t) = -K_d \frac{d}{dt}y(t)\]

<p>This eliminates derivative kick when the setpoint changes.</p>

<h3 id="setpoint-weighting">Setpoint Weighting</h3>

<p>Allows different responses to setpoint changes vs. disturbances:</p>

\[u(t) = K_p(b \cdot r(t) - y(t)) + K_i \int_0^t e(\tau) d\tau - K_d \frac{d}{dt}y(t)\]

<p>Where b is the setpoint weighting factor (typically 0.5-1.0).</p>

<h2 id="real-world-applications-and-case-studies">Real-World Applications and Case Studies</h2>

<h3 id="case-study-1-quadrotor-drone-stabilization">Case Study 1: Quadrotor Drone Stabilization</h3>

<p>Modern quadrotor drones use cascaded PID controllers:</p>

<p><strong>Attitude Control Loop (Inner):</strong></p>
<ul>
  <li><strong>Input</strong>: Desired vs. actual roll/pitch/yaw angles</li>
  <li><strong>Output</strong>: Motor thrust commands</li>
  <li><strong>Typical gains</strong>: Kp=4.0, Ki=0.1, Kd=0.8</li>
  <li><strong>Update rate</strong>: 1000 Hz</li>
</ul>

<p><strong>Position Control Loop (Outer):</strong></p>
<ul>
  <li><strong>Input</strong>: Desired vs. actual position</li>
  <li><strong>Output</strong>: Attitude commands to inner loop</li>
  <li><strong>Typical gains</strong>: Kp=1.5, Ki=0.3, Kd=1.2</li>
  <li><strong>Update rate</strong>: 100 Hz</li>
</ul>

<h3 id="case-study-2-industrial-temperature-control">Case Study 2: Industrial Temperature Control</h3>

<p>A pharmaceutical reactor requires ±0.1°C temperature control:</p>

<p><strong>System characteristics:</strong></p>
<ul>
  <li><strong>Time constant</strong>: 45 seconds (thermal mass)</li>
  <li><strong>Dead time</strong>: 8 seconds (sensor delay)</li>
  <li><strong>Disturbances</strong>: Ambient temperature, cooling water temperature</li>
</ul>

<p><strong>PID Configuration:</strong></p>
<ul>
  <li><strong>Kp</strong>: 2.5 (moderate for stability)</li>
  <li><strong>Ki</strong>: 0.08 (slow integration to prevent overshoot)</li>
  <li><strong>Kd</strong>: 15.0 (high derivative to counteract thermal lag)</li>
  <li><strong>Sample time</strong>: 1 second</li>
</ul>

<h3 id="case-study-3-spacecraft-attitude-control">Case Study 3: Spacecraft Attitude Control</h3>

<p>The International Space Station uses PID control for attitude maintenance:</p>

<p><strong>Challenges:</strong></p>
<ul>
  <li><strong>Microgravity environment</strong>: No gravitational restoring torque</li>
  <li><strong>Flexible structure</strong>: Solar panels and modules create structural resonances</li>
  <li><strong>Fuel conservation</strong>: Minimize thruster usage</li>
</ul>

<p><strong>Solution</strong>: Multi-mode PID with gain scheduling</p>
<ul>
  <li><strong>Fine pointing mode</strong>: High precision for experiments</li>
  <li><strong>Coarse pointing mode</strong>: Fuel-efficient for normal operations</li>
  <li><strong>Maneuver mode</strong>: Fast response for orbit adjustments</li>
</ul>

<h2 id="advanced-topics">Advanced Topics</h2>

<h3 id="gain-scheduling">Gain Scheduling</h3>

<p>PID gains can be adjusted based on operating conditions:</p>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">function</span> <span class="nf">updateGains</span><span class="p">(</span><span class="nx">operatingPoint</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">if </span><span class="p">(</span><span class="nx">operatingPoint</span><span class="p">.</span><span class="nx">velocity</span> <span class="o">&lt;</span> <span class="mf">0.1</span><span class="p">)</span> <span class="p">{</span>
        <span class="c1">// Fine control near target</span>
        <span class="nx">pid</span><span class="p">.</span><span class="nf">setGains</span><span class="p">(</span><span class="mf">1.5</span><span class="p">,</span> <span class="mf">0.2</span><span class="p">,</span> <span class="mf">0.8</span><span class="p">);</span>
    <span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
        <span class="c1">// Aggressive control far from target</span>
        <span class="nx">pid</span><span class="p">.</span><span class="nf">setGains</span><span class="p">(</span><span class="mf">3.0</span><span class="p">,</span> <span class="mf">0.1</span><span class="p">,</span> <span class="mf">0.3</span><span class="p">);</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="fuzzy-pid-control">Fuzzy PID Control</h3>

<p>Combines fuzzy logic with PID control for non-linear systems:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>IF error is Large AND error_rate is Positive 
THEN Kp is High AND Ki is Low AND Kd is Medium
</code></pre></div></div>

<h3 id="model-predictive-control-mpc">Model Predictive Control (MPC)</h3>

<p>The next evolution beyond PID, MPC optimizes control actions over a prediction horizon:</p>

\[\min_{u} \sum_{k=0}^{N-1} ||y(k+1) - r(k+1)||^2 + \lambda ||u(k)||^2\]

<p>Subject to constraints on inputs and outputs.</p>

<h2 id="implementation-best-practices">Implementation Best Practices</h2>

<h3 id="1-sample-time-selection">1. Sample Time Selection</h3>
<ul>
  <li><strong>Rule of thumb</strong>: Sample time should be 1/10 to 1/20 of the dominant time constant</li>
  <li><strong>Too fast</strong>: Wasted computation, noise amplification</li>
  <li><strong>Too slow</strong>: Poor performance, potential instability</li>
</ul>

<h3 id="2-integral-term-management">2. Integral Term Management</h3>
<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Reset integral term when switching modes</span>
<span class="k">if </span><span class="p">(</span><span class="nx">modeChanged</span><span class="p">)</span> <span class="p">{</span>
    <span class="nx">pid</span><span class="p">.</span><span class="nf">reset</span><span class="p">();</span>
<span class="p">}</span>

<span class="c1">// Limit integral accumulation</span>
<span class="kd">const</span> <span class="nx">maxIntegral</span> <span class="o">=</span> <span class="nx">outputRange</span> <span class="o">/</span> <span class="nx">Ki</span><span class="p">;</span>
<span class="nx">integral</span> <span class="o">=</span> <span class="nb">Math</span><span class="p">.</span><span class="nf">max</span><span class="p">(</span><span class="o">-</span><span class="nx">maxIntegral</span><span class="p">,</span> <span class="nb">Math</span><span class="p">.</span><span class="nf">min</span><span class="p">(</span><span class="nx">maxIntegral</span><span class="p">,</span> <span class="nx">integral</span><span class="p">));</span>
</code></pre></div></div>

<h3 id="3-derivative-term-filtering">3. Derivative Term Filtering</h3>
<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Low-pass filter for derivative term</span>
<span class="kd">const</span> <span class="nx">alpha</span> <span class="o">=</span> <span class="mf">0.1</span><span class="p">;</span> <span class="c1">// Filter coefficient</span>
<span class="nx">derivativeFiltered</span> <span class="o">=</span> <span class="nx">alpha</span> <span class="o">*</span> <span class="nx">derivative</span> <span class="o">+</span> <span class="p">(</span><span class="mi">1</span> <span class="o">-</span> <span class="nx">alpha</span><span class="p">)</span> <span class="o">*</span> <span class="nx">derivativeFiltered</span><span class="p">;</span>
</code></pre></div></div>

<h3 id="4-output-saturation-handling">4. Output Saturation Handling</h3>
<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">let</span> <span class="nx">output</span> <span class="o">=</span> <span class="nx">pTerm</span> <span class="o">+</span> <span class="nx">iTerm</span> <span class="o">+</span> <span class="nx">dTerm</span><span class="p">;</span>
<span class="k">if </span><span class="p">(</span><span class="nx">output</span> <span class="o">&gt;</span> <span class="nx">outputMax</span><span class="p">)</span> <span class="p">{</span>
    <span class="nx">output</span> <span class="o">=</span> <span class="nx">outputMax</span><span class="p">;</span>
    <span class="c1">// Back-calculate integral to prevent windup</span>
    <span class="nx">iTerm</span> <span class="o">=</span> <span class="nx">outputMax</span> <span class="o">-</span> <span class="nx">pTerm</span> <span class="o">-</span> <span class="nx">dTerm</span><span class="p">;</span>
    <span class="nx">integral</span> <span class="o">=</span> <span class="nx">iTerm</span> <span class="o">/</span> <span class="nx">Ki</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<h2 id="troubleshooting-common-issues">Troubleshooting Common Issues</h2>

<h3 id="problem-system-oscillates">Problem: System Oscillates</h3>
<p><strong>Symptoms</strong>: Continuous oscillation around setpoint
<strong>Causes</strong>:</p>
<ul>
  <li>P gain too high</li>
  <li>I gain too high</li>
  <li>Insufficient D gain
<strong>Solutions</strong>:</li>
  <li>Reduce P gain by 25%</li>
  <li>Reduce I gain by 50%</li>
  <li>Increase D gain by 25%</li>
</ul>

<h3 id="problem-slow-response">Problem: Slow Response</h3>
<p><strong>Symptoms</strong>: Takes too long to reach setpoint
<strong>Causes</strong>:</p>
<ul>
  <li>P gain too low</li>
  <li>I gain too low
<strong>Solutions</strong>:</li>
  <li>Increase P gain gradually</li>
  <li>Increase I gain moderately</li>
</ul>

<h3 id="problem-steady-state-error">Problem: Steady-State Error</h3>
<p><strong>Symptoms</strong>: System settles away from setpoint
<strong>Causes</strong>:</p>
<ul>
  <li>No integral term (I=0)</li>
  <li>Integral saturation</li>
  <li>Output saturation
<strong>Solutions</strong>:</li>
  <li>Add integral term</li>
  <li>Implement anti-windup</li>
  <li>Check actuator limits</li>
</ul>

<h3 id="problem-derivative-kick">Problem: Derivative Kick</h3>
<p><strong>Symptoms</strong>: Large output spike when setpoint changes
<strong>Causes</strong>:</p>
<ul>
  <li>Derivative acting on error instead of measurement
<strong>Solutions</strong>:</li>
  <li>Use derivative-on-measurement</li>
  <li>Add setpoint ramping</li>
</ul>

<h2 id="the-future-of-control">The Future of Control</h2>

<p>While PID control remains the workhorse of industrial automation, modern control strategies are emerging:</p>

<h3 id="machine-learning-enhanced-pid">Machine Learning Enhanced PID</h3>
<p>Neural networks can learn optimal PID gains for different operating conditions:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Simplified concept
</span><span class="n">pid_gains</span> <span class="o">=</span> <span class="n">neural_network</span><span class="p">.</span><span class="nf">predict</span><span class="p">([</span>
    <span class="n">current_error</span><span class="p">,</span>
    <span class="n">system_state</span><span class="p">,</span>
    <span class="n">disturbance_estimate</span>
<span class="p">])</span>
</code></pre></div></div>

<h3 id="adaptive-control">Adaptive Control</h3>
<p>Controllers that automatically adjust to changing system dynamics:</p>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Recursive least squares parameter estimation</span>
<span class="kd">function</span> <span class="nf">updateSystemModel</span><span class="p">(</span><span class="nx">input</span><span class="p">,</span> <span class="nx">output</span><span class="p">)</span> <span class="p">{</span>
    <span class="c1">// Update internal model of system</span>
    <span class="c1">// Adjust PID gains based on new model</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="digital-twin-integration">Digital Twin Integration</h3>
<p>Real-time system models that predict optimal control actions:</p>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">function</span> <span class="nf">predictiveControl</span><span class="p">()</span> <span class="p">{</span>
    <span class="kd">const</span> <span class="nx">futureStates</span> <span class="o">=</span> <span class="nx">digitalTwin</span><span class="p">.</span><span class="nf">simulate</span><span class="p">(</span><span class="nx">currentState</span><span class="p">,</span> <span class="nx">proposedActions</span><span class="p">);</span>
    <span class="k">return</span> <span class="nf">optimizeActions</span><span class="p">(</span><span class="nx">futureStates</span><span class="p">,</span> <span class="nx">objectives</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div>

<h2 id="conclusion">Conclusion</h2>

<p>PID control represents one of the most successful engineering solutions ever developed. Its mathematical elegance, practical effectiveness, and broad applicability have made it indispensable across countless industries. From the precision docking of spacecraft to the smooth operation of your car’s cruise control, PID controllers quietly work behind the scenes, providing the stable, predictable behavior we’ve come to expect from modern technology.</p>

<p>The beauty of PID lies not just in its mathematical foundation, but in its intuitive nature - proportional action for immediate response, integral action for long-term accuracy, and derivative action for smooth, stable behavior. These three simple concepts, when properly tuned and combined, can control everything from the temperature in your home to the attitude of satellites in orbit.</p>

<p>As we move toward an increasingly automated future, understanding PID control becomes ever more valuable. Whether you’re designing the next generation of autonomous vehicles, developing precision manufacturing equipment, or simply trying to understand how the technology around you works, PID control provides a fundamental framework for thinking about feedback, stability, and control.</p>

<p>The interactive simulation above is just the beginning. Try experimenting with different parameter combinations, observe how each component contributes to the overall behavior, and you’ll gain an intuitive understanding of one of engineering’s most powerful tools.</p>]]></content><author><name></name></author><category term="blog" /><category term="control-systems" /><category term="pid" /><category term="simulation" /><category term="spacecraft" /><summary type="html"><![CDATA[Understanding PID control through an interactive spacecraft docking simulation. Tune P, I, and D parameters to master the art of feedback control.]]></summary></entry><entry><title type="html">Energy Drift Playground — Simple Pendulum</title><link href="https://saeed1262.github.io/blog/2024/energy-drift-playground/" rel="alternate" type="text/html" title="Energy Drift Playground — Simple Pendulum" /><published>2024-08-24T00:00:00+00:00</published><updated>2024-08-24T00:00:00+00:00</updated><id>https://saeed1262.github.io/blog/2024/energy-drift-playground</id><content type="html" xml:base="https://saeed1262.github.io/blog/2024/energy-drift-playground/"><![CDATA[<h2 id="the-foundation-of-computational-physics">The Foundation of Computational Physics</h2>

<p>In the realm of computational physics, one fundamental challenge towers above all others: how do we preserve the essential properties of physical systems when translating continuous mathematics into discrete computations? This question lies at the heart of every physics simulation, from climate models predicting global warming to spacecraft navigation systems guiding interplanetary missions.</p>

<p><strong>Energy drift</strong> represents perhaps the most critical manifestation of numerical error in conservative systems. When simulating a frictionless pendulum, total mechanical energy must remain constant for all time—this is not merely a numerical preference, but a fundamental law of physics. Yet many integration schemes systematically violate this conservation, leading to completely unphysical behavior that can invalidate years of computational work.</p>

<p>What if we could visualize this phenomenon in real-time? What if we could <em>feel</em> how different mathematical approaches either preserve or destroy the fundamental structure of physical reality?</p>

<h2 id="the-mathematical-foundation-of-conservative-systems">The Mathematical Foundation of Conservative Systems</h2>

<h3 id="hamiltonian-mechanics-and-phase-space">Hamiltonian Mechanics and Phase Space</h3>

<p>A pendulum with length \(L\) and mass \(m\) under gravity \(g\) is governed by the Hamiltonian:</p>

\[H(q, p) = \frac{p^2}{2mL^2} + mgL(1 - \cos q)\]

<p>where \(q = \theta\) is the angular position and \(p = mL^2\dot{\theta}\) is the canonical momentum.</p>

<p>Hamilton’s equations provide the time evolution:</p>

\[\frac{dq}{dt} = \frac{\partial H}{\partial p} = \frac{p}{mL^2}\]

\[\frac{dp}{dt} = -\frac{\partial H}{\partial q} = -mgL\sin q\]

<p>This formulation reveals the <strong>symplectic structure</strong> underlying all Hamiltonian systems—a geometric property that governs how phase space volumes evolve over time.</p>

<h3 id="liouvilles-theorem-and-phase-space-conservation">Liouville’s Theorem and Phase Space Conservation</h3>

<p>Liouville’s theorem states that Hamiltonian flow preserves phase space volume:</p>

\[\frac{d}{dt}\int_{\Omega(t)} dq \, dp = 0\]

<p>This fundamental result has profound implications for numerical integration: <strong>any integration scheme that preserves phase space volume will maintain bounded energy errors over arbitrarily long time scales</strong>.</p>

<h3 id="the-symplectic-condition">The Symplectic Condition</h3>

<p>A transformation \((q_n, p_n) \rightarrow (q_{n+1}, p_{n+1})\) is symplectic if it preserves the symplectic form:</p>

\[dp_{n+1} \wedge dq_{n+1} = dp_n \wedge dq_n\]

<p>Equivalently, if we write the transformation as \(\mathbf{z}_{n+1} = \mathbf{M}\mathbf{z}_n\) where \(\mathbf{z} = (q, p)^T\), then \(\mathbf{M}\) must satisfy:</p>

\[\mathbf{M}^T \mathbf{J} \mathbf{M} = \mathbf{J}\]

<p>where \(\mathbf{J} = \begin{pmatrix} 0 &amp; 1 \\ -1 &amp; 0 \end{pmatrix}\) is the symplectic matrix.</p>

<h2 id="visualizing-phase-space-the-geometric-perspective">Visualizing Phase Space: The Geometric Perspective</h2>

<p>Before exploring individual integration methods, we must understand how they behave in <strong>phase space</strong>—the natural coordinate system for Hamiltonian dynamics.</p>

<h3 id="phase-space-trajectories-and-conservation">Phase Space Trajectories and Conservation</h3>

<p>In phase space, each point represents a complete state $(\theta, \omega)$ of the pendulum. As time evolves, the system traces out a trajectory in this space. For the conservative pendulum, these trajectories have remarkable properties:</p>

<p><strong>Energy Conservation as Geometric Constraint:</strong>
Each trajectory lies on a curve of constant energy:
\(H(\theta, \omega) = \frac{1}{2}\omega^2 + \frac{g}{L}(1 - \cos\theta) = E_0\)</p>

<p><strong>Three Regimes of Motion:</strong></p>
<ul>
  <li><strong>Small oscillations</strong> (\(E_0 &lt; 2g/L\)): Closed elliptical orbits around \((\theta, \omega) = (0, 0)\)</li>
  <li><strong>Large oscillations</strong> (\(E_0 &gt; 2g/L\)): Open trajectories representing continuous rotation</li>
  <li><strong>Separatrix</strong> (\(E_0 = 2g/L\)): The critical boundary between oscillation and rotation</li>
</ul>

<div id="phase-space-theory" style="max-width: 100%; margin: 30px 0;">
    <div style="background-color: var(--global-card-bg-color); border-radius: 12px; padding: 25px; color: var(--global-text-color); border: 1px solid var(--global-border-color); box-shadow: var(--global-box-shadow-lg);">
        <h4 style="margin: 0 0 20px 0; color: var(--global-theme-color);">Interactive Phase Space Explorer</h4>
        
        <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px; margin-bottom: 20px;">
            <div>
                <h5 style="color: var(--global-heading-color);">Theoretical Phase Portrait</h5>
                <canvas id="theory-phase-canvas" width="350" height="350" style="background-color: var(--global-bg-color-secondary); border-radius: 8px; width: 100%; border: 1px solid var(--global-border-color);"></canvas>
                
                <div style="margin-top: 10px; font-size: 12px;">
                    <div style="color: var(--global-text-color);">Click on the phase space to set initial conditions!</div>
                    <div style="color: #3b82f6;">Current Energy: <span id="theory-energy">2.45</span> J</div>
                </div>
            </div>
            
            <div>
                <h5 style="color: var(--global-heading-color);">Method Comparison</h5>
                <canvas id="comparison-phase-canvas" width="350" height="350" style="background-color: var(--global-bg-color-secondary); border-radius: 8px; width: 100%; border: 1px solid var(--global-border-color);"></canvas>
                
                <div style="margin-top: 10px;">
                    <label style="color: var(--global-text-color);">Integration Method: </label>
                    <select id="phase-method-select" style="background-color: var(--global-card-bg-color); color: var(--global-text-color); border: 1px solid var(--global-border-color); padding: 5px; border-radius: 4px;">
                        <option value="symplectic">Symplectic Euler</option>
                        <option value="euler">Explicit Euler</option>
                        <option value="verlet">Velocity Verlet</option>
                        <option value="rk4">RK4</option>
                        <option value="all">All Methods</option>
                    </select>
                </div>
            </div>
        </div>
        
        <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px; margin-bottom: 20px;">
            <div>
                <h5 style="color: var(--global-heading-color);">Simulation Parameters</h5>
                <div style="margin-bottom: 10px;">
                    <label style="color: var(--global-text-color);">Time Step: <span id="phase-timestep">5</span> ms</label>
                    <input type="range" id="phase-timestep-slider" min="1" max="20" value="5" style="width: 100%; accent-color: var(--global-theme-color);" />
                </div>
                <div style="margin-bottom: 10px;">
                    <label style="color: var(--global-text-color);">Simulation Speed: <span id="phase-speed">1</span>x</label>
                    <input type="range" id="phase-speed-slider" min="1" max="10" value="1" style="width: 100%; accent-color: var(--global-theme-color);" />
                </div>
            </div>
            
            <div>
                <h5 style="color: var(--global-heading-color);">Current State</h5>
                <div style="font-size: 12px;">
                    <div style="color: var(--global-text-color);">Time: <span id="phase-time">0.00</span>s</div>
                    <div style="color: var(--global-text-color);">θ: <span id="phase-theta">0.785</span> rad</div>
                    <div style="color: var(--global-text-color);">ω: <span id="phase-omega">0.000</span> rad/s</div>
                    <div style="color: #ef4444; margin-top: 5px;">Energy Error: <span id="phase-energy-error">0.00</span>%</div>
                </div>
            </div>
        </div>
        
        <div style="text-align: center;">
            <button id="phase-play-pause" style="background: var(--global-gradient-primary); border: none; color: white; padding: 8px 16px; border-radius: 6px; margin-right: 10px; font-weight: 600; cursor: pointer;">Pause</button>
            <button id="phase-reset" style="background: #FF6B6B; border: none; color: white; padding: 8px 16px; border-radius: 6px; margin-right: 10px; font-weight: 600; cursor: pointer;">Reset</button>
            <button id="phase-clear" style="background: #6B7280; border: none; color: white; padding: 8px 16px; border-radius: 6px; font-weight: 600; cursor: pointer;">Clear Trails</button>
        </div>
    </div>
</div>

<h3 id="the-symplectic-advantage-geometric-structure-preservation">The Symplectic Advantage: Geometric Structure Preservation</h3>

<p>The key insight is that <strong>symplectic methods preserve the geometric structure of phase space</strong>, while non-symplectic methods systematically distort it:</p>

<p><strong>Explicit Euler:</strong> Non-symplectic transformation causes trajectories to spiral outward, violating energy conservation.</p>

<p><strong>Symplectic Euler:</strong> Despite first-order accuracy, preserves the topological structure of phase space, keeping trajectories on correct energy surfaces.</p>

<p><strong>Velocity Verlet:</strong> Combines symplectic structure preservation with second-order accuracy, producing nearly perfect phase space trajectories.</p>

<p><strong>RK4:</strong> High local accuracy but lacks symplectic structure, leading to slow drift off energy surfaces.</p>

<h2 id="the-critical-challenge-long-term-stability-vs-local-accuracy">The Critical Challenge: Long-Term Stability vs. Local Accuracy</h2>

<p>The naive approach to numerical integration prioritizes <strong>local truncation error</strong>—how well each individual step approximates the true solution. However, for conservative systems, this focus can be catastrophically misguided.</p>

<p>Consider two fundamental questions:</p>
<ol>
  <li>Should we choose a method with fourth-order local accuracy that allows energy to drift systematically?</li>
  <li>Or should we prefer a first-order method that maintains perfect energy conservation structure?</li>
</ol>

<p>For short simulations, local accuracy dominates. For long-term evolution—climate modeling, orbital mechanics, molecular dynamics—geometric structure preservation becomes paramount.</p>

<h2 id="interactive-laboratory-four-integration-paradigms">Interactive Laboratory: Four Integration Paradigms</h2>

<p>The simple pendulum provides an ideal testbed because:</p>
<ul>
  <li><strong>Conservative system</strong>: Energy conservation is mathematically guaranteed</li>
  <li><strong>Nonlinear dynamics</strong>: Small errors can compound exponentially</li>
  <li><strong>Well-understood physics</strong>: We know the ground truth behavior</li>
  <li><strong>Rich phase space</strong>: Exhibits libration, circulation, and separatrix dynamics</li>
</ul>

<h3 id="method-1-explicit-euler---the-unstable-baseline">Method 1: Explicit Euler - The Unstable Baseline</h3>

<p>The <strong>Explicit Euler</strong> method represents the most straightforward approach to numerical integration:</p>

<p><strong>Algorithm:</strong>
\(\omega_{n+1} = \omega_n + h \cdot f(\theta_n, \omega_n)\)
\(\theta_{n+1} = \theta_n + h \cdot \omega_n\)</p>

<p>where \(f(\theta, \omega) = -\frac{g}{L}\sin(\theta)\) is the angular acceleration.</p>

<p><strong>Mathematical Analysis:</strong></p>
<ul>
  <li><strong>Order of accuracy</strong>: \(O(h)\)</li>
  <li><strong>Stability</strong>: Conditionally stable with severe restrictions on \(h\)</li>
  <li><strong>Energy behavior</strong>: Systematic energy growth due to the use of “stale” velocity information</li>
  <li><strong>Phase space</strong>: Non-symplectic transformation that violates Liouville’s theorem</li>
</ul>

<p><strong>The Fundamental Flaw:</strong>
Explicit Euler uses the velocity at time \(t_n\) to update position to time \(t_{n+1}\), while using the acceleration at time \(t_n\) to update velocity to time \(t_{n+1}\). This temporal mismatch systematically overestimates kinetic energy, leading to the characteristic exponential energy growth.</p>

<div id="euler-demo" style="max-width: 100%; margin: 30px 0; box-shadow: var(--global-box-shadow-lg);">
    <div style="background-color: var(--global-card-bg-color); border-radius: 12px; padding: 30px; color: var(--global-text-color); border: 1px solid var(--global-border-color);">
        <div style="display: flex; align-items: center; margin-bottom: 25px;">
            <div style="width: 4px; height: 30px; background: linear-gradient(to bottom, #ef4444, #dc2626); border-radius: 2px; margin-right: 15px;"></div>
            <h4 style="margin: 0; color: #ef4444; font-size: 1.4em; font-weight: 600;">Explicit Euler Method</h4>
            <span style="margin-left: auto; background: rgba(239, 68, 68, 0.15); color: #ef4444; padding: 4px 12px; border-radius: 20px; font-size: 0.8em; font-weight: 500;">Energy Growth</span>
        </div>
        
        <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 25px; margin-bottom: 25px;">
            <div style="background-color: var(--global-bg-color-secondary); border-radius: 12px; padding: 20px; border: 1px solid var(--global-border-color);">
                <h5 style="margin: 0 0 15px 0; color: var(--global-heading-color); font-size: 1.1em;">Pendulum Animation</h5>
                <div style="position: relative; border-radius: 10px; overflow: hidden; box-shadow: var(--global-box-shadow-sm);">
                    <canvas id="euler-canvas" width="280" height="280" style="background-color: var(--global-bg-color-secondary); width: 100%; display: block;"></canvas>
                </div>
            </div>
            
            <div style="background-color: var(--global-bg-color-secondary); border-radius: 12px; padding: 20px; border: 1px solid var(--global-border-color);">
                <h5 style="margin: 0 0 15px 0; color: var(--global-heading-color); font-size: 1.1em;">Energy Over Time</h5>
                <div style="position: relative; border-radius: 10px; overflow: hidden; box-shadow: var(--global-box-shadow-sm);">
                    <canvas id="euler-energy" width="280" height="280" style="background-color: var(--global-bg-color-secondary); width: 100%; display: block;"></canvas>
                </div>
            </div>
        </div>
        
        <div style="display: flex; align-items: center; justify-content: space-between; background-color: var(--global-bg-color-secondary); border-radius: 10px; padding: 15px; border: 1px solid var(--global-border-color);">
            <div style="display: flex; align-items: center; gap: 15px;">
                <label style="color: var(--global-text-color); font-weight: 500;">Time Step: <span id="euler-timestep" style="color: #ef4444; font-weight: 600;">10</span> ms</label>
                <input type="range" id="euler-slider" min="1" max="50" value="10" style="width: 200px; accent-color: #ef4444;" />
            </div>
            <div style="display: flex; gap: 10px;">
                <button id="euler-play-pause" style="background: linear-gradient(135deg, #10b981, #059669); border: none; color: white; padding: 8px 16px; border-radius: 8px; font-weight: 600; cursor: pointer; transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1); box-shadow: var(--global-box-shadow-md);">Pause</button>
                <button id="euler-reset" style="background: linear-gradient(135deg, #ef4444, #dc2626); border: none; color: white; padding: 8px 20px; border-radius: 8px; font-weight: 600; cursor: pointer; transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1); box-shadow: var(--global-box-shadow-md);">Reset</button>
            </div>
        </div>
        
        <div style="margin-top: 15px; text-align: center; font-size: 14px;">
            Energy Error: <span id="euler-error" style="background: rgba(239, 68, 68, 0.2); color: #ef4444; padding: 4px 12px; border-radius: 20px; font-weight: 600;">0.00%</span>
        </div>
    </div>
</div>

<h3 id="method-2-symplectic-euler---the-geometric-insight">Method 2: Symplectic Euler - The Geometric Insight</h3>

<p><strong>Symplectic Euler</strong> makes a subtle but revolutionary change: update momentum first, then use the updated momentum to advance position.</p>

<p><strong>Algorithm:</strong>
\(p_{n+1} = p_n + h \cdot F(q_n)\)
\(q_{n+1} = q_n + h \cdot \frac{p_{n+1}}{m}\)</p>

<p>For the pendulum: \(\omega_{n+1} = \omega_n + h \cdot \left(-\frac{g}{L}\sin(\theta_n)\right)\), then \(\theta_{n+1} = \theta_n + h \cdot \omega_{n+1}\)</p>

<p><strong>Symplectic Structure Preservation:</strong>
For the linearized harmonic oscillator, the transformation matrix for symplectic Euler is:
\(\mathbf{M} = \begin{pmatrix} 1 - h^2 \omega_0^2 &amp; h \\ - h \omega_0^2 &amp; 1 \end{pmatrix}\)</p>

<p>One can verify that \(\det(\mathbf{M}) = 1\), ensuring the method is symplectic (area-preserving in phase space).</p>

<p><strong>Key Properties:</strong></p>
<ul>
  <li><strong>Order of accuracy</strong>: Still \(O(h)\) locally</li>
  <li><strong>Global behavior</strong>: Energy oscillates with bounded amplitude</li>
  <li><strong>Reversibility</strong>: Time-reversible integration</li>
  <li><strong>Long-term stability</strong>: No secular drift in energy</li>
</ul>

<p><strong>The Geometric Miracle:</strong>
Despite having the same local accuracy as Explicit Euler, Symplectic Euler captures the essential geometric structure of Hamiltonian flow. This leads to fundamentally different long-term behavior—energy remains bounded rather than growing exponentially.</p>

<div id="symplectic-demo" style="max-width: 100%; margin: 30px 0; box-shadow: var(--global-box-shadow-lg);">
    <div style="background-color: var(--global-card-bg-color); border-radius: 12px; padding: 30px; color: var(--global-text-color); border: 1px solid var(--global-border-color);">
        <div style="display: flex; align-items: center; margin-bottom: 25px;">
            <div style="width: 4px; height: 30px; background: linear-gradient(to bottom, #22c55e, #16a34a); border-radius: 2px; margin-right: 15px;"></div>
            <h4 style="margin: 0; color: #22c55e; font-size: 1.4em; font-weight: 600;">Symplectic Euler Method</h4>
            <span style="margin-left: auto; background: rgba(34, 197, 94, 0.15); color: #22c55e; padding: 4px 12px; border-radius: 20px; font-size: 0.8em; font-weight: 500;">Energy Conservation</span>
        </div>
        
        <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 25px; margin-bottom: 25px;">
            <div style="background-color: var(--global-bg-color-secondary); border-radius: 12px; padding: 20px; border: 1px solid var(--global-border-color);">
                <h5 style="margin: 0 0 15px 0; color: var(--global-heading-color); font-size: 1.1em;">Pendulum Animation</h5>
                <div style="position: relative; border-radius: 10px; overflow: hidden; box-shadow: var(--global-box-shadow-sm);">
                    <canvas id="symplectic-canvas" width="280" height="280" style="background-color: var(--global-bg-color-secondary); width: 100%; display: block;"></canvas>
                </div>
            </div>
            
            <div style="background-color: var(--global-bg-color-secondary); border-radius: 12px; padding: 20px; border: 1px solid var(--global-border-color);">
                <h5 style="margin: 0 0 15px 0; color: var(--global-heading-color); font-size: 1.1em;">Energy Over Time</h5>
                <div style="position: relative; border-radius: 10px; overflow: hidden; box-shadow: var(--global-box-shadow-sm);">
                    <canvas id="symplectic-energy" width="280" height="280" style="background-color: var(--global-bg-color-secondary); width: 100%; display: block;"></canvas>
                </div>
            </div>
        </div>
        
        <div style="display: flex; align-items: center; justify-content: space-between; background-color: var(--global-bg-color-secondary); border-radius: 10px; padding: 15px; border: 1px solid var(--global-border-color);">
            <div style="display: flex; align-items: center; gap: 15px;">
                <label style="color: var(--global-text-color); font-weight: 500;">Time Step: <span id="symplectic-timestep" style="color: #22c55e; font-weight: 600;">10</span> ms</label>
                <input type="range" id="symplectic-slider" min="1" max="50" value="10" style="width: 200px; accent-color: #22c55e;" />
            </div>
            <div style="display: flex; gap: 10px;">
                <button id="symplectic-play-pause" style="background: linear-gradient(135deg, #10b981, #059669); border: none; color: white; padding: 8px 16px; border-radius: 8px; font-weight: 600; cursor: pointer; transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1); box-shadow: var(--global-box-shadow-md);">Pause</button>
                <button id="symplectic-reset" style="background: linear-gradient(135deg, #22c55e, #16a34a); border: none; color: white; padding: 8px 20px; border-radius: 8px; font-weight: 600; cursor: pointer; transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1); box-shadow: var(--global-box-shadow-md);">Reset</button>
            </div>
        </div>
        
        <div style="margin-top: 15px; text-align: center; font-size: 14px;">
            Energy Error: <span id="symplectic-error" style="background: rgba(34, 197, 94, 0.2); color: #22c55e; padding: 4px 12px; border-radius: 20px; font-weight: 600;">0.00%</span>
        </div>
    </div>
</div>

<h3 id="method-3-velocity-verlet---the-optimal-balance">Method 3: Velocity Verlet - The Optimal Balance</h3>

<p><strong>Velocity Verlet</strong> (also known as Leapfrog) combines symplectic structure with higher-order accuracy through a three-stage process:</p>

<p><strong>Algorithm:</strong>
\(v_{n+1/2} = v_n + \frac{h}{2} a(x_n)\)
\(x_{n+1} = x_n + h \cdot v_{n+1/2}\)
\(v_{n+1} = v_{n+1/2} + \frac{h}{2} a(x_{n+1})\)</p>

<p>For the pendulum:
\(\omega_{n+1/2} = \omega_n + \frac{h}{2} \left(-\frac{g}{L}\sin(\theta_n)\right)\)
\(\theta_{n+1} = \theta_n + h \cdot \omega_{n+1/2}\)
\(\omega_{n+1} = \omega_{n+1/2} + \frac{h}{2} \left(-\frac{g}{L}\sin(\theta_{n+1})\right)\)</p>

<p><strong>Superior Properties:</strong></p>
<ul>
  <li><strong>Order of accuracy</strong>: \(O(h^2)\) - second-order accurate</li>
  <li><strong>Symplectic</strong>: Preserves phase space structure exactly</li>
  <li><strong>Time-reversible</strong>: \(\mathbf{M}^{-1} = \mathbf{M}^T\)</li>
  <li><strong>Stability</strong>: Excellent long-term energy conservation</li>
</ul>

<p><strong>Computational Cost Analysis:</strong></p>
<ul>
  <li><strong>Function evaluations</strong>: 2 per timestep (vs. 1 for Euler methods)</li>
  <li><strong>Memory overhead</strong>: Minimal (stores half-step velocity)</li>
  <li><strong>Efficiency</strong>: Optimal balance of accuracy and computational cost</li>
</ul>

<p><strong>Why Verlet Dominates Molecular Dynamics:</strong>
The method’s symplectic nature combined with second-order accuracy makes it the gold standard for molecular dynamics simulations, where energy conservation over millions of timesteps is crucial.</p>

<div id="verlet-demo" style="max-width: 100%; margin: 30px 0; box-shadow: var(--global-box-shadow-lg);">
    <div style="background-color: var(--global-card-bg-color); border-radius: 12px; padding: 30px; color: var(--global-text-color); border: 1px solid var(--global-border-color);">
        <div style="display: flex; align-items: center; margin-bottom: 25px;">
            <div style="width: 4px; height: 30px; background: linear-gradient(to bottom, #3b82f6, #2563eb); border-radius: 2px; margin-right: 15px;"></div>
            <h4 style="margin: 0; color: #3b82f6; font-size: 1.4em; font-weight: 600;">Velocity Verlet Method</h4>
            <span style="margin-left: auto; background: rgba(59, 130, 246, 0.15); color: #3b82f6; padding: 4px 12px; border-radius: 20px; font-size: 0.8em; font-weight: 500;">Superior Accuracy</span>
        </div>
        
        <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 25px; margin-bottom: 25px;">
            <div style="background-color: var(--global-bg-color-secondary); border-radius: 12px; padding: 20px; border: 1px solid var(--global-border-color);">
                <h5 style="margin: 0 0 15px 0; color: var(--global-heading-color); font-size: 1.1em;">Pendulum Animation</h5>
                <div style="position: relative; border-radius: 10px; overflow: hidden; box-shadow: var(--global-box-shadow-sm);">
                    <canvas id="verlet-canvas" width="280" height="280" style="background-color: var(--global-bg-color-secondary); width: 100%; display: block;"></canvas>
                </div>
            </div>
            
            <div style="background-color: var(--global-bg-color-secondary); border-radius: 12px; padding: 20px; border: 1px solid var(--global-border-color);">
                <h5 style="margin: 0 0 15px 0; color: var(--global-heading-color); font-size: 1.1em;">Energy Over Time</h5>
                <div style="position: relative; border-radius: 10px; overflow: hidden; box-shadow: var(--global-box-shadow-sm);">
                    <canvas id="verlet-energy" width="280" height="280" style="background-color: var(--global-bg-color-secondary); width: 100%; display: block;"></canvas>
                </div>
            </div>
        </div>
        
        <div style="display: flex; align-items: center; justify-content: space-between; background-color: var(--global-bg-color-secondary); border-radius: 10px; padding: 15px; border: 1px solid var(--global-border-color);">
            <div style="display: flex; align-items: center; gap: 15px;">
                <label style="color: var(--global-text-color); font-weight: 500;">Time Step: <span id="verlet-timestep" style="color: #3b82f6; font-weight: 600;">10</span> ms</label>
                <input type="range" id="verlet-slider" min="1" max="50" value="10" style="width: 200px; accent-color: #3b82f6;" />
            </div>
            <div style="display: flex; gap: 10px;">
                <button id="verlet-play-pause" style="background: linear-gradient(135deg, #10b981, #059669); border: none; color: white; padding: 8px 16px; border-radius: 8px; font-weight: 600; cursor: pointer; transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1); box-shadow: var(--global-box-shadow-md);">Pause</button>
                <button id="verlet-reset" style="background: linear-gradient(135deg, #3b82f6, #2563eb); border: none; color: white; padding: 8px 20px; border-radius: 8px; font-weight: 600; cursor: pointer; transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1); box-shadow: var(--global-box-shadow-md);">Reset</button>
            </div>
        </div>
        
        <div style="margin-top: 15px; text-align: center; font-size: 14px;">
            Energy Error: <span id="verlet-error" style="background: rgba(59, 130, 246, 0.2); color: #3b82f6; padding: 4px 12px; border-radius: 20px; font-weight: 600;">0.00%</span>
        </div>
    </div>
</div>

<h3 id="method-4-runge-kutta-4th-order---the-accuracy-champion">Method 4: Runge-Kutta 4th Order - The Accuracy Champion</h3>

<p><strong>RK4</strong> achieves exceptional local accuracy through four intermediate evaluations per timestep:</p>

<p><strong>Algorithm:</strong>
\(k_1 = f(t_n, y_n)\)
\(k_2 = f(t_n + h/2, y_n + hk_1/2)\)
\(k_3 = f(t_n + h/2, y_n + hk_2/2)\)
\(k_4 = f(t_n + h, y_n + hk_3)\)
\(y_{n+1} = y_n + \frac{h}{6}(k_1 + 2k_2 + 2k_3 + k_4)\)</p>

<p><strong>Theoretical Foundation:</strong>
RK4 achieves fourth-order accuracy by matching the first four terms of the Taylor series expansion. The method can be derived through the theory of trees (Butcher trees) which provide a systematic approach to constructing high-order Runge-Kutta methods.</p>

<p><strong>The Symplectic Trade-off:</strong>
While RK4 excels in local accuracy, it is <strong>not symplectic</strong>. For dissipative systems (with damping), this is often acceptable or even desirable. For conservative systems, the lack of geometric structure preservation can lead to subtle but systematic energy drift over long time scales.</p>

<p><strong>Performance Characteristics:</strong></p>
<ul>
  <li><strong>Order of accuracy</strong>: \(O(h^4)\) - fourth-order accurate</li>
  <li><strong>Function evaluations</strong>: 4 per timestep</li>
  <li><strong>Memory overhead</strong>: Minimal temporary storage</li>
  <li><strong>Computational cost</strong>: 4× that of simple methods per timestep</li>
</ul>

<div id="rk4-demo" style="max-width: 100%; margin: 30px 0; box-shadow: var(--global-box-shadow-lg);">
    <div style="background-color: var(--global-card-bg-color); border-radius: 12px; padding: 30px; color: var(--global-text-color); border: 1px solid var(--global-border-color);">
        <div style="display: flex; align-items: center; margin-bottom: 25px;">
            <div style="width: 4px; height: 30px; background: linear-gradient(to bottom, #a855f7, #9333ea); border-radius: 2px; margin-right: 15px;"></div>
            <h4 style="margin: 0; color: #a855f7; font-size: 1.4em; font-weight: 600;">Runge-Kutta 4th Order</h4>
            <span style="margin-left: auto; background: rgba(168, 85, 247, 0.15); color: #a855f7; padding: 4px 12px; border-radius: 20px; font-size: 0.8em; font-weight: 500;">High Accuracy</span>
        </div>
        
        <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 25px; margin-bottom: 25px;">
            <div style="background-color: var(--global-bg-color-secondary); border-radius: 12px; padding: 20px; border: 1px solid var(--global-border-color);">
                <h5 style="margin: 0 0 15px 0; color: var(--global-heading-color); font-size: 1.1em;">Pendulum Animation</h5>
                <div style="position: relative; border-radius: 10px; overflow: hidden; box-shadow: var(--global-box-shadow-sm);">
                    <canvas id="rk4-canvas" width="280" height="280" style="background-color: var(--global-bg-color-secondary); width: 100%; display: block;"></canvas>
                </div>
            </div>
            
            <div style="background-color: var(--global-bg-color-secondary); border-radius: 12px; padding: 20px; border: 1px solid var(--global-border-color);">
                <h5 style="margin: 0 0 15px 0; color: var(--global-heading-color); font-size: 1.1em;">Energy Over Time</h5>
                <div style="position: relative; border-radius: 10px; overflow: hidden; box-shadow: var(--global-box-shadow-sm);">
                    <canvas id="rk4-energy" width="280" height="280" style="background-color: var(--global-bg-color-secondary); width: 100%; display: block;"></canvas>
                </div>
            </div>
        </div>
        
        <div style="display: flex; align-items: center; justify-content: space-between; background-color: var(--global-bg-color-secondary); border-radius: 10px; padding: 15px; border: 1px solid var(--global-border-color);">
            <div style="display: flex; align-items: center; gap: 15px;">
                <label style="color: var(--global-text-color); font-weight: 500;">Time Step: <span id="rk4-timestep" style="color: #a855f7; font-weight: 600;">10</span> ms</label>
                <input type="range" id="rk4-slider" min="1" max="50" value="10" style="width: 200px; accent-color: #a855f7;" />
            </div>
            <div style="display: flex; gap: 10px;">
                <button id="rk4-play-pause" style="background: linear-gradient(135deg, #10b981, #059669); border: none; color: white; padding: 8px 16px; border-radius: 8px; font-weight: 600; cursor: pointer; transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1); box-shadow: var(--global-box-shadow-md);">Pause</button>
                <button id="rk4-reset" style="background: linear-gradient(135deg, #a855f7, #9333ea); border: none; color: white; padding: 8px 20px; border-radius: 8px; font-weight: 600; cursor: pointer; transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1); box-shadow: var(--global-box-shadow-md);">Reset</button>
            </div>
        </div>
        
        <div style="margin-top: 15px; text-align: center; font-size: 14px;">
            Energy Error: <span id="rk4-error" style="background: rgba(168, 85, 247, 0.2); color: #a855f7; padding: 4px 12px; border-radius: 20px; font-weight: 600;">0.00%</span>
        </div>
    </div>
</div>

<h2 id="real-time-method-comparison-side-by-side-analysis">Real-Time Method Comparison: Side-by-Side Analysis</h2>

<p>Experience all four integration methods simultaneously to see their fundamental differences in action.</p>

<div id="realtime-comparison" style="max-width: 100%; margin: 30px 0;">
    <div style="background-color: var(--global-card-bg-color); border-radius: 12px; padding: 25px; color: var(--global-text-color); border: 1px solid var(--global-border-color); box-shadow: var(--global-box-shadow-lg);">
        <div style="display: flex; align-items: center; margin-bottom: 25px;">
            <div style="width: 4px; height: 30px; background: linear-gradient(to bottom, var(--global-theme-color), var(--global-theme-color-dark)); border-radius: 2px; margin-right: 15px;"></div>
            <h4 style="margin: 0; color: var(--global-theme-color); font-size: 1.4em; font-weight: 600;">Real-Time Method Comparison</h4>
            <span style="margin-left: auto; background: rgba(59, 130, 246, 0.15); color: var(--global-theme-color); padding: 4px 12px; border-radius: 20px; font-size: 0.8em; font-weight: 500;">All Methods</span>
        </div>
        
        <!-- Pendulum Animations Grid -->
        <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 15px; margin-bottom: 20px;">
            <div style="background-color: var(--global-bg-color-secondary); border-radius: 12px; padding: 15px; border: 1px solid var(--global-border-color);">
                <h5 style="margin: 0 0 10px 0; color: #ef4444; font-size: 0.9em; text-align: center;">Explicit Euler</h5>
                <canvas id="realtime-euler-canvas" width="200" height="200" style="background-color: var(--global-bg-color-secondary); width: 100%; display: block; border-radius: 8px;"></canvas>
            </div>
            
            <div style="background-color: var(--global-bg-color-secondary); border-radius: 12px; padding: 15px; border: 1px solid var(--global-border-color);">
                <h5 style="margin: 0 0 10px 0; color: #22c55e; font-size: 0.9em; text-align: center;">Symplectic Euler</h5>
                <canvas id="realtime-symplectic-canvas" width="200" height="200" style="background-color: var(--global-bg-color-secondary); width: 100%; display: block; border-radius: 8px;"></canvas>
            </div>
            
            <div style="background-color: var(--global-bg-color-secondary); border-radius: 12px; padding: 15px; border: 1px solid var(--global-border-color);">
                <h5 style="margin: 0 0 10px 0; color: #3b82f6; font-size: 0.9em; text-align: center;">Velocity Verlet</h5>
                <canvas id="realtime-verlet-canvas" width="200" height="200" style="background-color: var(--global-bg-color-secondary); width: 100%; display: block; border-radius: 8px;"></canvas>
            </div>
            
            <div style="background-color: var(--global-bg-color-secondary); border-radius: 12px; padding: 15px; border: 1px solid var(--global-border-color);">
                <h5 style="margin: 0 0 10px 0; color: #a855f7; font-size: 0.9em; text-align: center;">RK4</h5>
                <canvas id="realtime-rk4-canvas" width="200" height="200" style="background-color: var(--global-bg-color-secondary); width: 100%; display: block; border-radius: 8px;"></canvas>
            </div>
        </div>
        
        <!-- Energy Comparison Chart -->
        <div style="background-color: var(--global-bg-color-secondary); border-radius: 12px; padding: 20px; border: 1px solid var(--global-border-color); margin-bottom: 20px;">
            <h5 style="margin: 0 0 15px 0; color: var(--global-heading-color); font-size: 1.1em;">Energy Evolution Comparison</h5>
            <canvas id="realtime-energy-chart" width="600" height="300" style="background-color: var(--global-bg-color-secondary); width: 100%; display: block; border-radius: 8px;"></canvas>
        </div>
        
        <!-- Controls -->
        <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px; margin-bottom: 20px;">
            <div>
                <h5 style="color: var(--global-heading-color); margin-bottom: 10px;">Simulation Parameters</h5>
                <div style="margin-bottom: 10px;">
                    <label style="color: var(--global-text-color);">Initial Angle: <span id="realtime-angle">45</span>°</label>
                    <input type="range" id="realtime-angle-slider" min="10" max="170" value="45" style="width: 100%; accent-color: var(--global-theme-color);" />
                </div>
                <div style="margin-bottom: 10px;">
                    <label style="color: var(--global-text-color);">Time Step: <span id="realtime-timestep">10</span> ms</label>
                    <input type="range" id="realtime-timestep-slider" min="1" max="50" value="10" style="width: 100%; accent-color: var(--global-theme-color);" />
                </div>
            </div>
            
            <div>
                <h5 style="color: var(--global-heading-color); margin-bottom: 10px;">Current Energy Errors</h5>
                <div style="font-size: 12px; line-height: 1.4;">
                    <div style="color: var(--global-text-color);">Time: <span id="realtime-time">0.00</span>s</div>
                    <div style="color: #ef4444;">Explicit Euler: <span id="realtime-euler-error">0.00</span>%</div>
                    <div style="color: #22c55e;">Symplectic Euler: <span id="realtime-symplectic-error">0.00</span>%</div>
                    <div style="color: #3b82f6;">Velocity Verlet: <span id="realtime-verlet-error">0.00</span>%</div>
                    <div style="color: #a855f7;">RK4: <span id="realtime-rk4-error">0.00</span>%</div>
                </div>
            </div>
        </div>
        
        <!-- Action Buttons -->
        <div style="text-align: center;">
            <button id="realtime-play-pause" style="background: var(--global-gradient-primary); border: none; color: white; padding: 10px 20px; border-radius: 8px; margin-right: 10px; font-weight: 600; cursor: pointer; transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);">Pause</button>
            <button id="realtime-reset" style="background: #FF6B6B; border: none; color: white; padding: 10px 20px; border-radius: 8px; margin-right: 10px; font-weight: 600; cursor: pointer; transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);">Reset All</button>
            <button id="realtime-sync" style="background: #10B981; border: none; color: white; padding: 10px 20px; border-radius: 8px; font-weight: 600; cursor: pointer; transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);">Sync Methods</button>
        </div>
        
        <div style="margin-top: 15px; padding: 15px; background-color: rgba(59, 130, 246, 0.1); border-radius: 8px; font-size: 13px; color: var(--global-text-color);">
            <strong>💡 Watch carefully:</strong> Notice how Explicit Euler (red) spirals outward while Symplectic methods (green/blue) maintain bounded motion. RK4 (purple) shows subtle drift over longer periods.
        </div>
    </div>
</div>

<h2 id="comparative-analysis-and-stability-theory">Comparative Analysis and Stability Theory</h2>

<h3 id="computational-complexity-analysis">Computational Complexity Analysis</h3>

<table>
  <thead>
    <tr>
      <th>Method</th>
      <th>Order</th>
      <th>Function Evaluations</th>
      <th>Memory</th>
      <th>Symplectic</th>
      <th>Energy Behavior</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Explicit Euler</td>
      <td>\(O(h)\)</td>
      <td>1</td>
      <td>Minimal</td>
      <td>❌</td>
      <td>Exponential growth</td>
    </tr>
    <tr>
      <td>Symplectic Euler</td>
      <td>\(O(h)\)</td>
      <td>1</td>
      <td>Minimal</td>
      <td>✅</td>
      <td>Bounded oscillations</td>
    </tr>
    <tr>
      <td>Velocity Verlet</td>
      <td>\(O(h^2)\)</td>
      <td>2</td>
      <td>Low</td>
      <td>✅</td>
      <td>Excellent conservation</td>
    </tr>
    <tr>
      <td>RK4</td>
      <td>\(O(h^4)\)</td>
      <td>4</td>
      <td>Low</td>
      <td>❌</td>
      <td>Subtle long-term drift</td>
    </tr>
  </tbody>
</table>

<h3 id="the-fundamental-trade-off-accuracy-vs-structure">The Fundamental Trade-off: Accuracy vs. Structure</h3>

<p>For conservative Hamiltonian systems, we face a fundamental choice:</p>

<p><strong>Local Accuracy Priority</strong> (RK4, higher-order methods):</p>
<ul>
  <li>Superior accuracy for individual timesteps</li>
  <li>Excellent for short-term, high-precision calculations</li>
  <li>May exhibit energy drift over extended simulations</li>
  <li>Ideal for dissipative systems where energy conservation is not required</li>
</ul>

<p><strong>Geometric Structure Priority</strong> (Symplectic methods):</p>
<ul>
  <li>Preserve essential physical invariants</li>
  <li>Maintain bounded energy errors indefinitely</li>
  <li>Provide qualitatively correct long-term behavior</li>
  <li>Essential for conservative system simulation</li>
</ul>

<h3 id="stability-analysis-through-linear-theory">Stability Analysis Through Linear Theory</h3>

<p>For the linearized pendulum (\(\sin\theta \approx \theta\)), the equation becomes:</p>

\[\frac{d^2\theta}{dt^2} + \omega_0^2\theta = 0\]

<p>where \(\omega_0 = \sqrt{g/L}\) is the natural frequency.</p>

<p><strong>Explicit Euler Stability:</strong>
The amplification matrix eigenvalues are:
\(\lambda_{\pm} = 1 \pm i h \omega_0\)</p>

<p>Their magnitudes are:
\(|\lambda_{\pm}| = \sqrt{1 + (h\omega_0)^2} &gt; 1 \quad \text{for any } h&gt;0\)</p>

<table>
  <tbody>
    <tr>
      <td>This explains the exponential energy growth. For $$</td>
      <td>\lambda</td>
      <td>&gt; 1$$, the solution grows exponentially.</td>
    </tr>
  </tbody>
</table>

<p><strong>Symplectic Euler Stability:</strong>
The eigenvalues lie exactly on the unit circle: \(|\lambda| = 1\) for all \(h\). This guarantees bounded solutions for arbitrary timestep sizes.</p>

<h2 id="interactive-comparison-complete-behavioral-analysis">Interactive Comparison: Complete Behavioral Analysis</h2>

<div id="comparison-demo" style="max-width: 100%; margin: 20px 0;">
    <div style="background-color: var(--global-card-bg-color); border-radius: 12px; padding: 20px; color: var(--global-text-color); border: 1px solid var(--global-border-color); box-shadow: var(--global-box-shadow-lg);">
        <h4 style="margin: 0 0 20px 0; color: var(--global-theme-color);">Complete Comparison - All Integration Methods</h4>
        
        <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px; margin-bottom: 20px;">
            <div>
                <h5 style="color: var(--global-heading-color);">Selected Pendulum Animation</h5>
                <canvas id="comparison-canvas" width="300" height="300" style="background-color: var(--global-bg-color-secondary); border-radius: 8px; width: 100%; border: 1px solid var(--global-border-color);"></canvas>
                
                <div style="margin-top: 10px;">
                    <label style="color: var(--global-text-color);">Display Method: </label>
                    <select id="comparison-select" style="background-color: var(--global-card-bg-color); color: var(--global-text-color); border: 1px solid var(--global-border-color); padding: 5px; border-radius: 4px;">
                        <option value="symplectic">Symplectic Euler</option>
                        <option value="euler">Explicit Euler</option>
                        <option value="verlet">Velocity Verlet</option>
                        <option value="rk4">RK4</option>
                    </select>
                </div>
            </div>
            
            <div>
                <h5 style="color: var(--global-heading-color);">Energy Drift Comparison</h5>
                <canvas id="comparison-energy" width="300" height="300" style="background-color: var(--global-bg-color-secondary); border-radius: 8px; width: 100%; border: 1px solid var(--global-border-color);"></canvas>
                
                <div style="margin-top: 10px; display: flex; flex-wrap: wrap; gap: 10px; font-size: 11px;">
                    <div style="display: flex; align-items: center; gap: 4px;">
                        <div style="width: 12px; height: 12px; background: #ef4444; border-radius: 50%;"></div>
                        <span style="color: var(--global-text-color);">Explicit Euler</span>
                    </div>
                    <div style="display: flex; align-items: center; gap: 4px;">
                        <div style="width: 12px; height: 12px; background: #22c55e; border-radius: 50%;"></div>
                        <span style="color: var(--global-text-color);">Symplectic Euler</span>
                    </div>
                    <div style="display: flex; align-items: center; gap: 4px;">
                        <div style="width: 12px; height: 12px; background: #3b82f6; border-radius: 50%;"></div>
                        <span style="color: var(--global-text-color);">Velocity Verlet</span>
                    </div>
                    <div style="display: flex; align-items: center; gap: 4px;">
                        <div style="width: 12px; height: 12px; background: #a855f7; border-radius: 50%;"></div>
                        <span style="color: var(--global-text-color);">RK4</span>
                    </div>
                </div>
            </div>
        </div>
        
        <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px; margin-bottom: 20px;">
            <div>
                <h5 style="color: var(--global-heading-color);">Initial Conditions</h5>
                <div style="margin-bottom: 10px;">
                    <label style="color: var(--global-text-color);">Initial Angle: <span id="comparison-angle">45</span>°</label>
                    <input type="range" id="comparison-angle-slider" min="10" max="170" value="45" style="width: 100%; accent-color: var(--global-theme-color);" />
                </div>
                <div>
                    <label style="color: var(--global-text-color);">Time Step: <span id="comparison-timestep">10</span> ms</label>
                    <input type="range" id="comparison-timestep-slider" min="1" max="50" value="10" style="width: 100%; accent-color: var(--global-theme-color);" />
                </div>
            </div>
            
            <div>
                <h5 style="color: var(--global-heading-color);">Current Energy Errors</h5>
                <div style="font-size: 12px;">
                    <div style="color: var(--global-text-color);">Time: <span id="comparison-time">0.00</span>s</div>
                    <div style="color: #ef4444;">Explicit Euler: <span id="comparison-euler-error">0.00</span>%</div>
                    <div style="color: #22c55e;">Symplectic Euler: <span id="comparison-symplectic-error">0.00</span>%</div>
                    <div style="color: #3b82f6;">Velocity Verlet: <span id="comparison-verlet-error">0.00</span>%</div>
                    <div style="color: #a855f7;">RK4: <span id="comparison-rk4-error">0.00</span>%</div>
                </div>
            </div>
        </div>
        
        <div style="text-align: center;">
            <button id="comparison-play-pause" style="background: var(--global-gradient-primary); border: none; color: white; padding: 10px 20px; border-radius: 6px; margin-right: 10px; font-weight: 600; cursor: pointer; transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);">Pause</button>
            <button id="comparison-reset" style="background: #FF6B6B; border: none; color: white; padding: 10px 20px; border-radius: 6px; font-weight: 600; cursor: pointer; transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);">Reset All</button>
        </div>
    </div>
</div>

<h2 id="real-world-applications-and-case-studies">Real-World Applications and Case Studies</h2>

<h3 id="case-study-1-climate-modeling---the-ecmwf-experience">Case Study 1: Climate Modeling - The ECMWF Experience</h3>

<p>The European Centre for Medium-Range Weather Forecasts (ECMWF) faced a critical challenge: their atmospheric models were exhibiting systematic energy drift over long-term climate runs.</p>

<p><strong>The Problem:</strong></p>
<ul>
  <li><strong>Simulation duration</strong>: 100+ year climate projections</li>
  <li><strong>Energy drift</strong>: 0.1% per decade using standard methods</li>
  <li><strong>Consequence</strong>: Artificial warming trends corrupting climate predictions</li>
</ul>

<p><strong>The Solution:</strong>
Implementation of energy-conserving finite difference schemes based on symplectic principles:</p>

<div class="language-fortran highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">! Symplectic time stepping for atmospheric dynamics</span><span class="w">
</span><span class="k">do</span><span class="w"> </span><span class="n">k</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="mi">1</span><span class="p">,</span><span class="w"> </span><span class="n">nlevels</span><span class="w">
    </span><span class="c1">! Update momentum first (symplectic)</span><span class="w">
    </span><span class="n">u_new</span><span class="p">(</span><span class="n">k</span><span class="p">)</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">u_old</span><span class="p">(</span><span class="n">k</span><span class="p">)</span><span class="w"> </span><span class="o">+</span><span class="w"> </span><span class="n">dt</span><span class="w"> </span><span class="o">*</span><span class="w"> </span><span class="n">forcing_u</span><span class="p">(</span><span class="n">u_old</span><span class="p">,</span><span class="w"> </span><span class="n">v_old</span><span class="p">,</span><span class="w"> </span><span class="n">k</span><span class="p">)</span><span class="w">
    </span><span class="n">v_new</span><span class="p">(</span><span class="n">k</span><span class="p">)</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">v_old</span><span class="p">(</span><span class="n">k</span><span class="p">)</span><span class="w"> </span><span class="o">+</span><span class="w"> </span><span class="n">dt</span><span class="w"> </span><span class="o">*</span><span class="w"> </span><span class="n">forcing_v</span><span class="p">(</span><span class="n">u_old</span><span class="p">,</span><span class="w"> </span><span class="n">v_old</span><span class="p">,</span><span class="w"> </span><span class="n">k</span><span class="p">)</span><span class="w">
    
    </span><span class="c1">! Update positions using new momentum</span><span class="w">
    </span><span class="n">x</span><span class="p">(</span><span class="n">k</span><span class="p">)</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">x</span><span class="p">(</span><span class="n">k</span><span class="p">)</span><span class="w"> </span><span class="o">+</span><span class="w"> </span><span class="n">dt</span><span class="w"> </span><span class="o">*</span><span class="w"> </span><span class="n">u_new</span><span class="p">(</span><span class="n">k</span><span class="p">)</span><span class="w">
    </span><span class="n">y</span><span class="p">(</span><span class="n">k</span><span class="p">)</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">y</span><span class="p">(</span><span class="n">k</span><span class="p">)</span><span class="w"> </span><span class="o">+</span><span class="w"> </span><span class="n">dt</span><span class="w"> </span><span class="o">*</span><span class="w"> </span><span class="n">v_new</span><span class="p">(</span><span class="n">k</span><span class="p">)</span><span class="w">
</span><span class="k">end</span><span class="w"> </span><span class="k">do</span><span class="w">
</span></code></pre></div></div>

<p><strong>Results:</strong></p>
<ul>
  <li>Energy drift reduced to &lt; 0.01% per century</li>
  <li>Improved long-term climate stability</li>
  <li>More reliable temperature trend predictions</li>
</ul>

<h3 id="case-study-2-molecular-dynamics---the-gromacs-success-story">Case Study 2: Molecular Dynamics - The GROMACS Success Story</h3>

<p>GROMACS, one of the most widely used molecular dynamics packages, owes its success largely to the adoption of Velocity Verlet integration.</p>

<p><strong>System Characteristics:</strong></p>
<ul>
  <li><strong>Particle count</strong>: 10⁶ - 10⁹ atoms</li>
  <li><strong>Simulation time</strong>: nanoseconds to microseconds</li>
  <li><strong>Timestep</strong>: 1-2 femtoseconds</li>
  <li><strong>Total steps</strong>: 10⁹ - 10¹² integration steps</li>
</ul>

<p><strong>Why Symplectic Methods Are Essential:</strong></p>
<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Velocity Verlet implementation in GROMACS</span>
<span class="kt">void</span> <span class="nf">update_positions_and_velocities</span><span class="p">(</span><span class="kt">int</span> <span class="n">n</span><span class="p">,</span> <span class="n">real</span> <span class="n">dt</span><span class="p">,</span>
                                   <span class="n">rvec</span> <span class="n">x</span><span class="p">[],</span> <span class="n">rvec</span> <span class="n">v</span><span class="p">[],</span> <span class="n">rvec</span> <span class="n">f</span><span class="p">[],</span> <span class="n">real</span> <span class="n">mass</span><span class="p">[])</span> <span class="p">{</span>
    <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">n</span><span class="p">;</span> <span class="n">i</span><span class="o">++</span><span class="p">)</span> <span class="p">{</span>
        <span class="c1">// Half-step velocity update</span>
        <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">d</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">d</span> <span class="o">&lt;</span> <span class="n">DIM</span><span class="p">;</span> <span class="n">d</span><span class="o">++</span><span class="p">)</span> <span class="p">{</span>
            <span class="n">v</span><span class="p">[</span><span class="n">i</span><span class="p">][</span><span class="n">d</span><span class="p">]</span> <span class="o">+=</span> <span class="p">(</span><span class="n">dt</span> <span class="o">*</span> <span class="mf">0.5</span><span class="p">)</span> <span class="o">*</span> <span class="n">f</span><span class="p">[</span><span class="n">i</span><span class="p">][</span><span class="n">d</span><span class="p">]</span> <span class="o">/</span> <span class="n">mass</span><span class="p">[</span><span class="n">i</span><span class="p">];</span>
        <span class="p">}</span>
        
        <span class="c1">// Full-step position update</span>
        <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">d</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">d</span> <span class="o">&lt;</span> <span class="n">DIM</span><span class="p">;</span> <span class="n">d</span><span class="o">++</span><span class="p">)</span> <span class="p">{</span>
            <span class="n">x</span><span class="p">[</span><span class="n">i</span><span class="p">][</span><span class="n">d</span><span class="p">]</span> <span class="o">+=</span> <span class="n">dt</span> <span class="o">*</span> <span class="n">v</span><span class="p">[</span><span class="n">i</span><span class="p">][</span><span class="n">d</span><span class="p">];</span>
        <span class="p">}</span>
    <span class="p">}</span>
    
    <span class="c1">// Compute new forces at updated positions</span>
    <span class="n">compute_forces</span><span class="p">(</span><span class="n">x</span><span class="p">,</span> <span class="n">f</span><span class="p">);</span>
    
    <span class="c1">// Complete velocity update</span>
    <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">n</span><span class="p">;</span> <span class="n">i</span><span class="o">++</span><span class="p">)</span> <span class="p">{</span>
        <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">d</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">d</span> <span class="o">&lt;</span> <span class="n">DIM</span><span class="p">;</span> <span class="n">d</span><span class="o">++</span><span class="p">)</span> <span class="p">{</span>
            <span class="n">v</span><span class="p">[</span><span class="n">i</span><span class="p">][</span><span class="n">d</span><span class="p">]</span> <span class="o">+=</span> <span class="p">(</span><span class="n">dt</span> <span class="o">*</span> <span class="mf">0.5</span><span class="p">)</span> <span class="o">*</span> <span class="n">f</span><span class="p">[</span><span class="n">i</span><span class="p">][</span><span class="n">d</span><span class="p">]</span> <span class="o">/</span> <span class="n">mass</span><span class="p">[</span><span class="n">i</span><span class="p">];</span>
        <span class="p">}</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<p><strong>Critical Performance Metrics:</strong></p>
<ul>
  <li><strong>Energy conservation</strong>: &lt; 0.01% drift over microsecond simulations</li>
  <li><strong>Temperature stability</strong>: Canonical ensemble maintained without thermostats</li>
  <li><strong>Computational efficiency</strong>: Optimal balance of accuracy and speed</li>
</ul>

<h3 id="case-study-3-spacecraft-navigation---the-new-horizons-mission">Case Study 3: Spacecraft Navigation - The New Horizons Mission</h3>

<p>NASA’s New Horizons mission to Pluto required extraordinary precision in trajectory calculation over a 9-year journey.</p>

<p><strong>Navigation Challenges:</strong></p>
<ul>
  <li><strong>Journey duration</strong>: 9 years (2.8 × 10⁸ seconds)</li>
  <li><strong>Precision requirement</strong>: &lt; 3000 km accuracy at Pluto encounter</li>
  <li><strong>Perturbations</strong>: Gravitational effects from multiple bodies, solar radiation pressure</li>
</ul>

<p><strong>Integration Strategy:</strong>
NASA used a combination of symplectic methods for long-term stability with adaptive high-order methods for critical maneuvers:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">spacecraft_propagator</span><span class="p">(</span><span class="n">state</span><span class="p">,</span> <span class="n">dt</span><span class="p">,</span> <span class="n">adaptive</span><span class="o">=</span><span class="bp">False</span><span class="p">):</span>
    <span class="k">if</span> <span class="n">adaptive</span> <span class="ow">or</span> <span class="n">critical_maneuver</span><span class="p">:</span>
        <span class="c1"># High-precision RK4 with adaptive timestep
</span>        <span class="k">return</span> <span class="nf">rk4_adaptive</span><span class="p">(</span><span class="n">state</span><span class="p">,</span> <span class="n">dt</span><span class="p">,</span> <span class="n">tolerance</span><span class="o">=</span><span class="mf">1e-12</span><span class="p">)</span>
    <span class="k">else</span><span class="p">:</span>
        <span class="c1"># Symplectic integration for cruise phase
</span>        <span class="k">return</span> <span class="nf">symplectic_euler</span><span class="p">(</span><span class="n">state</span><span class="p">,</span> <span class="n">dt</span><span class="p">)</span>
</code></pre></div></div>

<p><strong>Results:</strong></p>
<ul>
  <li>Pluto encounter within 3000 km of predicted position</li>
  <li>Total trajectory error &lt; 0.01% after 9-year journey</li>
  <li>Successful flyby of Arrokoth (2014 MU69) in 2019</li>
</ul>

<h2 id="advanced-topics-in-numerical-integration">Advanced Topics in Numerical Integration</h2>

<h3 id="higher-order-symplectic-methods">Higher-Order Symplectic Methods</h3>

<p>While Symplectic Euler and Velocity Verlet are first and second-order accurate respectively, higher-order symplectic methods can be constructed using <strong>composition methods</strong>.</p>

<p><strong>Forest-Ruth Algorithm (4th Order Symplectic):</strong>
\(\mathbf{S}_4 = \mathbf{S}_h^{\theta_1} \circ \mathbf{S}_h^{\theta_2} \circ \mathbf{S}_h^{\theta_3} \circ \mathbf{S}_h^{\theta_2} \circ \mathbf{S}_h^{\theta_1}\)</p>

<p>where \(\mathbf{S}_h\) is the basic symplectic map and:</p>
<ul>
  <li>
\[\theta_1 = \theta_3 = \frac{1}{2 - 2^{1/3}}\]
  </li>
  <li>
\[\theta_2 = -\frac{2^{1/3}}{2 - 2^{1/3}}\]
  </li>
</ul>

<p><strong>Trade-offs:</strong></p>
<ul>
  <li><strong>Pros</strong>: Fourth-order accuracy with exact energy conservation</li>
  <li><strong>Cons</strong>: 5× computational cost per timestep vs. Velocity Verlet</li>
</ul>

<h3 id="adaptive-time-stepping-strategies">Adaptive Time Stepping Strategies</h3>

<p>For systems with multiple timescales, adaptive time stepping can dramatically improve efficiency:</p>

<p><strong>Richardson Extrapolation:</strong></p>
<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">adaptive_symplectic_step</span><span class="p">(</span><span class="n">state</span><span class="p">,</span> <span class="n">h_initial</span><span class="p">,</span> <span class="n">tolerance</span><span class="p">):</span>
    <span class="c1"># Take one step of size h
</span>    <span class="n">state_h</span> <span class="o">=</span> <span class="nf">symplectic_step</span><span class="p">(</span><span class="n">state</span><span class="p">,</span> <span class="n">h_initial</span><span class="p">)</span>
    
    <span class="c1"># Take two steps of size h/2
</span>    <span class="n">state_temp</span> <span class="o">=</span> <span class="nf">symplectic_step</span><span class="p">(</span><span class="n">state</span><span class="p">,</span> <span class="n">h_initial</span><span class="o">/</span><span class="mi">2</span><span class="p">)</span>
    <span class="n">state_h2</span> <span class="o">=</span> <span class="nf">symplectic_step</span><span class="p">(</span><span class="n">state_temp</span><span class="p">,</span> <span class="n">h_initial</span><span class="o">/</span><span class="mi">2</span><span class="p">)</span>
    
    <span class="c1"># Estimate error
</span>    <span class="n">error</span> <span class="o">=</span> <span class="nf">norm</span><span class="p">(</span><span class="n">state_h</span> <span class="o">-</span> <span class="n">state_h2</span><span class="p">)</span>
    
    <span class="k">if</span> <span class="n">error</span> <span class="o">&lt;</span> <span class="n">tolerance</span><span class="p">:</span>
        <span class="c1"># Accept step with Richardson extrapolation
</span>        <span class="nf">return </span><span class="p">(</span><span class="mi">4</span><span class="o">*</span><span class="n">state_h2</span> <span class="o">-</span> <span class="n">state_h</span><span class="p">)</span> <span class="o">/</span> <span class="mi">3</span><span class="p">,</span> <span class="n">h_initial</span>
    <span class="k">else</span><span class="p">:</span>
        <span class="c1"># Reduce timestep and retry
</span>        <span class="k">return</span> <span class="nf">adaptive_symplectic_step</span><span class="p">(</span><span class="n">state</span><span class="p">,</span> <span class="n">h_initial</span><span class="o">/</span><span class="mi">2</span><span class="p">,</span> <span class="n">tolerance</span><span class="p">)</span>
</code></pre></div></div>

<h3 id="multiple-time-scale-methods">Multiple Time Scale Methods</h3>

<p>Systems like planetary motion (fast orbital motion + slow precession) benefit from multiple timestep methods:</p>

<p><strong>Impulse-Velocity-Impulse (IVI) Decomposition:</strong></p>
<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">multiple_timestep_integrator</span><span class="p">(</span><span class="n">state</span><span class="p">,</span> <span class="n">dt_fast</span><span class="p">,</span> <span class="n">dt_slow</span><span class="p">,</span> <span class="n">n_substeps</span><span class="p">):</span>
    <span class="c1"># Slow force impulse (half step)
</span>    <span class="n">state</span> <span class="o">=</span> <span class="nf">apply_slow_forces</span><span class="p">(</span><span class="n">state</span><span class="p">,</span> <span class="n">dt_slow</span><span class="o">/</span><span class="mi">2</span><span class="p">)</span>
    
    <span class="c1"># Fast evolution with multiple substeps
</span>    <span class="n">dt_sub</span> <span class="o">=</span> <span class="n">dt_slow</span> <span class="o">/</span> <span class="n">n_substeps</span>
    <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">n_substeps</span><span class="p">):</span>
        <span class="n">state</span> <span class="o">=</span> <span class="nf">symplectic_step_fast_forces</span><span class="p">(</span><span class="n">state</span><span class="p">,</span> <span class="n">dt_sub</span><span class="p">)</span>
    
    <span class="c1"># Slow force impulse (half step)
</span>    <span class="n">state</span> <span class="o">=</span> <span class="nf">apply_slow_forces</span><span class="p">(</span><span class="n">state</span><span class="p">,</span> <span class="n">dt_slow</span><span class="o">/</span><span class="mi">2</span><span class="p">)</span>
    
    <span class="k">return</span> <span class="n">state</span>
</code></pre></div></div>

<h2 id="implementation-best-practices">Implementation Best Practices</h2>

<h3 id="1-timestep-selection-guidelines">1. Timestep Selection Guidelines</h3>

<p><strong>Conservative Systems (Hamiltonian):</strong></p>
<ul>
  <li><strong>Symplectic methods</strong>: \(h \leq \frac{T}{20}\) where \(T\) is the shortest characteristic period</li>
  <li><strong>RK4</strong>: \(h \leq \frac{T}{100}\) for comparable accuracy to symplectic methods</li>
</ul>

<p><strong>Dissipative Systems:</strong></p>
<ul>
  <li><strong>RK4 preferred</strong>: Higher local accuracy compensates for energy dissipation</li>
  <li><strong>Adaptive methods</strong>: Essential for stiff systems with multiple timescales</li>
</ul>

<h3 id="2-energy-conservation-monitoring">2. Energy Conservation Monitoring</h3>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">EnergyMonitor</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">initial_energy</span><span class="p">,</span> <span class="n">tolerance</span><span class="o">=</span><span class="mf">1e-6</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">E0</span> <span class="o">=</span> <span class="n">initial_energy</span>
        <span class="n">self</span><span class="p">.</span><span class="n">tolerance</span> <span class="o">=</span> <span class="n">tolerance</span>
        <span class="n">self</span><span class="p">.</span><span class="n">max_error</span> <span class="o">=</span> <span class="mi">0</span>
        
    <span class="k">def</span> <span class="nf">check_energy</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">current_energy</span><span class="p">,</span> <span class="n">time</span><span class="p">):</span>
        <span class="n">relative_error</span> <span class="o">=</span> <span class="nf">abs</span><span class="p">(</span><span class="n">current_energy</span> <span class="o">-</span> <span class="n">self</span><span class="p">.</span><span class="n">E0</span><span class="p">)</span> <span class="o">/</span> <span class="nf">abs</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">E0</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">max_error</span> <span class="o">=</span> <span class="nf">max</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">max_error</span><span class="p">,</span> <span class="n">relative_error</span><span class="p">)</span>
        
        <span class="k">if</span> <span class="n">relative_error</span> <span class="o">&gt;</span> <span class="n">self</span><span class="p">.</span><span class="n">tolerance</span><span class="p">:</span>
            <span class="n">warnings</span><span class="p">.</span><span class="nf">warn</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">Energy error </span><span class="si">{</span><span class="n">relative_error</span><span class="si">:</span><span class="p">.</span><span class="mi">2</span><span class="n">e</span><span class="si">}</span><span class="s"> exceeds tolerance at t=</span><span class="si">{</span><span class="n">time</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
            
        <span class="k">return</span> <span class="n">relative_error</span>
</code></pre></div></div>

<h3 id="3-numerical-precision-considerations">3. Numerical Precision Considerations</h3>

<p><strong>Double vs. Extended Precision:</strong></p>
<ul>
  <li>Standard double precision (64-bit): ~15 decimal digits</li>
  <li>For long-term simulations: Consider extended precision (80-bit) or arbitrary precision arithmetic</li>
  <li>GPU implementations: Be aware of reduced precision in single-precision calculations</li>
</ul>

<h3 id="4-symplectic-integrator-verification">4. Symplectic Integrator Verification</h3>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">verify_symplectic_property</span><span class="p">(</span><span class="n">integrator</span><span class="p">,</span> <span class="n">state0</span><span class="p">,</span> <span class="n">dt</span><span class="p">,</span> <span class="n">n_steps</span><span class="o">=</span><span class="mi">1000</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">Verify that phase space volume is conserved</span><span class="sh">"""</span>
    <span class="c1"># Create small perturbation in phase space
</span>    <span class="n">epsilon</span> <span class="o">=</span> <span class="mf">1e-8</span>
    <span class="n">states</span> <span class="o">=</span> <span class="p">[</span>
        <span class="n">state0</span><span class="p">,</span>
        <span class="n">state0</span> <span class="o">+</span> <span class="p">[</span><span class="n">epsilon</span><span class="p">,</span> <span class="mi">0</span><span class="p">],</span>
        <span class="n">state0</span> <span class="o">+</span> <span class="p">[</span><span class="mi">0</span><span class="p">,</span> <span class="n">epsilon</span><span class="p">],</span>
        <span class="n">state0</span> <span class="o">+</span> <span class="p">[</span><span class="n">epsilon</span><span class="p">,</span> <span class="n">epsilon</span><span class="p">]</span>
    <span class="p">]</span>
    
    <span class="c1"># Evolve all perturbed states
</span>    <span class="n">final_states</span> <span class="o">=</span> <span class="p">[]</span>
    <span class="k">for</span> <span class="n">state</span> <span class="ow">in</span> <span class="n">states</span><span class="p">:</span>
        <span class="n">current</span> <span class="o">=</span> <span class="n">state</span><span class="p">.</span><span class="nf">copy</span><span class="p">()</span>
        <span class="k">for</span> <span class="n">_</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">n_steps</span><span class="p">):</span>
            <span class="n">current</span> <span class="o">=</span> <span class="nf">integrator</span><span class="p">(</span><span class="n">current</span><span class="p">,</span> <span class="n">dt</span><span class="p">)</span>
        <span class="n">final_states</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">current</span><span class="p">)</span>
    
    <span class="c1"># Compute initial and final phase space volumes
</span>    <span class="n">initial_volume</span> <span class="o">=</span> <span class="nf">compute_phase_space_volume</span><span class="p">(</span><span class="n">states</span><span class="p">)</span>
    <span class="n">final_volume</span> <span class="o">=</span> <span class="nf">compute_phase_space_volume</span><span class="p">(</span><span class="n">final_states</span><span class="p">)</span>
    
    <span class="n">volume_change</span> <span class="o">=</span> <span class="nf">abs</span><span class="p">(</span><span class="n">final_volume</span> <span class="o">-</span> <span class="n">initial_volume</span><span class="p">)</span> <span class="o">/</span> <span class="n">initial_volume</span>
    
    <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">Phase space volume change: </span><span class="si">{</span><span class="n">volume_change</span><span class="si">:</span><span class="p">.</span><span class="mi">2</span><span class="n">e</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">volume_change</span> <span class="o">&lt;</span> <span class="mf">1e-10</span>  <span class="c1"># Should be essentially zero for symplectic methods
</span></code></pre></div></div>

<h2 id="troubleshooting-common-issues">Troubleshooting Common Issues</h2>

<h3 id="problem-energy-growth-in-symplectic-methods">Problem: Energy Growth in Symplectic Methods</h3>

<p><strong>Symptoms:</strong> Energy increases systematically despite using symplectic integrator</p>

<p><strong>Potential Causes:</strong></p>
<ol>
  <li><strong>Implementation error</strong>: Check that momentum is updated before position</li>
  <li><strong>Floating-point precision</strong>: Accumulation of round-off errors over millions of steps</li>
  <li><strong>Timestep too large</strong>: Linear stability analysis assumes small timesteps</li>
</ol>

<p><strong>Solutions:</strong></p>
<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Correct symplectic Euler implementation
</span><span class="k">def</span> <span class="nf">symplectic_euler_correct</span><span class="p">(</span><span class="n">theta</span><span class="p">,</span> <span class="n">omega</span><span class="p">,</span> <span class="n">dt</span><span class="p">,</span> <span class="n">g</span><span class="p">,</span> <span class="n">L</span><span class="p">):</span>
    <span class="c1"># CORRECT: Update omega first
</span>    <span class="n">omega_new</span> <span class="o">=</span> <span class="n">omega</span> <span class="o">-</span> <span class="p">(</span><span class="n">dt</span> <span class="o">*</span> <span class="n">g</span> <span class="o">/</span> <span class="n">L</span><span class="p">)</span> <span class="o">*</span> <span class="nf">sin</span><span class="p">(</span><span class="n">theta</span><span class="p">)</span>
    <span class="n">theta_new</span> <span class="o">=</span> <span class="n">theta</span> <span class="o">+</span> <span class="n">dt</span> <span class="o">*</span> <span class="n">omega_new</span>  <span class="c1"># Use NEW omega
</span>    <span class="k">return</span> <span class="n">theta_new</span><span class="p">,</span> <span class="n">omega_new</span>

<span class="c1"># INCORRECT implementation (produces energy growth)
</span><span class="k">def</span> <span class="nf">symplectic_euler_wrong</span><span class="p">(</span><span class="n">theta</span><span class="p">,</span> <span class="n">omega</span><span class="p">,</span> <span class="n">dt</span><span class="p">,</span> <span class="n">g</span><span class="p">,</span> <span class="n">L</span><span class="p">):</span>
    <span class="c1"># WRONG: Updates done simultaneously
</span>    <span class="n">omega_new</span> <span class="o">=</span> <span class="n">omega</span> <span class="o">-</span> <span class="p">(</span><span class="n">dt</span> <span class="o">*</span> <span class="n">g</span> <span class="o">/</span> <span class="n">L</span><span class="p">)</span> <span class="o">*</span> <span class="nf">sin</span><span class="p">(</span><span class="n">theta</span><span class="p">)</span>
    <span class="n">theta_new</span> <span class="o">=</span> <span class="n">theta</span> <span class="o">+</span> <span class="n">dt</span> <span class="o">*</span> <span class="n">omega</span>  <span class="c1"># Uses OLD omega
</span>    <span class="k">return</span> <span class="n">theta_new</span><span class="p">,</span> <span class="n">omega_new</span>
</code></pre></div></div>

<h3 id="problem-stiff-system-instability">Problem: Stiff System Instability</h3>

<p><strong>Symptoms:</strong> Simulation becomes unstable for small timesteps</p>

<p><strong>Cause:</strong> Stiff differential equations require implicit methods</p>

<p><strong>Solution:</strong> Use implicit-explicit (IMEX) methods:</p>
<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">imex_euler</span><span class="p">(</span><span class="n">q</span><span class="p">,</span> <span class="n">p</span><span class="p">,</span> <span class="n">dt</span><span class="p">,</span> <span class="n">force_explicit</span><span class="p">,</span> <span class="n">force_implicit</span><span class="p">):</span>
    <span class="c1"># Explicit update for non-stiff terms
</span>    <span class="n">p_temp</span> <span class="o">=</span> <span class="n">p</span> <span class="o">+</span> <span class="n">dt</span> <span class="o">*</span> <span class="nf">force_explicit</span><span class="p">(</span><span class="n">q</span><span class="p">,</span> <span class="n">p</span><span class="p">)</span>
    
    <span class="c1"># Implicit update for stiff terms (requires solver)
</span>    <span class="n">p_new</span> <span class="o">=</span> <span class="nf">solve_implicit</span><span class="p">(</span><span class="n">p_temp</span><span class="p">,</span> <span class="n">dt</span><span class="p">,</span> <span class="n">force_implicit</span><span class="p">,</span> <span class="n">q</span><span class="p">)</span>
    <span class="n">q_new</span> <span class="o">=</span> <span class="n">q</span> <span class="o">+</span> <span class="n">dt</span> <span class="o">*</span> <span class="n">p_new</span>
    
    <span class="k">return</span> <span class="n">q_new</span><span class="p">,</span> <span class="n">p_new</span>
</code></pre></div></div>

<h3 id="problem-resonance-induced-instability">Problem: Resonance-Induced Instability</h3>

<p><strong>Symptoms:</strong> Energy oscillates with growing amplitude over time</p>

<p><strong>Cause:</strong> Timestep resonates with system’s natural frequency</p>

<p><strong>Solution:</strong> Use irrational timestep ratios or variable timestep methods:</p>
<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Golden ratio timestep to avoid resonances
</span><span class="n">dt_base</span> <span class="o">=</span> <span class="mf">0.01</span>
<span class="n">golden_ratio</span> <span class="o">=</span> <span class="p">(</span><span class="mi">1</span> <span class="o">+</span> <span class="nf">sqrt</span><span class="p">(</span><span class="mi">5</span><span class="p">))</span> <span class="o">/</span> <span class="mi">2</span>
<span class="n">dt_adaptive</span> <span class="o">=</span> <span class="n">dt_base</span> <span class="o">/</span> <span class="n">golden_ratio</span>
</code></pre></div></div>

<h2 id="future-directions-and-research-frontiers">Future Directions and Research Frontiers</h2>

<h3 id="machine-learning-enhanced-integration">Machine Learning Enhanced Integration</h3>

<p>Recent developments combine traditional numerical methods with machine learning:</p>

<p><strong>Physics-Informed Neural Networks (PINNs):</strong></p>
<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">torch</span>
<span class="kn">import</span> <span class="n">torch.nn</span> <span class="k">as</span> <span class="n">nn</span>

<span class="k">class</span> <span class="nc">HamiltonianNet</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
        <span class="nf">super</span><span class="p">().</span><span class="nf">__init__</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">net</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Sequential</span><span class="p">(</span>
            <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="mi">2</span><span class="p">,</span> <span class="mi">50</span><span class="p">),</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Tanh</span><span class="p">(),</span>
            <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="mi">50</span><span class="p">,</span> <span class="mi">50</span><span class="p">),</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Tanh</span><span class="p">(),</span>
            <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="mi">50</span><span class="p">,</span> <span class="mi">1</span><span class="p">)</span>
        <span class="p">)</span>
    
    <span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">q</span><span class="p">,</span> <span class="n">p</span><span class="p">):</span>
        <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="nf">net</span><span class="p">(</span><span class="n">torch</span><span class="p">.</span><span class="nf">cat</span><span class="p">([</span><span class="n">q</span><span class="p">,</span> <span class="n">p</span><span class="p">],</span> <span class="n">dim</span><span class="o">=</span><span class="mi">1</span><span class="p">))</span>
    
    <span class="k">def</span> <span class="nf">symplectic_gradient</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">q</span><span class="p">,</span> <span class="n">p</span><span class="p">):</span>
        <span class="n">H</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">forward</span><span class="p">(</span><span class="n">q</span><span class="p">,</span> <span class="n">p</span><span class="p">)</span>
        <span class="n">dH_dq</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">autograd</span><span class="p">.</span><span class="nf">grad</span><span class="p">(</span><span class="n">H</span><span class="p">.</span><span class="nf">sum</span><span class="p">(),</span> <span class="n">q</span><span class="p">,</span> <span class="n">create_graph</span><span class="o">=</span><span class="bp">True</span><span class="p">)[</span><span class="mi">0</span><span class="p">]</span>
        <span class="n">dH_dp</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">autograd</span><span class="p">.</span><span class="nf">grad</span><span class="p">(</span><span class="n">H</span><span class="p">.</span><span class="nf">sum</span><span class="p">(),</span> <span class="n">p</span><span class="p">,</span> <span class="n">create_graph</span><span class="o">=</span><span class="bp">True</span><span class="p">)[</span><span class="mi">0</span><span class="p">]</span>
        <span class="k">return</span> <span class="o">-</span><span class="n">dH_dq</span><span class="p">,</span> <span class="n">dH_dp</span>  <span class="c1"># Hamilton's equations
</span></code></pre></div></div>

<h3 id="quantum-classical-hybrid-systems">Quantum-Classical Hybrid Systems</h3>

<p>Emerging applications require integration of quantum and classical dynamics:</p>

<p><strong>Mean Field Theory Integration:</strong>
For systems where quantum effects influence classical motion through expectation values.</p>

<h3 id="geometric-deep-learning">Geometric Deep Learning</h3>

<p>Using the geometric structure of phase space in neural network architectures:</p>

<p><strong>Symplectic Neural Networks:</strong>
Networks constrained to preserve symplectic structure exactly while learning complex dynamics.</p>

<h2 id="conclusion-the-art-and-science-of-numerical-integration">Conclusion: The Art and Science of Numerical Integration</h2>

<p>The exploration of energy drift in the simple pendulum reveals fundamental principles that extend far beyond this seemingly elementary system. We have witnessed how the choice of integration method can mean the difference between simulation success and catastrophic failure—between climate models that accurately predict future temperatures and those that exhibit spurious warming trends.</p>

<h3 id="key-insights-from-our-journey">Key Insights from Our Journey</h3>

<ol>
  <li>
    <p><strong>Geometric Structure Trumps Local Accuracy</strong>: Symplectic methods with first-order accuracy often outperform fourth-order methods that ignore geometric structure.</p>
  </li>
  <li>
    <p><strong>Conservation Laws Are Not Optional</strong>: For conservative systems, preserving invariants like energy, momentum, and phase space volume is essential for physical realism.</p>
  </li>
  <li>
    <p><strong>The Mathematics Has Real Consequences</strong>: The abstract concepts of symplectic geometry and Hamiltonian mechanics translate directly into practical simulation reliability.</p>
  </li>
  <li>
    <p><strong>Understanding Enables Optimization</strong>: Knowledge of stability theory, convergence analysis, and computational complexity allows informed selection of methods for specific applications.</p>
  </li>
</ol>

<h3 id="the-broader-impact">The Broader Impact</h3>

<p>These principles extend throughout computational science:</p>
<ul>
  <li><strong>Climate Science</strong>: Energy-conserving atmospheric models</li>
  <li><strong>Astronomy</strong>: Long-term planetary motion calculations</li>
  <li><strong>Materials Science</strong>: Molecular dynamics simulations</li>
  <li><strong>Engineering</strong>: Structural dynamics and vibration analysis</li>
  <li><strong>Biology</strong>: Protein folding and cellular dynamics</li>
  <li><strong>Finance</strong>: Long-term economic modeling</li>
</ul>

<h3 id="looking-forward">Looking Forward</h3>

<p>As computational power continues to grow exponentially, the temptation exists to simply use smaller timesteps with traditional methods. However, this brute-force approach fails for systems requiring integration over geological timescales or astronomical distances. The future belongs to methods that respect the underlying mathematical structure of physical systems.</p>

<p>The simple pendulum, in its elegant simplicity, embodies the rich mathematical structure that governs all of physics. By understanding how to preserve this structure numerically, we gain the power to simulate reality with unprecedented fidelity and extend our understanding of complex systems across all scales of nature.</p>

<p><em>The mathematics of numerical integration isn’t merely computational technique—it’s the bridge between theoretical understanding and practical prediction, between the elegance of continuous mathematics and the reality of discrete computation.</em></p>

<hr />

<h2 id="further-reading-and-references">Further Reading and References</h2>

<h3 id="essential-textbooks">Essential Textbooks</h3>
<ul>
  <li><strong>Hairer, E., Lubich, C., &amp; Wanner, G.</strong> (2006). <em>Geometric Numerical Integration: Structure-Preserving Algorithms for Ordinary Differential Equations</em>. Springer.</li>
  <li><strong>Leimkuhler, B., &amp; Reich, S.</strong> (2004). <em>Simulating Hamiltonian Dynamics</em>. Cambridge University Press.</li>
</ul>

<h3 id="research-papers">Research Papers</h3>
<ul>
  <li><strong>Forest, E., &amp; Ruth, R. D.</strong> (1990). Fourth-order symplectic integration. <em>Physica D</em>, 43(1), 105-117.</li>
  <li><strong>Yoshida, H.</strong> (1990). Construction of higher order symplectic integrators. <em>Physics Letters A</em>, 150(5), 262-268.</li>
</ul>

<h3 id="computational-tools">Computational Tools</h3>
<ul>
  <li><strong>GROMACS</strong>: <a href="http://www.gromacs.org">gromacs.org</a> - Molecular dynamics with Velocity Verlet</li>
  <li><strong>NAMD</strong>: <a href="https://www.ks.uiuc.edu/Research/namd/">namd.org</a> - Large-scale parallel MD simulations</li>
  <li><strong>yt-project</strong>: <a href="https://yt-project.org">yt-project.org</a> - Analysis tools for astrophysical simulations</li>
</ul>

<script>
// Individual Pendulum Simulation Classes
class SinglePendulumSim {
    constructor(canvasId, energyCanvasId, integrator, color) {
        this.canvas = document.getElementById(canvasId);
        this.energyCanvas = document.getElementById(energyCanvasId);
        
        if (!this.canvas || !this.energyCanvas) {
            console.warn(`Canvas elements not found: ${canvasId}, ${energyCanvasId}`);
            return;
        }
        
        this.ctx = this.canvas.getContext('2d');
        this.energyCtx = this.energyCanvas.getContext('2d');
        
        if (!this.ctx || !this.energyCtx) {
            console.warn(`Failed to get 2D context for canvases`);
            return;
        }
        
        this.integrator = integrator;
        this.color = color;
        
        // Parameters
        this.L = 1.0;
        this.g = 9.81;
        this.h = 0.01; // 10ms default
        
        // State
        this.theta = Math.PI / 4; // 45 degrees
        this.omega = 0;
        this.time = 0;
        this.running = true;
        
        // Energy tracking
        this.energyHistory = [];
        this.initialEnergy = this.energy(this.theta, this.omega);
        this.maxSamples = 300;
        
        this.animate();
    }
    
    togglePause() {
        this.running = !this.running;
        return this.running;
    }
    
    acceleration(theta) {
        return -(this.g / this.L) * Math.sin(theta);
    }
    
    energy(theta, omega) {
        return this.g * this.L * (1 - Math.cos(theta)) + 0.5 * (this.L * this.L) * omega * omega;
    }
    
    step() {
        if (!this.running) return;
        
        switch(this.integrator) {
            case 'euler':
                const domega = this.acceleration(this.theta);
                this.theta += this.h * this.omega;
                this.omega += this.h * domega;
                break;
                
            case 'symplectic':
                this.omega += this.h * this.acceleration(this.theta);
                this.theta += this.h * this.omega;
                break;
                
            case 'verlet':
                const a0 = this.acceleration(this.theta);
                const omegaHalf = this.omega + 0.5 * this.h * a0;
                this.theta += this.h * omegaHalf;
                const a1 = this.acceleration(this.theta);
                this.omega = omegaHalf + 0.5 * this.h * a1;
                break;
                
            case 'rk4':
                const f = (theta, omega) => [omega, this.acceleration(theta)];
                const k1 = f(this.theta, this.omega);
                const k2 = f(this.theta + 0.5 * this.h * k1[0], this.omega + 0.5 * this.h * k1[1]);
                const k3 = f(this.theta + 0.5 * this.h * k2[0], this.omega + 0.5 * this.h * k2[1]);
                const k4 = f(this.theta + this.h * k3[0], this.omega + this.h * k3[1]);
                this.theta += (this.h / 6) * (k1[0] + 2*k2[0] + 2*k3[0] + k4[0]);
                this.omega += (this.h / 6) * (k1[1] + 2*k2[1] + 2*k3[1] + k4[1]);
                break;
        }
        
        this.time += this.h;
        
        // Sample energy
        if (Math.floor(this.time * 50) % 2 === 0) {
            const E = this.energy(this.theta, this.omega);
            this.energyHistory.push({ t: this.time, E: E });
            if (this.energyHistory.length > this.maxSamples) {
                this.energyHistory.shift();
            }
        }
    }
    
    draw() {
        // Draw pendulum
        const ctx = this.ctx;
        const canvas = this.canvas;
        const scale = Math.min(canvas.width, canvas.height) * 0.35;
        const centerX = canvas.width / 2;
        const centerY = canvas.height * 0.2;
        
        ctx.clearRect(0, 0, canvas.width, canvas.height);
        
        const x = centerX + scale * Math.sin(this.theta);
        const y = centerY + scale * Math.cos(this.theta);
        
        // Rod
        ctx.strokeStyle = this.color;
        ctx.lineWidth = 3;
        ctx.beginPath();
        ctx.moveTo(centerX, centerY);
        ctx.lineTo(x, y);
        ctx.stroke();
        
        // Pivot
        ctx.fillStyle = '#666';
        ctx.beginPath();
        ctx.arc(centerX, centerY, 5, 0, 2 * Math.PI);
        ctx.fill();
        
        // Bob
        ctx.fillStyle = this.color;
        ctx.beginPath();
        ctx.arc(x, y, 12, 0, 2 * Math.PI);
        ctx.fill();
        
        // Draw energy chart
        this.drawEnergyChart();
    }
    
    drawEnergyChart() {
        const ctx = this.energyCtx;
        const canvas = this.energyCanvas;
        
        ctx.clearRect(0, 0, canvas.width, canvas.height);
        
        if (this.energyHistory.length < 2) return;
        
        const padding = 30;
        const chartWidth = canvas.width - 2 * padding;
        const chartHeight = canvas.height - 2 * padding;
        
        // Find bounds
        let tMin = this.energyHistory[0].t;
        let tMax = this.energyHistory[this.energyHistory.length - 1].t;
        let eMin = Math.min(...this.energyHistory.map(p => p.E));
        let eMax = Math.max(...this.energyHistory.map(p => p.E));
        
        if (eMax === eMin) {
            eMin -= 0.01;
            eMax += 0.01;
        }
        
        const timeRange = Math.max(tMax - tMin, 1);
        const energyRange = eMax - eMin;
        
        // Draw axes
        ctx.strokeStyle = '#666';
        ctx.lineWidth = 1;
        ctx.beginPath();
        ctx.moveTo(padding, canvas.height - padding);
        ctx.lineTo(canvas.width - padding, canvas.height - padding);
        ctx.moveTo(padding, padding);
        ctx.lineTo(padding, canvas.height - padding);
        ctx.stroke();
        
        // Draw energy line
        ctx.strokeStyle = this.color;
        ctx.lineWidth = 2;
        ctx.beginPath();
        
        this.energyHistory.forEach((point, i) => {
            const x = padding + ((point.t - tMin) / timeRange) * chartWidth;
            const y = canvas.height - padding - ((point.E - eMin) / energyRange) * chartHeight;
            
            if (i === 0) {
                ctx.moveTo(x, y);
            } else {
                ctx.lineTo(x, y);
            }
        });
        
        ctx.stroke();
        
        // Draw initial energy reference line
        const refY = canvas.height - padding - ((this.initialEnergy - eMin) / energyRange) * chartHeight;
        ctx.strokeStyle = '#666';
        ctx.setLineDash([5, 5]);
        ctx.beginPath();
        ctx.moveTo(padding, refY);
        ctx.lineTo(canvas.width - padding, refY);
        ctx.stroke();
        ctx.setLineDash([]);
        
        // Labels
        ctx.fillStyle = '#aaa';
        ctx.font = '10px Arial';
        ctx.textAlign = 'center';
        ctx.fillText('Time (s)', canvas.width / 2, canvas.height - 5);
        
        ctx.save();
        ctx.translate(10, canvas.height / 2);
        ctx.rotate(-Math.PI / 2);
        ctx.fillText('Energy', 0, 0);
        ctx.restore();
    }
    
    reset() {
        this.theta = Math.PI / 4;
        this.omega = 0;
        this.time = 0;
        this.energyHistory = [];
        this.initialEnergy = this.energy(this.theta, this.omega);
    }
    
    getEnergyError() {
        const currentEnergy = this.energy(this.theta, this.omega);
        return ((currentEnergy - this.initialEnergy) / this.initialEnergy) * 100;
    }
    
    animate() {
        try {
            if (this.ctx && this.energyCtx) {
                this.step();
                this.draw();
            }
        } catch (e) {
            console.warn('Animation error:', e);
        }
        requestAnimationFrame(() => this.animate());
    }
}

// Multi-integrator comparison simulation
class ComparisonSim {
    constructor() {
        this.canvas = document.getElementById('comparison-canvas');
        this.energyCanvas = document.getElementById('comparison-energy');
        
        if (!this.canvas || !this.energyCanvas) {
            console.warn('Comparison canvas elements not found');
            return;
        }
        
        this.ctx = this.canvas.getContext('2d');
        this.energyCtx = this.energyCanvas.getContext('2d');
        
        if (!this.ctx || !this.energyCtx) {
            console.warn('Failed to get 2D context for comparison canvases');
            return;
        }
        
        // Parameters
        this.L = 1.0;
        this.g = 9.81;
        this.h = 0.01;
        this.theta0 = Math.PI / 4;
        this.selectedIntegrator = 'symplectic';
        this.running = true;
        
        // States for all integrators
        this.states = {
            euler: { theta: this.theta0, omega: 0 },
            symplectic: { theta: this.theta0, omega: 0 },
            verlet: { theta: this.theta0, omega: 0 },
            rk4: { theta: this.theta0, omega: 0 }
        };
        
        this.energyHistory = {
            euler: [],
            symplectic: [],
            verlet: [],
            rk4: []
        };
        
        this.colors = {
            euler: '#ef4444',
            symplectic: '#22c55e',
            verlet: '#3b82f6',
            rk4: '#a855f7'
        };
        
        this.time = 0;
        this.initialEnergy = this.energy(this.theta0, 0);
        this.maxSamples = 400;
        
        this.setupControls();
        this.animate();
    }
    
    setupControls() {
        // Play/Pause
        document.getElementById('comparison-play-pause').addEventListener('click', () => {
            this.running = !this.running;
            document.getElementById('comparison-play-pause').textContent = this.running ? 'Pause' : 'Play';
        });
        
        // Reset
        document.getElementById('comparison-reset').addEventListener('click', () => {
            this.reset();
        });
        
        // Integrator selection
        document.getElementById('comparison-select').addEventListener('change', (e) => {
            this.selectedIntegrator = e.target.value;
        });
        
        // Angle slider
        document.getElementById('comparison-angle-slider').addEventListener('input', (e) => {
            this.theta0 = parseFloat(e.target.value) * Math.PI / 180;
            document.getElementById('comparison-angle').textContent = e.target.value;
            this.reset();
        });
        
        // Timestep slider
        document.getElementById('comparison-timestep-slider').addEventListener('input', (e) => {
            this.h = parseFloat(e.target.value) / 1000;
            document.getElementById('comparison-timestep').textContent = e.target.value;
        });
    }
    
    acceleration(theta) {
        return -(this.g / this.L) * Math.sin(theta);
    }
    
    energy(theta, omega) {
        return this.g * this.L * (1 - Math.cos(theta)) + 0.5 * (this.L * this.L) * omega * omega;
    }
    
    step() {
        if (!this.running) return;
        
        // Update Explicit Euler
        const s1 = this.states.euler;
        const domega1 = this.acceleration(s1.theta);
        this.states.euler = {
            theta: s1.theta + this.h * s1.omega,
            omega: s1.omega + this.h * domega1
        };
        
        // Update Symplectic Euler
        const s2 = this.states.symplectic;
        const omega2 = s2.omega + this.h * this.acceleration(s2.theta);
        this.states.symplectic = {
            theta: s2.theta + this.h * omega2,
            omega: omega2
        };
        
        // Update Velocity Verlet
        const s3 = this.states.verlet;
        const a0 = this.acceleration(s3.theta);
        const omegaHalf = s3.omega + 0.5 * this.h * a0;
        const theta3 = s3.theta + this.h * omegaHalf;
        const a1 = this.acceleration(theta3);
        this.states.verlet = {
            theta: theta3,
            omega: omegaHalf + 0.5 * this.h * a1
        };
        
        // Update RK4
        const s4 = this.states.rk4;
        const f = (theta, omega) => [omega, this.acceleration(theta)];
        const k1 = f(s4.theta, s4.omega);
        const k2 = f(s4.theta + 0.5 * this.h * k1[0], s4.omega + 0.5 * this.h * k1[1]);
        const k3 = f(s4.theta + 0.5 * this.h * k2[0], s4.omega + 0.5 * this.h * k2[1]);
        const k4 = f(s4.theta + this.h * k3[0], s4.omega + this.h * k3[1]);
        this.states.rk4 = {
            theta: s4.theta + (this.h / 6) * (k1[0] + 2*k2[0] + 2*k3[0] + k4[0]),
            omega: s4.omega + (this.h / 6) * (k1[1] + 2*k2[1] + 2*k3[1] + k4[1])
        };
        
        this.time += this.h;
        
        // Sample energy
        if (Math.floor(this.time * 50) % 3 === 0) {
            Object.keys(this.states).forEach(key => {
                const E = this.energy(this.states[key].theta, this.states[key].omega);
                this.energyHistory[key].push({ t: this.time, E: E });
                if (this.energyHistory[key].length > this.maxSamples) {
                    this.energyHistory[key].shift();
                }
            });
        }
        
        this.updateStatus();
    }
    
    updateStatus() {
        document.getElementById('comparison-time').textContent = this.time.toFixed(2);
        
        Object.keys(this.states).forEach(key => {
            const currentEnergy = this.energy(this.states[key].theta, this.states[key].omega);
            const relativeError = ((currentEnergy - this.initialEnergy) / this.initialEnergy) * 100;
            document.getElementById(`comparison-${key}-error`).textContent = relativeError.toFixed(2);
        });
    }
    
    draw() {
        this.drawPendulum();
        this.drawEnergyChart();
    }
    
    drawPendulum() {
        const ctx = this.ctx;
        const canvas = this.canvas;
        const scale = Math.min(canvas.width, canvas.height) * 0.35;
        const centerX = canvas.width / 2;
        const centerY = canvas.height * 0.2;
        
        ctx.clearRect(0, 0, canvas.width, canvas.height);
        
        const state = this.states[this.selectedIntegrator];
        const x = centerX + scale * Math.sin(state.theta);
        const y = centerY + scale * Math.cos(state.theta);
        
        // Rod
        ctx.strokeStyle = this.colors[this.selectedIntegrator];
        ctx.lineWidth = 3;
        ctx.beginPath();
        ctx.moveTo(centerX, centerY);
        ctx.lineTo(x, y);
        ctx.stroke();
        
        // Pivot
        ctx.fillStyle = '#666';
        ctx.beginPath();
        ctx.arc(centerX, centerY, 5, 0, 2 * Math.PI);
        ctx.fill();
        
        // Bob
        ctx.fillStyle = this.colors[this.selectedIntegrator];
        ctx.beginPath();
        ctx.arc(x, y, 15, 0, 2 * Math.PI);
        ctx.fill();
        
        // Method name
        ctx.fillStyle = 'white';
        ctx.font = '14px Arial';
        ctx.textAlign = 'center';
        const names = {
            euler: 'Explicit Euler',
            symplectic: 'Symplectic Euler',
            verlet: 'Velocity Verlet',
            rk4: 'RK4'
        };
        ctx.fillText(names[this.selectedIntegrator], centerX, canvas.height - 20);
    }
    
    drawEnergyChart() {
        const ctx = this.energyCtx;
        const canvas = this.energyCanvas;
        
        ctx.clearRect(0, 0, canvas.width, canvas.height);
        
        const padding = 35;
        const chartWidth = canvas.width - 2 * padding;
        const chartHeight = canvas.height - 2 * padding;
        
        // Find data bounds
        let tMin = Infinity, tMax = -Infinity, eMin = Infinity, eMax = -Infinity;
        
        Object.keys(this.energyHistory).forEach(key => {
            const history = this.energyHistory[key];
            if (history.length === 0) return;
            
            history.forEach(point => {
                tMin = Math.min(tMin, point.t);
                tMax = Math.max(tMax, point.t);
                eMin = Math.min(eMin, point.E);
                eMax = Math.max(eMax, point.E);
            });
        });
        
        if (!isFinite(tMin)) return;
        
        const timeRange = Math.max(tMax - tMin, 1);
        const energyRange = Math.max(eMax - eMin, 0.001);
        
        // Draw axes
        ctx.strokeStyle = '#666';
        ctx.lineWidth = 1;
        ctx.beginPath();
        ctx.moveTo(padding, padding);
        ctx.lineTo(padding, canvas.height - padding);
        ctx.lineTo(canvas.width - padding, canvas.height - padding);
        ctx.stroke();
        
        // Draw energy lines
        Object.keys(this.energyHistory).forEach(key => {
            const history = this.energyHistory[key];
            if (history.length < 2) return;
            
            ctx.strokeStyle = this.colors[key];
            ctx.lineWidth = 2;
            ctx.beginPath();
            
            let first = true;
            history.forEach(point => {
                const x = padding + ((point.t - tMin) / timeRange) * chartWidth;
                const y = canvas.height - padding - ((point.E - eMin) / energyRange) * chartHeight;
                
                if (first) {
                    ctx.moveTo(x, y);
                    first = false;
                } else {
                    ctx.lineTo(x, y);
                }
            });
            
            ctx.stroke();
        });
        
        // Labels
        ctx.fillStyle = '#aaa';
        ctx.font = '10px Arial';
        ctx.textAlign = 'center';
        ctx.fillText('Time (s)', canvas.width / 2, canvas.height - 5);
        
        ctx.save();
        ctx.translate(12, canvas.height / 2);
        ctx.rotate(-Math.PI / 2);
        ctx.fillText('Energy', 0, 0);
        ctx.restore();
    }
    
    reset() {
        this.time = 0;
        this.initialEnergy = this.energy(this.theta0, 0);
        
        Object.keys(this.states).forEach(key => {
            this.states[key] = { theta: this.theta0, omega: 0 };
            this.energyHistory[key] = [];
        });
    }
    
    animate() {
        try {
            if (this.ctx && this.energyCtx) {
                this.step();
                this.draw();
            }
        } catch (e) {
            console.warn('Comparison animation error:', e);
        }
        requestAnimationFrame(() => this.animate());
    }
}

// Phase Space Visualization System
class PhaseSpaceSim {
    constructor() {
        this.theoryCanvas = document.getElementById('theory-phase-canvas');
        this.comparisonCanvas = document.getElementById('comparison-phase-canvas');
        
        if (!this.theoryCanvas || !this.comparisonCanvas) {
            console.warn('Phase space canvas elements not found');
            return;
        }
        
        this.theoryCtx = this.theoryCanvas.getContext('2d');
        this.comparisonCtx = this.comparisonCanvas.getContext('2d');
        
        if (!this.theoryCtx || !this.comparisonCtx) {
            console.warn('Failed to get 2D context for phase space canvases');
            return;
        }
        
        // Parameters
        this.L = 1.0;
        this.g = 9.81;
        this.h = 0.005; // 5ms default
        this.speed = 1;
        this.running = true;
        
        // Current state
        this.theta = Math.PI / 4;
        this.omega = 0;
        this.time = 0;
        this.selectedMethod = 'symplectic';
        
        // Simulation states for all methods
        this.states = {
            euler: { theta: this.theta, omega: this.omega, trail: [] },
            symplectic: { theta: this.theta, omega: this.omega, trail: [] },
            verlet: { theta: this.theta, omega: this.omega, trail: [] },
            rk4: { theta: this.theta, omega: this.omega, trail: [] }
        };
        
        this.colors = {
            euler: '#ef4444',
            symplectic: '#22c55e',
            verlet: '#3b82f6',
            rk4: '#a855f7'
        };
        
        this.maxTrailLength = 500;
        this.initialEnergy = this.energy(this.theta, this.omega);
        
        this.setupControls();
        this.setupCanvasInteraction();
        this.animate();
    }
    
    setupControls() {
        // Play/Pause
        document.getElementById('phase-play-pause').addEventListener('click', () => {
            this.running = !this.running;
            document.getElementById('phase-play-pause').textContent = this.running ? 'Pause' : 'Play';
        });
        
        // Reset
        document.getElementById('phase-reset').addEventListener('click', () => {
            this.reset();
        });
        
        // Clear trails
        document.getElementById('phase-clear').addEventListener('click', () => {
            this.clearTrails();
        });
        
        // Method selection
        document.getElementById('phase-method-select').addEventListener('change', (e) => {
            this.selectedMethod = e.target.value;
        });
        
        // Timestep slider
        document.getElementById('phase-timestep-slider').addEventListener('input', (e) => {
            this.h = parseFloat(e.target.value) / 1000;
            document.getElementById('phase-timestep').textContent = e.target.value;
        });
        
        // Speed slider
        document.getElementById('phase-speed-slider').addEventListener('input', (e) => {
            this.speed = parseFloat(e.target.value);
            document.getElementById('phase-speed').textContent = e.target.value;
        });
    }
    
    setupCanvasInteraction() {
        this.theoryCanvas.addEventListener('click', (e) => {
            const rect = this.theoryCanvas.getBoundingClientRect();
            const x = e.clientX - rect.left;
            const y = e.clientY - rect.top;
            
            // Convert canvas coordinates to phase space coordinates
            const canvasSize = Math.min(this.theoryCanvas.width, this.theoryCanvas.height);
            const centerX = this.theoryCanvas.width / 2;
            const centerY = this.theoryCanvas.height / 2;
            const scale = canvasSize * 0.35;
            
            const newTheta = ((x - centerX) / scale) * Math.PI;
            const newOmega = -((y - centerY) / scale) * 6;
            
            // Clamp to reasonable bounds
            if (Math.abs(newTheta) <= Math.PI && Math.abs(newOmega) <= 6) {
                this.setInitialConditions(newTheta, newOmega);
            }
        });
    }
    
    setInitialConditions(theta, omega) {
        this.theta = theta;
        this.omega = omega;
        this.time = 0;
        this.initialEnergy = this.energy(theta, omega);
        
        // Reset all method states
        Object.keys(this.states).forEach(key => {
            this.states[key] = { theta: theta, omega: omega, trail: [] };
        });
        
        // Update display
        document.getElementById('theory-energy').textContent = this.initialEnergy.toFixed(2);
    }
    
    reset() {
        this.setInitialConditions(Math.PI / 4, 0);
    }
    
    clearTrails() {
        Object.keys(this.states).forEach(key => {
            this.states[key].trail = [];
        });
    }
    
    acceleration(theta) {
        return -(this.g / this.L) * Math.sin(theta);
    }
    
    energy(theta, omega) {
        return this.g * this.L * (1 - Math.cos(theta)) + 0.5 * (this.L * this.L) * omega * omega;
    }
    
    step() {
        if (!this.running) return;
        
        for (let i = 0; i < this.speed; i++) {
            // Update Explicit Euler
            const s1 = this.states.euler;
            const domega1 = this.acceleration(s1.theta);
            this.states.euler = {
                theta: s1.theta + this.h * s1.omega,
                omega: s1.omega + this.h * domega1,
                trail: s1.trail
            };
            
            // Update Symplectic Euler
            const s2 = this.states.symplectic;
            const omega2 = s2.omega + this.h * this.acceleration(s2.theta);
            this.states.symplectic = {
                theta: s2.theta + this.h * omega2,
                omega: omega2,
                trail: s2.trail
            };
            
            // Update Velocity Verlet
            const s3 = this.states.verlet;
            const a0 = this.acceleration(s3.theta);
            const omegaHalf = s3.omega + 0.5 * this.h * a0;
            const theta3 = s3.theta + this.h * omegaHalf;
            const a1 = this.acceleration(theta3);
            this.states.verlet = {
                theta: theta3,
                omega: omegaHalf + 0.5 * this.h * a1,
                trail: s3.trail
            };
            
            // Update RK4
            const s4 = this.states.rk4;
            const f = (theta, omega) => [omega, this.acceleration(theta)];
            const k1 = f(s4.theta, s4.omega);
            const k2 = f(s4.theta + 0.5 * this.h * k1[0], s4.omega + 0.5 * this.h * k1[1]);
            const k3 = f(s4.theta + 0.5 * this.h * k2[0], s4.omega + 0.5 * this.h * k2[1]);
            const k4 = f(s4.theta + this.h * k3[0], s4.omega + this.h * k3[1]);
            this.states.rk4 = {
                theta: s4.theta + (this.h / 6) * (k1[0] + 2*k2[0] + 2*k3[0] + k4[0]),
                omega: s4.omega + (this.h / 6) * (k1[1] + 2*k2[1] + 2*k3[1] + k4[1]),
                trail: s4.trail
            };
            
            this.time += this.h;
        }
        
        // Add points to trails every few steps
        if (Math.floor(this.time * 200) % 2 === 0) {
            Object.keys(this.states).forEach(key => {
                const state = this.states[key];
                state.trail.push({ theta: state.theta, omega: state.omega });
                if (state.trail.length > this.maxTrailLength) {
                    state.trail.shift();
                }
            });
        }
        
        this.updateStatus();
    }
    
    updateStatus() {
        document.getElementById('phase-time').textContent = this.time.toFixed(2);
        
        const currentState = this.states[this.selectedMethod] || this.states.symplectic;
        document.getElementById('phase-theta').textContent = currentState.theta.toFixed(3);
        document.getElementById('phase-omega').textContent = currentState.omega.toFixed(3);
        
        const currentEnergy = this.energy(currentState.theta, currentState.omega);
        const energyError = ((currentEnergy - this.initialEnergy) / this.initialEnergy) * 100;
        document.getElementById('phase-energy-error').textContent = energyError.toFixed(2);
    }
    
    drawTheoryPhasePortrait() {
        const ctx = this.theoryCtx;
        const canvas = this.theoryCanvas;
        const canvasSize = Math.min(canvas.width, canvas.height);
        const centerX = canvas.width / 2;
        const centerY = canvas.height / 2;
        const scale = canvasSize * 0.35;
        
        ctx.clearRect(0, 0, canvas.width, canvas.height);
        
        // Draw energy contours
        const energyLevels = [0.5, 1.0, 2.0, 4.0, 6.0, 8.0];
        energyLevels.forEach((E, index) => {
            ctx.strokeStyle = `rgba(100, 100, 100, ${0.3 + index * 0.1})`;
            ctx.lineWidth = 1;
            ctx.beginPath();
            
            let firstPoint = true;
            for (let theta = -Math.PI; theta <= Math.PI; theta += 0.02) {
                // Calculate omega from energy: E = 0.5*omega^2 + g*L*(1-cos(theta))
                const potentialEnergy = this.g * this.L * (1 - Math.cos(theta));
                if (E > potentialEnergy) {
                    const kineticEnergy = E - potentialEnergy;
                    const omega = Math.sqrt(2 * kineticEnergy) / this.L;
                    
                    // Draw positive omega branch
                    const x1 = centerX + (theta / Math.PI) * scale;
                    const y1 = centerY - (omega / 6) * scale;
                    
                    if (firstPoint) {
                        ctx.moveTo(x1, y1);
                        firstPoint = false;
                    } else {
                        ctx.lineTo(x1, y1);
                    }
                }
            }
            ctx.stroke();
            
            // Draw negative omega branch
            ctx.beginPath();
            firstPoint = true;
            for (let theta = -Math.PI; theta <= Math.PI; theta += 0.02) {
                const potentialEnergy = this.g * this.L * (1 - Math.cos(theta));
                if (E > potentialEnergy) {
                    const kineticEnergy = E - potentialEnergy;
                    const omega = -Math.sqrt(2 * kineticEnergy) / this.L;
                    
                    const x1 = centerX + (theta / Math.PI) * scale;
                    const y1 = centerY - (omega / 6) * scale;
                    
                    if (firstPoint) {
                        ctx.moveTo(x1, y1);
                        firstPoint = false;
                    } else {
                        ctx.lineTo(x1, y1);
                    }
                }
            }
            ctx.stroke();
        });
        
        // Draw axes
        ctx.strokeStyle = '#666';
        ctx.lineWidth = 2;
        ctx.beginPath();
        ctx.moveTo(0, centerY);
        ctx.lineTo(canvas.width, centerY);
        ctx.moveTo(centerX, 0);
        ctx.lineTo(centerX, canvas.height);
        ctx.stroke();
        
        // Draw current state
        const x = centerX + (this.theta / Math.PI) * scale;
        const y = centerY - (this.omega / 6) * scale;
        
        ctx.fillStyle = '#3b82f6';
        ctx.beginPath();
        ctx.arc(x, y, 6, 0, 2 * Math.PI);
        ctx.fill();
        
        // Labels
        ctx.fillStyle = '#aaa';
        ctx.font = '12px Arial';
        ctx.textAlign = 'center';
        ctx.fillText('θ (rad)', canvas.width / 2, canvas.height - 10);
        
        ctx.save();
        ctx.translate(10, canvas.height / 2);
        ctx.rotate(-Math.PI / 2);
        ctx.fillText('ω (rad/s)', 0, 0);
        ctx.restore();
    }
    
    drawComparisonPhaseSpace() {
        const ctx = this.comparisonCtx;
        const canvas = this.comparisonCanvas;
        const canvasSize = Math.min(canvas.width, canvas.height);
        const centerX = canvas.width / 2;
        const centerY = canvas.height / 2;
        const scale = canvasSize * 0.35;
        
        ctx.clearRect(0, 0, canvas.width, canvas.height);
        
        // Draw axes
        ctx.strokeStyle = '#666';
        ctx.lineWidth = 2;
        ctx.beginPath();
        ctx.moveTo(0, centerY);
        ctx.lineTo(canvas.width, centerY);
        ctx.moveTo(centerX, 0);
        ctx.lineTo(centerX, canvas.height);
        ctx.stroke();
        
        // Draw theoretical energy contour for reference
        const E = this.initialEnergy;
        ctx.strokeStyle = 'rgba(100, 100, 100, 0.3)';
        ctx.lineWidth = 2;
        ctx.setLineDash([5, 5]);
        ctx.beginPath();
        
        let firstPoint = true;
        for (let theta = -Math.PI; theta <= Math.PI; theta += 0.02) {
            const potentialEnergy = this.g * this.L * (1 - Math.cos(theta));
            if (E > potentialEnergy) {
                const kineticEnergy = E - potentialEnergy;
                const omega = Math.sqrt(2 * kineticEnergy) / this.L;
                
                const x1 = centerX + (theta / Math.PI) * scale;
                const y1 = centerY - (omega / 6) * scale;
                
                if (firstPoint) {
                    ctx.moveTo(x1, y1);
                    firstPoint = false;
                } else {
                    ctx.lineTo(x1, y1);
                }
            }
        }
        
        // Negative branch
        for (let theta = Math.PI; theta >= -Math.PI; theta -= 0.02) {
            const potentialEnergy = this.g * this.L * (1 - Math.cos(theta));
            if (E > potentialEnergy) {
                const kineticEnergy = E - potentialEnergy;
                const omega = -Math.sqrt(2 * kineticEnergy) / this.L;
                
                const x1 = centerX + (theta / Math.PI) * scale;
                const y1 = centerY - (omega / 6) * scale;
                ctx.lineTo(x1, y1);
            }
        }
        ctx.stroke();
        ctx.setLineDash([]);
        
        // Draw trails and current positions
        if (this.selectedMethod === 'all') {
            // Draw all methods
            Object.keys(this.states).forEach(key => {
                const state = this.states[key];
                const color = this.colors[key];
                
                // Draw trail
                if (state.trail.length > 1) {
                    ctx.strokeStyle = color + '80'; // Semi-transparent
                    ctx.lineWidth = 1;
                    ctx.beginPath();
                    
                    state.trail.forEach((point, i) => {
                        const x = centerX + (point.theta / Math.PI) * scale;
                        const y = centerY - (point.omega / 6) * scale;
                        
                        if (i === 0) {
                            ctx.moveTo(x, y);
                        } else {
                            ctx.lineTo(x, y);
                        }
                    });
                    ctx.stroke();
                }
                
                // Draw current position
                const x = centerX + (state.theta / Math.PI) * scale;
                const y = centerY - (state.omega / 6) * scale;
                
                ctx.fillStyle = color;
                ctx.beginPath();
                ctx.arc(x, y, 4, 0, 2 * Math.PI);
                ctx.fill();
            });
        } else {
            // Draw single method
            const state = this.states[this.selectedMethod];
            const color = this.colors[this.selectedMethod];
            
            // Draw trail
            if (state.trail.length > 1) {
                ctx.strokeStyle = color;
                ctx.lineWidth = 2;
                ctx.beginPath();
                
                state.trail.forEach((point, i) => {
                    const x = centerX + (point.theta / Math.PI) * scale;
                    const y = centerY - (point.omega / 6) * scale;
                    
                    if (i === 0) {
                        ctx.moveTo(x, y);
                    } else {
                        ctx.lineTo(x, y);
                    }
                });
                ctx.stroke();
            }
            
            // Draw current position
            const x = centerX + (state.theta / Math.PI) * scale;
            const y = centerY - (state.omega / 6) * scale;
            
            ctx.fillStyle = color;
            ctx.beginPath();
            ctx.arc(x, y, 6, 0, 2 * Math.PI);
            ctx.fill();
        }
        
        // Labels
        ctx.fillStyle = '#aaa';
        ctx.font = '12px Arial';
        ctx.textAlign = 'center';
        ctx.fillText('θ (rad)', canvas.width / 2, canvas.height - 10);
        
        ctx.save();
        ctx.translate(10, canvas.height / 2);
        ctx.rotate(-Math.PI / 2);
        ctx.fillText('ω (rad/s)', 0, 0);
        ctx.restore();
    }
    
    animate() {
        try {
            if (this.theoryCtx && this.comparisonCtx) {
                this.step();
                this.drawTheoryPhasePortrait();
                this.drawComparisonPhaseSpace();
            }
        } catch (e) {
            console.warn('Phase space animation error:', e);
        }
        requestAnimationFrame(() => this.animate());
    }
}

// Initialize all simulations when page loads
document.addEventListener('DOMContentLoaded', () => {
    try {
        // Individual simulations
        let eulerSim, symplecticSim, verletSim, rk4Sim;
        
        if (document.getElementById('euler-canvas') && document.getElementById('euler-energy')) {
            eulerSim = new SinglePendulumSim('euler-canvas', 'euler-energy', 'euler', '#ef4444');
        }
        if (document.getElementById('symplectic-canvas') && document.getElementById('symplectic-energy')) {
            symplecticSim = new SinglePendulumSim('symplectic-canvas', 'symplectic-energy', 'symplectic', '#22c55e');
        }
        if (document.getElementById('verlet-canvas') && document.getElementById('verlet-energy')) {
            verletSim = new SinglePendulumSim('verlet-canvas', 'verlet-energy', 'verlet', '#3b82f6');
        }
        if (document.getElementById('rk4-canvas') && document.getElementById('rk4-energy')) {
            rk4Sim = new SinglePendulumSim('rk4-canvas', 'rk4-energy', 'rk4', '#a855f7');
        }
        
        // Individual controls
        const setupIndividualControls = (sim, prefix) => {
            if (!sim) return;
            
            try {
                // Timestep slider
                const slider = document.getElementById(`${prefix}-slider`);
                const timestepDisplay = document.getElementById(`${prefix}-timestep`);
                if (slider && timestepDisplay) {
                    slider.addEventListener('input', (e) => {
                        sim.h = parseFloat(e.target.value) / 1000;
                        timestepDisplay.textContent = e.target.value;
                    });
                }
                
                // Play-pause button
                const playPauseButton = document.getElementById(`${prefix}-play-pause`);
                if (playPauseButton) {
                    playPauseButton.addEventListener('click', () => {
                        const isRunning = sim.togglePause();
                        playPauseButton.textContent = isRunning ? 'Pause' : 'Play';
                    });
                }
                
                // Reset button
                const resetButton = document.getElementById(`${prefix}-reset`);
                if (resetButton) {
                    resetButton.addEventListener('click', () => {
                        sim.reset();
                    });
                }
                
                // Update error display
                const errorDisplay = document.getElementById(`${prefix}-error`);
                if (errorDisplay) {
                    setInterval(() => {
                        try {
                            const error = sim.getEnergyError();
                            errorDisplay.textContent = error.toFixed(2);
                        } catch (e) {
                            console.warn(`Error updating ${prefix} display:`, e);
                        }
                    }, 100);
                }
            } catch (e) {
                console.warn(`Error setting up controls for ${prefix}:`, e);
            }
        };
        
        setupIndividualControls(eulerSim, 'euler');
        setupIndividualControls(symplecticSim, 'symplectic');
        setupIndividualControls(verletSim, 'verlet');
        setupIndividualControls(rk4Sim, 'rk4');
        
        // Comparison simulation
        if (document.getElementById('comparison-canvas') && document.getElementById('comparison-energy')) {
            const comparisonSim = new ComparisonSim();
        }
        
        // Phase space simulation (only initialize if elements exist)
        if (document.getElementById('theory-phase-canvas') && document.getElementById('comparison-phase-canvas')) {
            try {
                const phaseSpaceSim = new PhaseSpaceSim();
            } catch (e) {
                console.warn('Error initializing phase space simulation:', e);
            }
        }
        
        // Real-time comparison simulation
        if (document.getElementById('realtime-euler-canvas')) {
            try {
                const realtimeComparisonSim = new RealtimeComparisonSim();
            } catch (e) {
                console.warn('Error initializing real-time comparison simulation:', e);
            }
        }
    } catch (e) {
        console.error('Error initializing simulations:', e);
    }
});

// Real-time Method Comparison System
class RealtimeComparisonSim {
    constructor() {
        this.canvases = {
            euler: document.getElementById('realtime-euler-canvas'),
            symplectic: document.getElementById('realtime-symplectic-canvas'),
            verlet: document.getElementById('realtime-verlet-canvas'),
            rk4: document.getElementById('realtime-rk4-canvas')
        };
        
        // Check if any canvases are missing
        const missingCanvases = Object.keys(this.canvases).filter(key => !this.canvases[key]);
        if (missingCanvases.length > 0) {
            console.warn('Missing realtime comparison canvases:', missingCanvases);
            return;
        }
        
        this.contexts = {};
        Object.keys(this.canvases).forEach(key => {
            if (this.canvases[key]) {
                this.contexts[key] = this.canvases[key].getContext('2d');
                if (!this.contexts[key]) {
                    console.warn(`Failed to get 2D context for ${key} canvas`);
                }
            }
        });
        
        this.energyCanvas = document.getElementById('realtime-energy-chart');
        this.energyCtx = this.energyCanvas?.getContext('2d');
        
        if (!this.energyCanvas || !this.energyCtx) {
            console.warn('Realtime energy chart canvas not found or context unavailable');
        }
        
        // Parameters
        this.L = 1.0;
        this.g = 9.81;
        this.h = 0.01;
        this.theta0 = Math.PI / 4;
        this.running = true;
        
        // States for all methods
        this.states = {
            euler: { theta: this.theta0, omega: 0 },
            symplectic: { theta: this.theta0, omega: 0 },
            verlet: { theta: this.theta0, omega: 0 },
            rk4: { theta: this.theta0, omega: 0 }
        };
        
        this.energyHistory = {
            euler: [],
            symplectic: [],
            verlet: [],
            rk4: []
        };
        
        this.colors = {
            euler: '#ef4444',
            symplectic: '#22c55e',
            verlet: '#3b82f6',
            rk4: '#a855f7'
        };
        
        this.time = 0;
        this.initialEnergy = this.energy(this.theta0, 0);
        this.maxSamples = 300;
        
        this.setupControls();
        this.animate();
    }
    
    setupControls() {
        // Play/Pause
        document.getElementById('realtime-play-pause')?.addEventListener('click', () => {
            this.running = !this.running;
            document.getElementById('realtime-play-pause').textContent = this.running ? 'Pause' : 'Play';
        });
        
        // Reset
        document.getElementById('realtime-reset')?.addEventListener('click', () => {
            this.reset();
        });
        
        // Sync methods
        document.getElementById('realtime-sync')?.addEventListener('click', () => {
            this.syncMethods();
        });
        
        // Angle slider
        document.getElementById('realtime-angle-slider')?.addEventListener('input', (e) => {
            this.theta0 = parseFloat(e.target.value) * Math.PI / 180;
            document.getElementById('realtime-angle').textContent = e.target.value;
            this.reset();
        });
        
        // Timestep slider
        document.getElementById('realtime-timestep-slider')?.addEventListener('input', (e) => {
            this.h = parseFloat(e.target.value) / 1000;
            document.getElementById('realtime-timestep').textContent = e.target.value;
        });
    }
    
    syncMethods() {
        Object.keys(this.states).forEach(key => {
            this.states[key] = { theta: this.theta0, omega: 0 };
            this.energyHistory[key] = [];
        });
        this.time = 0;
        this.initialEnergy = this.energy(this.theta0, 0);
    }
    
    reset() {
        this.theta0 = Math.PI / 4;
        this.time = 0;
        this.initialEnergy = this.energy(this.theta0, 0);
        
        Object.keys(this.states).forEach(key => {
            this.states[key] = { theta: this.theta0, omega: 0 };
            this.energyHistory[key] = [];
        });
        
        document.getElementById('realtime-angle').textContent = '45';
        document.getElementById('realtime-angle-slider').value = '45';
    }
    
    acceleration(theta) {
        return -(this.g / this.L) * Math.sin(theta);
    }
    
    energy(theta, omega) {
        return this.g * this.L * (1 - Math.cos(theta)) + 0.5 * (this.L * this.L) * omega * omega;
    }
    
    step() {
        if (!this.running) return;
        
        // Update Explicit Euler
        const s1 = this.states.euler;
        const domega1 = this.acceleration(s1.theta);
        this.states.euler = {
            theta: s1.theta + this.h * s1.omega,
            omega: s1.omega + this.h * domega1
        };
        
        // Update Symplectic Euler
        const s2 = this.states.symplectic;
        const omega2 = s2.omega + this.h * this.acceleration(s2.theta);
        this.states.symplectic = {
            theta: s2.theta + this.h * omega2,
            omega: omega2
        };
        
        // Update Velocity Verlet
        const s3 = this.states.verlet;
        const a0 = this.acceleration(s3.theta);
        const omegaHalf = s3.omega + 0.5 * this.h * a0;
        const theta3 = s3.theta + this.h * omegaHalf;
        const a1 = this.acceleration(theta3);
        this.states.verlet = {
            theta: theta3,
            omega: omegaHalf + 0.5 * this.h * a1
        };
        
        // Update RK4
        const s4 = this.states.rk4;
        const f = (theta, omega) => [omega, this.acceleration(theta)];
        const k1 = f(s4.theta, s4.omega);
        const k2 = f(s4.theta + 0.5 * this.h * k1[0], s4.omega + 0.5 * this.h * k1[1]);
        const k3 = f(s4.theta + 0.5 * this.h * k2[0], s4.omega + 0.5 * this.h * k2[1]);
        const k4 = f(s4.theta + this.h * k3[0], s4.omega + this.h * k3[1]);
        this.states.rk4 = {
            theta: s4.theta + (this.h / 6) * (k1[0] + 2*k2[0] + 2*k3[0] + k4[0]),
            omega: s4.omega + (this.h / 6) * (k1[1] + 2*k2[1] + 2*k3[1] + k4[1])
        };
        
        this.time += this.h;
        
        // Sample energy
        if (Math.floor(this.time * 50) % 3 === 0) {
            Object.keys(this.states).forEach(key => {
                const E = this.energy(this.states[key].theta, this.states[key].omega);
                this.energyHistory[key].push({ t: this.time, E: E });
                if (this.energyHistory[key].length > this.maxSamples) {
                    this.energyHistory[key].shift();
                }
            });
        }
        
        this.updateStatus();
    }
    
    updateStatus() {
        document.getElementById('realtime-time').textContent = this.time.toFixed(2);
        
        Object.keys(this.states).forEach(key => {
            const currentEnergy = this.energy(this.states[key].theta, this.states[key].omega);
            const relativeError = ((currentEnergy - this.initialEnergy) / this.initialEnergy) * 100;
            document.getElementById(`realtime-${key}-error`).textContent = relativeError.toFixed(2);
        });
    }
    
    drawPendulum(key) {
        const ctx = this.contexts[key];
        const canvas = this.canvases[key];
        if (!ctx || !canvas) return;
        
        const scale = Math.min(canvas.width, canvas.height) * 0.3;
        const centerX = canvas.width / 2;
        const centerY = canvas.height * 0.25;
        
        ctx.clearRect(0, 0, canvas.width, canvas.height);
        
        const state = this.states[key];
        const x = centerX + scale * Math.sin(state.theta);
        const y = centerY + scale * Math.cos(state.theta);
        
        // Rod
        ctx.strokeStyle = this.colors[key];
        ctx.lineWidth = 3;
        ctx.beginPath();
        ctx.moveTo(centerX, centerY);
        ctx.lineTo(x, y);
        ctx.stroke();
        
        // Pivot
        ctx.fillStyle = '#666';
        ctx.beginPath();
        ctx.arc(centerX, centerY, 4, 0, 2 * Math.PI);
        ctx.fill();
        
        // Bob
        ctx.fillStyle = this.colors[key];
        ctx.beginPath();
        ctx.arc(x, y, 8, 0, 2 * Math.PI);
        ctx.fill();
        
        // Energy error display
        const currentEnergy = this.energy(state.theta, state.omega);
        const error = ((currentEnergy - this.initialEnergy) / this.initialEnergy) * 100;
        
        ctx.fillStyle = this.colors[key];
        ctx.font = '11px Arial';
        ctx.textAlign = 'center';
        ctx.fillText(`${error.toFixed(1)}%`, centerX, canvas.height - 10);
    }
    
    drawEnergyChart() {
        if (!this.energyCtx || !this.energyCanvas) return;
        
        const ctx = this.energyCtx;
        const canvas = this.energyCanvas;
        
        ctx.clearRect(0, 0, canvas.width, canvas.height);
        
        const padding = 40;
        const chartWidth = canvas.width - 2 * padding;
        const chartHeight = canvas.height - 2 * padding;
        
        // Find bounds
        let tMin = Infinity, tMax = -Infinity, eMin = Infinity, eMax = -Infinity;
        
        Object.keys(this.energyHistory).forEach(key => {
            const history = this.energyHistory[key];
            history.forEach(point => {
                tMin = Math.min(tMin, point.t);
                tMax = Math.max(tMax, point.t);
                eMin = Math.min(eMin, point.E);
                eMax = Math.max(eMax, point.E);
            });
        });
        
        if (!isFinite(tMin)) return;
        
        const timeRange = Math.max(tMax - tMin, 1);
        const energyRange = Math.max(eMax - eMin, 0.001);
        
        // Draw axes
        ctx.strokeStyle = '#666';
        ctx.lineWidth = 1;
        ctx.beginPath();
        ctx.moveTo(padding, padding);
        ctx.lineTo(padding, canvas.height - padding);
        ctx.lineTo(canvas.width - padding, canvas.height - padding);
        ctx.stroke();
        
        // Draw initial energy line
        const refY = canvas.height - padding - ((this.initialEnergy - eMin) / energyRange) * chartHeight;
        ctx.strokeStyle = '#666';
        ctx.setLineDash([5, 5]);
        ctx.beginPath();
        ctx.moveTo(padding, refY);
        ctx.lineTo(canvas.width - padding, refY);
        ctx.stroke();
        ctx.setLineDash([]);
        
        // Draw energy lines for each method
        Object.keys(this.energyHistory).forEach(key => {
            const history = this.energyHistory[key];
            if (history.length < 2) return;
            
            ctx.strokeStyle = this.colors[key];
            ctx.lineWidth = 2;
            ctx.beginPath();
            
            let first = true;
            history.forEach(point => {
                const x = padding + ((point.t - tMin) / timeRange) * chartWidth;
                const y = canvas.height - padding - ((point.E - eMin) / energyRange) * chartHeight;
                
                if (first) {
                    ctx.moveTo(x, y);
                    first = false;
                } else {
                    ctx.lineTo(x, y);
                }
            });
            
            ctx.stroke();
        });
        
        // Labels
        ctx.fillStyle = '#aaa';
        ctx.font = '12px Arial';
        ctx.textAlign = 'center';
        ctx.fillText('Time (s)', canvas.width / 2, canvas.height - 5);
        
        ctx.save();
        ctx.translate(15, canvas.height / 2);
        ctx.rotate(-Math.PI / 2);
        ctx.fillText('Energy', 0, 0);
        ctx.restore();
    }
    
    animate() {
        try {
            this.step();
            Object.keys(this.canvases).forEach(key => {
                if (this.canvases[key] && this.contexts[key]) {
                    this.drawPendulum(key);
                }
            });
            if (this.energyCtx) {
                this.drawEnergyChart();
            }
        } catch (e) {
            console.warn('Realtime comparison animation error:', e);
        }
        requestAnimationFrame(() => this.animate());
    }
}
</script>]]></content><author><name></name></author><category term="blog" /><category term="energy-drift" /><category term="physics" /><category term="simulation" /><category term="numerical-methods" /><category term="energy-conservation" /><category term="symplectic-integrators" /><category term="computational-physics" /><summary type="html"><![CDATA[Interactive exploration of numerical integration schemes and energy conservation in physics simulations. Master symplectic methods, stability analysis, and the mathematical foundations of computational physics.]]></summary></entry></feed>